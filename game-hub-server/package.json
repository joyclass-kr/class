{
  "name": "classroom-game-hub-server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "postinstall": "NODE_OPTIONS=--max-old-space-size=384 npm --prefix ../learning/literacy-numeracy/arithmetics ci --include=dev && NODE_OPTIONS=--max-old-space-size=384 npm --prefix ../learning/literacy-numeracy/arithmetics run build && NODE_OPTIONS=--max-old-space-size=384 npm --prefix ../learning/inquiry/korean-history ci --include=dev && NODE_OPTIONS=--max-old-space-size=384 npm --prefix ../learning/inquiry/korean-history run build && npm --prefix ../learning/inquiry/age-of-exploration ci --omit=dev",
    "build:reading-seed": "node ../scripts/build-reading-seed.js"
  },
  "dependencies": {
    "express": "^4.21.2",
    "google-auth-library": "^11.0.0",
    "pg": "^8.13.1",
    "ws": "^8.18.3"
  },
  "engines": {
    "node": ">=22"
  }
}
