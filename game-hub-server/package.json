{
  "name": "classroom-game-hub-server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "postinstall": "NODE_OPTIONS=--max-old-space-size=384 npm --prefix ../learning/basics/arithmetics ci --include=dev && NODE_OPTIONS=--max-old-space-size=384 npm --prefix ../learning/basics/arithmetics run build && NODE_OPTIONS=--max-old-space-size=384 npm --prefix ../learning/academics/korean-history ci --include=dev && NODE_OPTIONS=--max-old-space-size=384 npm --prefix ../learning/academics/korean-history run build && npm --prefix ../learning/academics/age-of-exploration ci --omit=dev",
    "build:reading-seed": "node ../scripts/build-reading-seed.js"
  },
  "dependencies": {
    "express": "^4.21.2",
    "google-auth-library": "^11.0.0",
    "pg": "^8.13.1",
    "ws": "^8.18.3"
  },
  "engines": {
    "node": ">=22"
  }
}
