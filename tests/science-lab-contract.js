const assert = require('node:assert/strict');
const fs = require('node:fs');

const root = fs.readFileSync('index.html', 'utf8');
const page = fs.readFileSync('learning/academics/science-lab/index.html', 'utf8');
const app = fs.readFileSync('learning/academics/science-lab/app.js', 'utf8');
const styles = fs.readFileSync('learning/academics/science-lab/styles.css', 'utf8');
const solubilityPage = fs.readFileSync('learning/academics/science-lab/solubility/index.html', 'utf8');
const solubilityApp = fs.readFileSync('learning/academics/science-lab/solubility/app.js', 'utf8');
const solubilityStyles = fs.readFileSync('learning/academics/science-lab/solubility/styles.css', 'utf8');

assert.match(root, /href="learning\/academics\/science-lab\/"/);
assert.match(root, /<strong>과학 실험실<\/strong>/);
assert.doesNotMatch(root, /href="learning\/academics\/chemistry-lab\/"/);
assert.match(page, /<h1>과학 실험실<\/h1>/);
assert.doesNotMatch(page, /<header class="site-header">/);
assert.match(page, /← 학습 메뉴/);
assert.match(page, /<h2 id="catalog-title">실험 목록<\/h2>/);
assert.match(page, /id="physics"/);
assert.match(page, /id="chemistry"/);
assert.match(page, /id="biology"/);
assert.match(page, /id="earth-science"/);
assert.match(page, /data-grade="elementary"/);
assert.match(page, /data-grade="middle"/);
assert.match(page, /data-grade="high"/);
assert.match(page, /href="#chemistry"/);
assert.match(page, /href="#physics"/);
assert.match(page, /href="#earth-science"/);
assert.match(page, /href="#biology"/);
assert.match(page, /href="solubility\/">용해와 용해도<\/a>/);
assert.match(page, /href="earthquake\/">지진파와 진앙<\/a>/);
assert.match(page, /지진파와 진앙/);
assert.match(page, /일기도와 전선/);
assert.match(page, /연주시차와 HR도/);
assert.match(page, /화산 분출 모형과 분출물/);
assert.match(page, /마그마의 점성과 화산 분출/);
assert.match(page, /운동량 보존과 충돌/);
assert.match(page, /거름·증류·크로마토그래피/);
assert.match(page, /DNA 추출/);
assert.match(page, /방사성 동위원소와 절대 연령/);
assert.doesNotMatch(page, /핵심|필수|시험 대비|한눈에|마스터/);
assert.match(app, /function showGrade\(grade\)/);
assert.match(app, /experiment\.hidden = grade !== 'all'/);
assert.match(styles, /grid-template-columns: repeat\(4, minmax\(0, 1fr\)\)/);
assert.match(styles, /@media \(max-width: 1050px\)/);
assert.match(styles, /@media \(max-width: 680px\)/);
assert.match(styles, /@media \(prefers-reduced-motion: reduce\)/);

assert.match(solubilityPage, /id="runExperimentBtn"/);
assert.match(solubilityPage, /href="\.\.\/">← 과학 실험실<\/a>/);
assert.match(solubilityPage, /기준: 물 100g\(약 100mL\)/);
assert.match(solubilityPage, /용질이 없어지는 것이 아니라 작은 입자로 물속에 고르게 퍼집니다/);
assert.match(solubilityPage, /소금의 용해도는 조금 변하고, 설탕의 용해도는 크게 변합니다/);
assert.match(solubilityApp, /function runExperiment\(\)/);
assert.match(solubilityApp, /amount: \{ min: 32, max: 42, step: 0\.5, initial: 36 \}/);
assert.match(solubilityApp, /function interpolateSolubility\(points, temperature\)/);
assert.match(solubilityApp, /particleType: 'ion'/);
assert.match(solubilityApp, /particleType: 'molecule'/);
assert.match(solubilityStyles, /\.sodium-ion/);
assert.match(solubilityStyles, /\.chloride-ion/);
assert.match(solubilityStyles, /\.sugar-molecule/);

console.log('science lab contract: validated');
