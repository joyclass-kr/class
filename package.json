{
  "name": "class-platform-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix game-hub-server install && npm --prefix game-hub-server run build:reading-seed",
    "start": "npm --prefix game-hub-server start"
  },
  "engines": {
    "node": ">=20"
  }
}
