const CHAPTERS = [
    {
        num: 1,
        title: "1장 · 개암 네 알",
        beats: [
            {
                art: "01-brothers.webp",
                emoji: "🌰",
                left: [
                    "옛날 어느 마을에 형제가 살았습니다.",
                    "형은 욕심이 많았고, 아우는 마음이 곱고 부지런했지요.",
                    "형은 좋은 것이 있으면 저부터 챙겼어요.",
                    "부모님을 모시고 사는 것도 아우 몫이었지요."
                ],
                right: [
                    "어느 날 아우가 산에 나무를 하러 갔어요.",
                    "가을이라 낙엽이 발목까지 쌓여 있었어요.",
                    "낙엽을 헤치는데 발밑에 무언가 데구루루 굴렀습니다.",
                    "반들반들한 개암 한 알이었지요.",
                    "아우가 허리를 굽혀 그것을 주웠지요."
                ]
            },
            {
                art: "02-pick.webp",
                emoji: "🤲",
                left: [
                    "\"오, 이건 아버지 드려야지.\"",
                    "아우는 개암을 소매로 곱게 닦아 주머니에 넣었어요.",
                    "조금 가다 두 번째 한 알을 주웠습니다.",
                    "\"이건 어머니 드려야지.\"",
                    "곧 세 번째 한 알이 눈에 띄었지요."
                ],
                right: [
                    "아우는 형 몫도 잊지 않았어요.",
                    "\"이건 형님 드려야지.\"",
                    "그러다 네 번째 한 알을 주우며 아우가 웃었어요.",
                    "\"이건 내가 먹어야겠다.\"",
                    "네 알이면 온 식구가 하나씩이었지요."
                ]
            },
            {
                art: "03-house.webp",
                emoji: "🏚️",
                left: [
                    "나무를 하다 보니 나뭇짐이 제법 묵직해졌어요.",
                    "어느새 해가 넘어가고 산속이 캄캄해졌습니다. 별빛 하나 없는 밤이었지요.",
                    "다행히 골짜기에 낡은 빈 집이 하나 있었지요."
                ],
                right: [
                    "아우는 그 집 마루에 지게를 내려놓았어요.",
                    "마루에 먼지가 뽀얗게 앉아 있었지요.",
                    "\"하룻밤만 신세 지자.\"",
                    "아우는 몸을 뉘고 팔베개를 한 채 눈을 감았답니다."
                ]
            },
            {
                art: "04-dokkaebi.webp",
                emoji: "👹",
                left: [
                    "한밤중이었어요.",
                    "쿵!",
                    "쿵!",
                    "쿵!",
                    "땅이 울리는 소리였지요.",
                    "아우가 눈을 번쩍 떴어요."
                ],
                right: [
                    "마당이 시끌벅적해졌습니다. 문틈으로 내다보니 도깨비들이 잔뜩 몰려와 있었어요.",
                    "뿔이 하나씩 난 도깨비들이 저마다 방망이를 하나씩 들고 있었지요.",
                    "아우는 삐걱거리는 사다리를 조심조심 올랐어요.",
                    "숨을 죽이고 다락으로 기어 올라갔답니다."
                ]
            }
        ]
    },
    {
        num: 2,
        title: "2장 · 금 나와라 뚝딱",
        beats: [
            {
                art: "05-feast.webp",
                emoji: "✨",
                left: [
                    "도깨비들이 마당 한가운데에 둘러앉았어요.",
                    "\"금 나와라, 뚝딱!\"",
                    "방망이를 두드리자 금덩이가 우수수 쏟아졌어요.",
                    "\"은 나와라, 뚝딱!\"",
                    "은덩이가 그 옆에 산처럼 쌓였지요."
                ],
                right: [
                    "\"떡 나와라, 뚝딱!\"",
                    "김이 모락모락 나는 떡이 쏟아졌어요.",
                    "도깨비들은 밤새 노래를 부르며 잔치를 벌였습니다.",
                    "다락 위 아우는 눈이 휘둥그레졌답니다."
                ]
            },
            {
                art: "06-hungry.webp",
                emoji: "😖",
                left: [
                    "다락 위에서 아우는 배가 고파 견딜 수가 없었어요. 떡 냄새가 코끝을 간질였거든요.",
                    "아침부터 아무것도 먹지 못했거든요.",
                    "배에서 꼬르륵 소리가 났지요.",
                    "'저 개암이라도 하나 먹어야겠다.'"
                ],
                right: [
                    "아우는 주머니에서 개암 한 알을 꺼내 입에 넣었습니다.",
                    "소리가 날까 봐 아주 천천히요.",
                    "그러고는 어금니로 꽉 깨물었지요.",
                    "개암이 어찌나 단단한지 잘 깨지지 않았어요."
                ]
            },
            {
                art: "07-crack.webp",
                emoji: "💥",
                left: [
                    "아우가 힘을 주는 순간이었어요.",
                    "딱!",
                    "낡은 집 안에서 그 소리가 어찌나 크게 울렸는지 몰라요.",
                    "텅 빈 집이 울림통 노릇을 한 것이지요."
                ],
                right: [
                    "\"으악! 집이 무너진다!\"",
                    "도깨비들이 화들짝 놀라 일어섰어요.",
                    "\"기둥이 부러졌다! 도망쳐!\"",
                    "도깨비들이 서로 밟고 넘어지며 문밖으로 쏟아져 나갔습니다.",
                    "금이며 떡이며 다 팽개치고 달아났지요."
                ]
            },
            {
                art: "08-left.webp",
                emoji: "🔨",
                left: [
                    "날이 밝았어요.",
                    "아우가 다락에서 조심조심 내려왔어요.",
                    "마당에는 아무도 없었습니다.",
                    "밤새 쏟아 놓은 금도 떡도 흔적 없이 사라지고 없었지요."
                ],
                right: [
                    "다만 도깨비 하나가 허둥대다 떨어뜨린 방망이가 툭 놓여 있었지요.",
                    "아우는 한참을 망설이다 그것을 집어 들었어요.",
                    "그러고는 지게에 얹고 집으로 내려왔지요.",
                    "\"금 나와라, 뚝딱!\"",
                    "금덩이가 마당에 쏟아졌답니다."
                ]
            }
        ]
    },
    {
        num: 3,
        title: "3장 · 따라 한 형",
        beats: [
            {
                art: "09-brother.webp",
                emoji: "😏",
                left: [
                    "아우네 집이 하루아침에 기와집이 되자 형이 달려왔어요.",
                    "소문이 하루 만에 온 마을에 퍼졌거든요.",
                    "\"대체 어찌 된 일이냐? 하나도 빠짐없이 말해 보아라.\"",
                    "형이 아우의 소매를 붙들고 늘어졌어요."
                ],
                right: [
                    "아우는 있었던 일을 그대로 이야기해 주었습니다. 아무것도 숨기지 않았지요.",
                    "형의 눈이 점점 커졌어요.",
                    "\"골짜기 빈 집이라… 개암이라…\"",
                    "형은 벌써 마음이 산으로 가 있었지요."
                ]
            },
            {
                art: "10-copy.webp",
                emoji: "🌰",
                left: [
                    "이튿날 형은 산으로 달려가 개암을 한 자루나 주웠어요.",
                    "개암이 자루에 그득했어요.",
                    "그러고는 그 빈 집을 찾아갔습니다.",
                    "형은 다락에 올라가 도깨비를 기다렸지요."
                ],
                right: [
                    "밤이 깊자 정말로 도깨비들이 몰려왔지요.",
                    "\"금 나와라, 뚝딱!\"",
                    "도깨비들이 방망이를 두드리자 금덩이가 쏟아졌어요.",
                    "형은 방망이만 쳐다보며 침을 꿀꺽 삼켰지요.",
                    "\"저것만 있으면 나도 부자다.\""
                ]
            },
            {
                art: "11-caught.webp",
                emoji: "😱",
                left: [
                    "형은 개암을 한 움큼 입에 넣고 힘껏 깨물었어요.",
                    "한 알로는 성에 차지 않았거든요.",
                    "딱!",
                    "딱!",
                    "따다닥!",
                    "요란한 소리가 났지요."
                ],
                right: [
                    "그런데 도깨비들은 달아나지 않았습니다.",
                    "도깨비들이 오히려 다락을 올려다보았어요.",
                    "\"저놈이다! 지난번에 우리 방망이를 가져간 놈!\"",
                    "도깨비들이 다락으로 우르르 올라왔지요.",
                    "형은 그제야 아차 싶었답니다."
                ]
            },
            {
                art: "12-nose.webp",
                emoji: "👃",
                left: [
                    "\"코 늘어나라, 뚝딱!\"",
                    "대장 도깨비가 방망이를 두드렸어요.",
                    "형의 코가 쭈욱 늘어나 방바닥에 끌렸어요. 도깨비들은 깔깔 웃으며 사라졌답니다.",
                    "형은 코를 붙잡고 엉엉 울었지요."
                ],
                right: [
                    "형은 코를 두 손으로 받쳐 들고 아우를 찾아 나섰어요.",
                    "밤새 걸어 아우네 집에 닿았지요.",
                    "아우는 잠자코 방망이를 들었어요.",
                    "\"코 줄어라, 뚝딱!\"",
                    "형은 그날부터 사람이 달라졌답니다."
                ]
            }
        ],
        moral: "아우는 개암 네 알을 주우며 세 알을 남에게 줄 생각부터 했어요. 형은 자루 가득 주우면서도 제 몫만 생각했지요. 같은 산에서 같은 것을 주웠지만 손에 남은 것은 달랐답니다.",
        question: "좋은 것이 생겼을 때, 나는 누구를 먼저 떠올리나요?"
    }
];

function artFrame(src, emoji) {
    return `
        <div class="art-frame">
            <img src="images/${src}" alt="" onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';">
            <div class="art-fallback" style="display:none">${emoji}</div>
        </div>`;
}

function coverToc() {
    const item = s => `
        <button type="button" data-goto="${s.num}">
            <span class="toc-num">${s.num}</span>
            <span>${s.title.replace(/^\d+장 · /, '')}</span>
        </button>`;
    return `
        <nav class="cover-toc">
            <h2>차례</h2>
            ${CHAPTERS.map(item).join('')}
            <button type="button" data-goto-kind="quiz">
                <span class="toc-num">❓</span>
                <span>이야기 문제</span>
            </button>
        </nav>`;
}

function coverPage() {
    return `
        <div class="page page-cover">
            <div class="story-page-left story-page-left-full">
                ${artFrame('cover.webp', '🔨')}
            </div>
            <div class="story-page-right">
                <h1>도깨비 방망이</h1>
                <p>도깨비 방망이는 지은이가 없는 구전 설화예요. 금 나와라 뚝딱, 은 나와라 뚝딱 하면 무엇이든 나오는 방망이가 나오는 이야기랍니다.</p>
                <p>우리 도깨비는 방망이를 늘 지니고 다녀요. 그 방망이를 두드리면 원하는 것이 나온다고 했지요. 그런데 도깨비는 겁이 아주 많아서, 집이 무너지는 소리나 닭 우는 소리에 놀라 물건을 두고 달아나곤 한답니다.</p>
                <p>개암은 개암나무에 열리는 작은 열매예요. 껍질이 아주 단단해서 이로 깨물면 딱 하고 큰 소리가 나지요. 하필 개암이 이야기에 나오는 것도 그 소리 때문이랍니다.</p>
                ${coverToc()}
            </div>
        </div>`;
}

function spreadPage(chapter, beat, isFirst) {
    const badgeHtml = isFirst ? `<div class="spread-chapter-badge">${chapter.title}</div>` : '';
    const leftHtml = beat.left.map(p => `<p>${p}</p>`).join('');
    const rightHtml = beat.right.map(p => `<p>${p}</p>`).join('');
    return `
        <div class="page page-story">
            <div class="spread-art">
                ${badgeHtml}
                ${artFrame(beat.art, beat.emoji)}
            </div>
            <div class="spread-text">
                <div class="spread-text-left">${leftHtml}</div>
                <div class="spread-text-right">${rightHtml}</div>
            </div>
        </div>`;
}


const QUIZ = [
    { q: "아우가 산에서 주운 것은 무엇인가요?", choices: ["금덩이 하나", "개암 몇 알", "낡은 방망이"], answer: 1 },
    { q: "아우는 첫 번째 개암을 누구에게 주려 했나요?", choices: ["어머니", "아버지", "형님"], answer: 1 },
    { q: "도깨비들이 방망이로 무엇을 했나요?", choices: ["두드려 금은을 냈다", "서로 때리며 놀았다", "땅을 파고 있었다"], answer: 0 },
    { q: "도깨비들은 무슨 일이 났다고 여겨 달아났나요?", choices: ["누가 방망이를 훔쳤다고", "날이 밝았다고", "집이 무너진다고"], answer: 2 },
    { q: "욕심쟁이 형은 어떻게 되었나요?", choices: ["코가 길게 늘어났다", "혹이 하나 더 붙었다", "금을 잔뜩 얻었다"], answer: 0 },
    { q: "코가 늘어난 형은 어디로 갔나요?", choices: ["다시 다락에 올라갔다", "아우를 찾아갔다", "골짜기 빈 집에 남았다"], answer: 1 }
];

function quizPage() {
    const items = QUIZ.map((item, i) => `
        <div class="quiz-item" data-qindex="${i}">
            <p class="quiz-question">${i + 1}. ${item.q}</p>
            <div class="quiz-choices">
                ${item.choices.map((c, ci) => `<button type="button" class="quiz-choice" data-choice="${ci}">${c}</button>`).join('')}
            </div>
        </div>`).join('');
    return `
        <div class="page page-quiz">
            <h2>이야기 문제</h2>
            <p class="quiz-intro-text" id="quizProgress">0 / 총 ${QUIZ.length}문항 완료</p>
            <div class="quiz-list">${items}</div>
        </div>`;
}


/* 읽고 나서 — 세계명작 트랙과 같은 형식이다. 동화틀은 쪽을 재서 나누지 않으므로
   펼침면마다 왼쪽·오른쪽 글을 손으로 나누어 둔다. */
const AFTERWORD = {
    title: '읽고 나서',
    emoji: '🌰',
    spreads: [
        {
            art: 'end.webp',
            left: [
                "착한 아우와 욕심 많은 형이 같은 곳에 가서 다른 것을 얻어 오는 이야기는 우리 옛이야기의 큰 틀 가운데 하나입니다. 흥부와 놀부도 같은 틀입니다.",
                "여기서 열쇠는 개암입니다. 산에서 절로 나는 작은 열매인데 껍질이 단단해서 깨물면 딱 소리가 크게 납니다. 도깨비들이 그 소리를 집 무너지는 소리로 들은 것이지요.",
                "다시 보면 아우는 도깨비를 속일 마음이 조금도 없었습니다. 배가 고파서 개암을 깨문 것뿐입니다. 얻은 것이 크지만 노린 것은 아무것도 없었습니다.",
                "형은 반대입니다. 개암을 자루로 주웠고, 깨물 때를 재고, 방망이를 어떻게 받을지까지 미리 정해 두었습니다. 그런데 도깨비들은 두 번째에는 속지 않았습니다."
            ],
            right: [
                "이야기가 두 사람을 갈라 놓은 자리는 재주가 아니라 무엇을 바라고 갔는가였습니다.",
                "아우는 개암 네 알을 주우면서 세 알을 남에게 줄 생각부터 했습니다. 형은 자루 가득 주우면서도 제 몫만 생각했습니다. 같은 산에서 같은 것을 주웠는데 손에 남은 것이 달랐습니다.",
                "좋은 것이 생겼을 때 나는 누구를 먼저 떠올리나요?"
            ]
        }
    ]
};

function afterPage(spread, isFirst) {
    const head = isFirst ? `<h2>${AFTERWORD.title}</h2>` : '';
    // 그림은 오른쪽 칸 맨 위 모서리에 꽉 붙인다. 학습 허브로 가는 길은 그 칸 맨 아래에 둔다.
    const art = spread.art ? `<div class="after-art">${artFrame(spread.art, AFTERWORD.emoji)}</div>` : '';
    const foot = spread.last ? `<p class="after-home"><a class="home-btn" href="../../../../../">학습 허브로 돌아가기</a></p>` : '';
    const col = (ps) => ps.map(t => `<p>${t}</p>`).join('');
    return `
        <div class="page page-after">
            <div class="after-col after-col-left">
                ${head}
                ${col(spread.left)}
            </div>
            <div class="after-col after-col-right${spread.art ? ' after-col-image' : ''}">
                ${art}
                ${col(spread.right)}
                ${foot}
            </div>
        </div>`;
}

const PAGES = [
    { kind: 'cover' },
    ...CHAPTERS.flatMap(chapter => chapter.beats.map((beat, i) => ({ kind: 'spread', chapter, beat, isFirst: i === 0 }))),
    { kind: 'quiz' },
    ...AFTERWORD.spreads.map((spread, i) => ({ kind: 'after', spread, isFirst: i === 0, last: i === AFTERWORD.spreads.length - 1 })),
];

const TWO_PAGE_KINDS = new Set(['spread', 'cover', 'after']);

let folioCounter = 0;
const FOLIOS = PAGES.map(p => {
    const width = TWO_PAGE_KINDS.has(p.kind) ? 2 : 1;
    const start = folioCounter + 1;
    folioCounter += width;
    return { start, width };
});

function renderPage(page) {
    switch (page.kind) {
        case 'cover':
            return coverPage();
        case 'spread':
            return spreadPage(page.chapter, page.beat, page.isFirst);
        case 'after':
            return afterPage({ ...page.spread, last: page.last }, page.isFirst);
        case 'quiz':
            return quizPage();
        default:
            return '';
    }
}

let current = 0;
let animating = false;

const spreadEl = document.getElementById('spread');
const prevBtn = document.getElementById('prevBtn');
const nextBtn = document.getElementById('nextBtn');
const indicatorEl = document.getElementById('pageIndicator');
const folioLeftEl = document.getElementById('folioLeft');
const folioRightEl = document.getElementById('folioRight');

function paint() {
    spreadEl.innerHTML = renderPage(PAGES[current]);
    prevBtn.disabled = current === 0;
    nextBtn.disabled = current === PAGES.length - 1;
    indicatorEl.textContent = `${current + 1} / ${PAGES.length}`;

    const folio = FOLIOS[current];
    folioLeftEl.classList.toggle('folio-center', folio.width === 1);
    if (folio.width === 2) {
        folioLeftEl.textContent = folio.start;
        folioRightEl.textContent = folio.start + 1;
        folioLeftEl.hidden = false;
        folioRightEl.hidden = false;
    } else {
        folioLeftEl.textContent = folio.start;
        folioLeftEl.hidden = false;
        folioRightEl.hidden = true;
    }

    spreadEl.querySelectorAll('[data-goto]').forEach(btn => {
        btn.addEventListener('click', () => {
            const chapterNum = Number(btn.dataset.goto);
            const idx = PAGES.findIndex(p => p.kind === 'spread' && p.chapter.num === chapterNum && p.isFirst);
            if (idx >= 0) goTo(idx);
        });
    });
    spreadEl.querySelectorAll('[data-goto-kind]').forEach(btn => {
        btn.addEventListener('click', () => {
            const idx = PAGES.findIndex(p => p.kind === btn.dataset.gotoKind);
            if (idx >= 0) goTo(idx);
        });
    });

    if (PAGES[current].kind === 'quiz') {
        initQuiz();
    }
}

function initQuiz() {
    let answeredCount = 0;
    const progressEl = document.getElementById('quizProgress');

    spreadEl.querySelectorAll('.quiz-item').forEach(item => {
        const qi = Number(item.dataset.qindex);
        const q = QUIZ[qi];
        item.querySelectorAll('.quiz-choice').forEach(btn => {
            btn.addEventListener('click', () => {
                if (item.classList.contains('graded')) return;
                item.classList.add('graded');
                const chosen = Number(btn.dataset.choice);
                item.querySelectorAll('.quiz-choice').forEach(b => {
                    const ci = Number(b.dataset.choice);
                    if (ci === q.answer) b.classList.add('correct');
                    else if (ci === chosen) b.classList.add('incorrect');
                });
                answeredCount++;
                progressEl.textContent = `${answeredCount} / 총 ${QUIZ.length}문항 완료`;
            });
        });
    });
}

function goTo(index) {
    if (animating || index === current || index < 0 || index >= PAGES.length) return;
    animating = true;
    const dir = index > current ? 'flip-next' : 'flip-prev';
    spreadEl.classList.add(dir);
    setTimeout(() => {
        current = index;
        paint();
    }, 230);
    setTimeout(() => {
        spreadEl.classList.remove('flip-next', 'flip-prev');
        animating = false;
    }, 480);
}

prevBtn.addEventListener('click', () => goTo(current - 1));
nextBtn.addEventListener('click', () => goTo(current + 1));

document.getElementById('tocLink').addEventListener('click', () => {
    // 그림책은 차례가 표지에 붙어 있다.
    const idx = PAGES.findIndex(p => p.kind === 'toc');
    goTo(idx >= 0 ? idx : 0);
});

document.addEventListener('keydown', (e) => {
    if (e.key === 'ArrowRight') goTo(current + 1);
    if (e.key === 'ArrowLeft') goTo(current - 1);
});

paint();
