const CHAPTERS = [
    {
        num: 1,
        title: "1장 · 밭에서 나온 항아리",
        beats: [
            {
                art: "01-dig.webp",
                emoji: "🏺",
                left: [
                    "옛날 어느 마을에 가난한 농부 부부가 살았습니다. 논도 밭도 손바닥만 했지요.",
                    "부지런하기로는 마을에서 으뜸이었지요.",
                    "봄이면 씨를 뿌리고 가을이면 거두었지만 늘 곳간이 비어 있었어요.",
                    "어느 봄날이었어요.",
                    "\"올해는 좀 나으려나.\""
                ],
                right: [
                    "쨍!",
                    "호미 날에 무언가 단단한 것이 부딪혔지요.",
                    "돌인가 싶어 흙을 헤쳐 보았어요.",
                    "커다란 항아리가 통째로 묻혀 있었답니다. 어른 하나가 들어가고도 남을 만큼 컸지요.",
                    "농부는 흙을 다 걷어 내고 한참을 들여다보았답니다."
                ]
            },
            {
                art: "02-hoe.webp",
                emoji: "😮",
                left: [
                    "\"쓸 만한 항아리로구먼.\"",
                    "흙을 털어 내고 이리저리 살펴보았지요. 농부는 항아리를 지게에 얹어 집으로 지고 왔어요.",
                    "그런데 마당에 내려놓다가 그만 호미를 항아리 속에 툭 빠뜨렸지요.",
                    "\"어이쿠, 이걸 어쩌나.\""
                ],
                right: [
                    "농부는 손을 쑥 넣어 호미를 꺼냈습니다.",
                    "그런데 항아리 속에 호미가 또 있는 게 아니겠어요?",
                    "농부는 제 눈을 의심했어요.",
                    "\"어라?\"",
                    "꺼내고 또 꺼내도 호미가 계속 나왔답니다. 마당에 호미가 수북이 쌓였지요."
                ]
            },
            {
                art: "03-coin.webp",
                emoji: "🪙",
                left: [
                    "\"여보! 이것 좀 봐요!\"",
                    "농부가 목청껏 아내를 불렀어요.",
                    "아내가 부엌에서 한달음에 뛰어나왔어요.",
                    "아내는 마당을 보고 입을 딱 벌렸지요.",
                    "농부가 엽전 한 닢을 넣어 보았지요.",
                    "꺼내니 두 닢. 또 꺼내니 세 닢."
                ],
                right: [
                    "\"이게 무슨 일이래!\"",
                    "아내가 두 손으로 입을 막았어요.",
                    "엽전은 꺼내도 꺼내도 끝이 없었습니다. 마당에 짤랑짤랑 소리가 가득했지요.",
                    "부부는 밤새 엽전을 세다가 그만 웃음을 터뜨렸답니다.",
                    "\"이제 굶을 일은 없겠구려.\""
                ]
            }
        ]
    },
    {
        num: 2,
        title: "2장 · 항아리를 빼앗은 부자",
        beats: [
            {
                art: "04-rich.webp",
                emoji: "🏡",
                left: [
                    "몇 달 지나지 않아 농부의 집은 몰라보게 달라졌어요.",
                    "초가지붕을 걷고 기와를 얹었지요. 곳간을 채우고 소도 두 마리 들였습니다.",
                    "부부는 이웃에게도 쌀을 나누어 주었어요.",
                    "누구도 그 집을 미워하지 않았지요. 마당에는 웃음소리가 그치지 않았답니다."
                ],
                right: [
                    "\"저 집이 갑자기 왜 저리 잘살지?\"",
                    "담 너머로 기웃거리는 사람이 부쩍 늘었어요.",
                    "\"밭에서 요술 항아리를 캤다지 뭔가.\"",
                    "소문은 금세 온 마을에 쫙 퍼졌답니다. 손바닥으로 하늘을 가릴 수는 없는 일이었지요."
                ]
            },
            {
                art: "05-seize.webp",
                emoji: "😤",
                left: [
                    "소문은 마을에서 제일 욕심 많은 부자 귀에도 들어갔어요.",
                    "부자는 벌써 곳간이 셋이나 있었어요. 그런데도 남의 것이 탐났지요.",
                    "부자는 밥을 먹다 말고 숟가락을 던지듯 놓았어요.",
                    "그러고는 곧장 농부의 집으로 달려왔습니다."
                ],
                right: [
                    "\"그 밭은 본래 우리 조상 땅이다!\"",
                    "부자가 마당에 들어서며 소리쳤어요.",
                    "\"그러니 거기서 나온 항아리도 내 것이지!\"",
                    "농부가 뭐라 말할 틈도 없었어요. 부자는 항아리를 번쩍 안고 가 버렸답니다.",
                    "농부 내외는 그 자리에 주저앉고 말았답니다."
                ]
            },
            {
                art: "06-gold.webp",
                emoji: "💰",
                left: [
                    "부자는 방문을 걸어 잠그고 항아리 앞에 딱 앉았습니다. 식구한테도 아무도 못 보게 하려는 것이었어요.",
                    "이번에는 엽전 따위를 넣지 않았어요.",
                    "금덩이 하나를 넣고 꺼내 보니 두 개.",
                    "부자는 두 손을 부들부들 떨었지요."
                ],
                right: [
                    "또 꺼내니 세 개.",
                    "금덩이가 마루에 산처럼 쌓였어요.",
                    "\"하하! 이제 내가 이 나라 제일 부자다!\"",
                    "부자는 팔이 아픈 줄도 모르고 꺼내고 또 꺼냈어요. 밤이 새도록 그 짓을 했답니다.",
                    "곳간이 하루 만에 가득 찼답니다."
                ]
            }
        ]
    },
    {
        num: 3,
        title: "3장 · 마당 가득 아버지",
        beats: [
            {
                art: "07-father.webp",
                emoji: "👴",
                left: [
                    "그때 늙은 아버지가 문틈으로 안을 기웃거렸어요. 아들 방에서 밤새 이상한 소리가 났거든요.",
                    "\"얘야, 대체 무얼 하느냐?\"",
                    "\"아버지는 저리 가 계세요!\"",
                    "그래도 아버지는 궁금해서 견딜 수가 없었지요."
                ],
                right: [
                    "아버지가 문고리를 잡아당겼어요. 까치발을 들고 항아리 속을 들여다보았지요.",
                    "몸을 더 기울인 순간이었어요.",
                    "풍덩!",
                    "그만 항아리 속으로 빠지고 말았습니다.",
                    "아버지의 다리가 허공에서 버둥거렸지요."
                ]
            },
            {
                art: "08-two.webp",
                emoji: "😳",
                left: [
                    "부자는 허둥지둥 팔을 뻗어 아버지를 끌어냈어요. 아버지가 물에 빠진 사람처럼 헐떡였어요.",
                    "\"아이고, 아버지! 괜찮으세요?\"",
                    "아버지는 젖은 옷을 털며 헛기침을 했지요.",
                    "부자는 가슴을 쓸어내렸지요."
                ],
                right: [
                    "그런데 항아리 속에서 목소리가 또 들렸습니다. 분명 아버지 목소리였어요.",
                    "\"얘야, 나도 좀 꺼내 다오.\"",
                    "\"이, 이게 무슨 일이야!\"",
                    "부자의 얼굴이 하얗게 질렸어요.",
                    "부자는 떨리는 손으로 항아리 속을 들여다보았답니다."
                ]
            },
            {
                art: "09-many.webp",
                emoji: "👨‍👨‍👦‍👦",
                left: [
                    "꺼내고 꺼내도 아버지는 계속 나왔습니다.",
                    "하나를 꺼내면 또 하나가 나왔어요. 부자는 팔이 저리도록 아버지를 꺼냈지요.",
                    "어느새 마당이 아버지들로 가득 찼지요. 다들 똑같은 얼굴에 똑같은 옷차림이었어요.",
                    "\"얘야, 배고프다.\""
                ],
                right: [
                    "\"얘야, 나도 배고프다. 어서 밥을 다오.\"",
                    "아버지들이 저마다 손을 내밀며 다가왔어요.",
                    "\"얘야, 나도!\"",
                    "수십 명이 한꺼번에 부자를 불렀답니다. 부자는 귀를 막고 그 자리에 주저앉았지요.",
                    "\"이걸 어쩐담!\""
                ]
            },
            {
                art: "10-ruined.webp",
                emoji: "😵",
                left: [
                    "부자는 아버지들 밥을 대느라 곳간을 열었어요. 아버지가 수십 분이니 밥도 수십 그릇이었어요.",
                    "쌀가마가 하루가 다르게 줄어들었지요.",
                    "금덩이도 논밭도 하나둘 팔려 나갔습니다.",
                    "부자는 밥 짓느라 하루가 다 갔지요."
                ],
                right: [
                    "결국 부자는 그 항아리마저 팔아 버렸어요.",
                    "\"저 집은 효자가 났구먼. 아버지가 수십 분이니.\"",
                    "마을 사람들은 그 말을 주고받으며 한참을 웃었답니다.",
                    "부자는 그제야 농부를 찾아가 미안하다고 했답니다."
                ]
            }
        ],
        moral: "남의 것을 억지로 빼앗으면 그 안에 무엇이 들었는지도 모른 채 가져오게 돼요. 부자가 손에 넣은 것은 재물이 아니라 감당할 수 없는 짐이었답니다.",
        question: "가지고 싶은 것을 옳지 않은 방법으로 얻으려 한 적이 있나요?"
    }
];

function artFrame(src, emoji) {
    return `
        <div class="art-frame">
            <img src="images/${src}" alt="" onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';">
            <div class="art-fallback" style="display:none">${emoji}</div>
        </div>`;
}

function coverToc() {
    const item = s => `
        <button type="button" data-goto="${s.num}">
            <span class="toc-num">${s.num}</span>
            <span>${s.title.replace(/^\d+장 · /, '')}</span>
        </button>`;
    return `
        <nav class="cover-toc">
            <h2>차례</h2>
            ${CHAPTERS.map(item).join('')}
            <button type="button" data-goto-kind="quiz">
                <span class="toc-num">❓</span>
                <span>이야기 문제</span>
            </button>
        </nav>`;
}

function coverPage() {
    return `
        <div class="page page-cover">
            <div class="story-page-left story-page-left-full">
                ${artFrame('cover.webp', '🏺')}
            </div>
            <div class="story-page-right">
                <h1>요술 항아리</h1>
                <p>요술 항아리는 지은이가 없는 구전 설화예요. 넣기만 하면 끝없이 나오는 그릇을 우리말로 화수분이라고 하는데, 지금도 돈이나 물건이 끝없이 나올 때 화수분 같다고 말한답니다.</p>
                <p>이야기의 짜임은 세계 곳곳의 이야기와 닮았어요. 끝없이 나오는 그릇이나 맷돌을 욕심 많은 사람이 빼앗았다가 도리어 낭패를 본다는 뼈대지요. 북유럽에도 소금을 갈아 내는 맷돌 이야기가 전한답니다.</p>
                <p>우리 옛이야기에서 욕심 많은 부자는 늘 같은 방식으로 벌을 받아요. 힘으로 빼앗은 물건이 도리어 자기를 괴롭히는 것이지요. 혹부리 영감이나 흥부와 놀부도 같은 짜임이랍니다.</p>
                ${coverToc()}
            </div>
        </div>`;
}

function spreadPage(chapter, beat, isFirst) {
    const badgeHtml = isFirst ? `<div class="spread-chapter-badge">${chapter.title}</div>` : '';
    const leftHtml = beat.left.map(p => `<p>${p}</p>`).join('');
    const rightHtml = beat.right.map(p => `<p>${p}</p>`).join('');
    return `
        <div class="page page-story">
            <div class="spread-art">
                ${badgeHtml}
                ${artFrame(beat.art, beat.emoji)}
            </div>
            <div class="spread-text">
                <div class="spread-text-left">${leftHtml}</div>
                <div class="spread-text-right">${rightHtml}</div>
            </div>
        </div>`;
}


const QUIZ = [
    { q: "농부가 항아리에 처음 빠뜨린 것은 무엇인가요?", choices: ["엽전", "호미", "금덩이"], answer: 1 },
    { q: "항아리에 물건을 넣으면 어떻게 되었나요?", choices: ["사라져 버렸다", "금으로 변했다", "자꾸 늘어났다"], answer: 2 },
    { q: "부자가 소문을 듣고 한 일은 무엇인가요?", choices: ["숟가락을 던지듯 놓았다", "곳간을 열었다", "담 너머로 기웃거렸다"], answer: 0 },
    { q: "항아리 속에 빠진 사람은 누구였나요?", choices: ["농부의 아내", "마을 원님", "부자의 아버지"], answer: 2 },
    { q: "마당에 가득한 아버지들은 무어라 했나요?", choices: ["배고프다고 했다", "항아리를 깼다", "집을 나갔다"], answer: 0 },
    { q: "욕심 많은 부자는 결국 어떻게 되었나요?", choices: ["항아리를 팔았다", "재산을 다 썼다", "마을을 떠났다"], answer: 1 },
    {
        q: "이 책을 읽고 난 반응으로 알맞지 않은 것은 무엇인가요?",
        wide: true,
        choices: [
            "호미 한 자루가 수북이 쌓인 것을 보면, 처음 놀란 것은 금이 아니라 호미였구나.",
            "부자가 조상 땅이라며 항아리를 안고 가 버린 것을 보면, 힘 있는 쪽의 말이 곧 법이던 때였나 보다.",
            "농부 내외가 이웃에게도 쌀을 나누어 준 것을 보면, 없이 살아 본 사람이 나눌 줄도 아는구나.",
            "부자가 아버지들 밥을 대느라 곳간을 다 연 것을 보면, 끝내는 아버지를 잘 모신 셈이구나."
        ],
        answer: 3
    }
];

/* 보기는 책을 열 때마다 자리를 바꾼다. 답의 자리를 외워 버리면 문제가 아니게 된다.
   섞는 것은 그리는 차례뿐이고, 채점은 data-choice 에 담긴 원래 번호로 한다.
   한 번 정한 차례는 책을 닫을 때까지 그대로다. 쪽을 오갈 때마다 바뀌면 헷갈린다. */
function shuffledOrder(n) {
    const a = [...Array(n).keys()];
    for (let i = a.length - 1; i > 0; i--) {
        const j = Math.floor(Math.random() * (i + 1));
        [a[i], a[j]] = [a[j], a[i]];
    }
    return a;
}

const QUIZ_ORDER = QUIZ.map(q => shuffledOrder(q.choices.length));
/* 고른 것을 기억해 둔다. 쪽을 옮기면 문제 쪽이 통째로 다시 그려지는데,
   그때 표시가 사라지면 아이가 처음부터 다시 풀어야 한다. */
const QUIZ_PICKED = new Array(QUIZ.length).fill(null);
const QUIZ_WRONG = QUIZ.map(() => new Set());

function quizPage() {
    const items = QUIZ.map((item, i) => {
        const graded = QUIZ_PICKED[i] !== null;
        const cls = ci => (graded && ci === item.answer) ? ' correct'
            : (QUIZ_WRONG[i].has(ci) ? ' incorrect' : '');
        return `
        <div class="quiz-item${graded ? ' graded' : ''}" data-qindex="${i}">
            <p class="quiz-question">${i + 1}. ${item.q}</p>
            <div class="quiz-choices${item.wide ? ' quiz-choices-stack' : ''}">
                ${QUIZ_ORDER[i].map(ci => `<button type="button" class="quiz-choice${cls(ci)}" data-choice="${ci}">${item.choices[ci]}</button>`).join('')}
            </div>
        </div>`;
    }).join('');
    return `
        <div class="page page-quiz">
            <h2>이야기 문제</h2>
            <p class="quiz-intro-text" id="quizProgress">${QUIZ_PICKED.filter(v => v !== null).length} / 총 ${QUIZ.length}문항 완료</p>
            <div class="quiz-list">${items}</div>
        </div>`;
}


/* 읽고 나서 — 세계명작 트랙과 같은 형식이다. 동화틀은 쪽을 재서 나누지 않으므로
   펼침면마다 왼쪽·오른쪽 글을 손으로 나누어 둔다. */
const AFTERWORD = {
    title: '읽고 나서',
    emoji: '🏺',
    spreads: [
        {
            art: 'end.webp',
            left: [
                "무엇이든 넣으면 두 개가 되어 나오는 그릇 이야기는 여러 나라에 있습니다. 그리고 그런 이야기의 끝은 늘 비슷합니다. 좋은 것만 늘어나지는 않습니다.",
                "이 이야기가 고른 방법이 특히 매섭습니다. 늘어난 것이 재물이 아니라 사람이었습니다. 그것도 밥을 달라는 사람이었지요.",
                "농부는 항아리를 잃고도 크게 아쉬워하지 않았습니다. 처음부터 밭에서 나온 것이었으니까요."
            ],
            right: [
                "남의 것을 억지로 빼앗으면 그 안에 무엇이 들었는지도 모른 채 가져오게 됩니다. 부자가 손에 넣은 것은 재물이 아니라 감당할 수 없는 짐이었습니다.",
                "가지고 싶은 것을 옳지 않은 방법으로 얻으려 한 적이 있나요?"
            ]
        }
    ]
};

function afterPage(spread, isFirst) {
    const head = isFirst ? `<h2>${AFTERWORD.title}</h2>` : '';
    // 그림은 오른쪽 칸 맨 위 모서리에 꽉 붙인다. 학습 허브로 가는 길은 그 칸 맨 아래에 둔다.
    const art = spread.art ? `<div class="after-art">${artFrame(spread.art, AFTERWORD.emoji)}</div>` : '';
    const foot = spread.last ? `<p class="after-home"><a class="home-btn" href="../../../../../">학습 허브로 돌아가기</a></p>` : '';
    const col = (ps) => ps.map(t => `<p>${t}</p>`).join('');
    return `
        <div class="page page-after">
            <div class="after-col after-col-left">
                ${head}
                ${col(spread.left)}
            </div>
            <div class="after-col after-col-right${spread.art ? ' after-col-image' : ''}">
                ${art}
                ${col(spread.right)}
                ${foot}
            </div>
        </div>`;
}

const PAGES = [
    { kind: 'cover' },
    ...CHAPTERS.flatMap(chapter => chapter.beats.map((beat, i) => ({ kind: 'spread', chapter, beat, isFirst: i === 0 }))),
    { kind: 'quiz' },
    ...AFTERWORD.spreads.map((spread, i) => ({ kind: 'after', spread, isFirst: i === 0, last: i === AFTERWORD.spreads.length - 1 })),
];

const TWO_PAGE_KINDS = new Set(['spread', 'cover', 'after']);

let folioCounter = 0;
const FOLIOS = PAGES.map(p => {
    const width = TWO_PAGE_KINDS.has(p.kind) ? 2 : 1;
    const start = folioCounter + 1;
    folioCounter += width;
    return { start, width };
});

function renderPage(page) {
    switch (page.kind) {
        case 'cover':
            return coverPage();
        case 'spread':
            return spreadPage(page.chapter, page.beat, page.isFirst);
        case 'after':
            return afterPage({ ...page.spread, last: page.last }, page.isFirst);
        case 'quiz':
            return quizPage();
        default:
            return '';
    }
}

let current = 0;
let animating = false;

const spreadEl = document.getElementById('spread');
const prevBtn = document.getElementById('prevBtn');
const nextBtn = document.getElementById('nextBtn');
const indicatorEl = document.getElementById('pageIndicator');
const folioLeftEl = document.getElementById('folioLeft');
const folioRightEl = document.getElementById('folioRight');

function paint() {
    spreadEl.innerHTML = renderPage(PAGES[current]);
    prevBtn.disabled = current === 0;
    nextBtn.disabled = current === PAGES.length - 1;
    indicatorEl.textContent = `${current + 1} / ${PAGES.length}`;

    const folio = FOLIOS[current];
    folioLeftEl.classList.toggle('folio-center', folio.width === 1);
    if (folio.width === 2) {
        folioLeftEl.textContent = folio.start;
        folioRightEl.textContent = folio.start + 1;
        folioLeftEl.hidden = false;
        folioRightEl.hidden = false;
    } else {
        folioLeftEl.textContent = folio.start;
        folioLeftEl.hidden = false;
        folioRightEl.hidden = true;
    }

    spreadEl.querySelectorAll('[data-goto]').forEach(btn => {
        btn.addEventListener('click', () => {
            const chapterNum = Number(btn.dataset.goto);
            const idx = PAGES.findIndex(p => p.kind === 'spread' && p.chapter.num === chapterNum && p.isFirst);
            if (idx >= 0) goTo(idx);
        });
    });
    spreadEl.querySelectorAll('[data-goto-kind]').forEach(btn => {
        btn.addEventListener('click', () => {
            const idx = PAGES.findIndex(p => p.kind === btn.dataset.gotoKind);
            if (idx >= 0) goTo(idx);
        });
    });

    if (PAGES[current].kind === 'quiz') {
        initQuiz();
    }
    fitAfterword();
}

/* 「읽고 나서」는 글씨를 줄이지 않는다. 세로 화면에서는 끝그림을 접어 두는 것이
   기본이고(styles.css), 여기서 그림을 켜 보아 자리가 남을 때만 그대로 둔다.
   그래서 이 함수가 한 번도 못 돌아도 글이 잘리지는 않는다. */
function fitAfterword() {
    const page = spreadEl.querySelector('.page-after');
    if (!page) return;
    page.classList.add('after-roomy');
    const spills = [...page.querySelectorAll('.after-col')]
        .some(col => col.scrollHeight > col.clientHeight);
    if (spills) page.classList.remove('after-roomy');
}

/* 화면 크기가 바뀌면 다시 잰다. 크기가 한 박자 늦게 잡히는 곳이 있어
   그 자리에서 한 번, 잠시 뒤에 한 번 더 잰다. */
let refitTimer = 0;
function refitAfterword() {
    fitAfterword();
    clearTimeout(refitTimer);
    refitTimer = setTimeout(fitAfterword, 250);
}

window.addEventListener('resize', refitAfterword);
window.addEventListener('orientationchange', refitAfterword);
/* 화면을 돌렸을 때 resize 가 안 잡히는 곳이 있다. 책 상자 크기를 직접 지켜본다. */
if (window.ResizeObserver) {
    const bookEl = document.getElementById('book');
    if (bookEl) new ResizeObserver(refitAfterword).observe(bookEl);
}
if (document.fonts && document.fonts.ready) document.fonts.ready.then(fitAfterword);

function initQuiz() {
    const progressEl = document.getElementById('quizProgress');

    spreadEl.querySelectorAll('.quiz-item').forEach(item => {
        const qi = Number(item.dataset.qindex);
        const q = QUIZ[qi];
        item.querySelectorAll('.quiz-choice').forEach(btn => {
            btn.addEventListener('click', () => {
                if (item.classList.contains('graded')) return;
                const chosen = Number(btn.dataset.choice);
                /* 틀리면 그 보기만 빨갛게 남기고, 맞는 것을 고를 때까지 다시 고르게 한다.
                   답을 미리 보여 주면 아이가 생각할 자리가 사라진다. */
                if (chosen !== q.answer) {
                    btn.classList.add('incorrect');
                    QUIZ_WRONG[qi].add(chosen);
                    return;
                }
                btn.classList.add('correct');
                item.classList.add('graded');
                QUIZ_PICKED[qi] = chosen;
                progressEl.textContent = `${QUIZ_PICKED.filter(v => v !== null).length} / 총 ${QUIZ.length}문항 완료`;
            });
        });
    });
}

function goTo(index) {
    if (animating || index === current || index < 0 || index >= PAGES.length) return;
    animating = true;
    const dir = index > current ? 'flip-next' : 'flip-prev';
    spreadEl.classList.add(dir);
    setTimeout(() => {
        current = index;
        paint();
    }, 230);
    setTimeout(() => {
        spreadEl.classList.remove('flip-next', 'flip-prev');
        animating = false;
    }, 480);
}

prevBtn.addEventListener('click', () => goTo(current - 1));
nextBtn.addEventListener('click', () => goTo(current + 1));

document.getElementById('tocLink').addEventListener('click', () => {
    // 그림책은 차례가 표지에 붙어 있다.
    const idx = PAGES.findIndex(p => p.kind === 'toc');
    goTo(idx >= 0 ? idx : 0);
});

document.addEventListener('keydown', (e) => {
    if (e.key === 'ArrowRight') goTo(current + 1);
    if (e.key === 'ArrowLeft') goTo(current - 1);
});

paint();
