const CHAPTERS = [
    {
        num: 1,
        title: "1장 · 굴러떨어진 진주",
        beats: [
            {
                art: "01-guest.webp",
                emoji: "🏯",
                left: [
                    "해가 뉘엿뉘엿 넘어갈 무렵이었어요. 먼 길을 걸어온 나그네가 커다란 기와집 앞에서 걸음을 멈췄습니다.",
                    "지팡이에 몸을 기댄 채 한참을 망설이다가, 조심스레 문을 두드렸지요.",
                    "\"하룻밤만 묵어 갈 수 있겠습니까?\"",
                    "\"실례가 안 된다면 하루만 신세 지겠습니다.\""
                ],
                right: [
                    "삐거덕—",
                    "대문이 열리고 흰 수염 난 주인이 나왔어요.",
                    "\"들어오시오. 마침 잘 오셨소. 손님이 오면 보여 주려고 벼르던 것이 있거든.\"",
                    "\"저녁상도 차리던 참이오.\"",
                    "주인은 흐뭇하게 웃으며 나그네를 방으로 들였답니다."
                ]
            },
            {
                art: "02-pearl.webp",
                emoji: "🫧",
                left: [
                    "방 안에 등잔불이 켜졌어요. 주인이 장롱 깊숙이 넣어 둔 비단 주머니를 꺼내 왔습니다.",
                    "주머니 끈을 푸는 손끝이 어쩐지 들떠 있었지요.",
                    "\"이걸 남에게 보이는 건 오랜만이오.\"",
                    "\"자, 보시오.\""
                ],
                right: [
                    "손바닥 위에 하얀 구슬 하나가 놓였어요. 등잔불을 받아 은은하게 빛나는 진주였습니다.",
                    "\"어떻소? 이만한 것은 이 고을에 다시없을 게요.\"",
                    "나그네도 저도 모르게 몸을 앞으로 기울였어요.",
                    "\"참으로 곱습니다.\"",
                    "그때 마당에 있던 거위가 뒤뚱뒤뚱 방문 앞까지 걸어 들어왔어요."
                ]
            },
            {
                art: "03-swallow.webp",
                emoji: "🦢",
                left: [
                    "주인이 진주를 도로 주머니에 넣으려던 참이었어요.",
                    "그런데 그만 손끝이 미끄러졌지 뭐예요.",
                    "\"어이쿠!\"",
                    "데구루루—",
                    "진주가 방바닥을 굴러갔습니다."
                ],
                right: [
                    "거위가 냉큼 목을 쭉 뻗었어요.",
                    "꿀꺽.",
                    "나그네는 그만 숨이 멎는 것 같았어요.",
                    "눈 깜짝할 사이였습니다. 하필 그때 주인은 등을 돌리고 보따리를 뒤지고 있었지요.",
                    "그 모습을 본 것은 나그네 하나뿐이었답니다."
                ]
            }
        ]
    },
    {
        num: 2,
        title: "2장 · 아무 말도 하지 않은 나그네",
        beats: [
            {
                art: "04-accuse.webp",
                emoji: "😨",
                left: [
                    "\"어? 진주가 어디 갔지?\"",
                    "주인의 얼굴에서 웃음기가 사라졌어요.",
                    "주인이 방바닥을 손바닥으로 쓸기 시작했어요.",
                    "이불을 들추고, 서랍을 빼고, 문지방 밑까지 들여다보았지요."
                ],
                right: [
                    "그래도 나오지 않았습니다.",
                    "주인의 손이 뚝 멈췄어요. 그러고는 아주 천천히 고개를 들어 나그네를 보았지요.",
                    "\"이 방에 있던 사람은 당신뿐이오.\"",
                    "\"설마 당신이…\"",
                    "나그네의 등에 식은땀이 흘렀습니다."
                ]
            },
            {
                art: "05-silent.webp",
                emoji: "🤐",
                left: [
                    "\"내놓으시오. 지금 내놓으면 없던 일로 하겠소.\"",
                    "주인의 목소리가 점점 사나워졌어요.",
                    "\"저는 가지고 있지 않습니다.\"",
                    "\"그럼 뒤져도 좋소?\""
                ],
                right: [
                    "나그네는 순순히 두 팔을 벌렸어요. 소매를 털고 봇짐까지 풀어 보였지만 아무것도 나오지 않았지요.",
                    "그런데 나그네는 끝내 그 말을 하지 않았습니다.",
                    "거위가 삼켰다는 말이었어요.",
                    "말만 하면 풀려날 수 있었는데도요.",
                    "주인의 얼굴이 붉으락푸르락해졌습니다."
                ]
            },
            {
                art: "06-tied.webp",
                emoji: "🪢",
                left: [
                    "\"날이 밝는 대로 관아로 넘기겠소!\"",
                    "\"이 도둑놈을 단단히 묶어라!\"",
                    "주인은 하인을 불러 나그네를 마당 기둥에 묶게 했어요.",
                    "밤바람이 차가웠지요. 별이 총총한 마당에 등불 하나만 흔들렸습니다."
                ],
                right: [
                    "그때 나그네가 딱 한 가지를 청했어요.",
                    "\"저 거위를 제 옆에 매어 주십시오.\"",
                    "\"거위를? 별 이상한 사람 다 보겠군.\"",
                    "나그네는 그저 고개만 끄덕였어요.",
                    "하인은 고개를 갸웃하면서도 거위를 끌어다 옆 말뚝에 매었답니다."
                ]
            }
        ]
    },
    {
        num: 3,
        title: "3장 · 아침에 반짝인 것",
        beats: [
            {
                art: "07-night.webp",
                emoji: "🌙",
                left: [
                    "밤이 깊어 갔어요.",
                    "주인은 방 안에서 몇 번이나 등잔불을 돋우었지만 좀처럼 잠들지 못했습니다.",
                    "\"아무래도 이상하단 말이야.\"",
                    "마당의 나그네는 끝내 아무 소리도 내지 않았지요."
                ],
                right: [
                    "묶인 팔이 저려 왔어요. 손끝이 얼얼해졌지만 나그네는 이를 악물었습니다.",
                    "옆에 매인 거위가 날개 속에 목을 파묻고 꾸벅꾸벅 졸았어요.",
                    "\"조금만 참으면 된다.\"",
                    "나그네는 그 거위를 오래도록 바라보았답니다."
                ]
            },
            {
                art: "08-morning.webp",
                emoji: "✨",
                left: [
                    "이튿날 아침이었어요.",
                    "거위가 기지개를 켜며 뒤뚱뒤뚱 자리를 옮겼습니다.",
                    "\"저 거위가 왜 저기 있었지?\"",
                    "그러자 거위가 앉아 있던 자리에서 무언가 반짝했지요."
                ],
                right: [
                    "\"저게 뭐야?\"",
                    "주인이 다가가 몸을 숙였어요. 그러고는 그 자리에 얼어붙었습니다.",
                    "주인의 손이 부들부들 떨렸어요.",
                    "거위 똥 속에서 하얀 진주가 또르르 굴러 나온 것이었어요.",
                    "\"이, 이게…!\""
                ]
            },
            {
                art: "09-untie.webp",
                emoji: "🙇",
                left: [
                    "주인이 맨발로 뛰어나와 밧줄을 풀었어요.",
                    "\"여보시오! 여보시오!\"",
                    "손이 어찌나 떨리는지 매듭이 자꾸 미끄러졌습니다.",
                    "\"내가 큰 잘못을 했소. 정말 미안하오.\""
                ],
                right: [
                    "나그네는 저린 팔을 천천히 주물렀어요.",
                    "주인이 견디다 못해 물었습니다.",
                    "주인은 도무지 알 수가 없었어요.",
                    "\"그런데 어째서 진작 말하지 않았소?\"",
                    "\"거위가 삼켰다고 한마디만 했으면 될 것을!\""
                ]
            },
            {
                art: "10-answer.webp",
                emoji: "🕊️",
                left: [
                    "나그네가 마당의 거위를 가만히 바라보았어요.",
                    "거위는 아무것도 모른 채 물그릇에 부리를 담그고 있었지요.",
                    "나그네의 목소리가 아주 조용했어요.",
                    "\"그 말을 했더라면요.\""
                ],
                right: [
                    "\"어젯밤에 저 거위의 배를 갈랐겠지요.\"",
                    "주인이 숨을 들이켰습니다.",
                    "\"저는 하룻밤 묶여 있으면 그만이지만요.\"",
                    "\"진주 한 알에 목숨을 빼앗을 수는 없지 않습니까.\"",
                    "주인은 그 자리에 고개를 숙이고 말았답니다."
                ]
            }
        ],
        moral: "나그네는 자기가 억울한 것보다 거위의 목숨을 먼저 생각했어요. 참는다는 것은 그냥 견디는 것이 아니라, 무엇을 지키려고 견디는지 아는 일이랍니다.",
        question: "억울해도 참는 것이 나은 때가 있을까요? 그때 나는 무엇을 지키려는 걸까요?"
    }
];

function artFrame(src, emoji) {
    return `
        <div class="art-frame">
            <img src="images/${src}" alt="" onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';">
            <div class="art-fallback" style="display:none">${emoji}</div>
        </div>`;
}

function coverToc() {
    const item = s => `
        <button type="button" data-goto="${s.num}">
            <span class="toc-num">${s.num}</span>
            <span>${s.title.replace(/^\d+장 · /, '')}</span>
        </button>`;
    return `
        <nav class="cover-toc">
            <h2>차례</h2>
            ${CHAPTERS.map(item).join('')}
            <button type="button" data-goto-kind="quiz">
                <span class="toc-num">❓</span>
                <span>이야기 문제</span>
            </button>
        </nav>`;
}

function coverPage() {
    return `
        <div class="page page-cover">
            <div class="story-page-left story-page-left-full">
                ${artFrame('cover.webp', '🦢')}
            </div>
            <div class="story-page-right">
                <h1>진주를 삼킨 거위</h1>
                <p>진주를 삼킨 거위는 지은이가 없는 구전 설화예요. 불교와 함께 전해진 이야기로 보는 학자가 많아, 절에서 스님이 들려주던 이야기로도 전한답니다.</p>
                <p>인도와 중국에도 이와 아주 닮은 이야기가 있어요. 이야기가 나라를 건너오면 등장인물과 배경이 그 나라 것으로 바뀌지요. 같은 뼈대에 우리 옷을 입혀 놓은 셈이랍니다.</p>
                <p>거위는 옛날 시골집 마당에서 흔히 기르던 새예요. 낯선 사람이 오면 요란하게 울어서 개 대신 집을 지키는 노릇도 했지요. 하필 거위가 진주를 삼킨 것도, 마당을 늘 어슬렁거리던 새였기 때문이랍니다.</p>
                ${coverToc()}
            </div>
        </div>`;
}

function spreadPage(chapter, beat, isFirst) {
    const badgeHtml = isFirst ? `<div class="spread-chapter-badge">${chapter.title}</div>` : '';
    const leftHtml = beat.left.map(p => `<p>${p}</p>`).join('');
    const rightHtml = beat.right.map(p => `<p>${p}</p>`).join('');
    return `
        <div class="page page-story">
            <div class="spread-art">
                ${badgeHtml}
                ${artFrame(beat.art, beat.emoji)}
            </div>
            <div class="spread-text">
                <div class="spread-text-left">${leftHtml}</div>
                <div class="spread-text-right">${rightHtml}</div>
            </div>
        </div>`;
}


const QUIZ = [
    { q: "진주를 삼킨 것은 무엇인가요?", choices: ["오리", "거위", "닭"], answer: 1 },
    { q: "주인은 누구를 의심했나요?", choices: ["이웃집에 사는 이", "집안에서 일하는 이", "묵어 가던 나그네"], answer: 2 },
    { q: "나그네가 주인에게 청한 것은 무엇인가요?", choices: ["거위를 옆에 매기", "밥을 달라는 것", "풀어 달라는 것"], answer: 0 },
    { q: "다음 날 아침 진주는 어디에서 나왔나요?", choices: ["마당 구석에서", "방 안 서랍에서", "거위의 똥에서"], answer: 2 },
    { q: "나그네는 왜 사실을 말하지 않았나요?", choices: ["거위가 죽을까 봐", "믿지 않을까 봐", "벌이 무서워서"], answer: 0 },
    { q: "나그네는 밤새 어디에 있었나요?", choices: ["따뜻한 방 안에", "마당 기둥에 묶여", "헛간 짚더미에"], answer: 1 }
];

function quizPage() {
    const items = QUIZ.map((item, i) => `
        <div class="quiz-item" data-qindex="${i}">
            <p class="quiz-question">${i + 1}. ${item.q}</p>
            <div class="quiz-choices">
                ${item.choices.map((c, ci) => `<button type="button" class="quiz-choice" data-choice="${ci}">${c}</button>`).join('')}
            </div>
        </div>`).join('');
    return `
        <div class="page page-quiz">
            <h2>이야기 문제</h2>
            <p class="quiz-intro-text" id="quizProgress">0 / 총 ${QUIZ.length}문항 완료</p>
            <div class="quiz-list">${items}</div>
        </div>`;
}


/* 읽고 나서 — 세계명작 트랙과 같은 형식이다. 동화틀은 쪽을 재서 나누지 않으므로
   펼침면마다 왼쪽·오른쪽 글을 손으로 나누어 둔다. */
const AFTERWORD = {
    title: '읽고 나서',
    emoji: '🪿',
    spreads: [
        {
            art: 'end.webp',
            left: [
                "이 이야기는 우리 옛이야기이면서 불교 이야기이기도 합니다. 스님이 남의 의심을 받고도 짐승 하나를 살리려고 입을 다무는 이야기가 여러 나라에 전해 옵니다.",
                "그런 이야기에서 억울함을 푸는 방법은 늘 같습니다. 말하지 않고 기다립니다. 그러면 시간이 지나 사실이 저절로 드러납니다.",
                "다시 보면 나그네가 참은 것은 자기가 억울해서가 아닙니다. 지금 말하면 거위의 배를 가를 것이기 때문입니다.",
                "묶여 있는 동안 나그네가 한 번도 거위를 원망하지 않은 것도 다시 보십시오."
            ],
            right: [
                "나그네는 자기가 억울한 것보다 거위의 목숨을 먼저 생각했습니다. 참는다는 것은 그냥 견디는 것이 아니라, 무엇을 지키려고 견디는지 아는 일입니다.",
                "억울해도 참는 것이 나은 때가 있을까요? 그때 나는 무엇을 지키려는 걸까요?"
            ]
        }
    ]
};

function afterPage(spread, isFirst) {
    const head = isFirst ? `<h2>${AFTERWORD.title}</h2>` : '';
    // 그림은 오른쪽 칸 맨 위 모서리에 꽉 붙인다. 학습 허브로 가는 길은 그 칸 맨 아래에 둔다.
    const art = spread.art ? `<div class="after-art">${artFrame(spread.art, AFTERWORD.emoji)}</div>` : '';
    const foot = spread.last ? `<p class="after-home"><a class="home-btn" href="../../../../../">학습 허브로 돌아가기</a></p>` : '';
    const col = (ps) => ps.map(t => `<p>${t}</p>`).join('');
    return `
        <div class="page page-after">
            <div class="after-col after-col-left">
                ${head}
                ${col(spread.left)}
            </div>
            <div class="after-col after-col-right${spread.art ? ' after-col-image' : ''}">
                ${art}
                ${col(spread.right)}
                ${foot}
            </div>
        </div>`;
}

const PAGES = [
    { kind: 'cover' },
    ...CHAPTERS.flatMap(chapter => chapter.beats.map((beat, i) => ({ kind: 'spread', chapter, beat, isFirst: i === 0 }))),
    { kind: 'quiz' },
    ...AFTERWORD.spreads.map((spread, i) => ({ kind: 'after', spread, isFirst: i === 0, last: i === AFTERWORD.spreads.length - 1 })),
];

const TWO_PAGE_KINDS = new Set(['spread', 'cover', 'after']);

let folioCounter = 0;
const FOLIOS = PAGES.map(p => {
    const width = TWO_PAGE_KINDS.has(p.kind) ? 2 : 1;
    const start = folioCounter + 1;
    folioCounter += width;
    return { start, width };
});

function renderPage(page) {
    switch (page.kind) {
        case 'cover':
            return coverPage();
        case 'spread':
            return spreadPage(page.chapter, page.beat, page.isFirst);
        case 'after':
            return afterPage({ ...page.spread, last: page.last }, page.isFirst);
        case 'quiz':
            return quizPage();
        default:
            return '';
    }
}

let current = 0;
let animating = false;

const spreadEl = document.getElementById('spread');
const prevBtn = document.getElementById('prevBtn');
const nextBtn = document.getElementById('nextBtn');
const indicatorEl = document.getElementById('pageIndicator');
const folioLeftEl = document.getElementById('folioLeft');
const folioRightEl = document.getElementById('folioRight');

function paint() {
    spreadEl.innerHTML = renderPage(PAGES[current]);
    prevBtn.disabled = current === 0;
    nextBtn.disabled = current === PAGES.length - 1;
    indicatorEl.textContent = `${current + 1} / ${PAGES.length}`;

    const folio = FOLIOS[current];
    folioLeftEl.classList.toggle('folio-center', folio.width === 1);
    if (folio.width === 2) {
        folioLeftEl.textContent = folio.start;
        folioRightEl.textContent = folio.start + 1;
        folioLeftEl.hidden = false;
        folioRightEl.hidden = false;
    } else {
        folioLeftEl.textContent = folio.start;
        folioLeftEl.hidden = false;
        folioRightEl.hidden = true;
    }

    spreadEl.querySelectorAll('[data-goto]').forEach(btn => {
        btn.addEventListener('click', () => {
            const chapterNum = Number(btn.dataset.goto);
            const idx = PAGES.findIndex(p => p.kind === 'spread' && p.chapter.num === chapterNum && p.isFirst);
            if (idx >= 0) goTo(idx);
        });
    });
    spreadEl.querySelectorAll('[data-goto-kind]').forEach(btn => {
        btn.addEventListener('click', () => {
            const idx = PAGES.findIndex(p => p.kind === btn.dataset.gotoKind);
            if (idx >= 0) goTo(idx);
        });
    });

    if (PAGES[current].kind === 'quiz') {
        initQuiz();
    }
}

function initQuiz() {
    let answeredCount = 0;
    const progressEl = document.getElementById('quizProgress');

    spreadEl.querySelectorAll('.quiz-item').forEach(item => {
        const qi = Number(item.dataset.qindex);
        const q = QUIZ[qi];
        item.querySelectorAll('.quiz-choice').forEach(btn => {
            btn.addEventListener('click', () => {
                if (item.classList.contains('graded')) return;
                item.classList.add('graded');
                const chosen = Number(btn.dataset.choice);
                item.querySelectorAll('.quiz-choice').forEach(b => {
                    const ci = Number(b.dataset.choice);
                    if (ci === q.answer) b.classList.add('correct');
                    else if (ci === chosen) b.classList.add('incorrect');
                });
                answeredCount++;
                progressEl.textContent = `${answeredCount} / 총 ${QUIZ.length}문항 완료`;
            });
        });
    });
}

function goTo(index) {
    if (animating || index === current || index < 0 || index >= PAGES.length) return;
    animating = true;
    const dir = index > current ? 'flip-next' : 'flip-prev';
    spreadEl.classList.add(dir);
    setTimeout(() => {
        current = index;
        paint();
    }, 230);
    setTimeout(() => {
        spreadEl.classList.remove('flip-next', 'flip-prev');
        animating = false;
    }, 480);
}

prevBtn.addEventListener('click', () => goTo(current - 1));
nextBtn.addEventListener('click', () => goTo(current + 1));

document.getElementById('tocLink').addEventListener('click', () => {
    // 그림책은 차례가 표지에 붙어 있다.
    const idx = PAGES.findIndex(p => p.kind === 'toc');
    goTo(idx >= 0 ? idx : 0);
});

document.addEventListener('keydown', (e) => {
    if (e.key === 'ArrowRight') goTo(current + 1);
    if (e.key === 'ArrowLeft') goTo(current - 1);
});

paint();
