const CHAPTERS = [
    {
        num: 1,
        title: "1장 · 재로 꼰 새끼줄",
        beats: [
            {
                art: "01-boy.webp",
                emoji: "🧒",
                left: [
                    "옛날 어느 고을에 꾀 많기로 이름난 소년이 살았어요. 나이는 이제 열두 살이었지요.",
                    "일찍 부모를 여의고 삼촌 집에 얹혀살았어요. 집은 몹시 가난했지요.",
                    "글을 배운 적도 없었어요.",
                    "그런데도 어른 열이 못 푸는 것을 척척 풀어냈답니다."
                ],
                right: [
                    "장터에 나가면 어른들이 소년을 붙들었어요.",
                    "\"얘야, 이 셈 좀 맞춰 보아라.\"",
                    "\"우리 집 소가 어디로 갔는지도 알겠느냐?\"",
                    "소년은 그때마다 씩 웃고는 답을 내놓았어요.",
                    "\"머리 하나는 정말 비상하다니까.\"",
                    "마을 사람들이 혀를 내둘렀답니다."
                ]
            },
            {
                art: "02-envoy.webp",
                emoji: "📜",
                left: [
                    "어느 날 이웃 나라에서 사신이 찾아왔어요. 관아 마당에 관리들이 죽 늘어섰지요.",
                    "사신이 두루마리를 척 펼쳤어요. 두루마리가 제 키만큼 길었지요.",
                    "\"여기 세 가지 문제가 있소.\"",
                    "마당이 조용해졌어요."
                ],
                right: [
                    "\"사흘 안에 다 풀지 못하면, 이 나라에는 지혜로운 이가 없다고 하겠소.\"",
                    "사신의 목소리에 비웃음이 섞여 있었어요.",
                    "원님의 얼굴이 굳어졌어요. 신하들은 서로 눈치만 보았지요.",
                    "아무도 나서지 않았답니다."
                ]
            },
            {
                art: "03-first.webp",
                emoji: "🪢",
                left: [
                    "관리 하나가 목청을 가다듬고 첫 번째 문제를 읽었어요.",
                    "\"재로 꼰 새끼줄을 가져오시오.\"",
                    "읽은 관리도 무슨 소린지 몰라 두루마리를 다시 들여다보았어요.",
                    "재라니요. 손에 쥐면 바스러지는 그 재 말입니다."
                ],
                right: [
                    "관아 마당이 벌집 쑤신 듯 술렁였어요.",
                    "\"재로 새끼를 꼰다고?\"",
                    "\"재가 손에 잡히기나 하나!\"",
                    "\"저건 사람더러 하라는 소리가 아닐세.\"",
                    "관리들이 서로 얼굴만 쳐다보았어요. 아무도 손을 들지 못했답니다."
                ]
            },
            {
                art: "04-solve1.webp",
                emoji: "🔥",
                left: [
                    "이틀이 지나도록 아무도 답을 내지 못했어요. 원님은 밤새 잠을 이루지 못했지요.",
                    "그때 열두 살짜리가 짚 한 단을 안고 마당으로 들어섰어요.",
                    "\"저 꼬마가 뭘 한다고?\"",
                    "소년은 짚단을 마당 한가운데에 척 내려놓았지요."
                ],
                right: [
                    "소년은 짚을 아주 굵고 단단하게 꼬았어요. 그것을 쟁반에 얹고 불을 붙였습니다.",
                    "치이익— 불이 새끼를 따라 천천히 기어갔어요.",
                    "불이 꺼지자 새끼줄 모양 그대로 재가 남아 있었지요.",
                    "사람들이 우르르 모여들었답니다."
                ]
            }
        ]
    },
    {
        num: 2,
        title: "2장 · 아홉 번 굽은 구슬",
        beats: [
            {
                art: "05-second.webp",
                emoji: "🔵",
                left: [
                    "사신의 눈이 휘둥그레졌어요. 사신은 헛기침을 두어 번 했지요.",
                    "\"좋소. 그럼 두 번째 문제요.\"",
                    "처음보다 목소리가 한결 낮아져 있었지요. 비웃음도 가셨어요.",
                    "사신이 손바닥에 작고 매끄러운 구슬 하나를 올려놓았습니다."
                ],
                right: [
                    "구슬은 밤톨만 했어요. 겉이 반들반들해서 빛이 미끄러졌지요.",
                    "\"이 구슬 속 구멍은 아홉 번이나 굽어 있소.\"",
                    "\"여기에 실을 꿰어 보시오.\"",
                    "사신이 실 한 오라기를 내밀었어요.",
                    "실은 머리카락처럼 가늘었답니다."
                ]
            },
            {
                art: "06-stuck.webp",
                emoji: "😖",
                left: [
                    "어른들이 번갈아 달려들었어요. 누구도 물러서지 않았지요.",
                    "실을 밀어 넣어 보고, 가는 대를 넣어 보고, 입으로 후 불어도 보았어요.",
                    "물에 적셔 넣는 사람도 있었지요.",
                    "하지만 실은 첫 굽이도 넘지 못했어요."
                ],
                right: [
                    "넣으면 도로 빠져나오고, 또 넣으면 또 빠져나왔어요. 해가 기울도록 그 짓만 되풀이했지요.",
                    "\"이건 사람이 할 수 있는 일이 아니오.\"",
                    "원님의 이마에 땀이 맺혔어요.",
                    "사신은 팔짱을 끼고 그 꼴을 지켜보았답니다."
                ]
            },
            {
                art: "07-ant.webp",
                emoji: "🐜",
                left: [
                    "소년은 마당 구석에 쪼그리고 앉았어요. 다들 소년이 포기한 줄 알았지요.",
                    "그런데 소년은 땅바닥만 들여다보고 있었어요.",
                    "그러더니 기어가던 개미 한 마리를 살며시 집어 들었습니다.",
                    "개미가 다리를 버둥거렸지요."
                ],
                right: [
                    "소년이 가느다란 실을 개미 허리에 조심조심 매었어요.",
                    "실이 개미 허리보다도 훨씬 가늘었지요.",
                    "\"쉿. 다들 숨도 쉬지 말고 가만있어 보시오.\"",
                    "사람들이 목을 길게 빼고 들여다보았어요. 숨소리도 나지 않았답니다."
                ]
            },
            {
                art: "08-honey.webp",
                emoji: "🍯",
                left: [
                    "소년은 구슬 한쪽 구멍에 꿀을 살짝 발랐어요.",
                    "그리고 반대쪽 구멍에 개미를 살그머니 놓아 주었습니다. 개미가 쏙 들어갔지요.",
                    "꿀 냄새가 구멍 속으로 솔솔 퍼졌어요.",
                    "하나, 둘, 셋… 소년이 손가락으로 헤아렸지요."
                ],
                right: [
                    "마당이 쥐 죽은 듯 조용해졌어요. 다들 구슬만 보았지요.",
                    "개미가 굽이를 하나 지나고, 둘 지나고, 셋 지났어요.",
                    "그때 반대쪽 구멍에서 까만 더듬이가 쏙 나왔습니다.",
                    "\"나왔다! 나왔어!\"",
                    "구슬에 실이 꿰여 있었답니다."
                ]
            }
        ]
    },
    {
        num: 3,
        title: "3장 · 통나무의 위아래",
        beats: [
            {
                art: "09-third.webp",
                emoji: "🪵",
                left: [
                    "사신의 얼굴이 붉어졌어요. 소년을 보는 눈빛이 아까와 달라져 있었지요.",
                    "\"마지막 문제요!\"",
                    "목소리가 아까보다 훨씬 컸지요. 지지 않겠다는 소리였어요.",
                    "사신이 껍질을 말끔히 벗긴 통나무 하나를 내놓았습니다."
                ],
                right: [
                    "어른이 두 팔로 안아야 할 만큼 굵은 나무였어요.",
                    "\"이 나무의 위와 아래를 가려 보시오.\"",
                    "굵기가 어디나 똑같아 도무지 알 수가 없었어요. 관리들이 나무를 이리 굴리고 저리 굴려 보았지요.",
                    "굴려 봐야 그게 그것이었답니다."
                ]
            },
            {
                art: "10-water.webp",
                emoji: "💧",
                left: [
                    "소년은 물을 가득 채운 통을 가져다 달라고 했어요.",
                    "장정 넷이 낑낑거리며 통을 들고 왔지요. 물이 찰랑찰랑 넘쳤어요.",
                    "소년이 그 통나무를 물속에 가만히 눕혔습니다.",
                    "모두가 숨을 죽이고 통나무만 들여다보았어요."
                ],
                right: [
                    "한참 뒤, 나무 한쪽이 슬며시 기울기 시작했어요.",
                    "천천히, 아주 천천히 기울었지요.",
                    "\"뿌리 쪽이 더 촘촘하고 무겁습니다.\"",
                    "\"가라앉는 쪽이 아래입니다.\"",
                    "소년이 그쪽을 손으로 가리켰어요. 사신의 입이 딱 벌어졌답니다."
                ]
            },
            {
                art: "11-bow.webp",
                emoji: "🙇",
                left: [
                    "사신은 한참 동안 아무 말도 하지 못했어요.",
                    "얼굴이 붉어졌다 하얘졌다 했지요. 손끝이 떨렸어요.",
                    "그러더니 열두 살짜리 앞에 정중히 고개를 숙였습니다.",
                    "관아 마당이 물을 끼얹은 듯 조용해졌지요. 다들 숨을 죽였어요."
                ],
                right: [
                    "\"이 나라에는 지혜로운 이가 있소.\"",
                    "사신의 목소리가 아까와 달리 아주 공손해졌어요.",
                    "\"돌아가 그대로 아뢰겠소.\"",
                    "관아 마당에 와— 하고 함성이 터졌어요. 원님이 달려와 소년의 손을 덥석 붙잡았답니다."
                ]
            },
            {
                art: "12-school.webp",
                emoji: "📖",
                left: [
                    "원님이 소년을 불렀어요. 금이든 비단이든 다 줄 참이었지요.",
                    "\"무엇이든 말해 보아라. 상을 내리마.\"",
                    "모두가 소년의 입을 쳐다보았어요. 무엇을 달라고 할지 궁금했거든요.",
                    "소년은 한참을 생각하다가 입을 열었지요."
                ],
                right: [
                    "\"마을에 서당을 하나 지어 주십시오.\"",
                    "원님이 눈을 크게 떴어요.",
                    "\"저 말고도 배우고 싶은 아이가 많거든요.\"",
                    "소년은 글을 배운 적이 없었어요. 그런데 상으로 글방을 달라고 했지요.",
                    "이듬해 봄, 그 마을에 서당이 섰답니다."
                ]
            }
        ],
        moral: "소년이 푼 세 문제는 힘으로도 재주로도 되지 않는 것이었어요. 다만 다르게 생각했을 뿐이지요. 안 된다고 여기는 순간 길이 막히고, 다른 쪽에서 보는 순간 길이 열린답니다.",
        question: "안 된다고 여겼던 일을 다른 방법으로 풀어 본 적이 있나요?"
    }
];

function artFrame(src, emoji) {
    return `
        <div class="art-frame">
            <img src="images/${src}" alt="" onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';">
            <div class="art-fallback" style="display:none">${emoji}</div>
        </div>`;
}

function coverToc() {
    const item = s => `
        <button type="button" data-goto="${s.num}">
            <span class="toc-num">${s.num}</span>
            <span>${s.title.replace(/^\d+장 · /, '')}</span>
        </button>`;
    return `
        <nav class="cover-toc">
            <h2>차례</h2>
            ${CHAPTERS.map(item).join('')}
            <button type="button" data-goto-kind="quiz">
                <span class="toc-num">❓</span>
                <span>이야기 문제</span>
            </button>
        </nav>`;
}

function coverPage() {
    return `
        <div class="page page-cover">
            <div class="story-page-left story-page-left-full">
                ${artFrame('cover.webp', '🧠')}
            </div>
            <div class="story-page-right">
                <h1>꾀 많은 소년</h1>
                <p>꾀 많은 소년은 지은이가 없는 구전 설화예요. 어른들이 쩔쩔매는 어려운 문제를 아이가 풀어내는 이야기 갈래에 속한답니다.</p>
                <p>이야기에 나오는 세 가지 문제는 우리나라 옛이야기에 오래도록 되풀이해 나오는 것들이에요. 재로 새끼줄 꼬기, 굽은 구슬에 실 꿰기, 통나무의 위아래 가리기. 다른 이야기에도 이 문제들이 하나씩 흩어져 나오는데, 이 책에서는 셋을 한자리에 모았답니다.</p>
                <p>굽은 구슬에 실을 꿰는 꾀는 옛 기록에도 남아 있어요. 개미 허리에 실을 매고 반대쪽 구멍에 꿀을 발라 두면 개미가 꿀 냄새를 따라 기어간다는 것이지요. 힘으로 되지 않는 일을 다른 생각으로 푸는 것이 이 이야기의 재미랍니다.</p>
                ${coverToc()}
            </div>
        </div>`;
}

function spreadPage(chapter, beat, isFirst) {
    const badgeHtml = isFirst ? `<div class="spread-chapter-badge">${chapter.title}</div>` : '';
    const leftHtml = beat.left.map(p => `<p>${p}</p>`).join('');
    const rightHtml = beat.right.map(p => `<p>${p}</p>`).join('');
    return `
        <div class="page page-story">
            <div class="spread-art">
                ${badgeHtml}
                ${artFrame(beat.art, beat.emoji)}
            </div>
            <div class="spread-text">
                <div class="spread-text-left">${leftHtml}</div>
                <div class="spread-text-right">${rightHtml}</div>
            </div>
        </div>`;
}


const QUIZ = [
    { q: "사신이 낸 첫 번째 문제는 무엇인가요?", choices: ["굽은 구슬에 실 꿰기", "재로 새끼줄 꼬기", "통나무 위아래 가리기"], answer: 1 },
    { q: "소년은 재 새끼줄을 어떻게 만들었나요?", choices: ["새끼를 재에 굴려서", "새끼에 잿물을 발라서", "꼰 새끼를 태워서"], answer: 2 },
    { q: "굽은 구슬에 실을 꿸 때 무엇을 썼나요?", choices: ["개미와 꿀", "가는 대나무", "짚 한 단"], answer: 0 },
    { q: "통나무의 위아래는 어떻게 가려냈나요?", choices: ["무게를 달아 보고", "껍질을 벗겨 보고", "물에 띄워 보고"], answer: 2 },
    { q: "사신은 마지막에 어떻게 했나요?", choices: ["정중히 고개를 숙였다", "두루마리를 펼쳤다", "헛기침을 했다"], answer: 0 },
    { q: "소년이 상으로 바란 것은 무엇인가요?", choices: ["금이나 비단 받기", "마을에 서당 짓기", "새끼줄 한 다발"], answer: 1 },
    {
        q: "이 책을 읽고 난 반응으로 알맞지 않은 것은 무엇인가요?",
        wide: true,
        choices: [
            "어른 여럿이 못 푼 것을 열두 살이 푼 것을 보면, 아는 것보다 달리 보는 것이 셀 때가 있구나.",
            "개미와 꿀을 쓴 것을 보면, 없는 것을 만들어 내기보다 있는 것을 잘 쓰는 편이구나.",
            "상으로 서당을 달라고 한 것을 보면, 제 몫보다 다음 사람을 먼저 생각했구나.",
            "소년이 서당에서 배운 것을 그대로 써서 세 문제를 푼 것을 보면, 배워 둔 것은 언젠가 쓰이는구나."
        ],
        answer: 3
    }
];

/* 보기는 책을 열 때마다 자리를 바꾼다. 답의 자리를 외워 버리면 문제가 아니게 된다.
   섞는 것은 그리는 차례뿐이고, 채점은 data-choice 에 담긴 원래 번호로 한다.
   한 번 정한 차례는 책을 닫을 때까지 그대로다. 쪽을 오갈 때마다 바뀌면 헷갈린다. */
function shuffledOrder(n) {
    const a = [...Array(n).keys()];
    for (let i = a.length - 1; i > 0; i--) {
        const j = Math.floor(Math.random() * (i + 1));
        [a[i], a[j]] = [a[j], a[i]];
    }
    return a;
}

const QUIZ_ORDER = QUIZ.map(q => shuffledOrder(q.choices.length));

function quizPage() {
    const items = QUIZ.map((item, i) => `
        <div class="quiz-item" data-qindex="${i}">
            <p class="quiz-question">${i + 1}. ${item.q}</p>
            <div class="quiz-choices${item.wide ? ' quiz-choices-stack' : ''}">
                ${QUIZ_ORDER[i].map(ci => `<button type="button" class="quiz-choice" data-choice="${ci}">${item.choices[ci]}</button>`).join('')}
            </div>
        </div>`).join('');
    return `
        <div class="page page-quiz">
            <h2>이야기 문제</h2>
            <p class="quiz-intro-text" id="quizProgress">0 / 총 ${QUIZ.length}문항 완료</p>
            <div class="quiz-list">${items}</div>
        </div>`;
}


/* 읽고 나서 — 세계명작 트랙과 같은 형식이다. 동화틀은 쪽을 재서 나누지 않으므로
   펼침면마다 왼쪽·오른쪽 글을 손으로 나누어 둔다. */
const AFTERWORD = {
    title: '읽고 나서',
    emoji: '🧠',
    spreads: [
        {
            art: 'end.webp',
            left: [
                "어려운 문제를 아이가 푸는 이야기는 세계 곳곳에 있습니다. 어른들이 다 막힌 자리에서 아이 하나가 답을 내는 틀입니다.",
                "그런 이야기에서 아이가 쓰는 것은 힘도 아니고 학식도 아닙니다. 문제를 낸 사람이 정해 놓은 틀 밖에서 보는 것입니다.",
                "다시 보면 재로 새끼를 꼬라는 문제는 풀 수 없는 문제입니다. 재는 손에 잡히지 않으니까요. 소년은 그 순서를 뒤집었습니다.",
                "새끼를 먼저 꼬고 나서 태운 것입니다. 문제는 그대로인데 하는 차례를 바꾼 것이지요."
            ],
            right: [
                "소년이 푼 세 문제는 힘으로도 재주로도 되지 않는 것이었습니다. 다만 다르게 생각했을 뿐입니다. 안 된다고 여기는 순간 길이 막히고, 다른 쪽에서 보는 순간 길이 열립니다.",
                "안 된다고 여겼던 일을 다른 방법으로 풀어 본 적이 있나요?"
            ]
        }
    ]
};

function afterPage(spread, isFirst) {
    const head = isFirst ? `<h2>${AFTERWORD.title}</h2>` : '';
    // 그림은 오른쪽 칸 맨 위 모서리에 꽉 붙인다. 학습 허브로 가는 길은 그 칸 맨 아래에 둔다.
    const art = spread.art ? `<div class="after-art">${artFrame(spread.art, AFTERWORD.emoji)}</div>` : '';
    const foot = spread.last ? `<p class="after-home"><a class="home-btn" href="../../../../../">학습 허브로 돌아가기</a></p>` : '';
    const col = (ps) => ps.map(t => `<p>${t}</p>`).join('');
    return `
        <div class="page page-after">
            <div class="after-col after-col-left">
                ${head}
                ${col(spread.left)}
            </div>
            <div class="after-col after-col-right${spread.art ? ' after-col-image' : ''}">
                ${art}
                ${col(spread.right)}
                ${foot}
            </div>
        </div>`;
}

const PAGES = [
    { kind: 'cover' },
    ...CHAPTERS.flatMap(chapter => chapter.beats.map((beat, i) => ({ kind: 'spread', chapter, beat, isFirst: i === 0 }))),
    { kind: 'quiz' },
    ...AFTERWORD.spreads.map((spread, i) => ({ kind: 'after', spread, isFirst: i === 0, last: i === AFTERWORD.spreads.length - 1 })),
];

const TWO_PAGE_KINDS = new Set(['spread', 'cover', 'after']);

let folioCounter = 0;
const FOLIOS = PAGES.map(p => {
    const width = TWO_PAGE_KINDS.has(p.kind) ? 2 : 1;
    const start = folioCounter + 1;
    folioCounter += width;
    return { start, width };
});

function renderPage(page) {
    switch (page.kind) {
        case 'cover':
            return coverPage();
        case 'spread':
            return spreadPage(page.chapter, page.beat, page.isFirst);
        case 'after':
            return afterPage({ ...page.spread, last: page.last }, page.isFirst);
        case 'quiz':
            return quizPage();
        default:
            return '';
    }
}

let current = 0;
let animating = false;

const spreadEl = document.getElementById('spread');
const prevBtn = document.getElementById('prevBtn');
const nextBtn = document.getElementById('nextBtn');
const indicatorEl = document.getElementById('pageIndicator');
const folioLeftEl = document.getElementById('folioLeft');
const folioRightEl = document.getElementById('folioRight');

function paint() {
    spreadEl.innerHTML = renderPage(PAGES[current]);
    prevBtn.disabled = current === 0;
    nextBtn.disabled = current === PAGES.length - 1;
    indicatorEl.textContent = `${current + 1} / ${PAGES.length}`;

    const folio = FOLIOS[current];
    folioLeftEl.classList.toggle('folio-center', folio.width === 1);
    if (folio.width === 2) {
        folioLeftEl.textContent = folio.start;
        folioRightEl.textContent = folio.start + 1;
        folioLeftEl.hidden = false;
        folioRightEl.hidden = false;
    } else {
        folioLeftEl.textContent = folio.start;
        folioLeftEl.hidden = false;
        folioRightEl.hidden = true;
    }

    spreadEl.querySelectorAll('[data-goto]').forEach(btn => {
        btn.addEventListener('click', () => {
            const chapterNum = Number(btn.dataset.goto);
            const idx = PAGES.findIndex(p => p.kind === 'spread' && p.chapter.num === chapterNum && p.isFirst);
            if (idx >= 0) goTo(idx);
        });
    });
    spreadEl.querySelectorAll('[data-goto-kind]').forEach(btn => {
        btn.addEventListener('click', () => {
            const idx = PAGES.findIndex(p => p.kind === btn.dataset.gotoKind);
            if (idx >= 0) goTo(idx);
        });
    });

    if (PAGES[current].kind === 'quiz') {
        initQuiz();
    }
    fitAfterword();
}

/* 「읽고 나서」는 글씨를 줄이지 않는다. 세로 화면에서는 끝그림을 접어 두는 것이
   기본이고(styles.css), 여기서 그림을 켜 보아 자리가 남을 때만 그대로 둔다.
   그래서 이 함수가 한 번도 못 돌아도 글이 잘리지는 않는다. */
function fitAfterword() {
    const page = spreadEl.querySelector('.page-after');
    if (!page) return;
    page.classList.add('after-roomy');
    const spills = [...page.querySelectorAll('.after-col')]
        .some(col => col.scrollHeight > col.clientHeight);
    if (spills) page.classList.remove('after-roomy');
}

/* 화면 크기가 바뀌면 다시 잰다. 크기가 한 박자 늦게 잡히는 곳이 있어
   그 자리에서 한 번, 잠시 뒤에 한 번 더 잰다. */
let refitTimer = 0;
function refitAfterword() {
    fitAfterword();
    clearTimeout(refitTimer);
    refitTimer = setTimeout(fitAfterword, 250);
}

window.addEventListener('resize', refitAfterword);
window.addEventListener('orientationchange', refitAfterword);
/* 화면을 돌렸을 때 resize 가 안 잡히는 곳이 있다. 책 상자 크기를 직접 지켜본다. */
if (window.ResizeObserver) {
    const bookEl = document.getElementById('book');
    if (bookEl) new ResizeObserver(refitAfterword).observe(bookEl);
}
if (document.fonts && document.fonts.ready) document.fonts.ready.then(fitAfterword);

function initQuiz() {
    let answeredCount = 0;
    const progressEl = document.getElementById('quizProgress');

    spreadEl.querySelectorAll('.quiz-item').forEach(item => {
        const qi = Number(item.dataset.qindex);
        const q = QUIZ[qi];
        item.querySelectorAll('.quiz-choice').forEach(btn => {
            btn.addEventListener('click', () => {
                if (item.classList.contains('graded')) return;
                item.classList.add('graded');
                const chosen = Number(btn.dataset.choice);
                item.querySelectorAll('.quiz-choice').forEach(b => {
                    const ci = Number(b.dataset.choice);
                    if (ci === q.answer) b.classList.add('correct');
                    else if (ci === chosen) b.classList.add('incorrect');
                });
                answeredCount++;
                progressEl.textContent = `${answeredCount} / 총 ${QUIZ.length}문항 완료`;
            });
        });
    });
}

function goTo(index) {
    if (animating || index === current || index < 0 || index >= PAGES.length) return;
    animating = true;
    const dir = index > current ? 'flip-next' : 'flip-prev';
    spreadEl.classList.add(dir);
    setTimeout(() => {
        current = index;
        paint();
    }, 230);
    setTimeout(() => {
        spreadEl.classList.remove('flip-next', 'flip-prev');
        animating = false;
    }, 480);
}

prevBtn.addEventListener('click', () => goTo(current - 1));
nextBtn.addEventListener('click', () => goTo(current + 1));

document.getElementById('tocLink').addEventListener('click', () => {
    // 그림책은 차례가 표지에 붙어 있다.
    const idx = PAGES.findIndex(p => p.kind === 'toc');
    goTo(idx >= 0 ? idx : 0);
});

document.addEventListener('keydown', (e) => {
    if (e.key === 'ArrowRight') goTo(current + 1);
    if (e.key === 'ArrowLeft') goTo(current - 1);
});

paint();
