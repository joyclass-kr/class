const CHAPTERS = [
    {
        num: 1,
        title: "1장 · 산에서 얻은 절구",
        beats: [
            {
                art: "01-mountain.webp",
                emoji: "🌫️",
                left: [
                    "옛날 어느 마을에 가난한 나무꾼이 살았습니다.",
                    "날마다 지게를 지고 산을 올랐지요.",
                    "그날도 나무꾼은 지게를 지고 깊은 산으로 들어갔어요.",
                    "웬일인지 마른 나뭇가지 하나 눈에 띄지 않았습니다.",
                    "\"오늘은 좀 많이 해야 할 텐데.\""
                ],
                right: [
                    "해 질 무렵, 안개 속에서 흰 수염 노인이 나타났어요.",
                    "나무꾼은 깜짝 놀라 뒤로 물러섰어요.",
                    "\"자네, 오늘 나무를 몇 짐이나 했나?\"",
                    "\"한 짐도 못 했습니다. 이대로 가면 식구들이 굶습니다.\"",
                    "나무꾼은 빈 지게를 내려놓고 고개를 숙였어요."
                ]
            },
            {
                art: "02-mortar.webp",
                emoji: "🪨",
                left: [
                    "노인은 품에서 작은 돌절구 하나를 꺼내 내밀었습니다.",
                    "돌절구는 어른 주먹만 했어요.",
                    "겉이 반들반들 윤이 났지요.",
                    "\"딱하구먼. 이것을 가져가게.\"",
                    "\"무엇이든 이름을 부르고 나와라, 하면 그대로 나온다네.\""
                ],
                right: [
                    "\"멈출 때는 그만, 하고 말하면 되고. 잊지 말게.\"",
                    "노인이 나무꾼의 손에 절구를 쥐여 주었어요.",
                    "나무꾼이 고맙다는 말을 하려고 고개를 들었어요.",
                    "그런데 노인은 이미 사라지고 없었답니다. 안개만 자욱했지요.",
                    "나무꾼은 한참을 서 있었답니다."
                ]
            },
            {
                art: "03-rice.webp",
                emoji: "🍚",
                left: [
                    "집에 돌아온 나무꾼은 절구를 방바닥 한가운데에 놓았어요.",
                    "아내와 아이들이 둘러앉았어요.",
                    "정말일까 싶어 조심스레 말해 보았지요.",
                    "\"설마 정말 나올까.\"",
                    "\"쌀아, 나와라.\"",
                    "그러자 절구에서 하얀 쌀이 콸콸 쏟아져 나왔습니다!"
                ],
                right: [
                    "방바닥이 온통 쌀로 덮였어요.",
                    "\"어이쿠, 그만! 그만!\"",
                    "쌀은 딱 멈췄어요.",
                    "나무꾼은 그 길로 이웃들을 불러 쌀을 나누어 주었습니다.",
                    "오랜만에 온 마을에 밥 짓는 연기가 피어올랐답니다."
                ]
            }
        ]
    },
    {
        num: 2,
        title: "2장 · 절구를 훔친 도둑",
        beats: [
            {
                art: "04-rumor.webp",
                emoji: "🗣️",
                left: [
                    "\"저 집 절구에서 쌀이 나온대!\"",
                    "\"부르기만 하면 뭐든 나온다지 뭐야.\"",
                    "처음에는 아무도 믿지 않았어요.",
                    "그런데 나무꾼네 굴뚝에서 날마다 연기가 났지요.",
                    "소문은 온 마을에, 그리고 옆 마을까지 쫙 퍼졌어요."
                ],
                right: [
                    "그 소문을 주막에 앉아 있던 도둑 하나가 들었지요.",
                    "도둑은 그날로 짐을 쌌어요.",
                    "'그 절구만 있으면 평생 놀고먹겠구나.'",
                    "도둑은 침을 꿀꺽 삼켰습니다. 그날부터 밤마다 나무꾼네 담 밖을 어슬렁거렸답니다.",
                    "개가 짖으면 얼른 몸을 숨겼지요."
                ]
            },
            {
                art: "05-steal.webp",
                emoji: "🌙",
                left: [
                    "달도 없는 캄캄한 밤이었어요.",
                    "온 식구가 깊이 잠든 뒤였어요.",
                    "도둑은 담을 훌쩍 넘었습니다. 살금살금.",
                    "마루가 삐걱 소리를 냈지요.",
                    "방문을 슬며시 열자 절구가 놓여 있었지요.",
                    "도둑은 절구를 자루에 담아 등에 지고 달아났답니다."
                ],
                right: [
                    "\"이제 나는 부자다!\"",
                    "도둑은 뒤도 안 돌아보고 바닷가로 내달렸어요.",
                    "이대로 육지에 있다가는 붙잡힐 것 같았거든요.",
                    "작은 배 한 척을 풀어 힘껏 노를 저었습니다.",
                    "손바닥이 다 벗겨지도록 저었지요."
                ]
            },
            {
                art: "06-salt.webp",
                emoji: "🧂",
                left: [
                    "배가 먼바다까지 나아갔을 때였어요.",
                    "뭍이 보이지 않을 만큼 멀리 나갔어요.",
                    "도둑은 절구를 꺼내 놓고 한참을 궁리했습니다.",
                    "'무엇을 달라고 할까? 쌀은 흔해서 값이 안 나가고…'",
                    "도둑은 절구를 무릎에 올려놓았지요."
                ],
                right: [
                    "\"옳지, 소금이다! 소금은 금값이지!\"",
                    "\"소금아, 나와라!\"",
                    "그러자 절구에서 하얀 소금이 콸콸 쏟아졌어요. 배 바닥에 소복이 쌓였지요.",
                    "도둑은 손뼉을 치며 껑충껑충 뛰었어요.",
                    "그것을 세어 보기까지 했답니다."
                ]
            }
        ]
    },
    {
        num: 3,
        title: "3장 · 바다가 짠 까닭",
        beats: [
            {
                art: "07-more.webp",
                emoji: "😰",
                left: [
                    "소금은 금세 배 바닥을 하얗게 덮었습니다.",
                    "도둑의 발목이 소금에 묻혔어요.",
                    "\"하하! 이만하면 됐다!\"",
                    "그런데 소금은 멈추지 않았어요. 쏟아지고 또 쏟아졌지요.",
                    "그제야 이상하다 싶었지요."
                ],
                right: [
                    "\"멈춰! 서라! 이러다 큰일 난다!\"",
                    "도둑이 절구를 두 손으로 붙들고 마구 흔들었어요.",
                    "바다에 던지려고도 했지요.",
                    "하지만 아무 소용이 없었습니다. 훔치기만 했지 멈추는 말은 듣지 못했거든요.",
                    "소금은 점점 더 빨리 쏟아졌지요."
                ]
            },
            {
                art: "08-sinking.webp",
                emoji: "🌊",
                left: [
                    "소금은 도둑의 무릎까지, 허리까지 차올랐어요.",
                    "배가 점점 낮아졌어요.",
                    "배가 기우뚱 기울기 시작했습니다.",
                    "뱃전으로 시커먼 바닷물이 넘어 들어왔지요.",
                    "도둑은 두 손으로 소금을 퍼냈지요."
                ],
                right: [
                    "\"살려 줘! 누가 좀 살려 줘!\"",
                    "파도가 뱃전을 넘어왔어요.",
                    "도둑은 목이 터져라 소리쳤어요.",
                    "하지만 먼바다 한가운데에는 아무도 없었답니다. 파도 소리만 철썩거렸지요.",
                    "소리는 파도에 묻혀 버렸답니다."
                ]
            },
            {
                art: "09-plank.webp",
                emoji: "🪵",
                left: [
                    "마침내 배가 스르르 가라앉았습니다.",
                    "뱃머리가 먼저 물속으로 사라졌어요.",
                    "도둑은 떠다니는 널빤지 하나를 겨우 붙잡았어요.",
                    "밤새도록 파도에 이리저리 떠밀려 다녔지요.",
                    "도둑은 죽을힘을 다해 널빤지에 매달렸지요."
                ],
                right: [
                    "이튿날 아침에야 겨우 뭍에 닿았습니다.",
                    "온몸이 소금에 절어 쓰라렸어요.",
                    "손에는 아무것도 남아 있지 않았어요.",
                    "절구도, 배도, 소금도 모두 바닷속이었지요.",
                    "도둑은 그제야 잘못을 뉘우쳤답니다."
                ]
            },
            {
                art: "10-sea.webp",
                emoji: "🌊",
                left: [
                    "절구는 바닷속 깊이깊이 가라앉았습니다.",
                    "아무도 그것을 건져 올릴 수 없었어요.",
                    "그런데 그 뒤로도 절구는 멈추지 않았어요.",
                    "아무도 그만, 하고 말해 주지 않았으니까요.",
                    "바닷속에는 아무도 없었으니까요."
                ],
                right: [
                    "지금도 저 깊은 바닷속에서 소금을 내고 있대요.",
                    "멈출 줄을 모른답니다.",
                    "콸콸, 콸콸.",
                    "바닷물이 그렇게 짠 것은 다 그 때문이라고 한답니다."
                ]
            }
        ],
        moral: "쓰는 법을 다 알지 못하면서 손에 넣기만 하면, 그 물건이 도리어 나를 삼켜요. 도둑은 나오게 하는 말만 알았을 뿐 멈추는 말은 몰랐답니다.",
        question: "잘 모르는 것을 서둘러 손에 넣으려다 곤란해진 적이 있나요?"
    }
];

function artFrame(src, emoji) {
    return `
        <div class="art-frame">
            <img src="images/${src}" alt="" onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';">
            <div class="art-fallback" style="display:none">${emoji}</div>
        </div>`;
}

function coverToc() {
    const item = s => `
        <button type="button" data-goto="${s.num}">
            <span class="toc-num">${s.num}</span>
            <span>${s.title.replace(/^\d+장 · /, '')}</span>
        </button>`;
    return `
        <nav class="cover-toc">
            <h2>차례</h2>
            ${CHAPTERS.map(item).join('')}
            <button type="button" data-goto-kind="quiz">
                <span class="toc-num">❓</span>
                <span>이야기 문제</span>
            </button>
        </nav>`;
}

function coverPage() {
    return `
        <div class="page page-cover">
            <div class="story-page-left story-page-left-full">
                ${artFrame('cover.webp', '🧂')}
            </div>
            <div class="story-page-right">
                <h1>신기한 절구</h1>
                <p>신기한 절구는 지은이가 없는 구전 설화예요. 신기한 맷돌, 소금 나오는 맷돌이라는 이름으로도 전해진답니다.</p>
                <p>이 이야기는 유래담이에요. 바닷물이 왜 짠지를 이야기로 설명하지요. 놀라운 것은 거의 같은 이야기가 아주 먼 나라에도 있다는 점이에요. 북유럽에는 그로티라는 맷돌 이야기가 있고, 일본에도 바닷속에서 소금을 갈아 내는 맷돌 이야기가 전해요. 서로 만난 적 없는 사람들이 같은 궁금증을 같은 방식으로 풀어낸 셈이지요.</p>
                <p>절구는 곡식의 껍질을 벗기거나 떡을 치는 데 쓰던 도구예요. 통나무나 돌을 파서 만든 통에 곡식을 넣고 공이로 쿵쿵 찧었지요. 집집마다 하나씩 있던 살림살이라 옛이야기에도 자주 나온답니다.</p>
                ${coverToc()}
            </div>
        </div>`;
}

function spreadPage(chapter, beat, isFirst) {
    const badgeHtml = isFirst ? `<div class="spread-chapter-badge">${chapter.title}</div>` : '';
    const leftHtml = beat.left.map(p => `<p>${p}</p>`).join('');
    const rightHtml = beat.right.map(p => `<p>${p}</p>`).join('');
    return `
        <div class="page page-story">
            <div class="spread-art">
                ${badgeHtml}
                ${artFrame(beat.art, beat.emoji)}
            </div>
            <div class="spread-text">
                <div class="spread-text-left">${leftHtml}</div>
                <div class="spread-text-right">${rightHtml}</div>
            </div>
        </div>`;
}


const QUIZ = [
    { q: "나무꾼은 절구를 누구에게 받았나요?", choices: ["이웃 부자", "산속의 노인", "마을 원님"], answer: 1 },
    { q: "절구에서 물건이 나오게 하는 말은 무엇이었나요?", choices: ["멈춰라", "돌아라", "나와라"], answer: 2 },
    { q: "도둑은 절구를 훔쳐 어디로 갔나요?", choices: ["배를 타고 바다로", "고개 너머 산속으로", "강 건너 이웃 마을로"], answer: 0 },
    { q: "도둑은 절구에게 무엇을 달라고 했나요?", choices: ["쌀", "금", "소금"], answer: 2 },
    { q: "도둑이 절구를 멈추지 못한 까닭은 무엇인가요?", choices: ["멈추는 말을 몰라서", "절구가 고장 나서", "파도가 너무 세서"], answer: 0 },
    { q: "바닷속에 가라앉은 뒤에도 절구는 어떻게 되었나요?", choices: ["널빤지에 실려 갔다", "도둑이 건져 갔다", "멈출 줄을 모른다"], answer: 2 }
];

function quizPage() {
    const items = QUIZ.map((item, i) => `
        <div class="quiz-item" data-qindex="${i}">
            <p class="quiz-question">${i + 1}. ${item.q}</p>
            <div class="quiz-choices">
                ${item.choices.map((c, ci) => `<button type="button" class="quiz-choice" data-choice="${ci}">${c}</button>`).join('')}
            </div>
        </div>`).join('');
    return `
        <div class="page page-quiz">
            <h2>이야기 문제</h2>
            <p class="quiz-intro-text" id="quizProgress">0 / 총 ${QUIZ.length}문항 완료</p>
            <div class="quiz-list">${items}</div>
        </div>`;
}


/* 읽고 나서 — 세계명작 트랙과 같은 형식이다. 동화틀은 쪽을 재서 나누지 않으므로
   펼침면마다 왼쪽·오른쪽 글을 손으로 나누어 둔다. */
const AFTERWORD = {
    title: '읽고 나서',
    emoji: '🧂',
    spreads: [
        {
            art: 'end.webp',
            left: [
                "북유럽 판본에서는 절구 대신 맷돌이 나옵니다. 그리고 그 맷돌은 지금도 바다 밑에서 돌고 있다고 합니다. 멈추는 말을 아는 이가 아직 없다는 뜻이지요.",
                "서로 멀리 떨어진 곳에서 같은 이야기가 나온 것이 아니라, 아주 오래전부터 사람에게서 사람에게로 건너다닌 것으로 봅니다.",
                "나무꾼은 절구를 얻고 나서 먼저 이웃에게 쌀을 나누었습니다. 같은 물건을 두고 두 사람이 처음 한 일이 달랐습니다."
            ],
            right: [
                "쓰는 법을 다 알지 못하면서 손에 넣기만 하면 그 물건이 도리어 나를 삼킵니다. 도둑은 나오게 하는 말만 알았을 뿐 멈추는 말은 몰랐습니다.",
                "잘 모르는 것을 서둘러 손에 넣으려다 곤란해진 적이 있나요?"
            ]
        }
    ]
};

function afterPage(spread, isFirst) {
    const head = isFirst ? `<h2>${AFTERWORD.title}</h2>` : '';
    // 그림은 오른쪽 칸 맨 위 모서리에 꽉 붙인다. 학습 허브로 가는 길은 그 칸 맨 아래에 둔다.
    const art = spread.art ? `<div class="after-art">${artFrame(spread.art, AFTERWORD.emoji)}</div>` : '';
    const foot = spread.last ? `<p class="after-home"><a class="home-btn" href="../../../../../">학습 허브로 돌아가기</a></p>` : '';
    const col = (ps) => ps.map(t => `<p>${t}</p>`).join('');
    return `
        <div class="page page-after">
            <div class="after-col after-col-left">
                ${head}
                ${col(spread.left)}
            </div>
            <div class="after-col after-col-right${spread.art ? ' after-col-image' : ''}">
                ${art}
                ${col(spread.right)}
                ${foot}
            </div>
        </div>`;
}

const PAGES = [
    { kind: 'cover' },
    ...CHAPTERS.flatMap(chapter => chapter.beats.map((beat, i) => ({ kind: 'spread', chapter, beat, isFirst: i === 0 }))),
    { kind: 'quiz' },
    ...AFTERWORD.spreads.map((spread, i) => ({ kind: 'after', spread, isFirst: i === 0, last: i === AFTERWORD.spreads.length - 1 })),
];

const TWO_PAGE_KINDS = new Set(['spread', 'cover', 'after']);

let folioCounter = 0;
const FOLIOS = PAGES.map(p => {
    const width = TWO_PAGE_KINDS.has(p.kind) ? 2 : 1;
    const start = folioCounter + 1;
    folioCounter += width;
    return { start, width };
});

function renderPage(page) {
    switch (page.kind) {
        case 'cover':
            return coverPage();
        case 'spread':
            return spreadPage(page.chapter, page.beat, page.isFirst);
        case 'after':
            return afterPage({ ...page.spread, last: page.last }, page.isFirst);
        case 'quiz':
            return quizPage();
        default:
            return '';
    }
}

let current = 0;
let animating = false;

const spreadEl = document.getElementById('spread');
const prevBtn = document.getElementById('prevBtn');
const nextBtn = document.getElementById('nextBtn');
const indicatorEl = document.getElementById('pageIndicator');
const folioLeftEl = document.getElementById('folioLeft');
const folioRightEl = document.getElementById('folioRight');

function paint() {
    spreadEl.innerHTML = renderPage(PAGES[current]);
    prevBtn.disabled = current === 0;
    nextBtn.disabled = current === PAGES.length - 1;
    indicatorEl.textContent = `${current + 1} / ${PAGES.length}`;

    const folio = FOLIOS[current];
    folioLeftEl.classList.toggle('folio-center', folio.width === 1);
    if (folio.width === 2) {
        folioLeftEl.textContent = folio.start;
        folioRightEl.textContent = folio.start + 1;
        folioLeftEl.hidden = false;
        folioRightEl.hidden = false;
    } else {
        folioLeftEl.textContent = folio.start;
        folioLeftEl.hidden = false;
        folioRightEl.hidden = true;
    }

    spreadEl.querySelectorAll('[data-goto]').forEach(btn => {
        btn.addEventListener('click', () => {
            const chapterNum = Number(btn.dataset.goto);
            const idx = PAGES.findIndex(p => p.kind === 'spread' && p.chapter.num === chapterNum && p.isFirst);
            if (idx >= 0) goTo(idx);
        });
    });
    spreadEl.querySelectorAll('[data-goto-kind]').forEach(btn => {
        btn.addEventListener('click', () => {
            const idx = PAGES.findIndex(p => p.kind === btn.dataset.gotoKind);
            if (idx >= 0) goTo(idx);
        });
    });

    if (PAGES[current].kind === 'quiz') {
        initQuiz();
    }
    fitAfterword();
}

/* 「읽고 나서」는 글씨를 줄이지 않는다. 세로 화면에서 글이 칸을 넘으면
   끝그림을 접어 그 자리를 글에 내준다. 접을 필요가 없으면 그대로 둔다. */
function fitAfterword() {
    const page = spreadEl.querySelector('.page-after');
    if (!page) return;
    page.classList.remove('after-tight');
    const spills = () => [...page.querySelectorAll('.after-col')]
        .some(col => col.scrollHeight - col.clientHeight > 1);
    if (spills()) page.classList.add('after-tight');
}

window.addEventListener('resize', fitAfterword);
if (document.fonts && document.fonts.ready) document.fonts.ready.then(fitAfterword);

function initQuiz() {
    let answeredCount = 0;
    const progressEl = document.getElementById('quizProgress');

    spreadEl.querySelectorAll('.quiz-item').forEach(item => {
        const qi = Number(item.dataset.qindex);
        const q = QUIZ[qi];
        item.querySelectorAll('.quiz-choice').forEach(btn => {
            btn.addEventListener('click', () => {
                if (item.classList.contains('graded')) return;
                item.classList.add('graded');
                const chosen = Number(btn.dataset.choice);
                item.querySelectorAll('.quiz-choice').forEach(b => {
                    const ci = Number(b.dataset.choice);
                    if (ci === q.answer) b.classList.add('correct');
                    else if (ci === chosen) b.classList.add('incorrect');
                });
                answeredCount++;
                progressEl.textContent = `${answeredCount} / 총 ${QUIZ.length}문항 완료`;
            });
        });
    });
}

function goTo(index) {
    if (animating || index === current || index < 0 || index >= PAGES.length) return;
    animating = true;
    const dir = index > current ? 'flip-next' : 'flip-prev';
    spreadEl.classList.add(dir);
    setTimeout(() => {
        current = index;
        paint();
    }, 230);
    setTimeout(() => {
        spreadEl.classList.remove('flip-next', 'flip-prev');
        animating = false;
    }, 480);
}

prevBtn.addEventListener('click', () => goTo(current - 1));
nextBtn.addEventListener('click', () => goTo(current + 1));

document.getElementById('tocLink').addEventListener('click', () => {
    // 그림책은 차례가 표지에 붙어 있다.
    const idx = PAGES.findIndex(p => p.kind === 'toc');
    goTo(idx >= 0 ? idx : 0);
});

document.addEventListener('keydown', (e) => {
    if (e.key === 'ArrowRight') goTo(current + 1);
    if (e.key === 'ArrowLeft') goTo(current - 1);
});

paint();
