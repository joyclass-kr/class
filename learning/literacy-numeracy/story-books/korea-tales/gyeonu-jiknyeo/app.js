const CHAPTERS = [
    {
        num: 1,
        title: "1장 · 하늘나라의 두 사람",
        beats: [
            {
                art: "01-gyeonu.webp",
                emoji: "🐂",
                left: [
                    "아주 먼 옛날, 하늘나라에 견우라는 젊은이가 살았습니다. 은하수 건너편 들판에서 소를 치는 일을 했지요.",
                    "새벽부터 밤까지 소를 몰고 풀밭을 오르내렸어요.",
                    "\"이랴, 이랴. 저 언덕까지만 가자.\"",
                    "견우의 목소리는 늘 다정했지요."
                ],
                right: [
                    "\"오늘도 잘 먹었느냐?\"",
                    "견우는 소들의 등을 하나하나 쓸어 주었어요. 소들은 견우를 따라 어디든 순하게 걸었지요.",
                    "하늘나라에서 견우만큼 부지런한 이가 없었어요.",
                    "해가 지고도 한참을 들판에 남아 있었답니다."
                ]
            },
            {
                art: "02-jiknyeo.webp",
                emoji: "🧵",
                left: [
                    "은하수 이쪽에는 직녀가 살았어요. 은하수는 하늘을 가로지르는 커다란 강이었지요.",
                    "직녀는 옥황상제의 하나뿐인 손녀였어요.",
                    "베를 짜는 솜씨가 하늘나라 으뜸이었습니다.",
                    "직녀는 하루 종일 베틀 앞에 앉아 있었지요."
                ],
                right: [
                    "찰칵, 찰칵, 찰칵, 찰칵.",
                    "베틀 북이 실 사이를 쉴 새 없이 오갔어요.",
                    "그 집에서 베틀 소리가 끊이는 날이 없었지요.",
                    "직녀가 짠 베로 하늘의 구름과 노을을 지었다고 해요.",
                    "하늘이 고운 것은 다 직녀 덕분이라고들 했답니다."
                ]
            },
            {
                art: "03-meet.webp",
                emoji: "💫",
                left: [
                    "옥황상제는 두 사람을 눈여겨보았어요. 아무리 보아도 둘만큼 부지런한 이가 없었거든요.",
                    "\"둘 다 참 성실하구나. 짝을 지어 주어야겠다.\"",
                    "옥황상제가 흐뭇하게 웃었어요.",
                    "그리하여 견우와 직녀는 부부가 되었습니다."
                ],
                right: [
                    "온 하늘나라 사람들이 나와 축하해 주었지요.",
                    "은하수에 다리가 놓이고, 둘은 그 다리를 건너 서로를 만났어요.",
                    "둘이 손을 맞잡은 날, 하늘나라 전체가 환하게 빛났대요.",
                    "두 사람은 세상에 부러울 것이 없었답니다."
                ]
            },
            {
                art: "04-idle.webp",
                emoji: "🕸️",
                left: [
                    "그런데 그때부터 일이 이상해졌어요. 아침에 눈을 뜨면 서로 얼굴부터 보았지요.",
                    "둘은 서로 곁에 있는 것이 좋아 다른 일이 손에 잡히지 않았습니다.",
                    "직녀는 베틀 앞에 앉지 않았어요.",
                    "베틀에는 먼지가 뽀얗게 앉았지요."
                ],
                right: [
                    "견우도 소 떼를 들판에 내버려 두었어요. 소들은 아무 데나 흩어져 남의 밭까지 들어갔지요.",
                    "그러는 사이 하늘에는 새 구름이 걸리지 않았습니다.",
                    "노을도 구름도 자꾸 흐릿해졌어요.",
                    "하늘빛이 날마다 흐려졌답니다."
                ]
            }
        ]
    },
    {
        num: 2,
        title: "2장 · 은하수 양쪽으로",
        beats: [
            {
                art: "05-anger.webp",
                emoji: "⚡",
                left: [
                    "마침내 옥황상제가 노했습니다. 옥황상제가 두 사람을 불러 세웠어요.",
                    "\"내가 너희를 맺어 준 것은 서로 게을러지라는 뜻이 아니었다!\"",
                    "\"하늘이 며칠째 잿빛인 것을 보아라!\"",
                    "옥황상제의 수염이 부르르 떨렸지요."
                ],
                right: [
                    "\"용서해 주십시오! 잘못했습니다!\"",
                    "견우와 직녀가 그 자리에 무릎을 꿇었어요.",
                    "\"다시는 그러지 않겠습니다. 한 번만 봐주십시오.\"",
                    "하지만 옥황상제는 천천히 고개를 저었어요. 이미 마음을 정한 뒤였답니다."
                ]
            },
            {
                art: "06-parted.webp",
                emoji: "🌌",
                left: [
                    "\"오늘부터 은하수 양쪽에서 따로 살아라.\"",
                    "옥황상제의 목소리가 아주 무거웠어요.",
                    "\"만나는 것은 일 년에 단 하루, 칠월 초이렛날뿐이다.\"",
                    "\"일 년에 하루라도 만나게 해 주는 것이 나의 마지막 정이다.\""
                ],
                right: [
                    "두 사람은 은하수 이쪽과 저쪽으로 갈라졌습니다. 손을 뻗어도 서로에게 닿지 않았어요.",
                    "목이 터져라 소리쳐도 물소리에 묻혔지요.",
                    "은하수 물소리만 밤낮으로 흘렀어요.",
                    "그 넓은 강을 건널 방법은 없었답니다."
                ]
            },
            {
                art: "07-year.webp",
                emoji: "🌙",
                left: [
                    "그날부터 견우는 다시 소를 몰았어요. 직녀도 다시 베를 짰지요.",
                    "소들이 다시 살이 오르고 베틀 소리가 다시 울렸어요.",
                    "하늘에 고운 노을이 돌아왔지요.",
                    "다만 일을 하다가도 자꾸 은하수 쪽을 바라보았습니다."
                ],
                right: [
                    "그렇게 봄이 가고 여름이 깊었어요. 견우는 날마다 손가락으로 남은 날을 세었지요.",
                    "직녀도 베틀 앞에서 같은 날을 세었어요.",
                    "그러다 드디어 칠월 초이렛날이 되었답니다.",
                    "일 년을 기다려 딱 하루 오는 날이었지요."
                ]
            },
            {
                art: "08-river.webp",
                emoji: "😢",
                left: [
                    "두 사람은 한달음에 은하수 강가로 달려갔어요.",
                    "숨이 턱에 차도록 뛰었지요. 신도 한 짝 잃어버렸어요.",
                    "강 건너에 서로의 모습이 조그맣게 보였습니다.",
                    "\"여보!\"",
                    "\"여보, 나 여기 있소!\"",
                    "둘 다 손을 흔들었지요."
                ],
                right: [
                    "그런데 은하수 강폭이 너무나 넓었습니다. 건널 다리는 이미 치워지고 없었어요.",
                    "아무리 목청껏 소리쳐도 목소리가 닿지 않았지요.",
                    "둘은 강가에 주저앉아 울기만 했습니다.",
                    "해가 지도록 그렇게 앉아 있었답니다."
                ]
            }
        ]
    },
    {
        num: 3,
        title: "3장 · 오작교",
        beats: [
            {
                art: "09-birds.webp",
                emoji: "🐦",
                left: [
                    "그 울음소리가 하늘을 넘어 땅에까지 들렸어요.",
                    "아주 슬프고 긴 울음소리였지요. 밤새 그치지 않았어요.",
                    "까치와 까마귀들이 고개를 갸웃했습니다.",
                    "\"저게 무슨 소리지?\"",
                    "새들이 일제히 하늘을 올려다보았어요."
                ],
                right: [
                    "사정을 알게 된 새들은 서로를 쳐다보았어요. 일 년에 하루도 못 만난다니 딱하기 그지없었거든요.",
                    "\"우리가 다리를 놓아 주면 되지 않을까?\"",
                    "\"그래! 어서 올라가자!\"",
                    "새들이 한꺼번에 날개를 폈답니다."
                ]
            },
            {
                art: "10-bridge.webp",
                emoji: "🌉",
                left: [
                    "온 나라의 까치와 까마귀가 한꺼번에 하늘로 날아올랐어요. 까맣게 하늘을 덮을 만큼 많았지요.",
                    "날갯짓 소리가 천둥 치듯 요란했어요.",
                    "새들은 은하수 위에 모여 머리를 맞대고 줄지어 섰습니다.",
                    "긴 줄이 강 이쪽에서 저쪽까지 이어졌지요."
                ],
                right: [
                    "새들은 부리를 물고 날개를 맞대었어요. 수천 마리의 새가 몸으로 놓은 다리였지요.",
                    "사람들은 그 다리를 오작교라고 불렀답니다.",
                    "까마귀 오, 까치 작, 다리 교.",
                    "이름 그대로 까마귀와 까치가 놓은 다리였지요."
                ]
            },
            {
                art: "11-reunion.webp",
                emoji: "🤝",
                left: [
                    "견우와 직녀는 새들의 등을 밟고 강 한가운데로 걸어갔어요. 새들의 등이 조금씩 흔들렸지요.",
                    "그래도 새들은 이를 악물고 꿈쩍하지 않았어요.",
                    "두 사람은 한달음에 서로에게 달려갔습니다.",
                    "\"잘 지냈소?\""
                ],
                right: [
                    "\"보고 싶었어요.\"",
                    "소 이야기도 하고 베틀 이야기도 했어요. 그러나 만남은 하룻밤뿐이었습니다.",
                    "동이 트자 새들이 하나둘 내려앉기 시작했지요.",
                    "새들도 힘이 다해 가고 있었거든요.",
                    "두 사람은 다시 각자의 자리로 돌아가야 했답니다."
                ]
            },
            {
                art: "12-rain.webp",
                emoji: "🌧️",
                left: [
                    "돌아서는 견우도 직녀도 소리 내어 울었어요. 그 눈물이 구름을 지나 땅으로 떨어졌지요.",
                    "그해 칠석에 내린 빗방울은 굵고 따뜻했어요.",
                    "칠석 무렵에 비가 자주 오는 것은 그래서라고 한답니다.",
                    "만나서 흘린 눈물, 헤어지며 흘린 눈물이래요."
                ],
                right: [
                    "그리고 그 무렵이면 까치와 까마귀의 머리가 성깁니다.",
                    "밤새 머리를 밟히고 서 있었으니 그럴 만도 하지요.",
                    "그래도 새들은 해마다 그날이면 다시 날아오른대요.",
                    "누가 시키지 않아도 저희끼리 모여 올라가는 것이랍니다."
                ]
            }
        ],
        moral: "옥황상제가 두 사람을 갈라놓은 것은 서로를 좋아했기 때문이 아니라, 좋아하느라 자기 몫의 일을 놓았기 때문이었어요. 아끼는 마음과 해야 할 일은 둘 다 지켜야 하는 것이랍니다.",
        question: "좋아하는 일에 빠져 해야 할 일을 놓친 적이 있나요?"
    }
];

function artFrame(src, emoji) {
    return `
        <div class="art-frame">
            <img src="images/${src}" alt="" onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';">
            <div class="art-fallback" style="display:none">${emoji}</div>
        </div>`;
}

function coverToc() {
    const item = s => `
        <button type="button" data-goto="${s.num}">
            <span class="toc-num">${s.num}</span>
            <span>${s.title.replace(/^\d+장 · /, '')}</span>
        </button>`;
    return `
        <nav class="cover-toc">
            <h2>차례</h2>
            ${CHAPTERS.map(item).join('')}
            <button type="button" data-goto-kind="quiz">
                <span class="toc-num">❓</span>
                <span>이야기 문제</span>
            </button>
        </nav>`;
}

function coverPage() {
    return `
        <div class="page page-cover">
            <div class="story-page-left story-page-left-full">
                ${artFrame('cover.webp', '🌌')}
            </div>
            <div class="story-page-right">
                <h1>견우와 직녀</h1>
                <p>견우와 직녀는 지은이가 없는 이야기예요. 밤하늘의 별을 보고 만들어 낸 이야기라, 우리나라뿐 아니라 중국과 일본에도 함께 전해진답니다.</p>
                <p>은하수를 사이에 두고 마주 보는 두 개의 밝은 별이 실제로 있어요. 여름밤 하늘에서 볼 수 있는 견우성과 직녀성이지요. 두 별이 은하수 양쪽에 떨어져 있는 모습에서 이 이야기가 시작되었답니다.</p>
                <p>음력 칠월 초이렛날을 칠석이라고 해요. 옛사람들은 이날 두 사람이 만난다고 여겨 별을 보며 소원을 빌었지요. 칠석 무렵에 비가 자주 오는 것도, 그 무렵 까치 머리가 성긴 것도 이 이야기로 설명했답니다.</p>
                ${coverToc()}
            </div>
        </div>`;
}

function spreadPage(chapter, beat, isFirst) {
    const badgeHtml = isFirst ? `<div class="spread-chapter-badge">${chapter.title}</div>` : '';
    const leftHtml = beat.left.map(p => `<p>${p}</p>`).join('');
    const rightHtml = beat.right.map(p => `<p>${p}</p>`).join('');
    return `
        <div class="page page-story">
            <div class="spread-art">
                ${badgeHtml}
                ${artFrame(beat.art, beat.emoji)}
            </div>
            <div class="spread-text">
                <div class="spread-text-left">${leftHtml}</div>
                <div class="spread-text-right">${rightHtml}</div>
            </div>
        </div>`;
}


const QUIZ = [
    { q: "견우가 하던 일은 무엇인가요?", choices: ["베를 짜는 일", "소를 돌보는 일", "별을 세는 일"], answer: 1 },
    { q: "직녀가 하던 일은 무엇인가요?", choices: ["소를 치는 일", "밭을 가는 일", "베를 짜는 일"], answer: 2 },
    { q: "둘이 부부가 된 뒤 하늘은 어떻게 되었나요?", choices: ["온통 환하게 빛났다", "노을이 더 고와졌다", "며칠째 잿빛이었다"], answer: 2 },
    { q: "두 사람이 갈라선 뒤 은하수에서 없어진 것은 무엇인가요?", choices: ["물", "다리", "구름"], answer: 1 },
    { q: "다리를 놓아 준 것은 누구인가요?", choices: ["까치와 까마귀", "구름과 바람", "해와 달"], answer: 0 },
    { q: "칠석 무렵에 비가 오는 까닭은 무엇이라고 했나요?", choices: ["구름이 몰려와서라고", "두 사람의 눈물이라고", "용이 노해서라고"], answer: 1 }
];

function quizPage() {
    const items = QUIZ.map((item, i) => `
        <div class="quiz-item" data-qindex="${i}">
            <p class="quiz-question">${i + 1}. ${item.q}</p>
            <div class="quiz-choices">
                ${item.choices.map((c, ci) => `<button type="button" class="quiz-choice" data-choice="${ci}">${c}</button>`).join('')}
            </div>
        </div>`).join('');
    return `
        <div class="page page-quiz">
            <h2>이야기 문제</h2>
            <p class="quiz-intro-text" id="quizProgress">0 / 총 ${QUIZ.length}문항 완료</p>
            <div class="quiz-list">${items}</div>
        </div>`;
}


/* 읽고 나서 — 세계명작 트랙과 같은 형식이다. 동화틀은 쪽을 재서 나누지 않으므로
   펼침면마다 왼쪽·오른쪽 글을 손으로 나누어 둔다. */
const AFTERWORD = {
    title: '읽고 나서',
    emoji: '🌌',
    spreads: [
        {
            art: 'end.webp',
            left: [
                "두 별 사이는 실제로 아주 멉니다. 빛으로 달려도 열여섯 해가 걸리는 거리입니다. 옛사람들은 그 거리를 몰랐지만 은하수를 사이에 두고 떨어져 있다는 것만은 눈으로 보았습니다.",
                "그 별들이 일 년에 한 번 가장 가까워 보이는 때가 음력 칠월 초이레 무렵입니다. 그래서 그날을 칠석이라 하고 이 이야기를 붙여 두었습니다.",
                "그래서 벌도 일과 이어져 있습니다. 소를 몰던 사람과 베를 짜던 사람을 각자 제자리로 돌려보낸 것이지요.",
                "칠석 무렵에 비가 오면 두 사람이 만나 흘리는 눈물이라 했습니다. 실제로 그때가 우리나라 장마 끝 무렵입니다."
            ],
            right: [
                "옥황상제가 두 사람을 갈라놓은 것은 서로를 좋아했기 때문이 아니라, 좋아하느라 제 몫의 일을 놓았기 때문입니다. 아끼는 마음과 해야 할 일은 둘 다 지켜야 하는 것입니다.",
                "좋아하는 일에 빠져 해야 할 일을 놓친 적이 있나요?"
            ]
        }
    ]
};

function afterPage(spread, isFirst) {
    const head = isFirst ? `<h2>${AFTERWORD.title}</h2>` : '';
    // 그림은 오른쪽 칸 맨 위 모서리에 꽉 붙인다. 학습 허브로 가는 길은 그 칸 맨 아래에 둔다.
    const art = spread.art ? `<div class="after-art">${artFrame(spread.art, AFTERWORD.emoji)}</div>` : '';
    const foot = spread.last ? `<p class="after-home"><a class="home-btn" href="../../../../../">학습 허브로 돌아가기</a></p>` : '';
    const col = (ps) => ps.map(t => `<p>${t}</p>`).join('');
    return `
        <div class="page page-after">
            <div class="after-col after-col-left">
                ${head}
                ${col(spread.left)}
            </div>
            <div class="after-col after-col-right${spread.art ? ' after-col-image' : ''}">
                ${art}
                ${col(spread.right)}
                ${foot}
            </div>
        </div>`;
}

const PAGES = [
    { kind: 'cover' },
    ...CHAPTERS.flatMap(chapter => chapter.beats.map((beat, i) => ({ kind: 'spread', chapter, beat, isFirst: i === 0 }))),
    { kind: 'quiz' },
    ...AFTERWORD.spreads.map((spread, i) => ({ kind: 'after', spread, isFirst: i === 0, last: i === AFTERWORD.spreads.length - 1 })),
];

const TWO_PAGE_KINDS = new Set(['spread', 'cover', 'after']);

let folioCounter = 0;
const FOLIOS = PAGES.map(p => {
    const width = TWO_PAGE_KINDS.has(p.kind) ? 2 : 1;
    const start = folioCounter + 1;
    folioCounter += width;
    return { start, width };
});

function renderPage(page) {
    switch (page.kind) {
        case 'cover':
            return coverPage();
        case 'spread':
            return spreadPage(page.chapter, page.beat, page.isFirst);
        case 'after':
            return afterPage({ ...page.spread, last: page.last }, page.isFirst);
        case 'quiz':
            return quizPage();
        default:
            return '';
    }
}

let current = 0;
let animating = false;

const spreadEl = document.getElementById('spread');
const prevBtn = document.getElementById('prevBtn');
const nextBtn = document.getElementById('nextBtn');
const indicatorEl = document.getElementById('pageIndicator');
const folioLeftEl = document.getElementById('folioLeft');
const folioRightEl = document.getElementById('folioRight');

function paint() {
    spreadEl.innerHTML = renderPage(PAGES[current]);
    prevBtn.disabled = current === 0;
    nextBtn.disabled = current === PAGES.length - 1;
    indicatorEl.textContent = `${current + 1} / ${PAGES.length}`;

    const folio = FOLIOS[current];
    folioLeftEl.classList.toggle('folio-center', folio.width === 1);
    if (folio.width === 2) {
        folioLeftEl.textContent = folio.start;
        folioRightEl.textContent = folio.start + 1;
        folioLeftEl.hidden = false;
        folioRightEl.hidden = false;
    } else {
        folioLeftEl.textContent = folio.start;
        folioLeftEl.hidden = false;
        folioRightEl.hidden = true;
    }

    spreadEl.querySelectorAll('[data-goto]').forEach(btn => {
        btn.addEventListener('click', () => {
            const chapterNum = Number(btn.dataset.goto);
            const idx = PAGES.findIndex(p => p.kind === 'spread' && p.chapter.num === chapterNum && p.isFirst);
            if (idx >= 0) goTo(idx);
        });
    });
    spreadEl.querySelectorAll('[data-goto-kind]').forEach(btn => {
        btn.addEventListener('click', () => {
            const idx = PAGES.findIndex(p => p.kind === btn.dataset.gotoKind);
            if (idx >= 0) goTo(idx);
        });
    });

    if (PAGES[current].kind === 'quiz') {
        initQuiz();
    }
    fitAfterword();
}

/* 「읽고 나서」는 글씨를 줄이지 않는다. 세로 화면에서 글이 칸을 넘으면
   끝그림을 접어 그 자리를 글에 내준다. 접을 필요가 없으면 그대로 둔다. */
function fitAfterword() {
    const page = spreadEl.querySelector('.page-after');
    if (!page) return;
    page.classList.remove('after-tight');
    const spills = () => [...page.querySelectorAll('.after-col')]
        .some(col => col.scrollHeight - col.clientHeight > 1);
    if (spills()) page.classList.add('after-tight');
}

window.addEventListener('resize', fitAfterword);
if (document.fonts && document.fonts.ready) document.fonts.ready.then(fitAfterword);

function initQuiz() {
    let answeredCount = 0;
    const progressEl = document.getElementById('quizProgress');

    spreadEl.querySelectorAll('.quiz-item').forEach(item => {
        const qi = Number(item.dataset.qindex);
        const q = QUIZ[qi];
        item.querySelectorAll('.quiz-choice').forEach(btn => {
            btn.addEventListener('click', () => {
                if (item.classList.contains('graded')) return;
                item.classList.add('graded');
                const chosen = Number(btn.dataset.choice);
                item.querySelectorAll('.quiz-choice').forEach(b => {
                    const ci = Number(b.dataset.choice);
                    if (ci === q.answer) b.classList.add('correct');
                    else if (ci === chosen) b.classList.add('incorrect');
                });
                answeredCount++;
                progressEl.textContent = `${answeredCount} / 총 ${QUIZ.length}문항 완료`;
            });
        });
    });
}

function goTo(index) {
    if (animating || index === current || index < 0 || index >= PAGES.length) return;
    animating = true;
    const dir = index > current ? 'flip-next' : 'flip-prev';
    spreadEl.classList.add(dir);
    setTimeout(() => {
        current = index;
        paint();
    }, 230);
    setTimeout(() => {
        spreadEl.classList.remove('flip-next', 'flip-prev');
        animating = false;
    }, 480);
}

prevBtn.addEventListener('click', () => goTo(current - 1));
nextBtn.addEventListener('click', () => goTo(current + 1));

document.getElementById('tocLink').addEventListener('click', () => {
    // 그림책은 차례가 표지에 붙어 있다.
    const idx = PAGES.findIndex(p => p.kind === 'toc');
    goTo(idx >= 0 ? idx : 0);
});

document.addEventListener('keydown', (e) => {
    if (e.key === 'ArrowRight') goTo(current + 1);
    if (e.key === 'ArrowLeft') goTo(current - 1);
});

paint();
