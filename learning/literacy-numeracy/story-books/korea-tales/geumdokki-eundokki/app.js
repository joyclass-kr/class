const CHAPTERS = [
    {
        num: 1,
        title: "1장 · 풍덩, 빠진 도끼",
        beats: [
            {
                art: "01-woodcutter.webp",
                emoji: "🪓",
                left: [
                    "옛날 어느 산골에 가난한 나무꾼이 살았습니다.",
                    "가진 것이라고는 지게 하나와 낡은 쇠도끼 한 자루뿐이었지요.",
                    "아내와 어린 아이 둘이 그 도끼 하나만 바라보고 살았어요.",
                    "새벽에 산에 올라 해가 져야 내려왔지요."
                ],
                right: [
                    "\"이 도끼가 우리 식구를 먹여 살린다.\"",
                    "도끼는 제 몸처럼 아끼는 물건이었지요. 나무꾼은 밤마다 도끼날을 숫돌에 갈았어요.",
                    "날이 무디면 나무가 안 넘어가거든요.",
                    "스윽, 스윽 하는 소리가 밤늦도록 났답니다."
                ]
            },
            {
                art: "02-splash.webp",
                emoji: "💦",
                left: [
                    "그날 나무꾼은 연못가 나무를 찍고 있었어요.",
                    "아름드리나무라 좀처럼 넘어가지 않았지요.",
                    "팍! 팍!",
                    "도끼질을 할 때마다 이마에서 땀이 비 오듯 흘렀습니다.",
                    "손잡이가 자꾸 미끄러웠어요.",
                    "그래도 나무꾼은 쉬지 않았지요."
                ],
                right: [
                    "그러다 그만 손잡이가 땀에 쭉 미끄러졌지 뭐예요. 도끼가 손에서 훌쩍 날아올랐어요.",
                    "풍덩!",
                    "물이 사방으로 높이 튀어 올랐지요.",
                    "도끼는 깊은 연못 한가운데로 가라앉았습니다.",
                    "\"아이고, 저를 어째! 내 도끼!\""
                ]
            },
            {
                art: "03-cry.webp",
                emoji: "😭",
                left: [
                    "나무꾼은 물가에 털썩 주저앉았어요. 연못은 바닥이 안 보일 만큼 깊었습니다.",
                    "헤엄쳐 들어가 볼 엄두도 나지 않았지요.",
                    "물속을 한참이나 들여다보았어요.",
                    "\"저 도끼가 없으면 우리 식구는 뭘 먹고 사나.\""
                ],
                right: [
                    "나무꾼은 무릎에 얼굴을 묻고 엉엉 울었지요. 아이들 얼굴이 눈앞에 아른거렸어요.",
                    "빈손으로는 집에 갈 수가 없었습니다.",
                    "그러는 사이 해가 뉘엿뉘엿 넘어갔어요.",
                    "나무꾼은 그래도 일어서지 못했답니다."
                ]
            },
            {
                art: "04-spirit.webp",
                emoji: "✨",
                left: [
                    "그때였습니다.",
                    "연못 한가운데가 환해지더니 물이 스르르 갈라졌어요. 갈라진 물 사이로 하얀 빛이 솟았지요.",
                    "흰 수염에 흰옷을 입은 노인이 물 위로 올라섰습니다.",
                    "나무꾼은 눈이 부셔 손으로 얼굴을 가렸어요."
                ],
                right: [
                    "\"어인 일로 그리 서럽게 우느냐?\"",
                    "그 목소리가 온 산에 우렁우렁 울렸어요.",
                    "나무꾼은 두 눈을 비비고 또 비볐지요.",
                    "꿈이 아니었습니다. 이 산의 산신령이었어요.",
                    "나무꾼은 물가에 그대로 넙죽 엎드렸답니다."
                ]
            }
        ]
    },
    {
        num: 2,
        title: "2장 · 이것이 네 도끼냐",
        beats: [
            {
                art: "05-gold.webp",
                emoji: "🥇",
                left: [
                    "산신령이 물속으로 손을 넣더니 도끼 하나를 꺼냈어요. 번쩍번쩍하는 금도끼였지요.",
                    "어두워진 연못가가 대번에 환해졌어요.",
                    "날에서 금빛이 뚝뚝 떨어지는 것 같았습니다.",
                    "나무꾼은 그런 도끼를 본 적이 없었지요."
                ],
                right: [
                    "\"이것이 네 도끼냐?\"",
                    "산신령이 그 금도끼를 나무꾼 앞에 쑥 내밀었어요. 나무꾼은 도끼를 빤히 들여다보았습니다.",
                    "\"아닙니다. 제 것이 아닙니다.\"",
                    "나무꾼은 얼른 고개를 저었지요.",
                    "조금도 망설이지 않았답니다."
                ]
            },
            {
                art: "06-silver.webp",
                emoji: "🥈",
                left: [
                    "산신령이 다시 물속에 손을 넣어 은도끼를 꺼냈어요. 은도끼가 달빛처럼 하얗게 빛났습니다.",
                    "손잡이에는 은실까지 곱게 감겨 있었지요.",
                    "\"그러면 이것이 네 도끼냐?\"",
                    "평생 한 번도 본 적 없는 도끼였어요."
                ],
                right: [
                    "\"그것도 제 것이 아닙니다. 제 도끼는 쇠도끼입니다.\"",
                    "나무꾼은 또 한 번 고개를 저었어요. 목소리가 조금도 흔들리지 않았지요.",
                    "산신령이 나무꾼의 얼굴을 가만히 살폈습니다.",
                    "나무꾼은 눈도 피하지 않았답니다."
                ]
            },
            {
                art: "07-iron.webp",
                emoji: "🪓",
                left: [
                    "산신령이 세 번째로 손을 넣었어요. 이번에 꺼낸 것은 금도 은도 아니었지요.",
                    "날이 다 닳고 손잡이가 반들반들해진 낡은 쇠도끼였습니다.",
                    "여기저기 이가 빠진 볼품없는 도끼였어요.",
                    "그런데 나무꾼의 얼굴이 대번에 환해졌지요."
                ],
                right: [
                    "\"예! 그겁니다! 그게 제 도끼입니다!\"",
                    "나무꾼이 저도 모르게 소리를 질렀지요. 두 손을 내밀며 펄쩍펄쩍 뛰었어요.",
                    "아이처럼 좋아서 어쩔 줄을 몰랐습니다.",
                    "금도끼를 볼 때와는 얼굴이 아주 달랐답니다."
                ]
            },
            {
                art: "08-reward.webp",
                emoji: "🎁",
                left: [
                    "산신령이 그만 껄껄 웃고 말았어요.",
                    "흰 수염이 들썩일 만큼 한참을 크게 웃었지요.",
                    "\"금과 은을 앞에 두고도 제 것만 찾는구나.\"",
                    "\"요즘 세상에 참으로 드문 일이로구나.\"",
                    "\"그러니 이 도끼 셋을 모두 가져가거라.\""
                ],
                right: [
                    "산신령이 도끼 셋을 모두 내밀었어요.",
                    "\"예? 저는 이 쇠도끼면 됩니다!\"",
                    "나무꾼이 손사래를 쳤지요.",
                    "\"그러니 주는 것이다.\"",
                    "산신령은 그 말만 남기고 사라졌습니다. 나무꾼은 그날 부자가 되었답니다."
                ]
            }
        ]
    },
    {
        num: 3,
        title: "3장 · 따라 한 사람",
        beats: [
            {
                art: "09-copy.webp",
                emoji: "😏",
                left: [
                    "이웃에 사는 욕심쟁이가 그 소문을 들었어요.",
                    "\"연못에 도끼만 빠뜨리면 된다고?\"",
                    "밤새 잠도 자지 않고 궁리했지요. 이튿날 욕심쟁이는 제 도끼를 들고 연못으로 달려갔습니다.",
                    "그러고는 있는 힘껏 물 한가운데로 던졌어요."
                ],
                right: [
                    "\"아이고, 내 도끼! 내 도끼! 엉엉!\"",
                    "일부러 눈물까지 짜내며 울었지요. 그런데 우는 소리가 어찌나 어색한지 몰라요.",
                    "울다가 슬쩍 연못 쪽을 곁눈질하기도 했어요.",
                    "그래도 한참을 그렇게 울어 댔답니다."
                ]
            },
            {
                art: "10-greedy.webp",
                emoji: "💨",
                left: [
                    "그런데 정말로 물이 갈라지고 산신령이 나타났어요. 산신령이 금도끼를 꺼내 들고 물었지요.",
                    "욕심쟁이는 속으로 옳지 하고 좋아했어요.",
                    "\"이것이 네 도끼냐?\"",
                    "\"예! 바로 그겁니다! 제 것이 맞습니다!\"",
                    "대답이 너무 빨랐지요."
                ],
                right: [
                    "\"네 것이 아닌 줄 알면서 그러느냐.\"",
                    "산신령이 욕심쟁이를 한참 내려다보았어요. 그러고는 금도끼를 도로 물속에 넣고 스르르 사라졌습니다.",
                    "욕심쟁이는 제가 던진 도끼마저 끝내 찾지 못했어요.",
                    "빈손으로 터덜터덜 집에 돌아갔답니다."
                ]
            }
        ],
        moral: "나무꾼이 금도끼를 마다한 것은 욕심이 없어서가 아니었어요. 가난한 사람에게 금도끼가 왜 탐나지 않았겠어요. 다만 제 것이 아닌 것을 제 것이라 말할 수가 없었을 뿐이지요.",
        question: "내 것이 아닌 줄 알면서 갖고 싶었던 것이 있나요?"
    }
];

function artFrame(src, emoji) {
    return `
        <div class="art-frame">
            <img src="images/${src}" alt="" onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';">
            <div class="art-fallback" style="display:none">${emoji}</div>
        </div>`;
}

function coverToc() {
    const item = s => `
        <button type="button" data-goto="${s.num}">
            <span class="toc-num">${s.num}</span>
            <span>${s.title.replace(/^\d+장 · /, '')}</span>
        </button>`;
    return `
        <nav class="cover-toc">
            <h2>차례</h2>
            ${CHAPTERS.map(item).join('')}
            <button type="button" data-goto-kind="quiz">
                <span class="toc-num">❓</span>
                <span>이야기 문제</span>
            </button>
        </nav>`;
}

function coverPage() {
    return `
        <div class="page page-cover">
            <div class="story-page-left story-page-left-full">
                ${artFrame('cover.webp', '🪓')}
            </div>
            <div class="story-page-right">
                <h1>금도끼 은도끼</h1>
                <p>금도끼 은도끼는 우리나라에서 모르는 사람이 거의 없는 이야기예요. 그런데 뿌리는 우리 옛이야기가 아니라 이솝 우화랍니다.</p>
                <p>이솝 우화에 실린 원래 제목은 나무꾼과 헤르메스예요. 헤르메스는 그리스 신화에 나오는 심부름꾼 신이고, 도끼를 빠뜨린 곳도 연못이 아니라 강가였답니다.</p>
                <p>우리나라에는 1906년 초등소학이라는 교과서에 처음 실렸어요. 그때는 신도 산신령도 아니고 그냥 노인이었지요. 강가가 산속 연못이 되고 노인이 산신령이 된 것은 그 뒤의 일이랍니다.</p>
                <p>산신령은 우리 옛이야기에서 산을 지키는 신이에요. 흰 수염에 흰옷을 입고 나타나 사람을 시험하거나 도와주지요. 호랑이를 데리고 다니는 모습으로도 자주 그려진답니다.</p>
                ${coverToc()}
            </div>
        </div>`;
}

function spreadPage(chapter, beat, isFirst) {
    const badgeHtml = isFirst ? `<div class="spread-chapter-badge">${chapter.title}</div>` : '';
    const leftHtml = beat.left.map(p => `<p>${p}</p>`).join('');
    const rightHtml = beat.right.map(p => `<p>${p}</p>`).join('');
    return `
        <div class="page page-story">
            <div class="spread-art">
                ${badgeHtml}
                ${artFrame(beat.art, beat.emoji)}
            </div>
            <div class="spread-text">
                <div class="spread-text-left">${leftHtml}</div>
                <div class="spread-text-right">${rightHtml}</div>
            </div>
        </div>`;
}


const QUIZ = [
    { q: "나무꾼이 연못에 빠뜨린 것은 무엇인가요?", choices: ["지게 하나", "낡은 쇠도끼", "숫돌 한 덩이"], answer: 1 },
    { q: "나무꾼은 밤마다 무엇을 했나요?", choices: ["지게를 손보고 고쳤다", "숫돌에 도끼를 갈았다", "연못가에 나가 보았다"], answer: 1 },
    { q: "산신령이 가장 먼저 내민 것은 무엇인가요?", choices: ["금도끼", "은도끼", "쇠도끼"], answer: 0 },
    { q: "나무꾼은 어느 도끼가 제 것이라고 했나요?", choices: ["금도끼", "은도끼", "쇠도끼"], answer: 2 },
    { q: "산신령은 나무꾼에게 무엇을 주었나요?", choices: ["도끼 셋을 모두", "금도끼 하나만", "쇠도끼만 도로"], answer: 0 },
    { q: "욕심쟁이는 어떻게 했나요?", choices: ["나무꾼의 금도끼를 훔쳤다", "일부러 도끼를 빠뜨렸다", "산신령을 찾아다녔다"], answer: 1 },
    { q: "산신령은 욕심쟁이에게 뭐라고 했나요?", choices: ["요즘 세상에 드문 일이로구나", "네 것이 아닌 줄 알면서 그러느냐", "이 셋을 모두 가져가거라"], answer: 1 },
    { q: "욕심쟁이는 어떻게 되었나요?", choices: ["금도끼를 얻어 갔다", "산신령에게 혼났다", "제 도끼마저 잃었다"], answer: 2 },
    {
        q: "이 책을 읽고 난 반응으로 알맞지 않은 것은 무엇인가요?",
        wide: true,
        choices: [
            "낡은 쇠도끼를 보고서야 얼굴이 환해진 것을 보면, 제 것을 아는 사람은 표정부터 다르구나.",
            "욕심쟁이가 우는 소리가 어색했다는 것을 보면, 꾸민 마음은 어딘가 티가 나는구나.",
            "나무꾼이 밤마다 도끼를 갈았다는 것을 보면, 그 도끼가 어떤 도끼였는지 알겠다.",
            "산신령이 욕심쟁이의 도끼를 도로 건져 준 것을 보면, 끝내 빈손으로 보내지는 않았구나."
        ],
        answer: 3
    }
];

/* 보기는 책을 열 때마다 자리를 바꾼다. 답의 자리를 외워 버리면 문제가 아니게 된다.
   섞는 것은 그리는 차례뿐이고, 채점은 data-choice 에 담긴 원래 번호로 한다.
   한 번 정한 차례는 책을 닫을 때까지 그대로다. 쪽을 오갈 때마다 바뀌면 헷갈린다. */
function shuffledOrder(n) {
    const a = [...Array(n).keys()];
    for (let i = a.length - 1; i > 0; i--) {
        const j = Math.floor(Math.random() * (i + 1));
        [a[i], a[j]] = [a[j], a[i]];
    }
    return a;
}

const QUIZ_ORDER = QUIZ.map(q => shuffledOrder(q.choices.length));

function quizPage() {
    const items = QUIZ.map((item, i) => `
        <div class="quiz-item" data-qindex="${i}">
            <p class="quiz-question">${i + 1}. ${item.q}</p>
            <div class="quiz-choices${item.wide ? ' quiz-choices-stack' : ''}">
                ${QUIZ_ORDER[i].map(ci => `<button type="button" class="quiz-choice" data-choice="${ci}">${item.choices[ci]}</button>`).join('')}
            </div>
        </div>`).join('');
    return `
        <div class="page page-quiz">
            <h2>이야기 문제</h2>
            <p class="quiz-intro-text" id="quizProgress">0 / 총 ${QUIZ.length}문항 완료</p>
            <div class="quiz-list">${items}</div>
        </div>`;
}


/* 읽고 나서 — 세계명작 트랙과 같은 형식이다. 동화틀은 쪽을 재서 나누지 않으므로
   펼침면마다 왼쪽·오른쪽 글을 손으로 나누어 둔다. */
const AFTERWORD = {
    title: '읽고 나서',
    emoji: '🪓',
    spreads: [
        {
            art: 'end.webp',
            left: [
                "이 이야기의 뿌리는 우리 것이 아닙니다. 이천 년도 더 전에 그리스에서 이솝이 모은 이야기 가운데 똑같은 것이 있습니다. 그쪽에서는 산신령 자리에 헤르메스라는 신이 나옵니다.",
                "그 이야기가 아주 오랜 세월에 걸쳐 여러 나라를 건너오면서 신이 산신령으로 바뀌고, 강이 연못으로 바뀌었습니다. 이야기는 이렇게 옮겨 다니며 그 땅의 옷으로 갈아입습니다.",
                "욕심쟁이가 잃은 것도 다시 보십시오. 금도끼를 못 얻은 것이 아니라 제 쇠도끼마저 잃었습니다. 얻으러 갔다가 있던 것을 놓친 것이지요."
            ],
            right: [
                "나무꾼이 금도끼를 마다한 것은 욕심이 없어서가 아니었습니다. 가난한 사람에게 금도끼가 왜 탐나지 않았겠습니까. 다만 제 것이 아닌 것을 제 것이라 말할 수가 없었을 뿐입니다.",
                "내 것이 아닌 줄 알면서 갖고 싶었던 것이 있나요?"
            ]
        }
    ]
};

function afterPage(spread, isFirst) {
    const head = isFirst ? `<h2>${AFTERWORD.title}</h2>` : '';
    // 그림은 오른쪽 칸 맨 위 모서리에 꽉 붙인다. 학습 허브로 가는 길은 그 칸 맨 아래에 둔다.
    const art = spread.art ? `<div class="after-art">${artFrame(spread.art, AFTERWORD.emoji)}</div>` : '';
    const foot = spread.last ? `<p class="after-home"><a class="home-btn" href="../../../../../">학습 허브로 돌아가기</a></p>` : '';
    const col = (ps) => ps.map(t => `<p>${t}</p>`).join('');
    return `
        <div class="page page-after">
            <div class="after-col after-col-left">
                ${head}
                ${col(spread.left)}
            </div>
            <div class="after-col after-col-right${spread.art ? ' after-col-image' : ''}">
                ${art}
                ${col(spread.right)}
                ${foot}
            </div>
        </div>`;
}

const PAGES = [
    { kind: 'cover' },
    ...CHAPTERS.flatMap(chapter => chapter.beats.map((beat, i) => ({ kind: 'spread', chapter, beat, isFirst: i === 0 }))),
    { kind: 'quiz' },
    ...AFTERWORD.spreads.map((spread, i) => ({ kind: 'after', spread, isFirst: i === 0, last: i === AFTERWORD.spreads.length - 1 })),
];

const TWO_PAGE_KINDS = new Set(['spread', 'cover', 'after']);

let folioCounter = 0;
const FOLIOS = PAGES.map(p => {
    const width = TWO_PAGE_KINDS.has(p.kind) ? 2 : 1;
    const start = folioCounter + 1;
    folioCounter += width;
    return { start, width };
});

function renderPage(page) {
    switch (page.kind) {
        case 'cover':
            return coverPage();
        case 'spread':
            return spreadPage(page.chapter, page.beat, page.isFirst);
        case 'after':
            return afterPage({ ...page.spread, last: page.last }, page.isFirst);
        case 'quiz':
            return quizPage();
        default:
            return '';
    }
}

let current = 0;
let animating = false;

const spreadEl = document.getElementById('spread');
const prevBtn = document.getElementById('prevBtn');
const nextBtn = document.getElementById('nextBtn');
const indicatorEl = document.getElementById('pageIndicator');
const folioLeftEl = document.getElementById('folioLeft');
const folioRightEl = document.getElementById('folioRight');

function paint() {
    spreadEl.innerHTML = renderPage(PAGES[current]);
    prevBtn.disabled = current === 0;
    nextBtn.disabled = current === PAGES.length - 1;
    indicatorEl.textContent = `${current + 1} / ${PAGES.length}`;

    const folio = FOLIOS[current];
    folioLeftEl.classList.toggle('folio-center', folio.width === 1);
    if (folio.width === 2) {
        folioLeftEl.textContent = folio.start;
        folioRightEl.textContent = folio.start + 1;
        folioLeftEl.hidden = false;
        folioRightEl.hidden = false;
    } else {
        folioLeftEl.textContent = folio.start;
        folioLeftEl.hidden = false;
        folioRightEl.hidden = true;
    }

    spreadEl.querySelectorAll('[data-goto]').forEach(btn => {
        btn.addEventListener('click', () => {
            const chapterNum = Number(btn.dataset.goto);
            const idx = PAGES.findIndex(p => p.kind === 'spread' && p.chapter.num === chapterNum && p.isFirst);
            if (idx >= 0) goTo(idx);
        });
    });
    spreadEl.querySelectorAll('[data-goto-kind]').forEach(btn => {
        btn.addEventListener('click', () => {
            const idx = PAGES.findIndex(p => p.kind === btn.dataset.gotoKind);
            if (idx >= 0) goTo(idx);
        });
    });

    if (PAGES[current].kind === 'quiz') {
        initQuiz();
    }
    fitAfterword();
}

/* 「읽고 나서」는 글씨를 줄이지 않는다. 세로 화면에서는 끝그림을 접어 두는 것이
   기본이고(styles.css), 여기서 그림을 켜 보아 자리가 남을 때만 그대로 둔다.
   그래서 이 함수가 한 번도 못 돌아도 글이 잘리지는 않는다. */
function fitAfterword() {
    const page = spreadEl.querySelector('.page-after');
    if (!page) return;
    page.classList.add('after-roomy');
    const spills = [...page.querySelectorAll('.after-col')]
        .some(col => col.scrollHeight > col.clientHeight);
    if (spills) page.classList.remove('after-roomy');
}

/* 화면 크기가 바뀌면 다시 잰다. 크기가 한 박자 늦게 잡히는 곳이 있어
   그 자리에서 한 번, 잠시 뒤에 한 번 더 잰다. */
let refitTimer = 0;
function refitAfterword() {
    fitAfterword();
    clearTimeout(refitTimer);
    refitTimer = setTimeout(fitAfterword, 250);
}

window.addEventListener('resize', refitAfterword);
window.addEventListener('orientationchange', refitAfterword);
/* 화면을 돌렸을 때 resize 가 안 잡히는 곳이 있다. 책 상자 크기를 직접 지켜본다. */
if (window.ResizeObserver) {
    const bookEl = document.getElementById('book');
    if (bookEl) new ResizeObserver(refitAfterword).observe(bookEl);
}
if (document.fonts && document.fonts.ready) document.fonts.ready.then(fitAfterword);

function initQuiz() {
    let answeredCount = 0;
    const progressEl = document.getElementById('quizProgress');

    spreadEl.querySelectorAll('.quiz-item').forEach(item => {
        const qi = Number(item.dataset.qindex);
        const q = QUIZ[qi];
        item.querySelectorAll('.quiz-choice').forEach(btn => {
            btn.addEventListener('click', () => {
                if (item.classList.contains('graded')) return;
                item.classList.add('graded');
                const chosen = Number(btn.dataset.choice);
                item.querySelectorAll('.quiz-choice').forEach(b => {
                    const ci = Number(b.dataset.choice);
                    if (ci === q.answer) b.classList.add('correct');
                    else if (ci === chosen) b.classList.add('incorrect');
                });
                answeredCount++;
                progressEl.textContent = `${answeredCount} / 총 ${QUIZ.length}문항 완료`;
            });
        });
    });
}

function goTo(index) {
    if (animating || index === current || index < 0 || index >= PAGES.length) return;
    animating = true;
    const dir = index > current ? 'flip-next' : 'flip-prev';
    spreadEl.classList.add(dir);
    setTimeout(() => {
        current = index;
        paint();
    }, 230);
    setTimeout(() => {
        spreadEl.classList.remove('flip-next', 'flip-prev');
        animating = false;
    }, 480);
}

prevBtn.addEventListener('click', () => goTo(current - 1));
nextBtn.addEventListener('click', () => goTo(current + 1));

document.getElementById('tocLink').addEventListener('click', () => {
    // 그림책은 차례가 표지에 붙어 있다.
    const idx = PAGES.findIndex(p => p.kind === 'toc');
    goTo(idx >= 0 ? idx : 0);
});

document.addEventListener('keydown', (e) => {
    if (e.key === 'ArrowRight') goTo(current + 1);
    if (e.key === 'ArrowLeft') goTo(current - 1);
});

paint();
