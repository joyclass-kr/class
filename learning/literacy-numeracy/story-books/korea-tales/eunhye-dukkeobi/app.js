const CHAPTERS = [
    {
        num: 1,
        title: "1장 · 부뚜막의 손님",
        beats: [
            {
                art: "01-girl.webp",
                emoji: "🍚",
                left: [
                    "옛날 어느 마을에 가난한 소녀가 살았습니다. 부모를 일찍 여의고 남의 집 부엌일을 하며 지냈지요.",
                    "주인집 아주머니는 소녀에게 살갑지 않았어요.",
                    "새벽부터 물을 긷고 불을 때고 설거지를 했어요. 손이 늘 트고 갈라져 있었답니다.",
                    "\"밥값은 해야지.\""
                ],
                right: [
                    "하루 종일 밥을 지어도 소녀 몫은 늘 한 줌뿐이었어요. 그마저도 식은 밥일 때가 많았어요.",
                    "소녀는 부엌 구석에서 먹었지요.",
                    "그래도 소녀는 투덜대는 법이 없었습니다. 밥그릇을 받으면 고맙다고 인사부터 했어요.",
                    "부모님이 그리 하라고 가르쳤거든요."
                ]
            },
            {
                art: "02-toad.webp",
                emoji: "🐸",
                left: [
                    "어느 날 저녁, 소녀가 마지막 설거지를 하던 참이었어요.",
                    "부뚜막 구석에서 무언가 꿈틀했어요. 그림자가 어른거렸지요.",
                    "\"어머!\"",
                    "소녀는 깜짝 놀라 뒤로 물러섰습니다. 부지깽이를 든 손이 부들부들 떨렸답니다."
                ],
                right: [
                    "들여다보니 두꺼비 한 마리가 눈을 껌뻑이고 있었습니다. 등이 울퉁불퉁하고 배가 홀쭉했지요.",
                    "두꺼비도 소녀를 올려다보았어요.",
                    "소녀는 부지깽이를 슬며시 내려놓았어요.",
                    "\"너도 배가 고파서 여기까지 왔구나.\"",
                    "소녀는 저도 모르게 웃었답니다."
                ]
            },
            {
                art: "03-share.webp",
                emoji: "🤲",
                left: [
                    "소녀는 제 밥그릇에서 밥알을 덜어 부뚜막에 놓았어요.",
                    "그러잖아도 모자란 밥이었지요. 그것이 소녀의 저녁 몫 전부였거든요.",
                    "두꺼비가 우물우물 밥알을 받아먹었습니다. 소녀는 턱을 괴고 바라보았답니다.",
                    "그래도 아깝다는 생각이 들지 않았지요."
                ],
                right: [
                    "\"내일도 오려무나. 나 혼자 먹으면 심심하잖니.\"",
                    "두꺼비는 밥알을 남기지 않았어요.",
                    "그날부터 소녀는 날마다 밥을 덜어 두었어요.",
                    "적을 때는 밥알 몇 알, 많을 때는 한 숟갈이었지요.",
                    "소녀는 그 시간이 하루 중 제일 좋았답니다."
                ]
            },
            {
                art: "04-grow.webp",
                emoji: "📏",
                left: [
                    "달이 가고 해가 갔습니다. 두꺼비는 밥을 먹을수록 무럭무럭 자랐어요.",
                    "처음에는 소녀의 손바닥만 하더니, 어느새 두 손으로도 다 안기지 않을 만큼 커졌지요.",
                    "부뚜막이 좁아 보일 지경이었어요.",
                    "소녀는 그것이 신기하기만 했지요."
                ],
                right: [
                    "\"너 참 많이 컸구나.\"",
                    "소녀가 두꺼비의 등을 쓰다듬으며 웃었습니다.",
                    "두꺼비의 등이 따뜻했어요.",
                    "두꺼비는 알아듣는다는 듯 눈을 끔뻑였어요. 소녀에게는 그 두꺼비가 하나뿐인 동무였답니다.",
                    "소녀는 두꺼비에게 속엣말을 다 했지요."
                ]
            }
        ]
    },
    {
        num: 2,
        title: "2장 · 사당에서의 하룻밤",
        beats: [
            {
                art: "05-shrine.webp",
                emoji: "⛩️",
                left: [
                    "그 마을에는 오랜 근심이 하나 있었어요.",
                    "뒷산 사당에 커다란 지네가 산다는 것이었지요.",
                    "사람 발길이 끊긴 빈 사당이었지요.",
                    "지네가 성이 나면 논밭이 마르고 짐승이 병든다고 했습니다. 아무도 그 말을 의심하지 않았답니다.",
                ],
                right: [
                    "그래서 마을에서는 해마다 한 사람을 뽑아 사당에서 밤을 지키게 했어요.",
                    "제비를 뽑아 정하는 것이 마을 법이었어요.",
                    "사당에서 지내는 하룻밤은 무섭기로 온 고을에 이름이 나 있었어요.",
                    "그 밤을 넘기고 돌아온 사람은 여태 없었답니다."
                ]
            },
            {
                art: "06-chosen.webp",
                emoji: "😰",
                left: [
                    "그해에 뽑힌 사람은 바로 소녀였습니다. 부모도 없고 편들어 줄 사람도 없었으니까요.",
                    "제비를 뽑기도 전에 정해진 것이나 마찬가지였지요.",
                    "소녀는 아무 말도 하지 않았어요. 그저 고개만 한 번 끄덕였지요.",
                    "아무도 소녀를 말리지 않았지요."
                ],
                right: [
                    "\"미안하구나. 우리도 어쩔 수 없단다.\"",
                    "주인집 아주머니도 고개를 돌렸어요.",
                    "마을 사람들은 소녀와 눈을 마주치지 못했어요.",
                    "해가 기울자 소녀는 흰옷으로 갈아입고 산길을 올랐답니다. 아무도 따라오지 않았어요.",
                    "소녀는 뒤도 돌아보지 않았지요."
                ]
            },
            {
                art: "07-hidden.webp",
                emoji: "🫥",
                left: [
                    "낡은 사당 안은 어둡고 조용했습니다. 문틈으로 바람이 휘휘 들어왔지요.",
                    "달빛이 마룻바닥에 가늘게 깔렸지요.",
                    "소녀가 구석에 무릎을 안고 앉았을 때였어요. 치마폭이 이상하게 묵직했습니다.",
                    "무언가 안에서 꼼지락거렸지요."
                ],
                right: [
                    "소녀가 치마를 들춰 보았어요. 그 안에 두꺼비가 웅크리고 있었지요.",
                    "두꺼비가 소녀를 올려다보았어요.",
                    "\"너… 언제부터 따라온 거니?\"",
                    "두꺼비는 대답 대신 소녀의 무릎 위로 기어 올라왔답니다.",
                    "소녀는 그제야 눈물이 났답니다."
                ]
            },
            {
                art: "08-centipede.webp",
                emoji: "😱",
                left: [
                    "한밤중이었습니다.",
                    "천장에서 스르륵— 무언가 쓸리는 소리가 났어요.",
                    "소녀가 아주 천천히 고개를 들었지요. 숨을 쉬는 것도 잊어버렸답니다.",
                    "소녀는 치마폭을 꼭 움켜쥐었어요."
                ],
                right: [
                    "천장 아래 허공에 커다란 것이 떠 있었어요. 어마어마하게 큰 지네였지요.",
                    "몸통이 기둥만큼 굵고, 마디마다 구불구불 굽이쳤어요.",
                    "몸에서 시퍼런 빛이 뿜어져 나와 사당 안을 온통 물들였습니다.",
                    "소녀는 눈을 질끈 감았답니다."
                ]
            }
        ]
    },
    {
        num: 3,
        title: "3장 · 두 빛이 맞부딪히다",
        beats: [
            {
                art: "09-glow.webp",
                emoji: "✨",
                left: [
                    "그때 치마폭이 들썩하더니 두꺼비가 훌쩍 뛰어나왔어요. 그러고는 소녀 앞을 딱 가로막고 섰지요.",
                    "두꺼비의 몸이 점점 부풀기 시작했습니다.",
                    "등이 풍선처럼 커다랗게 불어올랐어요.",
                    "눈에서도 빛이 났지요."
                ],
                right: [
                    "두꺼비 등에서 따뜻한 노란빛이 피어올랐습니다.",
                    "사당 안이 대낮처럼 환해졌어요.",
                    "시퍼런 빛과 노란빛이 쩌엉— 하고 맞부딪혔어요.",
                    "기둥이 부르르 떨리고 천장에서 먼지가 쏟아졌지요.",
                    "소녀는 두꺼비 뒤에 몸을 붙이고 지켜보았답니다."
                ]
            },
            {
                art: "10-retreat.webp",
                emoji: "🌀",
                left: [
                    "얼마나 지났을까요. 두 빛이 밀고 밀렸어요.",
                    "사당이 통째로 흔들렸지요.",
                    "노란빛이 한 뼘, 또 한 뼘 밀고 올라갔습니다.",
                    "시퍼런 빛이 조금씩 흐려지기 시작했어요. 지네의 몸이 힘없이 축 늘어졌지요."
                ],
                right: [
                    "지네가 마지막으로 크게 몸부림쳤어요.",
                    "마침내 지네는 검은 연기가 되어 스르르 흩어졌습니다.",
                    "노란빛도 촛불이 꺼지듯 스러져 갔어요. 사당 안이 다시 캄캄해졌답니다.",
                    "두꺼비도 그 자리에 스르르 주저앉았답니다."
                ]
            },
            {
                art: "11-morning.webp",
                emoji: "🌅",
                left: [
                    "날이 밝자 마을 사람들이 조심조심 사당으로 올라왔어요. 발소리를 죽이며 문 앞까지 다가갔지요.",
                    "밤새 아무 소리도 나지 않았거든요.",
                    "\"설마…\"",
                    "아무도 선뜻 문을 열지 못했습니다.",
                    "다들 나쁜 일을 각오하고 있었어요."
                ],
                right: [
                    "끼익 하고 문을 열자 소녀가 무사히 앉아 있었어요. 사람들이 입을 딱 벌렸지요.",
                    "소녀의 옷에는 흙 한 점 묻지 않았어요.",
                    "소녀의 품에는 축 늘어진 두꺼비가 안겨 있었습니다.",
                    "\"제발… 눈 좀 떠 봐.\"",
                    "소녀의 목소리가 잠겨 있었답니다."
                ]
            },
            {
                art: "12-recover.webp",
                emoji: "🌿",
                left: [
                    "소녀는 두꺼비를 집으로 데려가 정성껏 돌보았어요. 밤새 물수건을 갈아 주고 밥알을 입에 대 주었지요.",
                    "주인집 아주머니가 좋은 방을 내주었어요.",
                    "온 마을이 소녀를 도왔지요.",
                    "이레가 지나자 두꺼비가 눈을 껌뻑 떴습니다."
                ],
                right: [
                    "\"고맙다. 정말 고맙다.\"",
                    "두꺼비가 소녀의 손가락을 툭 건드렸어요.",
                    "소녀는 두꺼비를 안고 한참을 울었어요.",
                    "그 뒤로 사당에서 밤을 지키는 일은 없어졌답니다. 지네가 다시는 나타나지 않았거든요.",
                    "두꺼비는 그 뒤로도 오래오래 소녀 곁에 있었지요."
                ]
            }
        ],
        moral: "소녀가 두꺼비에게 나눠 준 것은 남은 밥이 아니라 자기 몫의 밥이었어요. 넉넉해서 준 것이 아니라 모자란데도 나눈 것이지요. 그 마음이 여러 해 뒤에 소녀를 지켜 주었답니다.",
        question: "넉넉하지 않을 때에도 나눠 본 적이 있나요?"
    }
];

function artFrame(src, emoji) {
    return `
        <div class="art-frame">
            <img src="images/${src}" alt="" onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';">
            <div class="art-fallback" style="display:none">${emoji}</div>
        </div>`;
}

function coverToc() {
    const item = s => `
        <button type="button" data-goto="${s.num}">
            <span class="toc-num">${s.num}</span>
            <span>${s.title.replace(/^\d+장 · /, '')}</span>
        </button>`;
    return `
        <nav class="cover-toc">
            <h2>차례</h2>
            ${CHAPTERS.map(item).join('')}
            <button type="button" data-goto-kind="quiz">
                <span class="toc-num">❓</span>
                <span>이야기 문제</span>
            </button>
        </nav>`;
}

function coverPage() {
    return `
        <div class="page page-cover">
            <div class="story-page-left story-page-left-full">
                ${artFrame('cover.webp', '🐸')}
            </div>
            <div class="story-page-right">
                <h1>은혜 갚은 두꺼비</h1>
                <p>은혜 갚은 두꺼비는 지은이가 없는 구전 설화예요. 사람이 거두어 준 짐승이 되갚는 이야기 갈래에 속한답니다.</p>
                <p>옛날 우리 조상들은 두꺼비를 집을 지켜 주는 짐승으로 여겼어요. 부엌이나 마루 밑에 두꺼비가 들면 복이 든다고 해서 쫓지 않고 그냥 두었지요. 두꺼비가 은혜를 갚는 이야기가 여럿 전해지는 것도 그런 마음 때문이랍니다.</p>
                <p>이야기에 나오는 지네는 실제 벌레보다 훨씬 크고 무서운 존재로 그려져요. 옛이야기에서 지네는 오래 묵어 이상한 힘을 얻은 짐승으로 자주 나오는데, 두꺼비와 지네가 서로 맞서는 짜임은 우리나라 여러 고장에서 똑같이 전한답니다.</p>
                ${coverToc()}
            </div>
        </div>`;
}

function spreadPage(chapter, beat, isFirst) {
    const badgeHtml = isFirst ? `<div class="spread-chapter-badge">${chapter.title}</div>` : '';
    const leftHtml = beat.left.map(p => `<p>${p}</p>`).join('');
    const rightHtml = beat.right.map(p => `<p>${p}</p>`).join('');
    return `
        <div class="page page-story">
            <div class="spread-art">
                ${badgeHtml}
                ${artFrame(beat.art, beat.emoji)}
            </div>
            <div class="spread-text">
                <div class="spread-text-left">${leftHtml}</div>
                <div class="spread-text-right">${rightHtml}</div>
            </div>
        </div>`;
}


const QUIZ = [
    { q: "부뚜막에서 두꺼비를 본 소녀는 어떻게 했나요?", choices: ["부지깽이로 쫓아냈다", "부지깽이를 슬며시 내려놓았다", "주인집 아주머니를 불렀다"], answer: 1 },
    { q: "마을 사람들의 오랜 근심은 무엇이었나요?", choices: ["해마다 드는 가뭄", "산에서 오는 호랑이", "사당의 커다란 지네"], answer: 2 },
    { q: "마을 사람들이 해마다 사당을 지킨 까닭은 무엇인가요?", choices: ["사당이 낡고 위험해서", "도둑이 자꾸 들어와서", "지네가 성을 낼까 봐"], answer: 2 },
    { q: "한밤중 사당 천장에서 난 소리는 어땠나요?", choices: ["스르륵 쓸리는 소리", "문틈으로 바람이 휘휘 부는 소리", "밥알을 우물우물 먹는 소리"], answer: 0 },
    { q: "두꺼비와 지네는 무엇으로 겨루었나요?", choices: ["뿜어내는 빛으로", "큰 소리를 질러서", "서로 밀어붙여서"], answer: 0 },
    { q: "그 뒤로 마을은 어떻게 되었나요?", choices: ["사당을 새로 지었다", "밤을 지키지 않게 됐다", "두꺼비를 길렀다"], answer: 1 }
];

function quizPage() {
    const items = QUIZ.map((item, i) => `
        <div class="quiz-item" data-qindex="${i}">
            <p class="quiz-question">${i + 1}. ${item.q}</p>
            <div class="quiz-choices">
                ${item.choices.map((c, ci) => `<button type="button" class="quiz-choice" data-choice="${ci}">${c}</button>`).join('')}
            </div>
        </div>`).join('');
    return `
        <div class="page page-quiz">
            <h2>이야기 문제</h2>
            <p class="quiz-intro-text" id="quizProgress">0 / 총 ${QUIZ.length}문항 완료</p>
            <div class="quiz-list">${items}</div>
        </div>`;
}


/* 읽고 나서 — 세계명작 트랙과 같은 형식이다. 동화틀은 쪽을 재서 나누지 않으므로
   펼침면마다 왼쪽·오른쪽 글을 손으로 나누어 둔다. */
const AFTERWORD = {
    title: '읽고 나서',
    emoji: '🐸',
    spreads: [
        {
            art: 'end.webp',
            left: [
                "짐승을 길러 준 사람이 뒷날 그 짐승에게 도움을 받는 이야기를 보은 이야기라고 합니다. 우리 옛이야기에서 가장 자주 나오는 틀 가운데 하나입니다.",
                "이 이야기에는 그것 말고 또 하나가 겹쳐 있습니다. 마을이 해마다 사람을 바치던 대목입니다. 무서운 것을 달래려고 사람을 내주는 이야기는 세계 곳곳에 있습니다.",
                "그런 이야기에서 사람을 구하는 것은 대개 힘센 장수입니다. 그런데 여기서는 소녀가 밥을 나눠 준 두꺼비가 옵니다.",
                "이 이야기가 마을을 구한 것으로 끝나지 않고 그 뒤 소녀가 어떻게 살았는지까지 말해 주는 것도 눈여겨볼 만합니다."
            ],
            right: [
                "두꺼비는 왜 소녀를 도왔을까요? 밥 때문일까요, 다른 무엇 때문일까요.",
                "소녀가 두꺼비에게 나눠 준 것은 남은 밥이 아니라 제 몫의 밥이었습니다. 넉넉해서 준 것이 아니라 모자란데도 나눈 것입니다. 그 마음이 여러 해 뒤에 소녀를 지켜 주었습니다.",
                "넉넉하지 않을 때에도 나눠 본 적이 있나요?"
            ]
        }
    ]
};

function afterPage(spread, isFirst) {
    const head = isFirst ? `<h2>${AFTERWORD.title}</h2>` : '';
    // 그림은 오른쪽 칸 맨 위 모서리에 꽉 붙인다. 학습 허브로 가는 길은 그 칸 맨 아래에 둔다.
    const art = spread.art ? `<div class="after-art">${artFrame(spread.art, AFTERWORD.emoji)}</div>` : '';
    const foot = spread.last ? `<p class="after-home"><a class="home-btn" href="../../../../../">학습 허브로 돌아가기</a></p>` : '';
    const col = (ps) => ps.map(t => `<p>${t}</p>`).join('');
    return `
        <div class="page page-after">
            <div class="after-col after-col-left">
                ${head}
                ${col(spread.left)}
            </div>
            <div class="after-col after-col-right${spread.art ? ' after-col-image' : ''}">
                ${art}
                ${col(spread.right)}
                ${foot}
            </div>
        </div>`;
}

const PAGES = [
    { kind: 'cover' },
    ...CHAPTERS.flatMap(chapter => chapter.beats.map((beat, i) => ({ kind: 'spread', chapter, beat, isFirst: i === 0 }))),
    { kind: 'quiz' },
    ...AFTERWORD.spreads.map((spread, i) => ({ kind: 'after', spread, isFirst: i === 0, last: i === AFTERWORD.spreads.length - 1 })),
];

const TWO_PAGE_KINDS = new Set(['spread', 'cover', 'after']);

let folioCounter = 0;
const FOLIOS = PAGES.map(p => {
    const width = TWO_PAGE_KINDS.has(p.kind) ? 2 : 1;
    const start = folioCounter + 1;
    folioCounter += width;
    return { start, width };
});

function renderPage(page) {
    switch (page.kind) {
        case 'cover':
            return coverPage();
        case 'spread':
            return spreadPage(page.chapter, page.beat, page.isFirst);
        case 'after':
            return afterPage({ ...page.spread, last: page.last }, page.isFirst);
        case 'quiz':
            return quizPage();
        default:
            return '';
    }
}

let current = 0;
let animating = false;

const spreadEl = document.getElementById('spread');
const prevBtn = document.getElementById('prevBtn');
const nextBtn = document.getElementById('nextBtn');
const indicatorEl = document.getElementById('pageIndicator');
const folioLeftEl = document.getElementById('folioLeft');
const folioRightEl = document.getElementById('folioRight');

function paint() {
    spreadEl.innerHTML = renderPage(PAGES[current]);
    prevBtn.disabled = current === 0;
    nextBtn.disabled = current === PAGES.length - 1;
    indicatorEl.textContent = `${current + 1} / ${PAGES.length}`;

    const folio = FOLIOS[current];
    folioLeftEl.classList.toggle('folio-center', folio.width === 1);
    if (folio.width === 2) {
        folioLeftEl.textContent = folio.start;
        folioRightEl.textContent = folio.start + 1;
        folioLeftEl.hidden = false;
        folioRightEl.hidden = false;
    } else {
        folioLeftEl.textContent = folio.start;
        folioLeftEl.hidden = false;
        folioRightEl.hidden = true;
    }

    spreadEl.querySelectorAll('[data-goto]').forEach(btn => {
        btn.addEventListener('click', () => {
            const chapterNum = Number(btn.dataset.goto);
            const idx = PAGES.findIndex(p => p.kind === 'spread' && p.chapter.num === chapterNum && p.isFirst);
            if (idx >= 0) goTo(idx);
        });
    });
    spreadEl.querySelectorAll('[data-goto-kind]').forEach(btn => {
        btn.addEventListener('click', () => {
            const idx = PAGES.findIndex(p => p.kind === btn.dataset.gotoKind);
            if (idx >= 0) goTo(idx);
        });
    });

    if (PAGES[current].kind === 'quiz') {
        initQuiz();
    }
    fitAfterword();
}

/* 「읽고 나서」는 글씨를 줄이지 않는다. 세로 화면에서는 끝그림을 접어 두는 것이
   기본이고(styles.css), 여기서 그림을 켜 보아 자리가 남을 때만 그대로 둔다.
   그래서 이 함수가 한 번도 못 돌아도 글이 잘리지는 않는다. */
function fitAfterword() {
    const page = spreadEl.querySelector('.page-after');
    if (!page) return;
    page.classList.add('after-roomy');
    const spills = [...page.querySelectorAll('.after-col')]
        .some(col => col.scrollHeight > col.clientHeight);
    if (spills) page.classList.remove('after-roomy');
}

/* 화면 크기가 바뀌면 다시 잰다. 크기가 한 박자 늦게 잡히는 곳이 있어
   그 자리에서 한 번, 잠시 뒤에 한 번 더 잰다. */
let refitTimer = 0;
function refitAfterword() {
    fitAfterword();
    clearTimeout(refitTimer);
    refitTimer = setTimeout(fitAfterword, 250);
}

window.addEventListener('resize', refitAfterword);
window.addEventListener('orientationchange', refitAfterword);
/* 화면을 돌렸을 때 resize 가 안 잡히는 곳이 있다. 책 상자 크기를 직접 지켜본다. */
if (window.ResizeObserver) {
    const bookEl = document.getElementById('book');
    if (bookEl) new ResizeObserver(refitAfterword).observe(bookEl);
}
if (document.fonts && document.fonts.ready) document.fonts.ready.then(fitAfterword);

function initQuiz() {
    let answeredCount = 0;
    const progressEl = document.getElementById('quizProgress');

    spreadEl.querySelectorAll('.quiz-item').forEach(item => {
        const qi = Number(item.dataset.qindex);
        const q = QUIZ[qi];
        item.querySelectorAll('.quiz-choice').forEach(btn => {
            btn.addEventListener('click', () => {
                if (item.classList.contains('graded')) return;
                item.classList.add('graded');
                const chosen = Number(btn.dataset.choice);
                item.querySelectorAll('.quiz-choice').forEach(b => {
                    const ci = Number(b.dataset.choice);
                    if (ci === q.answer) b.classList.add('correct');
                    else if (ci === chosen) b.classList.add('incorrect');
                });
                answeredCount++;
                progressEl.textContent = `${answeredCount} / 총 ${QUIZ.length}문항 완료`;
            });
        });
    });
}

function goTo(index) {
    if (animating || index === current || index < 0 || index >= PAGES.length) return;
    animating = true;
    const dir = index > current ? 'flip-next' : 'flip-prev';
    spreadEl.classList.add(dir);
    setTimeout(() => {
        current = index;
        paint();
    }, 230);
    setTimeout(() => {
        spreadEl.classList.remove('flip-next', 'flip-prev');
        animating = false;
    }, 480);
}

prevBtn.addEventListener('click', () => goTo(current - 1));
nextBtn.addEventListener('click', () => goTo(current + 1));

document.getElementById('tocLink').addEventListener('click', () => {
    // 그림책은 차례가 표지에 붙어 있다.
    const idx = PAGES.findIndex(p => p.kind === 'toc');
    goTo(idx >= 0 ? idx : 0);
});

document.addEventListener('keydown', (e) => {
    if (e.key === 'ArrowRight') goTo(current + 1);
    if (e.key === 'ArrowLeft') goTo(current - 1);
});

paint();
