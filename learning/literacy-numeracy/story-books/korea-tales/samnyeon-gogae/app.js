const CHAPTERS = [
    {
        num: 1,
        title: "1장 · 고개 아래 마을",
        beats: [
            {
                art: "01-pass.webp",
                emoji: "⛰️",
                left: [
                    "마을 뒤에 고개가 하나 있습니다. 이름은 삼년 고개.",
                    "높지도 낮지도 않은 흔한 고개였어요.",
                    "\"저 고개에서 넘어지면 삼 년밖에 못 산대.\"",
                    "어른도 아이도 다 아는 말이었어요. 언제부터 그랬는지는 아무도 몰랐지요.",
                    "그래도 그 말만은 온 마을이 믿었지요."
                ],
                right: [
                    "그래서 사람들은 고개를 넘을 때마다 발밑만 보고 걸었습니다.",
                    "어머니들은 아이 손을 꼭 붙들었어요.",
                    "\"뛰지 마라! 넘어지면 큰일 난다!\"",
                    "고개를 다 넘고 나서야 다들 휴 하고 숨을 내쉬었어요.",
                    "고개 하나 넘는 데 반나절이 걸리기도 했답니다."
                ]
            },
            {
                art: "02-sandals.webp",
                emoji: "🪢",
                left: [
                    "고개 아래 첫 집에는 박 노인이 살았습니다. 일흔이 넘었지만 허리는 대나무처럼 꼿꼿했어요.",
                    "젊은 시절부터 짚신을 삼아 살았지요.",
                    "밤마다 등불 앞에 앉아 짚신을 삼았지요. 볏짚 꼬는 소리가 사각사각 났습니다.",
                    "손끝이 굳은살로 두툼했어요."
                ],
                right: [
                    "\"박 노인 짚신은 일 년을 신어도 멀쩡하다니까.\"",
                    "\"나도 한 켤레 주시오!\"",
                    "박 노인의 짚신은 발이 편하기로 이름났어요.",
                    "장에 나가기만 하면 눈 깜짝할 사이에 다 팔렸어요. 지게가 텅 빌 때까지 일어나지도 못했지요.",
                    "박 노인은 그것이 늘 뿌듯했답니다."
                ]
            },
            {
                art: "03-homeward.webp",
                emoji: "🌄",
                left: [
                    "그날도 짚신은 하나도 남지 않았습니다.",
                    "장이 파할 무렵이었어요.",
                    "박 노인은 빈 지게를 지고 콧노래를 불렀어요. 주머니에서 엽전이 짤랑짤랑 소리를 냈지요.",
                    "\"흠흠, 오늘은 참 좋은 날이로구나.\"",
                    "박 노인은 지게를 고쳐 멨지요."
                ],
                right: [
                    "해가 뉘엿뉘엿 넘어갑니다. 집에 가려면 삼년 고개를 넘어야 하지요.",
                    "박 노인은 성큼성큼 고갯길을 올랐어요.",
                    "하지만 박 노인은 조금도 걱정이 없었어요.",
                    "'내가 이 고개를 몇십 년을 넘었는데. 한 번도 안 넘어졌지.'",
                    "콧노래가 절로 나왔답니다."
                ]
            }
        ]
    },
    {
        num: 2,
        title: "2장 · 넘어진 날",
        beats: [
            {
                art: "04-stumble.webp",
                emoji: "😲",
                left: [
                    "고갯마루가 코앞이었어요.",
                    "박 노인은 앞만 보고 걸었지요.",
                    "바로 그때였습니다.",
                    "툭!",
                    "\"어어어?\"",
                    "돌부리에 발이 걸린 박 노인이 팔을 허우적거리며 그대로 고꾸라졌어요."
                ],
                right: [
                    "쿵!",
                    "엽전이 사방으로 흩어졌어요.",
                    "다행히 크게 다치지는 않았습니다. 무릎이 조금 쓸렸을 뿐이지요.",
                    "박 노인은 옷에 묻은 흙을 툭툭 털며 일어났어요.",
                    "\"어이쿠, 늙으면 별수 없구먼.\"",
                    "박 노인은 엽전을 주우며 웃었지요."
                ]
            },
            {
                art: "05-realize.webp",
                emoji: "😰",
                left: [
                    "그런데 고개를 든 순간이었어요.",
                    "박 노인이 사방을 둘러보았습니다.",
                    "낯익은 것들이 눈에 들어왔지요.",
                    "길가의 늙은 소나무. 쩍 갈라진 바위.",
                    "\"서, 설마…\""
                ],
                right: [
                    "\"여기가 삼년 고개잖아!\"",
                    "박 노인의 얼굴이 하얗게 질렸어요.",
                    "박 노인은 그 자리에 털썩 주저앉았어요.",
                    "\"삼 년… 나는 이제 삼 년밖에 못 사는구나.\"",
                    "빈 지게가 옆으로 데구루루 굴렀습니다.",
                    "박 노인은 한참을 일어나지 못했답니다."
                ]
            },
            {
                art: "06-bedridden.webp",
                emoji: "🛏️",
                left: [
                    "그날부터 박 노인은 자리에서 일어나지 않았습니다. 밤마다 사각거리던 짚신 삼는 소리도 뚝 끊겼지요.",
                    "이불을 머리끝까지 덮고 누웠어요.",
                    "\"여보, 밥 좀 드세요.\"",
                    "\"됐네… 삼 년밖에 못 사는데 밥은 먹어 무엇 하나.\"",
                    "밥도 물도 마다했지요."
                ],
                right: [
                    "할머니가 아무리 달래도 소용없었어요. 박 노인은 하루 종일 벽만 보고 누워 있었습니다.",
                    "할머니는 문가에서 눈물을 훔치는 날이 늘었지요.",
                    "\"삼 년… 삼 년…\"",
                    "그 소리만 중얼거렸지요.",
                    "온 마을이 그 소식을 알게 되었답니다."
                ]
            },
            {
                art: "07-doctor.webp",
                emoji: "🩺",
                left: [
                    "마을 의원이 한달음에 달려왔습니다.",
                    "의원은 약상자까지 들고 왔어요.",
                    "맥을 짚어 보고, 혀를 들여다보고, 배를 꾹꾹 눌러 보았어요. 그러더니 고개를 갸웃했지요.",
                    "\"이상하네요. 아픈 데가 한 군데도 없는데요?\"",
                    "아무리 살펴도 병이 없었지요."
                ],
                right: [
                    "\"아닐세. 나는 삼 년 뒤에 죽네.\"",
                    "박 노인은 벽만 보고 대답했어요.",
                    "\"예?\"",
                    "의원은 어이가 없어 그냥 돌아갔어요. 이웃들이 찾아와 달래 봐도 마찬가지였습니다.",
                    "박 노인은 날마다 조금씩 야위어 갔지요.",
                    "볼이 홀쭉해지고 눈이 퀭해졌지요."
                ]
            }
        ]
    },
    {
        num: 3,
        title: "3장 · 다시 구른 고개",
        beats: [
            {
                art: "08-deoki.webp",
                emoji: "👦",
                left: [
                    "이웃집 덕이가 그 소문을 들었습니다. 열 살짜리 덕이는 꾀가 많기로 이름난 아이였지요.",
                    "덕이는 그 말이 도무지 이상했어요. 넘어진다고 죽을 리가 없잖아요.",
                    "덕이는 곧장 박 노인네 방문을 열고 들어갔어요.",
                    "\"할아버지, 왜 누워 계세요?\""
                ],
                right: [
                    "덕이가 박 노인 곁에 앉았어요.",
                    "\"삼년 고개에서 넘어졌단다. 이제 삼 년밖에 못 살아.\"",
                    "박 노인이 힘없이 대답했습니다.",
                    "덕이는 고개를 갸웃하고 한참을 생각하더니, 갑자기 눈을 반짝였어요.",
                    "무언가 좋은 수가 떠오른 얼굴이었지요."
                ]
            },
            {
                art: "09-counting.webp",
                emoji: "🔢",
                left: [
                    "\"할아버지, 한 번 넘어지면 삼 년이지요?\"",
                    "덕이의 목소리가 아주 또랑또랑했어요.",
                    "\"그렇지…\"",
                    "박 노인이 힘없이 대답했지요.",
                    "\"그럼 두 번 넘어지면요?\""
                ],
                right: [
                    "\"육 년이지… 응?\"",
                    "덕이가 손가락을 하나씩 꼽았어요.",
                    "\"세 번이면 구 년! 열 번 구르면 삼십 년이에요!\"",
                    "박 노인이 눈을 크게 떴습니다. 감고 있던 이불이 스르르 흘러내렸지요.",
                    "방 안이 쥐 죽은 듯 조용해졌습니다.",
                    "박 노인이 이불을 걷어찼답니다."
                ]
            },
            {
                art: "10-rolling.webp",
                emoji: "🤸",
                left: [
                    "\"옳지! 그렇구나!\"",
                    "박 노인이 벌떡 일어나 신발도 제대로 못 신고 삼년 고개로 달려갔습니다.",
                    "데굴데굴, 데구루루.",
                    "\"하나! 둘! 셋!\"",
                    "할머니가 뒤에서 부르는데도 들리지 않았지요."
                ],
                right: [
                    "소문을 들은 사람들이 모여 큰 소리로 세어 주었어요.",
                    "아이들이 손뼉을 치며 따라 세었어요.",
                    "스무 번을 구르고 일어선 박 노인에게 화색이 돌았습니다.",
                    "\"허허, 내가 삼백 년은 살겠구먼!\"",
                    "그날부터 다시 짚신 삼는 소리가 났답니다."
                ]
            }
        ],
        moral: "무서운 일이라고 믿으면 아무렇지 않은 일도 무서워져요. 같은 일도 어떻게 보느냐에 따라 두려움이 되기도 하고, 아무것도 아닌 일이 되기도 한답니다.",
        question: "걱정 때문에 아무것도 못 하고 있던 적이 있나요? 그 일을 다르게 볼 방법은 없을까요?"
    }
];

function artFrame(src, emoji) {
    return `
        <div class="art-frame">
            <img src="images/${src}" alt="" onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';">
            <div class="art-fallback" style="display:none">${emoji}</div>
        </div>`;
}

function coverToc() {
    const item = s => `
        <button type="button" data-goto="${s.num}">
            <span class="toc-num">${s.num}</span>
            <span>${s.title.replace(/^\d+장 · /, '')}</span>
        </button>`;
    return `
        <nav class="cover-toc">
            <h2>차례</h2>
            ${CHAPTERS.map(item).join('')}
            <button type="button" data-goto-kind="quiz">
                <span class="toc-num">❓</span>
                <span>이야기 문제</span>
            </button>
        </nav>`;
}

function coverPage() {
    return `
        <div class="page page-cover">
            <div class="story-page-left story-page-left-full">
                ${artFrame('cover.webp', '⛰️')}
            </div>
            <div class="story-page-right">
                <h1>삼년 고개</h1>
                <p>삼년 고개는 지은이가 없는 이야기예요. 어느 한 사람이 만든 것이 아니라 수백 년 동안 이 사람 저 사람의 입을 거치며 다듬어진 이야기지요. 이런 이야기를 구전 설화라고 부른답니다.</p>
                <p>지은이가 없다 보니 정해진 원본도 없어요. 고을마다 고개 이름이 다르고, 넘어진 사람이 할머니인 이야기도, 꾀를 알려 주는 이가 나그네인 이야기도 남아 있답니다.</p>
                <p>1979년부터 1988년까지 학자들이 전국의 마을을 돌며 어른들의 옛이야기를 녹음해 여든 권이 넘는 책으로 묶었어요. 한국구비문학대계라고 한답니다.</p>
                <p>삼년 고개는 그 가운데 지혜담에 속해요. 힘이나 도술이 아니라 생각을 한 번 뒤집는 것만으로 답이 나오는 이야기지요.</p>
                ${coverToc()}
            </div>
        </div>`;
}

function spreadPage(chapter, beat, isFirst) {
    const badgeHtml = isFirst ? `<div class="spread-chapter-badge">${chapter.title}</div>` : '';
    const leftHtml = beat.left.map(p => `<p>${p}</p>`).join('');
    const rightHtml = beat.right.map(p => `<p>${p}</p>`).join('');
    return `
        <div class="page page-story">
            <div class="spread-art">
                ${badgeHtml}
                ${artFrame(beat.art, beat.emoji)}
            </div>
            <div class="spread-text">
                <div class="spread-text-left">${leftHtml}</div>
                <div class="spread-text-right">${rightHtml}</div>
            </div>
        </div>`;
}


const QUIZ = [
    { q: "고개를 다 넘고 나서 사람들은 어떻게 했나요?", choices: ["발밑만 보고 걸었다", "휴 하고 숨을 쉬었다", "아이 손을 붙들었다"], answer: 1 },
    { q: "박 노인이 밤마다 등불 앞에서 한 일은 무엇인가요?", choices: ["엽전 세기", "지게 고치기", "짚신 삼기"], answer: 2 },
    { q: "박 노인은 왜 고개에서 넘어졌나요?", choices: ["돌부리에 발이 걸려서", "지게가 무거워서", "소나무에 부딪혀서"], answer: 0 },
    { q: "마을 의원은 박 노인을 보고 뭐라고 했나요?", choices: ["다리뼈가 부러졌다", "약을 오래 먹어야 한다", "몸에 아픈 데가 없다"], answer: 2 },
    { q: "덕이는 열 번 구르면 몇 년이라고 했나요?", choices: ["삼십 년", "구 년", "삼백 년"], answer: 0 },
    { q: "고갯마루에서 구르고 난 박 노인은 어떻게 되었나요?", choices: ["더 크게 다쳤다", "기운을 되찾았다", "그대로 앓아누웠다"], answer: 1 }
];

function quizPage() {
    const items = QUIZ.map((item, i) => `
        <div class="quiz-item" data-qindex="${i}">
            <p class="quiz-question">${i + 1}. ${item.q}</p>
            <div class="quiz-choices">
                ${item.choices.map((c, ci) => `<button type="button" class="quiz-choice" data-choice="${ci}">${c}</button>`).join('')}
            </div>
        </div>`).join('');
    return `
        <div class="page page-quiz">
            <h2>이야기 문제</h2>
            <p class="quiz-intro-text" id="quizProgress">0 / 총 ${QUIZ.length}문항 완료</p>
            <div class="quiz-list">${items}</div>
        </div>`;
}


/* 읽고 나서 — 세계명작 트랙과 같은 형식이다. 동화틀은 쪽을 재서 나누지 않으므로
   펼침면마다 왼쪽·오른쪽 글을 손으로 나누어 둔다. */
const AFTERWORD = {
    title: '읽고 나서',
    emoji: '⛰️',
    spreads: [
        {
            art: 'end.webp',
            left: [
                "고개 이름에 얽힌 이야기는 우리나라 곳곳에 있습니다. 사람들이 자주 넘던 고개마다 이런 이야기가 하나씩 붙어 있었습니다.",
                "삼년 고개라는 이름 자체가 무서움의 씨앗입니다. 넘어지면 삼 년밖에 못 산다는 말이 이름에 박혀 있으니, 그 고개를 넘는 사람마다 조심하게 됩니다.",
                "다시 보면 아이가 한 일은 사실을 바꾼 것이 아닙니다. 한 번 넘어지면 삼 년이니 열 번 넘어지면 삼십 년이라고 셈을 바꾼 것뿐입니다.",
                "같은 말인데 방향이 뒤집혔습니다. 무서운 규칙이 그대로 좋은 규칙이 되었지요."
            ],
            right: [
                "무서운 일이라고 믿으면 아무렇지 않은 일도 무서워집니다. 같은 일도 어떻게 보느냐에 따라 두려움이 되기도 하고 아무것도 아닌 일이 되기도 합니다.",
                "걱정 때문에 아무것도 못 하고 있던 적이 있나요? 그 일을 다르게 볼 방법은 없을까요?"
            ]
        }
    ]
};

function afterPage(spread, isFirst) {
    const head = isFirst ? `<h2>${AFTERWORD.title}</h2>` : '';
    // 그림은 오른쪽 칸 맨 위 모서리에 꽉 붙인다. 학습 허브로 가는 길은 그 칸 맨 아래에 둔다.
    const art = spread.art ? `<div class="after-art">${artFrame(spread.art, AFTERWORD.emoji)}</div>` : '';
    const foot = spread.last ? `<p class="after-home"><a class="home-btn" href="../../../../../">학습 허브로 돌아가기</a></p>` : '';
    const col = (ps) => ps.map(t => `<p>${t}</p>`).join('');
    return `
        <div class="page page-after">
            <div class="after-col after-col-left">
                ${head}
                ${col(spread.left)}
            </div>
            <div class="after-col after-col-right${spread.art ? ' after-col-image' : ''}">
                ${art}
                ${col(spread.right)}
                ${foot}
            </div>
        </div>`;
}

const PAGES = [
    { kind: 'cover' },
    ...CHAPTERS.flatMap(chapter => chapter.beats.map((beat, i) => ({ kind: 'spread', chapter, beat, isFirst: i === 0 }))),
    { kind: 'quiz' },
    ...AFTERWORD.spreads.map((spread, i) => ({ kind: 'after', spread, isFirst: i === 0, last: i === AFTERWORD.spreads.length - 1 })),
];

const TWO_PAGE_KINDS = new Set(['spread', 'cover', 'after']);

let folioCounter = 0;
const FOLIOS = PAGES.map(p => {
    const width = TWO_PAGE_KINDS.has(p.kind) ? 2 : 1;
    const start = folioCounter + 1;
    folioCounter += width;
    return { start, width };
});

function renderPage(page) {
    switch (page.kind) {
        case 'cover':
            return coverPage();
        case 'spread':
            return spreadPage(page.chapter, page.beat, page.isFirst);
        case 'after':
            return afterPage({ ...page.spread, last: page.last }, page.isFirst);
        case 'quiz':
            return quizPage();
        default:
            return '';
    }
}

let current = 0;
let animating = false;

const spreadEl = document.getElementById('spread');
const prevBtn = document.getElementById('prevBtn');
const nextBtn = document.getElementById('nextBtn');
const indicatorEl = document.getElementById('pageIndicator');
const folioLeftEl = document.getElementById('folioLeft');
const folioRightEl = document.getElementById('folioRight');

function paint() {
    spreadEl.innerHTML = renderPage(PAGES[current]);
    prevBtn.disabled = current === 0;
    nextBtn.disabled = current === PAGES.length - 1;
    indicatorEl.textContent = `${current + 1} / ${PAGES.length}`;

    const folio = FOLIOS[current];
    folioLeftEl.classList.toggle('folio-center', folio.width === 1);
    if (folio.width === 2) {
        folioLeftEl.textContent = folio.start;
        folioRightEl.textContent = folio.start + 1;
        folioLeftEl.hidden = false;
        folioRightEl.hidden = false;
    } else {
        folioLeftEl.textContent = folio.start;
        folioLeftEl.hidden = false;
        folioRightEl.hidden = true;
    }

    spreadEl.querySelectorAll('[data-goto]').forEach(btn => {
        btn.addEventListener('click', () => {
            const chapterNum = Number(btn.dataset.goto);
            const idx = PAGES.findIndex(p => p.kind === 'spread' && p.chapter.num === chapterNum && p.isFirst);
            if (idx >= 0) goTo(idx);
        });
    });
    spreadEl.querySelectorAll('[data-goto-kind]').forEach(btn => {
        btn.addEventListener('click', () => {
            const idx = PAGES.findIndex(p => p.kind === btn.dataset.gotoKind);
            if (idx >= 0) goTo(idx);
        });
    });

    if (PAGES[current].kind === 'quiz') {
        initQuiz();
    }
}

function initQuiz() {
    let answeredCount = 0;
    const progressEl = document.getElementById('quizProgress');

    spreadEl.querySelectorAll('.quiz-item').forEach(item => {
        const qi = Number(item.dataset.qindex);
        const q = QUIZ[qi];
        item.querySelectorAll('.quiz-choice').forEach(btn => {
            btn.addEventListener('click', () => {
                if (item.classList.contains('graded')) return;
                item.classList.add('graded');
                const chosen = Number(btn.dataset.choice);
                item.querySelectorAll('.quiz-choice').forEach(b => {
                    const ci = Number(b.dataset.choice);
                    if (ci === q.answer) b.classList.add('correct');
                    else if (ci === chosen) b.classList.add('incorrect');
                });
                answeredCount++;
                progressEl.textContent = `${answeredCount} / 총 ${QUIZ.length}문항 완료`;
            });
        });
    });
}

function goTo(index) {
    if (animating || index === current || index < 0 || index >= PAGES.length) return;
    animating = true;
    const dir = index > current ? 'flip-next' : 'flip-prev';
    spreadEl.classList.add(dir);
    setTimeout(() => {
        current = index;
        paint();
    }, 230);
    setTimeout(() => {
        spreadEl.classList.remove('flip-next', 'flip-prev');
        animating = false;
    }, 480);
}

prevBtn.addEventListener('click', () => goTo(current - 1));
nextBtn.addEventListener('click', () => goTo(current + 1));

document.getElementById('tocLink').addEventListener('click', () => {
    // 그림책은 차례가 표지에 붙어 있다.
    const idx = PAGES.findIndex(p => p.kind === 'toc');
    goTo(idx >= 0 ? idx : 0);
});

document.addEventListener('keydown', (e) => {
    if (e.key === 'ArrowRight') goTo(current + 1);
    if (e.key === 'ArrowLeft') goTo(current - 1);
});

paint();
