const CHAPTERS = [
    {
        num: 1,
        title: "1장 · 눈밭의 콩 한 알",
        beats: [
            {
                art: "01-family.webp",
                emoji: "🐦",
                left: [
                    "옛날 눈 많은 산골에 꿩 한 쌍이 살았습니다. 수꿩은 장끼, 암꿩은 까투리라 한답니다.",
                    "장끼는 꽁지가 길고 멋졌어요. 까투리는 깃털이 수수했지요.",
                    "둘 사이에는 아들이 아홉, 딸이 열둘이었습니다.",
                    "\"어머니, 배고파요.\"",
                    "아이들이 자꾸 보챘어요."
                ],
                right: [
                    "겨울이 깊어 먹을 것이 다 떨어졌어요. 눈이 산을 덮어 풀씨 한 알 보이지 않았지요.",
                    "\"오늘은 온 식구가 나서서 찾아봅시다.\"",
                    "장끼가 꽁지를 세우며 말했어요.",
                    "까투리가 앞장서고 스물한 마리가 줄지어 눈밭으로 나갔지요."
                ]
            },
            {
                art: "02-bean.webp",
                emoji: "🫘",
                left: [
                    "얼마나 걸었을까요. 눈밭 한가운데에 무언가 붉은 것이 보였어요.",
                    "\"저것 봐라! 콩이다, 콩!\"",
                    "장끼가 날개를 퍼덕이며 달려갔어요.",
                    "하얀 눈 위에 붉은 콩 한 알이 놓여 있었어요. 굵고 붉은 것이 눈에 확 띄었지요."
                ],
                right: [
                    "그런데 까투리는 걸음을 멈췄어요.",
                    "\"여보, 잠깐만요.\"",
                    "\"콩이 왜 눈 위에 한 알만 있을까요? 이 산에는 콩밭도 없는데요.\"",
                    "까투리가 사방을 둘러보았지요. 눈 위에 발자국 같은 것이 어렴풋했어요.",
                    "\"아무래도 사람이 놓아둔 것 같습니다.\""
                ]
            },
            {
                art: "03-dream.webp",
                emoji: "💭",
                left: [
                    "\"게다가 간밤 꿈이 영 좋지 않았어요.\"",
                    "까투리의 목소리가 떨렸어요.",
                    "\"꿈에 당신 머리에 검은 갓이 씌워지더니, 당신이 물 위로 둥둥 떠내려갔어요.\"",
                    "\"제발 그 콩은 두고 갑시다.\"",
                    "아이들도 어머니 곁으로 모여들었답니다."
                ],
                right: [
                    "장끼는 껄껄 웃었어요.",
                    "\"검은 갓은 벼슬을 하는 꿈이고, 물 위에 뜬 것은 복이 넘친다는 뜻이오.\"",
                    "\"꿈이란 좋게 풀면 다 좋은 법이오.\"",
                    "장끼는 콩에서 눈을 떼지 못했어요. 벌써 입에 침이 고였지요.",
                    "까투리 말은 귀에 들어오지도 않았지요."
                ]
            },
            {
                art: "04-scold.webp",
                emoji: "😤",
                left: [
                    "\"여보, 제 말 좀 들어 보세요.\"",
                    "까투리가 장끼의 날개를 붙들었어요.",
                    "그러자 장끼가 날개를 홱 뿌리쳤습니다.",
                    "\"아녀자가 무얼 안다고 자꾸 나서시오! 집안일이나 하시오.\"",
                    "장끼의 목소리가 산을 울렸어요. 아이들이 깜짝 놀라 움츠러들었지요."
                ],
                right: [
                    "아녀자란 여자를 낮추어 부르던 말이에요. 까투리는 가슴이 내려앉았지요.",
                    "\"아버지, 어머니 말씀대로 해요.\"",
                    "맏아들이 나섰지만 장끼는 고개를 저었어요.",
                    "\"사내가 한번 먹겠다고 했으면 먹는 것이다.\"",
                    "장끼가 콩을 향해 걸어갔답니다."
                ]
            }
        ]
    },
    {
        num: 2,
        title: "2장 · 덫",
        beats: [
            {
                art: "05-trap.webp",
                emoji: "🪤",
                left: [
                    "까투리는 두 눈을 꼭 감았어요. 아이들은 어머니 치마폭에 얼굴을 묻었지요.",
                    "눈밭이 쥐 죽은 듯 고요했어요.",
                    "장끼가 콩 앞에 섰습니다.",
                    "\"이렇게 굵은 콩은 처음 보는구나.\"",
                    "장끼가 부리를 쭉 내밀어 콩을 콕 쪼았어요. 바로 그때였어요."
                ],
                right: [
                    "탁!",
                    "눈 속에 숨어 있던 덫이 튀어 올라 장끼의 목을 꽉 물었습니다. 덫은 짐승을 잡으려고 사람이 놓아둔 틀이에요.",
                    "콩은 미끼였던 것이지요.",
                    "\"여, 여보……\"",
                    "장끼가 날개를 파닥였지만 덫은 꿈쩍도 하지 않았어요."
                ]
            },
            {
                art: "06-lastwords.webp",
                emoji: "🍂",
                left: [
                    "까투리가 달려가 덫을 부리로 쪼고 발로 밀었어요. 아이들도 매달렸지요.",
                    "하지만 쇠로 만든 덫은 꿈쩍도 하지 않았습니다.",
                    "장끼의 숨이 점점 가늘어졌어요.",
                    "\"당신 말을…… 들을 것을……\"",
                    "장끼의 눈에서 눈물이 굴러떨어졌지요."
                ],
                right: [
                    "\"내가 죽거든 아이들을 잘 키워 주오.\"",
                    "\"그리고 다시는 시집가지 마오. 혼자 살며 나만 생각해 주오.\"",
                    "장끼는 그 말을 남기고 숨을 거두었습니다.",
                    "눈밭 위에 긴 꽁지깃 하나가 바람에 흔들렸어요.",
                    "까투리는 그 자리에 한참을 앉아 있었답니다."
                ]
            },
            {
                art: "07-burial.webp",
                emoji: "❄️",
                left: [
                    "까투리와 스물한 마리 아이들이 소리 내어 울었어요. 울음소리가 온 산에 퍼졌지요.",
                    "\"아버지! 아버지!\"",
                    "해가 기울자 까투리가 눈물을 닦고 일어섰어요.",
                    "\"이대로 둘 수는 없다. 아버지를 묻어 드리자.\"",
                    "아이들이 발로 눈을 파기 시작했답니다."
                ],
                right: [
                    "양지바른 언덕에 장끼를 눕히고 눈을 덮었어요. 그 위에 그 콩을 놓았지요.",
                    "\"이 콩 때문에 가셨으니, 이 콩이나 가지고 가세요.\"",
                    "까투리의 목소리가 담담했어요.",
                    "아이들은 어머니 날개 밑으로 파고들었지요.",
                    "까투리는 밤새 눈을 붙이지 못했답니다."
                ]
            },
            {
                art: "08-crow.webp",
                emoji: "🐦‍⬛",
                left: [
                    "이튿날, 장끼 소식이 온 산에 퍼졌어요.",
                    "새들이 하나둘 조문을 왔어요. 조문은 슬픈 집을 찾아가 위로하는 일이지요.",
                    "제일 먼저 온 것은 까마귀였어요.",
                    "\"아이고, 이런 변이 있나.\"",
                    "까마귀가 검은 날개로 얼굴을 가리며 울었지요."
                ],
                right: [
                    "그런데 까마귀는 울면서도 까투리를 흘끔흘끔 살폈어요.",
                    "\"까투리 부인, 이제 어찌 사시려오.\"",
                    "\"아이들이 스물하나나 되는데 혼자서는 어렵지 않겠소.\"",
                    "까투리는 대답하지 않았어요.",
                    "뒤이어 부엉이와 물오리도 날아왔답니다."
                ]
            }
        ]
    },
    {
        num: 3,
        title: "3장 · 까투리의 선택",
        beats: [
            {
                art: "09-suitors.webp",
                emoji: "🦆",
                left: [
                    "새들은 슬퍼하러 온 것이 아니었어요. 저마다 제 자랑을 늘어놓았지요.",
                    "\"내 검은 옷은 상복이오. 나와 살면 남편을 늘 기리는 셈이지.\"",
                    "상복은 초상 때 입는 옷이지요.",
                    "부엉이도 나섰어요. \"나는 밤눈이 밝아 밤마다 아이들을 지켜 주겠소.\""
                ],
                right: [
                    "\"우리 집은 물 위라오. 여름에 시원하고 물고기가 넘치지.\"",
                    "물오리도 끼어들었어요.",
                    "까투리가 조용히 입을 열었지요.",
                    "\"우리 아이들은 헤엄을 못 칩니다. 밤에는 자야 하고요. 검은 옷은 저도 있고요.\"",
                    "새들은 머쓱해져 날아가 버렸답니다."
                ]
            },
            {
                art: "10-alone.webp",
                emoji: "🌙",
                left: [
                    "새들이 떠나고 산이 다시 조용해졌어요.",
                    "까투리는 언덕 위 무덤을 바라보았습니다. 아이들은 곁에서 잠이 들었지요.",
                    "'다시 시집가지 마오.'",
                    "장끼의 마지막 말이 귓가에 맴돌았어요.",
                    "'그런데 내가 어떻게 살고 싶은지는 아무도 묻지 않는구나.'"
                ],
                right: [
                    "겨울이 지나갔어요. 까투리는 혼자 아이들을 먹여 살렸지요.",
                    "눈을 헤쳐 풀뿌리를 찾고 덤불에서 열매를 찾았어요.",
                    "아이들은 하루가 다르게 자랐어요.",
                    "\"어머니, 우리도 이제 먹이를 찾을 수 있어요.\"",
                    "맏딸이 도토리 한 알을 내밀었답니다."
                ]
            },
            {
                art: "11-spring.webp",
                emoji: "🌱",
                left: [
                    "봄이 왔어요. 눈이 녹고 산에 새순이 돋았지요.",
                    "어느 날 산 너머에서 낯선 장끼 한 마리가 찾아왔습니다. 짝을 잃고 혼자 사는 장끼였어요.",
                    "\"지나다가 들렀습니다.\"",
                    "그 장끼는 자랑을 늘어놓지 않았어요. 아이들 이름을 하나하나 물었지요."
                ],
                right: [
                    "함께 먹이를 찾다가 풀숲에서 콩 한 알을 보았어요.",
                    "까투리의 가슴이 철렁했습니다.",
                    "그런데 낯선 장끼가 콩을 보더니 까투리를 돌아보았어요.",
                    "\"이 콩, 먹어도 되겠습니까? 부인 보기에는 어떻습니까.\"",
                    "까투리는 그만 웃음이 터졌답니다."
                ]
            },
            {
                art: "12-choice.webp",
                emoji: "🌸",
                left: [
                    "그날 밤 까투리는 아이들을 불러 모았어요.",
                    "\"어머니는 이제 저 장끼와 살아 볼까 한다. 너희 생각은 어떠니.\"",
                    "\"아버지가 시집가지 말라고 하셨잖아요.\"",
                    "맏아들이 물었지요.",
                    "\"그건 아버지 생각이었지. 이번에는 어머니가 정하마.\""
                ],
                right: [
                    "봄 산에 새 식구가 생겼어요. 아이들 뒤로 장끼와 까투리가 나란히 걸었지요.",
                    "새 장끼는 무엇이든 까투리에게 먼저 물었어요. 까투리도 제 뜻을 말했지요.",
                    "언덕 위 무덤에는 봄풀이 돋았답니다.",
                    "그 위에 두었던 콩에서도 싹이 텄대요."
                ]
            }
        ],
        moral: "장끼는 까투리의 말을 아녀자 말이라며 듣지 않다가 목숨을 잃었어요. 까투리는 남편의 마지막 말 대신 제 뜻을 따랐지요. 남의 말을 듣는 것과 제 뜻을 정하는 것, 둘 다 사는 데 꼭 필요하답니다.",
        question: "누군가의 말을 흘려듣다가 낭패를 본 적이 있나요? 반대로, 남의 말 대신 내 뜻대로 정해 본 적은요?"
    }
];

function artFrame(src, emoji) {
    return `
        <div class="art-frame">
            <img src="images/${src}" alt="" onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';">
            <div class="art-fallback" style="display:none">${emoji}</div>
        </div>`;
}

function coverToc() {
    const item = s => `
        <button type="button" data-goto="${s.num}">
            <span class="toc-num">${s.num}</span>
            <span>${s.title.replace(/^\d+장 · /, '')}</span>
        </button>`;
    return `
        <nav class="cover-toc">
            <h2>차례</h2>
            ${CHAPTERS.map(item).join('')}
            <button type="button" data-goto-kind="quiz">
                <span class="toc-num">❓</span>
                <span>이야기 문제</span>
            </button>
        </nav>`;
}

function coverPage() {
    return `
        <div class="page page-cover">
            <div class="story-page-left story-page-left-full">
                ${artFrame('cover.webp', '🐦')}
            </div>
            <div class="story-page-right">
                <h1>장끼전</h1>
                <p>장끼전은 지은이가 없는 옛이야기예요. 원래는 소리꾼이 북장단에 맞춰 부르던 판소리 「장끼타령」이었는데, 소리는 끊기고 글만 남았답니다.</p>
                <p>꿩 부부가 사람처럼 말하고 다투는 이야기예요. 짐승 이야기 같지만 사실은 그 시절 사람들 이야기지요. 대놓고 하기 어려운 말을 꿩의 입을 빌려 한 것이랍니다.</p>
                <p>눈밭에 떨어진 콩 한 알을 두고 하루 사이에 벌어진 일이에요. 그 하루가 한 집안의 겨울을 통째로 바꿔 놓는답니다.</p>
                ${coverToc()}
            </div>
        </div>`;
}

function spreadPage(chapter, beat, isFirst) {
    const badgeHtml = isFirst ? `<div class="spread-chapter-badge">${chapter.title}</div>` : '';
    const leftHtml = beat.left.map(p => `<p>${p}</p>`).join('');
    const rightHtml = beat.right.map(p => `<p>${p}</p>`).join('');
    return `
        <div class="page page-story">
            <div class="spread-art">
                ${badgeHtml}
                ${artFrame(beat.art, beat.emoji)}
            </div>
            <div class="spread-text">
                <div class="spread-text-left">${leftHtml}</div>
                <div class="spread-text-right">${rightHtml}</div>
            </div>
        </div>`;
}


const QUIZ = [
    { q: "장끼네 식구가 눈밭으로 나선 까닭은 무엇인가요?", choices: ["먹을 것이 다 떨어져서", "봄나들이를 가려고", "이웃 잔치에 가려고"], answer: 0 },
    { q: "까투리가 콩을 보고 이상하게 여긴 것은 무엇인가요?", choices: ["콩 빛깔이 새까매서", "눈 위에 한 알만 있어서", "콩밭 한가운데 있어서"], answer: 1 },
    { q: "까투리가 간밤에 꾼 꿈은 무엇인가요?", choices: ["콩이 산더미처럼 쌓이는 꿈", "아이들이 하늘을 나는 꿈", "장끼가 검은 갓을 쓰는 꿈"], answer: 2 },
    { q: "장끼는 까투리의 말을 어떻게 했나요?", choices: ["아녀자 말이라며 뿌리쳤다", "곰곰이 따져 본 뒤 따랐다", "아이들에게 먼저 물어보았다"], answer: 0 },
    { q: "장끼가 콩을 쪼는 순간 무슨 일이 있었나요?", choices: ["사냥꾼이 달려왔다", "콩이 굴러 달아났다", "덫이 목을 물었다"], answer: 2 },
    { q: "장끼가 숨을 거두며 남긴 말은 무엇인가요?", choices: ["다시 시집가지 마오", "콩을 아이들에게 주오", "나를 산 위에 묻어 주오"], answer: 0 },
    { q: "제일 먼저 조문을 온 새는 누구인가요?", choices: ["부엉이", "까마귀", "물오리"], answer: 1 },
    { q: "봄에 찾아온 장끼는 콩을 보고 어떻게 했나요?", choices: ["얼른 집어 삼켰다", "아이들에게 나눠 주었다", "까투리에게 먼저 물었다"], answer: 2 },
    {
        q: "이 책을 읽고 난 반응으로 알맞지 않은 것은 무엇인가요?",
        wide: true,
        choices: [
            "장끼가 아녀자 말이라며 흘려들은 것을 보면, 안 들은 값이 목숨이었구나.",
            "조문 온 새들이 슬퍼하기보다 제 자랑부터 한 것을 보면, 남의 슬픔에도 제 셈을 하는 이가 있네.",
            "봄에 온 장끼가 콩을 보고 먼저 물어본 것을 보면, 까투리가 고른 것은 잘생긴 새가 아니었어.",
            "까투리가 남편 말대로 끝까지 혼자 산 것을 보면, 죽은 이의 말이 산 이보다 앞섰나 봐."
        ],
        answer: 3
    }
];

/* 보기는 책을 열 때마다 자리를 바꾼다. 답의 자리를 외워 버리면 문제가 아니게 된다.
   섞는 것은 그리는 차례뿐이고, 채점은 data-choice 에 담긴 원래 번호로 한다.
   한 번 정한 차례는 책을 닫을 때까지 그대로다. 쪽을 오갈 때마다 바뀌면 헷갈린다. */
function shuffledOrder(n) {
    const a = [...Array(n).keys()];
    for (let i = a.length - 1; i > 0; i--) {
        const j = Math.floor(Math.random() * (i + 1));
        [a[i], a[j]] = [a[j], a[i]];
    }
    return a;
}

const QUIZ_ORDER = QUIZ.map(q => shuffledOrder(q.choices.length));
/* 고른 것을 기억해 둔다. 쪽을 옮기면 문제 쪽이 통째로 다시 그려지는데,
   그때 표시가 사라지면 아이가 처음부터 다시 풀어야 한다. */
const QUIZ_PICKED = new Array(QUIZ.length).fill(null);
const QUIZ_WRONG = QUIZ.map(() => new Set());

function quizPage() {
    const items = QUIZ.map((item, i) => {
        const graded = QUIZ_PICKED[i] !== null;
        const cls = ci => (graded && ci === item.answer) ? ' correct'
            : (QUIZ_WRONG[i].has(ci) ? ' incorrect' : '');
        return `
        <div class="quiz-item${graded ? ' graded' : ''}" data-qindex="${i}">
            <p class="quiz-question">${i + 1}. ${item.q}</p>
            <div class="quiz-choices${item.wide ? ' quiz-choices-stack' : ''}">
                ${QUIZ_ORDER[i].map(ci => `<button type="button" class="quiz-choice${cls(ci)}" data-choice="${ci}">${item.choices[ci]}</button>`).join('')}
            </div>
        </div>`;
    }).join('');
    return `
        <div class="page page-quiz">
            <h2>이야기 문제</h2>
            <p class="quiz-intro-text" id="quizProgress">${QUIZ_PICKED.filter(v => v !== null).length} / 총 ${QUIZ.length}문항 완료</p>
            <div class="quiz-list">${items}</div>
        </div>`;
}


/* 읽고 나서 — 세계명작 트랙과 같은 형식이다. 동화틀은 쪽을 재서 나누지 않으므로
   펼침면마다 왼쪽·오른쪽 글을 손으로 나누어 둔다. */
const AFTERWORD = {
    title: '읽고 나서',
    emoji: '🌸',
    spreads: [
        {
            art: 'end.webp',
            left: [
                "이 이야기는 원래 판소리 「장끼타령」이었습니다. 판소리 열두 마당 가운데 소리가 끊긴 일곱 마당의 하나로, 옹고집전과 같은 처지입니다. 글로만 읽을 수 있습니다.",
                "꿩이 사람처럼 말하고 혼인하고 다툽니다. 이런 이야기를 우화라고 합니다. 옛사람들은 사람에게 대놓고 하기 어려운 말을 짐승의 입을 빌려 했습니다.",
                "장끼가 까투리에게 한 「아녀자가 무얼 안다고」는 그 시절 남자들이 흔히 하던 말입니다. 이야기는 그 말을 한 장끼를 덫에 걸리게 합니다. 누구 편인지 분명합니다.",
                "장끼의 마지막 말도 그렇습니다. 그때는 남편을 잃은 여자가 다시 혼인하는 것을 나쁘게 보았습니다. 까투리는 그 말을 따르지 않았습니다. 이 책은 까투리가 스스로 고르는 쪽으로 끝냈습니다."
            ],
            right: [
                "그러니 이것은 꿩 이야기가 아닙니다. 남의 말을 듣지 않은 사람과, 남이 정해 준 대로 살지 않은 사람의 이야기입니다. 콩 한 알이 그 둘을 다 보여 줍니다.",
                "누군가의 말을 흘려듣다 낭패를 본 적이 있나요? 반대로 남이 정해 준 것 대신 내 뜻대로 골라 본 적은요?"
            ]
        }
    ]
};

function afterPage(spread, isFirst) {
    const head = isFirst ? `<h2>${AFTERWORD.title}</h2>` : '';
    // 그림은 오른쪽 칸 맨 위 모서리에 꽉 붙인다. 학습 허브로 가는 길은 그 칸 맨 아래에 둔다.
    const art = spread.art ? `<div class="after-art">${artFrame(spread.art, AFTERWORD.emoji)}</div>` : '';
    const foot = spread.last ? `<p class="after-home"><a class="home-btn" href="../../../../../">학습 허브로 돌아가기</a></p>` : '';
    const col = (ps) => ps.map(t => `<p>${t}</p>`).join('');
    return `
        <div class="page page-after">
            <div class="after-col after-col-left">
                ${head}
                ${col(spread.left)}
            </div>
            <div class="after-col after-col-right${spread.art ? ' after-col-image' : ''}">
                ${art}
                ${col(spread.right)}
                ${foot}
            </div>
        </div>`;
}

const PAGES = [
    { kind: 'cover' },
    ...CHAPTERS.flatMap(chapter => chapter.beats.map((beat, i) => ({ kind: 'spread', chapter, beat, isFirst: i === 0 }))),
    { kind: 'quiz' },
    ...AFTERWORD.spreads.map((spread, i) => ({ kind: 'after', spread, isFirst: i === 0, last: i === AFTERWORD.spreads.length - 1 })),
];

const TWO_PAGE_KINDS = new Set(['spread', 'cover', 'after']);

let folioCounter = 0;
const FOLIOS = PAGES.map(p => {
    const width = TWO_PAGE_KINDS.has(p.kind) ? 2 : 1;
    const start = folioCounter + 1;
    folioCounter += width;
    return { start, width };
});

function renderPage(page) {
    switch (page.kind) {
        case 'cover':
            return coverPage();
        case 'spread':
            return spreadPage(page.chapter, page.beat, page.isFirst);
        case 'after':
            return afterPage({ ...page.spread, last: page.last }, page.isFirst);
        case 'quiz':
            return quizPage();
        default:
            return '';
    }
}

let current = 0;
let animating = false;

const spreadEl = document.getElementById('spread');
const prevBtn = document.getElementById('prevBtn');
const nextBtn = document.getElementById('nextBtn');
const indicatorEl = document.getElementById('pageIndicator');
const folioLeftEl = document.getElementById('folioLeft');
const folioRightEl = document.getElementById('folioRight');

function paint() {
    spreadEl.innerHTML = renderPage(PAGES[current]);
    prevBtn.disabled = current === 0;
    nextBtn.disabled = current === PAGES.length - 1;
    indicatorEl.textContent = `${current + 1} / ${PAGES.length}`;

    const folio = FOLIOS[current];
    folioLeftEl.classList.toggle('folio-center', folio.width === 1);
    if (folio.width === 2) {
        folioLeftEl.textContent = folio.start;
        folioRightEl.textContent = folio.start + 1;
        folioLeftEl.hidden = false;
        folioRightEl.hidden = false;
    } else {
        folioLeftEl.textContent = folio.start;
        folioLeftEl.hidden = false;
        folioRightEl.hidden = true;
    }

    spreadEl.querySelectorAll('[data-goto]').forEach(btn => {
        btn.addEventListener('click', () => {
            const chapterNum = Number(btn.dataset.goto);
            const idx = PAGES.findIndex(p => p.kind === 'spread' && p.chapter.num === chapterNum && p.isFirst);
            if (idx >= 0) goTo(idx);
        });
    });
    spreadEl.querySelectorAll('[data-goto-kind]').forEach(btn => {
        btn.addEventListener('click', () => {
            const idx = PAGES.findIndex(p => p.kind === btn.dataset.gotoKind);
            if (idx >= 0) goTo(idx);
        });
    });

    if (PAGES[current].kind === 'quiz') {
        initQuiz();
    }
    fitAfterword();
}

/* 「읽고 나서」는 글씨를 줄이지 않는다. 세로 화면에서는 끝그림을 접어 두는 것이
   기본이고(styles.css), 여기서 그림을 켜 보아 자리가 남을 때만 그대로 둔다.
   그래서 이 함수가 한 번도 못 돌아도 글이 잘리지는 않는다. */
function fitAfterword() {
    const page = spreadEl.querySelector('.page-after');
    if (!page) return;
    page.classList.add('after-roomy');
    const spills = [...page.querySelectorAll('.after-col')]
        .some(col => col.scrollHeight > col.clientHeight);
    if (spills) page.classList.remove('after-roomy');
}

/* 화면 크기가 바뀌면 다시 잰다. 크기가 한 박자 늦게 잡히는 곳이 있어
   그 자리에서 한 번, 잠시 뒤에 한 번 더 잰다. */
let refitTimer = 0;
function refitAfterword() {
    fitAfterword();
    clearTimeout(refitTimer);
    refitTimer = setTimeout(fitAfterword, 250);
}

window.addEventListener('resize', refitAfterword);
window.addEventListener('orientationchange', refitAfterword);
/* 화면을 돌렸을 때 resize 가 안 잡히는 곳이 있다. 책 상자 크기를 직접 지켜본다. */
if (window.ResizeObserver) {
    const bookEl = document.getElementById('book');
    if (bookEl) new ResizeObserver(refitAfterword).observe(bookEl);
}
if (document.fonts && document.fonts.ready) document.fonts.ready.then(fitAfterword);

function initQuiz() {
    const progressEl = document.getElementById('quizProgress');

    spreadEl.querySelectorAll('.quiz-item').forEach(item => {
        const qi = Number(item.dataset.qindex);
        const q = QUIZ[qi];
        item.querySelectorAll('.quiz-choice').forEach(btn => {
            btn.addEventListener('click', () => {
                if (item.classList.contains('graded')) return;
                const chosen = Number(btn.dataset.choice);
                /* 틀리면 그 보기만 빨갛게 남기고, 맞는 것을 고를 때까지 다시 고르게 한다.
                   답을 미리 보여 주면 아이가 생각할 자리가 사라진다. */
                if (chosen !== q.answer) {
                    btn.classList.add('incorrect');
                    QUIZ_WRONG[qi].add(chosen);
                    return;
                }
                btn.classList.add('correct');
                item.classList.add('graded');
                QUIZ_PICKED[qi] = chosen;
                progressEl.textContent = `${QUIZ_PICKED.filter(v => v !== null).length} / 총 ${QUIZ.length}문항 완료`;
            });
        });
    });
}

function goTo(index) {
    if (animating || index === current || index < 0 || index >= PAGES.length) return;
    animating = true;
    const dir = index > current ? 'flip-next' : 'flip-prev';
    spreadEl.classList.add(dir);
    setTimeout(() => {
        current = index;
        paint();
    }, 230);
    setTimeout(() => {
        spreadEl.classList.remove('flip-next', 'flip-prev');
        animating = false;
    }, 480);
}

prevBtn.addEventListener('click', () => goTo(current - 1));
nextBtn.addEventListener('click', () => goTo(current + 1));

document.getElementById('tocLink').addEventListener('click', () => {
    // 그림책은 차례가 표지에 붙어 있다.
    const idx = PAGES.findIndex(p => p.kind === 'toc');
    goTo(idx >= 0 ? idx : 0);
});

document.addEventListener('keydown', (e) => {
    if (e.key === 'ArrowRight') goTo(current + 1);
    if (e.key === 'ArrowLeft') goTo(current - 1);
});

paint();
