const CHAPTERS = [
    {
        num: 1,
        title: "1장 · 재투성이 소녀",
        hook: "새어머니와 언니들 틈에서 신데렐라는 어떻게 지냈을까?",
        art: "story-01-chores.png",
        art2: "story-01-chores-2.png",
        emoji: "🧹",
        open: [
            "오래전 어느 나라에 마음씨 착한 소녀가 살았어요. 소녀의 어머니는 소녀가 어렸을 때 세상을 떠났고, 아버지는 얼마 후 새어머니를 맞아들였어요. 새어머니에게는 이미 딸이 둘 있었는데, 둘 다 성격이 거칠고 심술궂었어요.",
            "얼마 지나지 않아 아버지마저 먼 길을 떠나 돌아오지 못하게 되자, 집안의 모든 살림은 소녀의 몫이 되었어요. 새어머니와 언니들은 좋은 방과 예쁜 옷을 독차지했고, 소녀에게는 부엌 옆 다락방과 낡은 옷가지만 남겨주었답니다."
        ],
        close: [
            "소녀는 하루 종일 물을 긷고, 불을 때고, 마루를 닦았어요. 일을 마치고 나면 벽난로 옆 재투성이 바닥에 앉아 몸을 녹이는 게 유일한 휴식이었죠. 그 모습을 보고 심술궂은 언니들은 소녀를 '재투성이'라고 놀려 불렀어요.",
            "하지만 소녀는 불평하지 않았어요. 힘든 하루를 보내면서도 창밖의 별을 보며 조용히 미소 짓곤 했답니다. '언젠가는 나에게도 좋은 날이 오겠지.' 소녀는 그렇게 스스로를 다독이며 하루하루를 버텨냈어요."
        ],
        moral: "",
        question: ""
    },
    {
        num: 2,
        title: "2장 · 무도회 초대장",
        hook: "온 나라의 처녀들을 초대한 무도회, 신데렐라도 갈 수 있을까?",
        art: "story-02-invite.png",
        art2: "story-02-invite-2.png",
        emoji: "💌",
        open: [
            "어느 날, 온 나라에 놀라운 소식이 전해졌어요. 왕자님이 신붓감을 찾기 위해 사흘 밤 동안 성대한 무도회를 연다는 소식이었죠. 나라의 모든 젊은 처녀가 초대받았고, 새어머니의 두 딸도 뛸 듯이 기뻐했어요.",
            "언니들은 며칠 내내 어떤 드레스를 입을지, 머리를 어떻게 꾸밀지 떠들썩하게 준비했어요. 신데렐라는 조심스럽게 물었어요. '저도 무도회에 가고 싶어요.' 하지만 새어머니는 코웃음을 쳤어요. '네가 입을 옷이 어디 있다고 그런 소리를 하니?'"
        ],
        close: [
            "새어머니는 신데렐라에게 산더미 같은 콩을 골라내라는 일을 시키며 말했어요. '이걸 저녁까지 다 골라내면 생각해보마.' 그러고는 두 딸을 데리고 성으로 떠날 채비를 서둘렀어요.",
            "무도회 날 저녁, 화려하게 꾸민 새어머니와 언니들은 마차를 타고 떠나버렸어요. 신데렐라는 혼자 남아 산더미 같은 콩 앞에 주저앉아 눈물을 흘렸답니다. '나도 딱 한 번만이라도 무도회에 가보고 싶은데...'"
        ],
        moral: "",
        question: ""
    },
    {
        num: 3,
        title: "3장 · 요정 대모의 마법",
        hook: "눈물을 흘리던 신데렐라 앞에 누가 나타났을까?",
        art: "story-03-magic.png",
        art2: "story-03-magic-2.png",
        emoji: "🪄",
        open: [
            "신데렐라가 흐느끼고 있을 때, 갑자기 부드러운 빛과 함께 한 노부인이 나타났어요. '얘야, 왜 울고 있니?' 신데렐라는 깜짝 놀라 눈물을 닦으며 사정을 이야기했어요. 노부인은 인자하게 웃으며 말했어요. '나는 너의 요정 대모란다. 네 소원을 들어줄 수 있을 것 같구나.'",
            "요정 대모는 마당에 있던 커다란 호박을 가리키며 지팡이를 휘둘렀어요. 순식간에 호박은 반짝이는 황금 마차로 변했어요! 신데렐라는 입을 다물지 못했답니다."
        ],
        close: [
            "요정 대모는 쥐구멍 속 생쥐들을 늠름한 백마로, 낡은 옷을 눈부신 드레스로, 신데렐라의 신발을 반짝이는 유리구두로 바꾸어주었어요. 신데렐라는 거울 속 자신의 모습을 보고 눈이 휘둥그레졌어요.",
            "요정 대모는 마지막으로 진지한 얼굴로 당부했어요. '이 마법은 자정이 지나면 모두 풀린단다. 그 전에 반드시 집으로 돌아와야 해, 잊지 말렴.' 신데렐라는 고개를 힘차게 끄덕이고는 마차에 올라탔답니다."
        ],
        moral: "",
        question: ""
    },
    {
        num: 4,
        title: "4장 · 무도회의 밤",
        hook: "왕자님과 함께 춤춘 신비한 소녀는 자정이 되자 어떻게 됐을까?",
        art: "story-04-ball.png",
        art2: "story-04-ball-2.png",
        emoji: "💃",
        open: [
            "성에 도착한 신데렐라는 눈부신 드레스 차림으로 무도회장에 들어섰어요. 사람들은 저마다 '저 아름다운 아가씨는 누구지?' 하며 수군거렸어요. 새어머니와 언니들조차 신데렐라를 알아보지 못했답니다.",
            "왕자는 신데렐라를 보자마자 한눈에 반해, 그날 밤 내내 다른 누구와도 춤추지 않고 오직 신데렐라하고만 춤을 추었어요. 신데렐라도 왕자와 함께한 시간이 꿈만 같았어요."
        ],
        close: [
            "신나게 춤을 추던 중, 저 멀리 시계탑에서 종이 울리기 시작했어요. 뎅, 뎅... 자정을 알리는 종소리였어요! 신데렐라는 화들짝 놀라 몸을 돌렸어요. '이런, 벌써 시간이!'",
            "신데렐라는 왕자를 뒤로하고 급히 계단을 뛰어 내려갔어요. 그러다 그만 유리구두 한 짝이 계단에서 벗겨지고 말았어요. 신데렐라는 돌아볼 겨를도 없이 어둠 속으로 사라졌고, 왕자는 계단에 남겨진 유리구두 한 짝만을 손에 쥐고 멍하니 서 있었답니다."
        ],
        moral: "",
        question: ""
    },
    {
        num: 5,
        title: "5장 · 유리구두를 찾아서",
        hook: "왕자는 유리구두의 주인을 어떻게 찾으려 했을까?",
        art: "story-05-search.png",
        art2: "story-05-search-2.png",
        emoji: "🔍",
        open: [
            "다음 날부터 왕자는 신하들에게 명령했어요. '이 유리구두가 발에 꼭 맞는 처녀를 찾아라. 그녀가 바로 내가 찾던 사람이다.' 신하들은 유리구두를 들고 온 나라의 집집마다 찾아다니기 시작했어요.",
            "마침내 신데렐라의 집에도 신하가 찾아왔어요. 새어머니의 두 딸은 서로 먼저 신어보겠다며 다투었어요. 첫째 언니는 발가락이 아파도 억지로 구두에 발을 욱여넣으려 했지만, 도저히 들어가지 않았어요."
        ],
        close: [
            "둘째 언니도 마찬가지였어요. 발뒤꿈치를 구겨 넣어봐도 유리구두는 꿈쩍도 하지 않았죠. 신하는 고개를 저으며 돌아서려 했어요. 그때 신데렐라가 조심스럽게 말했어요. '저도 한번 신어봐도 될까요?'",
            "새어머니와 언니들은 어이없다는 듯 코웃음을 쳤어요. '재투성이 네가 무슨 유리구두야!' 하지만 신하는 왕자의 명령을 떠올리며 신데렐라에게도 기회를 주기로 했답니다."
        ],
        moral: "",
        question: ""
    },
    {
        num: 6,
        title: "6장 · 신데렐라의 발",
        hook: "유리구두는 과연 누구의 것으로 밝혀졌을까?",
        art: "story-06-fit.png",
        art2: "story-06-fit-2.png",
        emoji: "👠",
        open: [
            "신데렐라가 조심스럽게 발을 내밀자, 유리구두는 마치 원래 그녀의 것이었던 것처럼 꼭 맞았어요. 방 안에 있던 모든 사람이 숨을 죽였어요. 새어머니와 언니들은 믿을 수 없다는 표정으로 눈을 크게 떴어요.",
            "신데렐라는 품에서 나머지 한 짝의 유리구두를 꺼내 신어 보였어요. 신하는 뛸 듯이 기뻐하며 외쳤어요. '찾았다! 바로 이분이십니다!'"
        ],
        close: [
            "소식을 들은 왕자가 한걸음에 달려왔어요. 재투성이 옷을 입은 신데렐라를 보고도 왕자는 단번에 알아보았어요. '당신이었군요. 그날 밤 저와 함께 춤췄던 그 사람이.' 신데렐라도 그제야 활짝 웃었답니다.",
            "새어머니와 언니들은 그동안 신데렐라를 구박했던 걸 부끄러워하며 고개를 들지 못했어요. 하지만 신데렐라는 그들을 원망하는 대신, 오히려 담담하게 손을 내밀어 인사를 건넸답니다."
        ],
        moral: "",
        question: ""
    },
    {
        num: 7,
        title: "7장 · 행복한 시작",
        hook: "신데렐라는 그 후 어떤 삶을 살게 됐을까?",
        art: "story-07-wedding.png",
        art2: "story-07-wedding-2.png",
        emoji: "💍",
        open: [
            "신데렐라와 왕자는 얼마 후 성대한 결혼식을 올렸어요. 온 나라 사람들이 모여 두 사람을 축복했고, 신데렐라는 그동안의 힘든 시간을 뒤로하고 새로운 삶을 시작하게 되었어요.",
            "신데렐라는 왕비가 된 뒤에도 예전의 마음씨를 잃지 않았어요. 어려운 사람들을 살피고, 자신을 힘들게 했던 새어머니와 언니들에게도 너그럽게 대했답니다."
        ],
        close: [
            "사람들은 신데렐라의 이야기를 들으며 이렇게 이야기하곤 했어요. '착한 마음은 아무리 힘든 순간에도 빛을 잃지 않는구나.' 신데렐라는 화려한 옷이나 마차 때문이 아니라, 끝까지 지켜온 마음씨 덕분에 진짜 행복을 찾은 거였답니다.",
            "그렇게 신데렐라는 오래오래 행복하게 살았대요."
        ],
        moral: "어려운 상황에서도 마음씨를 지킨 사람은, 결국 자기 힘으로 행복을 찾아가요. 화려함보다 중요한 건 그 사람이 끝까지 지켜온 마음이랍니다.",
        question: "힘든 순간에도 나답게 마음을 지켰던 적이 있나요?"
    }
];

function artFrame(src, emoji) {
    return `
        <div class="art-frame">
            <img src="images/${src}" alt="" onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';">
            <div class="art-fallback" style="display:none">${emoji}</div>
        </div>`;
}

function coverPage() {
    return `
        <div class="page page-cover">
            <div class="story-page-left story-page-left-full">
                ${artFrame('cover.png', '👠')}
            </div>
            <div class="story-page-right">
                <h1>신데렐라</h1>
                <p class="cover-tag">일곱 개의 장으로 이어지는 이야기</p>
                <p>오래전 유럽에서 전해 내려오는 이야기예요. 착한 마음씨를 가진 소녀 신데렐라가 어려운 상황 속에서도 희망을 잃지 않고, 결국 자기 힘으로 행복을 찾아가는 이야기랍니다.</p>
                <p>이 책은 하나의 이야기를 일곱 개의 장으로 나누어 담았어요. 1장부터 순서대로 읽으면 신데렐라의 이야기가 처음부터 끝까지 이어진답니다.</p>
            </div>
        </div>`;
}

function tocPage() {
    const itemHtml = s => `
        <li>
            <button type="button" data-goto="${s.num}">
                <span class="toc-num">${s.num}</span>
                <span>
                    <strong>${s.title.replace(/^\d+장 · /, '')}</strong>
                    <small>${s.hook}</small>
                </span>
            </button>
        </li>`;
    const quizItemHtml = `
        <li>
            <button type="button" data-goto-kind="quiz">
                <span class="toc-num">❓</span>
                <span>
                    <strong>이야기 문제</strong>
                    <small>다 읽고 나서 확인해봐요</small>
                </span>
            </button>
        </li>`;
    const half = Math.ceil(CHAPTERS.length / 2);
    const leftItems = CHAPTERS.slice(0, half).map(itemHtml).join('');
    const rightItems = CHAPTERS.slice(half).map(itemHtml).join('') + quizItemHtml;
    return `
        <div class="page page-toc">
            <div class="story-page-left">
                <h2>차례</h2>
                <ul class="toc-list">${leftItems}</ul>
            </div>
            <div class="story-page-right">
                <ul class="toc-list">${rightItems}</ul>
            </div>
        </div>`;
}

function storyMedia(story, artFile, caption) {
    const captionHtml = caption ? `<div class="art-caption">${caption}</div>` : '';
    return `
        <div class="story-media">
            ${captionHtml}
            ${artFrame(artFile, story.emoji)}
        </div>`;
}

function storyOpenPage(story) {
    const paras = story.open.map(p => `<p>${p}</p>`).join('');
    return `
        <div class="page page-story">
            <div class="story-page-left">
                <h2>${story.title}</h2>
                ${paras}
            </div>
            <div class="story-page-right story-page-right-image">
                ${storyMedia(story, story.art, '')}
            </div>
        </div>`;
}

function storyClosePage(story, closeParas) {
    const paras = closeParas.map(p => `<p>${p}</p>`).join('');
    const moralHtml = story.moral ? `
                <div class="moral-box">
                    <strong>이야기를 다 읽고</strong>
                    <p>${story.moral}</p>
                    <p style="margin-top:6px;font-style:italic;">${story.question}</p>
                </div>` : '';
    return `
        <div class="page page-story">
            <div class="story-page-left">
                ${paras}
            </div>
            <div class="story-page-right">
                ${storyMedia(story, story.art2, story.title.replace(/^\d+장 · /, ''))}
                ${moralHtml}
            </div>
        </div>`;
}

function storyExtraArt(story) {
    return story.art2.replace(/-2(\.\w+)$/, '-3$1');
}

function storyExtraPage(story, paras) {
    const html = paras.map(p => `<p>${p}</p>`).join('');
    return `
        <div class="page page-story">
            <div class="story-page-left">
                ${html}
            </div>
            <div class="story-page-right">
                ${storyMedia(story, storyExtraArt(story), story.title.replace(/^\d+장 · /, ''))}
            </div>
        </div>`;
}

const CLOSE_SPLIT_THRESHOLD = 480;

function storyPageEntries(story) {
    const closeLen = story.close.reduce((sum, p) => sum + p.length, 0);
    if (closeLen > CLOSE_SPLIT_THRESHOLD && story.close.length >= 2) {
        const mid = Math.ceil(story.close.length / 2);
        return [
            { kind: 'story-open', story },
            { kind: 'story-extra', story, paras: story.close.slice(0, mid) },
            { kind: 'story-close', story, paras: story.close.slice(mid) }
        ];
    }
    return [
        { kind: 'story-open', story },
        { kind: 'story-close', story, paras: story.close }
    ];
}

const QUIZ = [
    { q: "새어머니와 언니들은 신데렐라를 뭐라고 놀려 불렀나요?", choices: ["공주님", "재투성이", "막내딸"], answer: 1 },
    { q: "신데렐라는 왜 무도회에 갈 수 없었나요?", choices: ["몸이 아파서", "입을 옷이 없어서", "방법을 몰라서"], answer: 1 },
    { q: "요정 대모는 호박을 무엇으로 바꿨나요?", choices: ["황금 마차", "유리구두", "드레스"], answer: 0 },
    { q: "요정 대모가 신신당부한 것은 무엇인가요?", choices: ["예의 바르게 행동할 것", "자정 전에 돌아올 것", "왕자에게 먼저 말 걸 것"], answer: 1 },
    { q: "신데렐라는 계단에서 무엇을 잃어버렸나요?", choices: ["머리핀", "유리구두 한 짝", "목걸이"], answer: 1 },
    { q: "언니들은 왜 유리구두를 신지 못했나요?", choices: ["구두가 너무 커서", "발이 맞지 않아서", "구두를 잃어버려서"], answer: 1 },
    { q: "유리구두는 결국 누구의 발에 맞았나요?", choices: ["첫째 언니", "둘째 언니", "신데렐라"], answer: 2 }
];

function quizPage() {
    const items = QUIZ.map((item, i) => `
        <div class="quiz-item" data-qindex="${i}">
            <p class="quiz-question">${i + 1}. ${item.q}</p>
            <div class="quiz-choices">
                ${item.choices.map((c, ci) => `<button type="button" class="quiz-choice" data-choice="${ci}">${c}</button>`).join('')}
            </div>
        </div>`).join('');
    return `
        <div class="page page-quiz">
            <h2>이야기 문제</h2>
            <p class="quiz-intro-text" id="quizProgress">0 / 총 ${QUIZ.length}문항 완료</p>
            <div class="quiz-list">${items}</div>
        </div>`;
}

function endPage() {
    return `
        <div class="page page-end">
            ${artFrame('end.png', '🌟')}
            <h2>신데렐라 이야기를 다 읽었어요!</h2>
            <a class="home-btn" href="../../../../../">학습 허브로 돌아가기</a>
        </div>`;
}

const PAGES = [
    { kind: 'cover' },
    { kind: 'toc' },
    ...CHAPTERS.flatMap(storyPageEntries),
    { kind: 'quiz' },
    { kind: 'end' }
];

const TWO_PAGE_KINDS = new Set(['story-open', 'story-close', 'story-extra', 'toc', 'cover']);

let folioCounter = 0;
const FOLIOS = PAGES.map(p => {
    const width = TWO_PAGE_KINDS.has(p.kind) ? 2 : 1;
    const start = folioCounter + 1;
    folioCounter += width;
    return { start, width };
});

function renderPage(page) {
    switch (page.kind) {
        case 'cover':
            return coverPage();
        case 'toc':
            return tocPage();
        case 'story-open':
            return storyOpenPage(page.story);
        case 'story-extra':
            return storyExtraPage(page.story, page.paras);
        case 'story-close':
            return storyClosePage(page.story, page.paras);
        case 'quiz':
            return quizPage();
        case 'end':
            return endPage();
        default:
            return '';
    }
}

let current = 0;
let animating = false;

const spreadEl = document.getElementById('spread');
const prevBtn = document.getElementById('prevBtn');
const nextBtn = document.getElementById('nextBtn');
const indicatorEl = document.getElementById('pageIndicator');
const folioLeftEl = document.getElementById('folioLeft');
const folioRightEl = document.getElementById('folioRight');

function paint() {
    spreadEl.innerHTML = renderPage(PAGES[current]);
    prevBtn.disabled = current === 0;
    nextBtn.disabled = current === PAGES.length - 1;
    indicatorEl.textContent = `${current + 1} / ${PAGES.length}`;

    const folio = FOLIOS[current];
    folioLeftEl.classList.toggle('folio-center', folio.width === 1);
    if (folio.width === 2) {
        folioLeftEl.textContent = folio.start;
        folioRightEl.textContent = folio.start + 1;
        folioLeftEl.hidden = false;
        folioRightEl.hidden = false;
    } else {
        folioLeftEl.textContent = folio.start;
        folioLeftEl.hidden = false;
        folioRightEl.hidden = true;
    }

    spreadEl.querySelectorAll('[data-goto]').forEach(btn => {
        btn.addEventListener('click', () => {
            const chapterNum = Number(btn.dataset.goto);
            const idx = PAGES.findIndex(p => p.kind === 'story-open' && p.story.num === chapterNum);
            if (idx >= 0) goTo(idx);
        });
    });
    spreadEl.querySelectorAll('[data-goto-kind]').forEach(btn => {
        btn.addEventListener('click', () => {
            const idx = PAGES.findIndex(p => p.kind === btn.dataset.gotoKind);
            if (idx >= 0) goTo(idx);
        });
    });

    if (PAGES[current].kind === 'quiz') {
        initQuiz();
    }
}

function initQuiz() {
    let answeredCount = 0;
    const progressEl = document.getElementById('quizProgress');

    spreadEl.querySelectorAll('.quiz-item').forEach(item => {
        const qi = Number(item.dataset.qindex);
        const q = QUIZ[qi];
        item.querySelectorAll('.quiz-choice').forEach(btn => {
            btn.addEventListener('click', () => {
                if (item.classList.contains('graded')) return;
                item.classList.add('graded');
                const chosen = Number(btn.dataset.choice);
                item.querySelectorAll('.quiz-choice').forEach(b => {
                    const ci = Number(b.dataset.choice);
                    if (ci === q.answer) b.classList.add('correct');
                    else if (ci === chosen) b.classList.add('incorrect');
                });
                answeredCount++;
                progressEl.textContent = `${answeredCount} / 총 ${QUIZ.length}문항 완료`;
            });
        });
    });
}

function goTo(index) {
    if (animating || index === current || index < 0 || index >= PAGES.length) return;
    animating = true;
    const dir = index > current ? 'flip-next' : 'flip-prev';
    spreadEl.classList.add(dir);
    setTimeout(() => {
        current = index;
        paint();
    }, 230);
    setTimeout(() => {
        spreadEl.classList.remove('flip-next', 'flip-prev');
        animating = false;
    }, 480);
}

prevBtn.addEventListener('click', () => goTo(current - 1));
nextBtn.addEventListener('click', () => goTo(current + 1));

document.getElementById('tocLink').addEventListener('click', () => {
    const idx = PAGES.findIndex(p => p.kind === 'toc');
    if (idx >= 0) goTo(idx);
});

document.addEventListener('keydown', (e) => {
    if (e.key === 'ArrowRight') goTo(current + 1);
    if (e.key === 'ArrowLeft') goTo(current - 1);
});

paint();
