const CHAPTERS = [
    {
        "num": 1,
        "title": "1장 · 창가의 장미",
        "beats": [
            {
                "art": "01-roses.png",
                "emoji": "🌹",
                "left": [
                    "지붕이 맞붙은 두 집에 게르다와 카이가 살았습니다.",
                    "두 아이는 창가 화분에서 장미를 함께 길렀습니다.",
                    "\"우리 장미가 제일 예쁘다, 그렇지?\""
                ],
                "right": [
                    "여름에는 창을 열어 놓고 마주 보며 놀았습니다.",
                    "겨울에는 난롯가에 앉아 할머니의 이야기를 들었습니다.",
                    "두 아이는 하루도 떨어져 지낸 적이 없었습니다."
                ]
            }
        ]
    },
    {
        "num": 2,
        "title": "2장 · 눈에 박힌 조각",
        "beats": [
            {
                "art": "02-shard.png",
                "emoji": "🪞",
                "left": [
                    "어느 날 하늘에서 아주 작은 유리 조각들이 떨어졌습니다.",
                    "무엇이든 밉게 보이게 만드는 마법 거울의 조각이었습니다.",
                    "\"앗, 눈에 뭐가 들어갔어!\""
                ],
                "right": [
                    "조각 하나가 카이의 눈에, 또 하나가 가슴에 박혔습니다.",
                    "그때부터 카이는 달라졌습니다.",
                    "\"이 장미 좀 봐. 벌레 먹고 볼품없잖아.\"",
                    "게르다는 그런 카이를 이해할 수 없었습니다."
                ]
            }
        ]
    },
    {
        "num": 3,
        "title": "3장 · 하얀 썰매",
        "beats": [
            {
                "art": "03-sleigh.png",
                "emoji": "🛷",
                "left": [
                    "겨울 어느 날, 광장에 새하얀 썰매 한 대가 나타났습니다.",
                    "카이는 장난삼아 자기 썰매를 그 뒤에 매달았습니다.",
                    "썰매는 점점 빨라지더니 마을을 벗어났습니다."
                ],
                "right": [
                    "눈보라 속에서 썰매가 멈추고 여인이 돌아보았습니다.",
                    "\"추우니? 이리 오렴.\"",
                    "눈의 여왕이 이마에 입을 맞추자 카이는 추위도 집도 잊었습니다.",
                    "그렇게 카이는 사라졌습니다."
                ]
            }
        ]
    },
    {
        "num": 4,
        "title": "4장 · 게르다가 길을 나서다",
        "beats": [
            {
                "art": "04-journey.png",
                "emoji": "👣",
                "left": [
                    "봄이 되어도 카이는 돌아오지 않았습니다.",
                    "\"내가 찾으러 갈래.\"",
                    "게르다는 빨간 신을 신고 강가로 갔습니다."
                ],
                "right": [
                    "\"강물아, 카이를 데려다주면 이 신을 줄게.\"",
                    "게르다는 작은 배에 올라탔습니다.",
                    "배는 물살을 타고 천천히 흘러갔습니다.",
                    "그렇게 긴 여행이 시작되었습니다."
                ]
            }
        ]
    },
    {
        "num": 5,
        "title": "5장 · 도둑 소녀",
        "beats": [
            {
                "art": "05-robber.png",
                "emoji": "🗡️",
                "left": [
                    "숲을 지나던 게르다는 도둑들에게 붙잡혔습니다.",
                    "그중 사나운 도둑 소녀가 게르다를 붙들었습니다.",
                    "\"이 애는 내 거야. 아무도 못 건드려!\""
                ],
                "right": [
                    "게르다는 밤새 카이 이야기를 들려주었습니다.",
                    "이야기를 다 들은 도둑 소녀의 얼굴이 조금 누그러졌습니다.",
                    "\"내 순록을 타고 가. 북쪽으로 데려다줄 거야.\"",
                    "도둑 소녀는 털옷과 장갑까지 챙겨 주었습니다."
                ]
            }
        ]
    },
    {
        "num": 6,
        "title": "6장 · 북쪽 끝으로",
        "beats": [
            {
                "art": "06-reindeer.png",
                "emoji": "🦌",
                "left": [
                    "순록은 눈밭을 가로질러 북쪽으로 달렸습니다.",
                    "하늘에는 초록빛 오로라가 넘실거렸습니다.",
                    "\"저 너머가 눈의 여왕의 성이야.\""
                ],
                "right": [
                    "순록은 성이 보이는 언덕에서 걸음을 멈췄습니다.",
                    "\"나는 여기까지밖에 못 가.\"",
                    "게르다는 장갑도 신발도 없이 눈밭으로 걸어 들어갔습니다.",
                    "\"고마워. 나머지는 내가 갈게.\""
                ]
            }
        ]
    },
    {
        "num": 7,
        "title": "7장 · 얼음 궁전",
        "beats": [
            {
                "art": "07-ice-palace.png",
                "emoji": "🧊",
                "left": [
                    "얼음 궁전 한가운데에 카이가 앉아 있었습니다.",
                    "입술이 파랗게 질린 채 얼음 조각을 맞추고 있었습니다.",
                    "\"카이야!\""
                ],
                "right": [
                    "카이는 게르다를 알아보지 못했습니다.",
                    "게르다는 카이를 꼭 끌어안았습니다.",
                    "따뜻한 눈물이 카이의 가슴 위로 떨어졌습니다.",
                    "그러자 가슴에 박혀 있던 거울 조각이 녹아내렸습니다."
                ]
            }
        ]
    },
    {
        "num": 8,
        "title": "8장 · 다시 여름",
        "beats": [
            {
                "art": "08-home.png",
                "emoji": "🌸",
                "left": [
                    "\"게르다야, 여기가 어디지? 왜 이렇게 추워?\"",
                    "카이의 눈에서도 조각이 흘러나왔습니다.",
                    "두 아이는 손을 잡고 성을 걸어 나왔습니다."
                ],
                "right": [
                    "돌아오는 길에 얼음은 눈 녹듯 사라졌습니다.",
                    "집에 닿았을 때 창가 화분에는 장미가 활짝 피어 있었습니다.",
                    "두 아이는 여전히 그 창가에 마주 앉았습니다.",
                    "다만 지난겨울보다 조금 더 자라 있었습니다."
                ]
            }
        ],
        "moral": "차갑게 굳은 마음도 진심 어린 정성 앞에서는 녹아내려요. 포기하지 않는 우정은 먼 길도 견디게 해 준답니다."
    }
];

function artFrame(src, emoji) {
    return `
        <div class="art-frame">
            <img src="images/${src}" alt="" onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';">
            <div class="art-fallback" style="display:none">${emoji}</div>
        </div>`;
}

function coverPage() {
    return `
        <div class="page page-cover">
            <div class="story-page-left story-page-left-full">
                ${artFrame('cover.png', '❄️')}
            </div>
            <div class="story-page-right">
                <h1>눈의 여왕</h1>
                <p>눈의 여왕은 덴마크의 작가 한스 크리스티안 안데르센이 1844년에 발표한 이야기예요. 안데르센이 쓴 이야기 가운데 가장 길고 짜임이 복잡한 작품으로 꼽힙니다.</p>
                <p>원래는 일곱 개의 이야기로 나뉘어 있고, 게르다가 카이를 찾아가는 길에서 만나는 사람들이 저마다 한 대목씩을 차지해요. 이 책에서는 그 여정을 여덟 장으로 간추려 담았습니다.</p>
            </div>
        </div>`;
}

function tocPage() {
    const itemHtml = s => `
        <li>
            <button type="button" data-goto="${s.num}">
                <span class="toc-num">${s.num}</span>
                <span>
                    <strong>${s.title.replace(/^\d+장 · /, '')}</strong>
                </span>
            </button>
        </li>`;
    const quizItemHtml = `
        <li>
            <button type="button" data-goto-kind="quiz">
                <span class="toc-num">❓</span>
                <span>
                    <strong>이야기 문제</strong>
                </span>
            </button>
        </li>`;
    const half = Math.ceil(CHAPTERS.length / 2);
    const leftItems = CHAPTERS.slice(0, half).map(itemHtml).join('');
    const rightItems = CHAPTERS.slice(half).map(itemHtml).join('') + quizItemHtml;
    return `
        <div class="page page-toc">
            <div class="story-page-left">
                <h2>차례</h2>
                <ul class="toc-list">${leftItems}</ul>
            </div>
            <div class="story-page-right">
                <ul class="toc-list">${rightItems}</ul>
            </div>
        </div>`;
}

function spreadPage(chapter, beat, isFirst, isLast) {
    const badgeHtml = isFirst ? `<div class="spread-chapter-badge">${chapter.title}</div>` : '';
    const leftHtml = beat.left.map(p => `<p>${p}</p>`).join('');
    let rightHtml = beat.right.map(p => `<p>${p}</p>`).join('');
    const moralHtml = isLast && chapter.moral ? `<p class="fable-moral">${chapter.moral}</p>` : '';
    return `
        <div class="page page-story${moralHtml ? ' spread-final' : ''}">
            <div class="spread-art">
                ${badgeHtml}
                ${artFrame(beat.art, beat.emoji)}
            </div>
            <div class="spread-text">
                <div class="spread-text-left">${leftHtml}</div>
                <div class="spread-text-right">${rightHtml}</div>
                ${moralHtml}
            </div>
        </div>`;
}

const QUIZ = [
    {
        "q": "게르다와 카이가 함께 기른 것은 무엇인가요?",
        "choices": [
            "장미",
            "해바라기",
            "나무"
        ],
        "answer": 0
    },
    {
        "q": "카이가 달라진 까닭은 무엇인가요?",
        "choices": [
            "병이 나서",
            "이사를 가서",
            "거울 조각이 박혀서"
        ],
        "answer": 2
    },
    {
        "q": "카이는 무엇을 타고 사라졌나요?",
        "choices": [
            "하얀 썰매",
            "말",
            "배"
        ],
        "answer": 0
    },
    {
        "q": "게르다가 강물에게 준 것은 무엇인가요?",
        "choices": [
            "빵",
            "빨간 신",
            "목걸이"
        ],
        "answer": 1
    },
    {
        "q": "도둑 소녀가 게르다에게 내준 것은 무엇인가요?",
        "choices": [
            "썰매",
            "순록",
            "말"
        ],
        "answer": 1
    },
    {
        "q": "카이 가슴의 거울 조각을 녹인 것은 무엇인가요?",
        "choices": [
            "게르다의 눈물",
            "햇빛",
            "불"
        ],
        "answer": 0
    },
    {
        "q": "집에 돌아왔을 때 창가에는 무엇이 피어 있었나요?",
        "choices": [
            "눈꽃",
            "아무것도 없었다",
            "장미"
        ],
        "answer": 2
    }
];

function quizPage() {
    const items = QUIZ.map((item, i) => `
        <div class="quiz-item" data-qindex="${i}">
            <p class="quiz-question">${i + 1}. ${item.q}</p>
            <div class="quiz-choices">
                ${item.choices.map((c, ci) => `<button type="button" class="quiz-choice" data-choice="${ci}">${c}</button>`).join('')}
            </div>
        </div>`).join('');
    return `
        <div class="page page-quiz">
            <h2>이야기 문제</h2>
            <p class="quiz-intro-text" id="quizProgress">0 / 총 ${QUIZ.length}문항 완료</p>
            <div class="quiz-list">${items}</div>
        </div>`;
}

function endPage() {
    return `
        <div class="page page-end">
            ${artFrame('end.png', '🌟')}
            <h2>눈의 여왕를 다 읽었어요!</h2>
            <a class="home-btn" href="../../../../../">학습 허브로 돌아가기</a>
        </div>`;
}

const PAGES = [
    { kind: 'cover' },
    { kind: 'toc' },
    ...CHAPTERS.flatMap((chapter, ci) => chapter.beats.map((beat, i) => ({
        kind: 'spread', chapter, beat,
        isFirst: i === 0,
        isLast: ci === CHAPTERS.length - 1 && i === chapter.beats.length - 1
    }))),
    { kind: 'quiz' },
    { kind: 'end' }
];

const TWO_PAGE_KINDS = new Set(['spread', 'toc', 'cover']);

let folioCounter = 0;
const FOLIOS = PAGES.map(p => {
    const width = TWO_PAGE_KINDS.has(p.kind) ? 2 : 1;
    const start = folioCounter + 1;
    folioCounter += width;
    return { start, width };
});

function renderPage(page) {
    switch (page.kind) {
        case 'cover':
            return coverPage();
        case 'toc':
            return tocPage();
        case 'spread':
            return spreadPage(page.chapter, page.beat, page.isFirst, page.isLast);
        case 'quiz':
            return quizPage();
        case 'end':
            return endPage();
        default:
            return '';
    }
}

let current = 0;
let animating = false;

const spreadEl = document.getElementById('spread');
const prevBtn = document.getElementById('prevBtn');
const nextBtn = document.getElementById('nextBtn');
const indicatorEl = document.getElementById('pageIndicator');
const folioLeftEl = document.getElementById('folioLeft');
const folioRightEl = document.getElementById('folioRight');

function paint() {
    spreadEl.innerHTML = renderPage(PAGES[current]);
    prevBtn.disabled = current === 0;
    nextBtn.disabled = current === PAGES.length - 1;
    indicatorEl.textContent = `${current + 1} / ${PAGES.length}`;

    const folio = FOLIOS[current];
    folioLeftEl.classList.toggle('folio-center', folio.width === 1);
    if (folio.width === 2) {
        folioLeftEl.textContent = folio.start;
        folioRightEl.textContent = folio.start + 1;
        folioLeftEl.hidden = false;
        folioRightEl.hidden = false;
    } else {
        folioLeftEl.textContent = folio.start;
        folioLeftEl.hidden = false;
        folioRightEl.hidden = true;
    }

    spreadEl.querySelectorAll('[data-goto]').forEach(btn => {
        btn.addEventListener('click', () => {
            const chapterNum = Number(btn.dataset.goto);
            const idx = PAGES.findIndex(p => p.kind === 'spread' && p.chapter.num === chapterNum && p.isFirst);
            if (idx >= 0) goTo(idx);
        });
    });
    spreadEl.querySelectorAll('[data-goto-kind]').forEach(btn => {
        btn.addEventListener('click', () => {
            const idx = PAGES.findIndex(p => p.kind === btn.dataset.gotoKind);
            if (idx >= 0) goTo(idx);
        });
    });

    if (PAGES[current].kind === 'quiz') {
        initQuiz();
    }
}

function initQuiz() {
    let answeredCount = 0;
    const progressEl = document.getElementById('quizProgress');

    spreadEl.querySelectorAll('.quiz-item').forEach(item => {
        const qi = Number(item.dataset.qindex);
        const q = QUIZ[qi];
        item.querySelectorAll('.quiz-choice').forEach(btn => {
            btn.addEventListener('click', () => {
                if (item.classList.contains('graded')) return;
                item.classList.add('graded');
                const chosen = Number(btn.dataset.choice);
                item.querySelectorAll('.quiz-choice').forEach(b => {
                    const ci = Number(b.dataset.choice);
                    if (ci === q.answer) b.classList.add('correct');
                    else if (ci === chosen) b.classList.add('incorrect');
                });
                answeredCount++;
                progressEl.textContent = `${answeredCount} / 총 ${QUIZ.length}문항 완료`;
            });
        });
    });
}

function goTo(index) {
    if (animating || index === current || index < 0 || index >= PAGES.length) return;
    animating = true;
    const dir = index > current ? 'flip-next' : 'flip-prev';
    spreadEl.classList.add(dir);
    setTimeout(() => {
        current = index;
        paint();
    }, 230);
    setTimeout(() => {
        spreadEl.classList.remove('flip-next', 'flip-prev');
        animating = false;
    }, 480);
}

prevBtn.addEventListener('click', () => goTo(current - 1));
nextBtn.addEventListener('click', () => goTo(current + 1));

document.getElementById('tocLink').addEventListener('click', () => {
    const idx = PAGES.findIndex(p => p.kind === 'toc');
    if (idx >= 0) goTo(idx);
});

document.addEventListener('keydown', (e) => {
    if (e.key === 'ArrowRight') goTo(current + 1);
    if (e.key === 'ArrowLeft') goTo(current - 1);
});

paint();
