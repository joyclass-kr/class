const CHAPTERS = [
    {
        num: 1,
        title: "1장 · 떡은 누가 다 먹지",
        beats: [
            {
                art: "01-cake.webp",
                emoji: "🍡",
                left: [
                    "어느 가을날, 호랑이와 토끼와 두꺼비가 한자리에 모였습니다.",
                    "김이 모락모락 나는 떡시루가 놓여 있었지요. 갓 쪄 낸 시루떡이 층층이 쌓여 있었습니다.",
                    "마을 잔치에서 얻어 온 떡이었어요.",
                    "고소한 냄새가 언덕 아래까지 퍼져 나갔어요.",
                    "셋이 나란히 언덕에 올라앉았지요."
                ],
                right: [
                    "\"야, 맛있겠다!\"",
                    "토끼가 코를 벌름거렸어요.",
                    "\"고소한 냄새가 여기까지 나네요.\"",
                    "셋은 시루를 빙 둘러싸고 앉았습니다.",
                    "호랑이는 벌써 입맛을 쩝쩝 다셨지요.",
                    "두꺼비는 시루 옆에 조용히 앉아 있었지요."
                ]
            },
            {
                art: "02-argue.webp",
                emoji: "😤",
                left: [
                    "그런데 문제가 생겼어요.",
                    "호랑이가 먼저 손을 뻗었어요.",
                    "\"이걸 누가 다 먹지?\"",
                    "\"당연히 나지!\"",
                    "호랑이가 앞발을 쿵 내리쳤습니다. 시루가 들썩할 만큼 세게요.",
                    "토끼가 얼른 그 앞을 막아섰지요."
                ],
                right: [
                    "\"내가 제일 힘이 세니까!\"",
                    "호랑이가 이빨을 드러냈어요.",
                    "\"힘이 세면 다 먹나요? 저는 제일 빠른데요.\"",
                    "토끼도 지지 않고 귀를 쫑긋 세웠어요.",
                    "둘은 코가 닿을 만큼 얼굴을 들이밀었습니다.",
                    "떡은 식어 가는데 다툼은 그치지 않았답니다."
                ]
            },
            {
                art: "03-quiet.webp",
                emoji: "🐸",
                left: [
                    "호랑이와 토끼가 한참을 옥신각신했어요.",
                    "해가 벌써 기울고 있었어요.",
                    "그동안 두꺼비는 한마디도 하지 않았지요. 시루 옆에 앉아 눈만 껌뻑거렸습니다.",
                    "\"두꺼비, 너는 왜 가만있어?\"",
                    "토끼가 두꺼비를 툭 건드렸지요."
                ],
                right: [
                    "\"음… 저는 힘도 없고 빠르지도 않으니까요.\"",
                    "두꺼비는 느릿느릿 대답했어요.",
                    "얼굴에는 아무 기색도 없었어요.",
                    "호랑이와 토끼는 코웃음을 치고는 다시 다투기 시작했지요.",
                    "두꺼비는 그저 언덕 아래만 내려다보았지요."
                ]
            }
        ]
    },
    {
        num: 2,
        title: "2장 · 시루를 굴리자",
        beats: [
            {
                art: "04-idea.webp",
                emoji: "💡",
                left: [
                    "그러다 두꺼비가 슬며시 입을 열었습니다.",
                    "두꺼비의 목소리가 아주 태연했어요.",
                    "\"공평하게 내기를 하는 게 어떨까요?\"",
                    "둘이 동시에 두꺼비를 돌아보았어요.",
                    "다투던 둘이 딱 멈췄지요."
                ],
                right: [
                    "\"내기? 무슨 내기?\"",
                    "호랑이가 눈을 크게 떴어요.",
                    "\"이 시루를 언덕 아래로 굴리는 거예요.\"",
                    "\"먼저 잡는 이가 다 먹기로 하고요.\"",
                    "두꺼비는 언덕 아래를 턱으로 가리켰습니다.",
                    "언덕은 길고 비탈이 제법 가팔랐답니다."
                ]
            },
            {
                art: "05-agree.webp",
                emoji: "😁",
                left: [
                    "호랑이가 껄껄 웃었어요.",
                    "앞발로 제 가슴을 쿵쿵 쳤지요.",
                    "\"좋다! 나보다 빠른 놈이 어디 있겠느냐!\"",
                    "\"어림없어요. 달리기는 제가 최고예요!\"",
                    "토끼도 지지 않고 뛰어올랐지요."
                ],
                right: [
                    "둘은 벌써 출발선에 서서 몸을 풀었습니다. 호랑이는 발톱으로 땅을 긁고, 토끼는 뒷다리를 폈다 굽혔다 했지요.",
                    "둘 다 두꺼비는 안중에도 없었어요.",
                    "두꺼비는 그저 느긋하게 앉아 있었어요.",
                    "서두르는 기색이 조금도 없었습니다.",
                    "두꺼비는 눈만 껌뻑였답니다."
                ]
            },
            {
                art: "06-roll.webp",
                emoji: "🏃",
                left: [
                    "\"하나, 둘, 셋!\"",
                    "두꺼비가 시루를 슬쩍 밀었어요.",
                    "떡시루가 언덕 아래로 데굴데굴 굴러갔습니다.",
                    "\"내 거다!\"",
                    "\"제가 먼저예요!\""
                ],
                right: [
                    "호랑이와 토끼는 쏜살같이 시루를 쫓아 내려갔어요.",
                    "둘은 뒤도 돌아보지 않았어요.",
                    "흙먼지가 뿌옇게 일었지요.",
                    "두꺼비는 그 뒷모습을 한참 바라보다가 엉금엉금 걷기 시작했습니다.",
                    "뒤도 돌아보지 않고요."
                ]
            }
        ]
    },
    {
        num: 3,
        title: "3장 · 언덕에 흩어진 떡",
        beats: [
            {
                art: "07-scatter.webp",
                emoji: "🍘",
                left: [
                    "두꺼비는 느릿느릿 언덕을 내려갔어요.",
                    "한 걸음, 또 한 걸음. 서두르는 기색이라고는 조금도 없었지요.",
                    "대신 눈은 부지런히 땅바닥을 살폈습니다.",
                    "숨도 차지 않았지요."
                ],
                right: [
                    "그런데 시루가 구를 때마다 떡이 한 조각씩 툭, 툭 떨어졌어요.",
                    "언덕 여기저기에 하얀 떡이 흩어졌습니다.",
                    "두꺼비는 그것을 하나도 놓치지 않았지요.",
                    "떡은 풀 위에 얌전히 놓여 있었지요."
                ]
            },
            {
                art: "08-eat.webp",
                emoji: "😋",
                left: [
                    "떡 한 조각이 발밑에 놓여 있었어요.",
                    "두꺼비는 떨어진 떡을 하나씩 주워 먹었습니다.",
                    "\"어이쿠, 이건 참 잘 익었네.\"",
                    "우물우물, 냠냠.",
                    "아직 따뜻했지요."
                ],
                right: [
                    "떡은 주워도 주워도 계속 나왔어요. 몇 걸음마다 한 조각씩 놓여 있었으니까요.",
                    "두꺼비의 배가 점점 볼록해졌습니다.",
                    "마지막 조각을 삼키고는 배를 툭툭 두드렸지요.",
                    "두꺼비는 아주 흐뭇했답니다."
                ]
            },
            {
                art: "09-empty.webp",
                emoji: "😳",
                left: [
                    "언덕 아래에서는 호랑이와 토끼가 동시에 시루를 붙잡았어요.",
                    "둘 다 숨이 턱까지 차 있었어요.",
                    "시루가 나무에 부딪혀 멈춘 참이었지요.",
                    "\"내가 먼저!\" \"아니에요, 제가 먼저!\"",
                    "둘은 숨을 헐떡이며 서로를 노려보았습니다."
                ],
                right: [
                    "그런데 시루 안을 들여다본 순간, 둘 다 입을 다물었어요.",
                    "호랑이가 시루를 뒤집어 보았어요.",
                    "텅.",
                    "떡이 한 조각도 남아 있지 않았거든요. 굴러오는 동안 죄다 흘린 것이지요.",
                    "둘은 서로 얼굴만 쳐다보았지요."
                ]
            },
            {
                art: "10-belly.webp",
                emoji: "🐸",
                left: [
                    "둘은 부랴부랴 언덕을 다시 올라갔습니다.",
                    "떡이 어디로 갔는지 알아야 했거든요.",
                    "두꺼비가 볼록한 배를 두드리며 앉아 있었지요.",
                    "\"아이고 배불러. 잘 먹었습니다.\""
                ],
                right: [
                    "\"너, 너 언제 다…!\"",
                    "토끼가 두꺼비의 배를 가리켰어요.",
                    "호랑이는 말을 잇지 못했어요.",
                    "두꺼비는 대답 대신 눈만 껌뻑껌뻑했답니다.",
                    "그날 이후 셋은 떡을 미리 나누기로 했답니다."
                ]
            }
        ],
        moral: "빨리 가는 것보다 어디에 무엇이 떨어지는지 보는 눈이 나을 때가 있어요. 호랑이와 토끼는 시루만 보고 달렸고, 두꺼비는 길을 보며 걸었답니다.",
        question: "서두르다가 정작 중요한 것을 놓친 적이 있나요?"
    }
];

function artFrame(src, emoji) {
    return `
        <div class="art-frame">
            <img src="images/${src}" alt="" onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';">
            <div class="art-fallback" style="display:none">${emoji}</div>
        </div>`;
}

function coverToc() {
    const item = s => `
        <button type="button" data-goto="${s.num}">
            <span class="toc-num">${s.num}</span>
            <span>${s.title.replace(/^\d+장 · /, '')}</span>
        </button>`;
    return `
        <nav class="cover-toc">
            <h2>차례</h2>
            ${CHAPTERS.map(item).join('')}
            <button type="button" data-goto-kind="quiz">
                <span class="toc-num">❓</span>
                <span>이야기 문제</span>
            </button>
        </nav>`;
}

function coverPage() {
    return `
        <div class="page page-cover">
            <div class="story-page-left story-page-left-full">
                ${artFrame('cover.webp', '🐸')}
            </div>
            <div class="story-page-right">
                <h1>두꺼비와 토끼와 호랑이</h1>
                <p>두꺼비와 토끼와 호랑이는 지은이가 없는 구전 설화예요. 떡 내기라는 이름으로도 전해진답니다.</p>
                <p>우리 옛이야기에는 셋이 겨루는 이야기가 유난히 많아요. 대개 가장 크고 힘센 쪽, 가장 빠른 쪽, 그리고 가장 작고 느린 쪽이 나오는데 이기는 것은 늘 마지막 하나지요. 듣는 아이가 자기를 어디에 놓고 들을지 생각해 보면 왜 그런 짜임이 되었는지 알 수 있답니다.</p>
                <p>시루는 떡을 찌는 그릇이에요. 바닥에 구멍이 뚫려 있어서 솥 위에 얹고 김을 올려 떡을 쪄 냈지요. 잔칫날이나 고사를 지낼 때 쓰던 것이라 시루떡은 옛사람들에게 아주 귀한 음식이었답니다.</p>
                ${coverToc()}
            </div>
        </div>`;
}

function spreadPage(chapter, beat, isFirst) {
    const badgeHtml = isFirst ? `<div class="spread-chapter-badge">${chapter.title}</div>` : '';
    const leftHtml = beat.left.map(p => `<p>${p}</p>`).join('');
    const rightHtml = beat.right.map(p => `<p>${p}</p>`).join('');
    return `
        <div class="page page-story">
            <div class="spread-art">
                ${badgeHtml}
                ${artFrame(beat.art, beat.emoji)}
            </div>
            <div class="spread-text">
                <div class="spread-text-left">${leftHtml}</div>
                <div class="spread-text-right">${rightHtml}</div>
            </div>
        </div>`;
}

function reflectionPage(chapter) {
    return `
        <div class="page page-reflection">
            <h2>이야기를 다 읽고</h2>
            <p class="reflection-moral">${chapter.moral}</p>
            <p class="reflection-question">${chapter.question}</p>
        </div>`;
}

const QUIZ = [
    { q: "셋이 다툰 까닭은 무엇인가요?", choices: ["누가 더 빠른지", "떡을 누가 먹을지", "누가 더 힘센지"], answer: 1 },
    { q: "내기를 하자고 한 것은 누구인가요?", choices: ["호랑이", "토끼", "두꺼비"], answer: 2 },
    { q: "셋은 어떤 내기를 하기로 했나요?", choices: ["시루 굴려 잡기", "언덕 오르기", "물속 오래 참기"], answer: 0 },
    { q: "시루가 굴러갈 때 무슨 일이 있었나요?", choices: ["시루가 깨졌다", "길이 막혔다", "떡이 떨어졌다"], answer: 2 },
    { q: "언덕 아래에서 시루 안은 어땠나요?", choices: ["텅 비어 있었다", "떡이 가득했다", "물이 차 있었다"], answer: 0 },
    { q: "떡을 차지한 쪽은 어떤 방법을 썼나요?", choices: ["시루에서 꺼내어서", "떨어진 것을 주워서", "몰래 감춰 두었다가"], answer: 1 }
];

function quizPage() {
    const items = QUIZ.map((item, i) => `
        <div class="quiz-item" data-qindex="${i}">
            <p class="quiz-question">${i + 1}. ${item.q}</p>
            <div class="quiz-choices">
                ${item.choices.map((c, ci) => `<button type="button" class="quiz-choice" data-choice="${ci}">${c}</button>`).join('')}
            </div>
        </div>`).join('');
    return `
        <div class="page page-quiz">
            <h2>이야기 문제</h2>
            <p class="quiz-intro-text" id="quizProgress">0 / 총 ${QUIZ.length}문항 완료</p>
            <div class="quiz-list">${items}</div>
        </div>`;
}

function endPage() {
    return `
        <div class="page page-end">
            ${artFrame('end.webp', '🌟')}
            <h2>두꺼비와 토끼와 호랑이 이야기를 다 읽었어요!</h2>
            <a class="home-btn" href="../../../../../">학습 허브로 돌아가기</a>
        </div>`;
}

/* 읽고 나서 — 세계명작 트랙과 같은 형식이다. 동화틀은 쪽을 재서 나누지 않으므로
   펼침면마다 왼쪽·오른쪽 글을 손으로 나누어 둔다. */
const AFTERWORD = {
    title: '읽고 나서',
    emoji: '🐸',
    spreads: [
        {
            art: 'end.webp',
            left: [
                "느린 쪽이 빠른 쪽을 이기는 이야기는 세계 어디에나 있습니다. 토끼와 거북이 가장 널리 알려진 것이지요.",
                "그런데 이 이야기는 그것들과 한 군데가 다릅니다. 토끼와 거북에서는 토끼가 잠을 잡니다. 빠른 쪽이 실수를 해야 느린 쪽이 이기는 것이지요.",
                "여기서는 아무도 실수하지 않습니다. 호랑이와 토끼는 있는 힘껏 달렸습니다. 두꺼비는 그 둘보다 나은 것을 한 것이 아니라, 다른 것을 보고 있었을 뿐입니다.",
                "다시 보십시오. 둘은 시루를 보고 달렸고 두꺼비는 땅을 보며 걸었습니다. 굴러가는 시루에서 떡이 흘렀다는 것을 안 것은 땅을 본 쪽뿐이었습니다."
            ],
            right: [
                "무엇을 얻느냐는 얼마나 빠른가보다 어디를 보고 있는가에 달릴 때가 있습니다.",
                "두꺼비는 처음부터 떡이 흘릴 줄 알았을까요? 답은 적어 두지 않겠습니다."
            ]
        }
    ]
};

function afterPage(spread, isFirst) {
    const head = isFirst ? `<h2>${AFTERWORD.title}</h2>` : '';
    // 그림은 오른쪽 위 모서리에 꽉 붙인다. 글은 그 아래로 이어진다.
    const art = spread.art ? `<div class="after-art">${artFrame(spread.art, AFTERWORD.emoji)}</div>` : '';
    const col = (ps) => ps.map(t => `<p>${t}</p>`).join('');
    return `
        <div class="page page-after">
            <div class="after-col after-col-left">
                ${head}
                ${col(spread.left)}
            </div>
            <div class="after-col after-col-right${spread.art ? ' after-col-image' : ''}">
                ${art}
                ${col(spread.right)}
            </div>
        </div>`;
}

const PAGES = [
    { kind: 'cover' },
    ...CHAPTERS.flatMap(chapter => chapter.beats.map((beat, i) => ({ kind: 'spread', chapter, beat, isFirst: i === 0 }))),
    { kind: 'reflection', chapter: CHAPTERS[CHAPTERS.length - 1] },
    { kind: 'quiz' },
    ...AFTERWORD.spreads.map((spread, i) => ({ kind: 'after', spread, isFirst: i === 0 })),
    { kind: 'end' }
];

const TWO_PAGE_KINDS = new Set(['spread', 'cover', 'after']);

let folioCounter = 0;
const FOLIOS = PAGES.map(p => {
    const width = TWO_PAGE_KINDS.has(p.kind) ? 2 : 1;
    const start = folioCounter + 1;
    folioCounter += width;
    return { start, width };
});

function renderPage(page) {
    switch (page.kind) {
        case 'cover':
            return coverPage();
        case 'spread':
            return spreadPage(page.chapter, page.beat, page.isFirst);
        case 'reflection':
            return reflectionPage(page.chapter);
        case 'after':
            return afterPage(page.spread, page.isFirst);
        case 'quiz':
            return quizPage();
        case 'end':
            return endPage();
        default:
            return '';
    }
}

let current = 0;
let animating = false;

const spreadEl = document.getElementById('spread');
const prevBtn = document.getElementById('prevBtn');
const nextBtn = document.getElementById('nextBtn');
const indicatorEl = document.getElementById('pageIndicator');
const folioLeftEl = document.getElementById('folioLeft');
const folioRightEl = document.getElementById('folioRight');

function paint() {
    spreadEl.innerHTML = renderPage(PAGES[current]);
    prevBtn.disabled = current === 0;
    nextBtn.disabled = current === PAGES.length - 1;
    indicatorEl.textContent = `${current + 1} / ${PAGES.length}`;

    const folio = FOLIOS[current];
    folioLeftEl.classList.toggle('folio-center', folio.width === 1);
    if (folio.width === 2) {
        folioLeftEl.textContent = folio.start;
        folioRightEl.textContent = folio.start + 1;
        folioLeftEl.hidden = false;
        folioRightEl.hidden = false;
    } else {
        folioLeftEl.textContent = folio.start;
        folioLeftEl.hidden = false;
        folioRightEl.hidden = true;
    }

    spreadEl.querySelectorAll('[data-goto]').forEach(btn => {
        btn.addEventListener('click', () => {
            const chapterNum = Number(btn.dataset.goto);
            const idx = PAGES.findIndex(p => p.kind === 'spread' && p.chapter.num === chapterNum && p.isFirst);
            if (idx >= 0) goTo(idx);
        });
    });
    spreadEl.querySelectorAll('[data-goto-kind]').forEach(btn => {
        btn.addEventListener('click', () => {
            const idx = PAGES.findIndex(p => p.kind === btn.dataset.gotoKind);
            if (idx >= 0) goTo(idx);
        });
    });

    if (PAGES[current].kind === 'quiz') {
        initQuiz();
    }
}

function initQuiz() {
    let answeredCount = 0;
    const progressEl = document.getElementById('quizProgress');

    spreadEl.querySelectorAll('.quiz-item').forEach(item => {
        const qi = Number(item.dataset.qindex);
        const q = QUIZ[qi];
        item.querySelectorAll('.quiz-choice').forEach(btn => {
            btn.addEventListener('click', () => {
                if (item.classList.contains('graded')) return;
                item.classList.add('graded');
                const chosen = Number(btn.dataset.choice);
                item.querySelectorAll('.quiz-choice').forEach(b => {
                    const ci = Number(b.dataset.choice);
                    if (ci === q.answer) b.classList.add('correct');
                    else if (ci === chosen) b.classList.add('incorrect');
                });
                answeredCount++;
                progressEl.textContent = `${answeredCount} / 총 ${QUIZ.length}문항 완료`;
            });
        });
    });
}

function goTo(index) {
    if (animating || index === current || index < 0 || index >= PAGES.length) return;
    animating = true;
    const dir = index > current ? 'flip-next' : 'flip-prev';
    spreadEl.classList.add(dir);
    setTimeout(() => {
        current = index;
        paint();
    }, 230);
    setTimeout(() => {
        spreadEl.classList.remove('flip-next', 'flip-prev');
        animating = false;
    }, 480);
}

prevBtn.addEventListener('click', () => goTo(current - 1));
nextBtn.addEventListener('click', () => goTo(current + 1));

document.getElementById('tocLink').addEventListener('click', () => {
    // 그림책은 차례가 표지에 붙어 있다.
    const idx = PAGES.findIndex(p => p.kind === 'toc');
    goTo(idx >= 0 ? idx : 0);
});

document.addEventListener('keydown', (e) => {
    if (e.key === 'ArrowRight') goTo(current + 1);
    if (e.key === 'ArrowLeft') goTo(current - 1);
});

paint();
