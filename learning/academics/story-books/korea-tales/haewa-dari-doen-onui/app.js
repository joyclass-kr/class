const CHAPTERS = [
    {
        num: 1,
        title: "1장 · 떡 하나 주면 안 잡아먹지",
        beats: [
            {
                art: "01-family.png",
                emoji: "🏠",
                left: [
                    "옛날 깊은 산 밑에 오누이가 어머니와 살았습니다. 아버지는 일찍 여의고 셋이서 오순도순 지냈지요.",
                    "\"어머니, 오늘도 일 나가세요?\""
                ],
                right: [
                    "\"고개 너머 잔칫집에서 부르는구나.\"",
                    "\"문 꼭 잠그고 있어라. 누가 와도 열어 주면 안 된다.\""
                ]
            },
            {
                art: "02-tiger.png",
                emoji: "🐯",
                left: [
                    "어머니는 품삯으로 받은 떡을 광주리에 이고 돌아왔어요.",
                    "첫 번째 고개에 막 올라섰을 때였습니다."
                ],
                right: [
                    "\"어흥!\"",
                    "커다란 호랑이가 길을 떡 막아섰지요.",
                    "\"떡 하나 주면 안 잡아먹지.\""
                ]
            },
            {
                art: "03-pass.png",
                emoji: "🍡",
                left: [
                    "어머니는 얼른 떡 하나를 던져 주고 달아났어요.",
                    "그런데 두 번째 고개에도 그 호랑이가 앉아 있지 뭐예요."
                ],
                right: [
                    "\"떡 하나 주면 안 잡아먹지.\"",
                    "세 번째 고개에서도, 네 번째 고개에서도 똑같았습니다.",
                    "광주리가 점점 가벼워졌어요."
                ]
            },
            {
                art: "04-empty.png",
                emoji: "🌫️",
                left: [
                    "마지막 고개에 이르자 광주리가 텅 비었습니다.",
                    "\"이제 정말 아무것도 없다.\"",
                    "호랑이가 눈을 번뜩였어요."
                ],
                right: [
                    "그날 밤, 어머니는 끝내 집에 돌아오지 못했답니다.",
                    "오누이는 등잔불을 켜 놓고 밤새 문만 바라보았지요."
                ]
            }
        ]
    },
    {
        num: 2,
        title: "2장 · 어머니 목소리",
        beats: [
            {
                art: "05-knock.png",
                emoji: "🚪",
                left: [
                    "그때 문 두드리는 소리가 났어요.",
                    "\"얘들아, 엄마다. 문 열어라.\""
                ],
                right: [
                    "오누이가 벌떡 일어섰습니다.",
                    "그런데 오빠가 누이의 소매를 잡았어요.",
                    "\"…어머니 목소리가 왜 저래?\""
                ]
            },
            {
                art: "06-hand.png",
                emoji: "🐾",
                left: [
                    "\"고개를 넘느라 목이 쉬었단다.\"",
                    "\"그럼 손을 좀 보여 주세요.\""
                ],
                right: [
                    "문틈으로 손 하나가 쑥 들어왔어요. 시커먼 털이 북슬북슬한 손이었지요. 오누이는 소리도 못 내고 서로를 붙들었습니다."
                ]
            },
            {
                art: "07-escape.png",
                emoji: "🏃",
                left: [
                    "\"바, 밥부터 지을게요! 조금만 기다리세요!\"",
                    "오빠가 큰 소리로 말하고는 누이의 손을 잡았어요."
                ],
                right: [
                    "둘은 뒷문으로 살금살금 빠져나갔습니다. 마당 건너 우물가에 커다란 나무가 있었지요. 오누이는 그 나무 위로 기어올랐어요."
                ]
            },
            {
                art: "08-well.png",
                emoji: "💧",
                left: [
                    "한참 뒤 호랑이가 문을 부수고 들어왔어요. 방은 텅 비어 있었지요.",
                    "밖으로 나온 호랑이가 우물 속을 들여다보았습니다."
                ],
                right: [
                    "물에 오누이의 얼굴이 비쳤거든요.",
                    "\"옳지, 저 안에 있구나!\""
                ]
            }
        ]
    },
    {
        num: 3,
        title: "3장 · 하늘에서 내려온 동아줄",
        beats: [
            {
                art: "09-laugh.png",
                emoji: "😬",
                left: [
                    "호랑이가 바가지로 우물물을 푸기 시작했어요.",
                    "그 꼴이 어찌나 우스운지, 누이가 그만 \"풋\" 하고 웃어 버렸습니다."
                ],
                right: [
                    "호랑이가 번쩍 고개를 들었어요. 나무 위에 오누이가 매달려 있었지요.",
                    "\"거기 있었구나! 어떻게 올라갔느냐?\""
                ]
            },
            {
                art: "10-climb.png",
                emoji: "🪓",
                left: [
                    "오빠가 얼른 둘러댔어요.",
                    "\"참기름을 바르고 올라왔지!\"",
                    "호랑이는 정말로 참기름을 발랐다가 주르륵 미끄러졌습니다."
                ],
                right: [
                    "그때 누이가 저도 모르게 소리쳤어요.",
                    "\"바보야, 도끼로 찍고 올라오면 되잖아!\"",
                    "\"…누나!\""
                ]
            },
            {
                art: "11-rope.png",
                emoji: "🪢",
                left: [
                    "호랑이가 도끼를 들고 나무를 쿵쿵 찍으며 올라왔어요.",
                    "더는 올라갈 가지가 없었습니다."
                ],
                right: [
                    "오누이는 두 손을 모으고 하늘에 빌었어요.",
                    "\"저희를 살리시려거든 튼튼한 동아줄을 내려 주세요!\"",
                    "그러자 하늘에서 새 동아줄이 스르르 내려왔지요."
                ]
            },
            {
                art: "12-sky.png",
                emoji: "🌞",
                left: [
                    "오누이가 동아줄을 타고 올라가자 호랑이도 빌었어요.",
                    "\"나도 동아줄을 내려 주시오!\"",
                    "호랑이에게는 낡고 썩은 동아줄이 내려왔습니다."
                ],
                right: [
                    "뚝— 호랑이는 수수밭으로 떨어지고 말았지요. 수숫대가 붉은 것은 그때부터라고 해요. 오빠는 달이 되고 누이는 해가 되었어요.",
                    "누이는 사람들이 쳐다보는 것이 부끄러워, 볼 때마다 눈이 부시게 했답니다."
                ]
            }
        ],
        moral: "오누이를 살린 것은 힘이 아니었어요. 어머니 목소리가 이상하다고 느꼈을 때 그냥 넘기지 않은 것, 손을 보여 달라고 한 번 더 물어본 것이었지요. 이상하다는 마음이 들면 그것부터 확인해 보는 것이 안전한 일이랍니다.",
        question: "무언가 이상하다고 느꼈는데 그냥 넘어간 적이 있나요?"
    }
];

function artFrame(src, emoji) {
    return `
        <div class="art-frame">
            <img src="images/${src}" alt="" onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';">
            <div class="art-fallback" style="display:none">${emoji}</div>
        </div>`;
}

function coverToc() {
    const item = s => `
        <button type="button" data-goto="${s.num}">
            <span class="toc-num">${s.num}</span>
            <span>${s.title.replace(/^\d+장 · /, '')}</span>
        </button>`;
    return `
        <nav class="cover-toc">
            <h2>차례</h2>
            ${CHAPTERS.map(item).join('')}
            <button type="button" data-goto-kind="quiz">
                <span class="toc-num">❓</span>
                <span>이야기 문제</span>
            </button>
        </nav>`;
}

function coverPage() {
    return `
        <div class="page page-cover">
            <div class="story-page-left story-page-left-full">
                ${artFrame('cover.png', '🌙')}
            </div>
            <div class="story-page-right">
                <h1>해와 달이 된 오누이</h1>
                <p>해와 달이 된 오누이는 지은이가 없는 구전 설화예요. 우리나라 곳곳에서 전해 내려왔고, 옛이야기 가운데 가장 널리 알려진 것에 속한답니다.</p>
                <p>이 이야기는 유래담이기도 해요. 해와 달이 어떻게 생겼는지, 수숫대가 왜 붉은지를 이야기 하나로 설명하지요.</p>
                <p>짐승이 어머니나 할머니로 꾸미고 아이들을 노리는 이야기는 이웃 나라에도 있어요. 대만에는 호랑이가 할머니로 꾸미고 찾아오는 이야기가, 일본에는 도깨비에게 쫓긴 아이들이 하늘로 올라가는 이야기가 전한답니다.</p>
                <p>고을마다 조금씩 달라요. 오누이가 아니라 세 남매인 판본도 있고, 누이가 해가 되는 판본과 오빠가 해가 되는 판본이 함께 전해지지요.</p>
                ${coverToc()}
            </div>
        </div>`;
}

function spreadPage(chapter, beat, isFirst) {
    const badgeHtml = isFirst ? `<div class="spread-chapter-badge">${chapter.title}</div>` : '';
    const leftHtml = beat.left.map(p => `<p>${p}</p>`).join('');
    const rightHtml = beat.right.map(p => `<p>${p}</p>`).join('');
    return `
        <div class="page page-story">
            <div class="spread-art">
                ${badgeHtml}
                ${artFrame(beat.art, beat.emoji)}
            </div>
            <div class="spread-text">
                <div class="spread-text-left">${leftHtml}</div>
                <div class="spread-text-right">${rightHtml}</div>
            </div>
        </div>`;
}

function reflectionPage(chapter) {
    return `
        <div class="page page-reflection">
            <h2>이야기를 다 읽고</h2>
            <p class="reflection-moral">${chapter.moral}</p>
            <p class="reflection-question">${chapter.question}</p>
        </div>`;
}

const QUIZ = [
    { q: "어머니는 무엇을 이고 고개를 넘었나요?", choices: ["물동이 하나", "떡 광주리", "나물 바구니"], answer: 1 },
    { q: "호랑이가 고개마다 한 말은 무엇인가요?", choices: ["길을 비켜 주면 봐주지", "노래를 하면 살려 주지", "떡 하나 주면 안 잡아먹지"], answer: 2 },
    { q: "오누이는 무엇을 보고 어머니가 아닌 줄 알았나요?", choices: ["문틈으로 내민 털북숭이 손", "등에 지고 온 커다란 짐", "머리에 쓰고 온 낡은 수건"], answer: 0 },
    { q: "오누이는 어디로 올라가 숨었나요?", choices: ["지붕 위 굴뚝 뒤", "뒷산 바위 틈", "우물가 나무 위"], answer: 2 },
    { q: "호랑이가 오누이를 찾아낸 것은 무엇 때문인가요?", choices: ["누이가 그만 웃어서", "오빠가 소리를 질러서", "나뭇가지가 부러져서"], answer: 0 },
    { q: "하늘에서 오누이에게 내려온 것은 무엇인가요?", choices: ["금으로 만든 사다리", "튼튼한 새 동아줄", "커다란 구름 다리"], answer: 1 },
    { q: "수숫대가 붉은 까닭은 무엇이라고 했나요?", choices: ["해가 오래도록 비추어서", "호랑이가 떨어진 자리라서", "가을에 곱게 물이 들어서"], answer: 1 },
    { q: "누이가 해가 된 뒤에 한 일은 무엇인가요?", choices: ["밤에만 얼굴을 내밀었다", "구름 뒤에 늘 숨었다", "쳐다보면 눈부시게 했다"], answer: 2 }
];

function quizPage() {
    const items = QUIZ.map((item, i) => `
        <div class="quiz-item" data-qindex="${i}">
            <p class="quiz-question">${i + 1}. ${item.q}</p>
            <div class="quiz-choices">
                ${item.choices.map((c, ci) => `<button type="button" class="quiz-choice" data-choice="${ci}">${c}</button>`).join('')}
            </div>
        </div>`).join('');
    return `
        <div class="page page-quiz">
            <h2>이야기 문제</h2>
            <p class="quiz-intro-text" id="quizProgress">0 / 총 ${QUIZ.length}문항 완료</p>
            <div class="quiz-list">${items}</div>
        </div>`;
}

function endPage() {
    return `
        <div class="page page-end">
            ${artFrame('end.png', '🌟')}
            <h2>해와 달이 된 오누이 이야기를 다 읽었어요!</h2>
            <a class="home-btn" href="../../../../../">학습 허브로 돌아가기</a>
        </div>`;
}

const PAGES = [
    { kind: 'cover' },
    ...CHAPTERS.flatMap(chapter => chapter.beats.map((beat, i) => ({ kind: 'spread', chapter, beat, isFirst: i === 0 }))),
    { kind: 'reflection', chapter: CHAPTERS[CHAPTERS.length - 1] },
    { kind: 'quiz' },
    { kind: 'end' }
];

const TWO_PAGE_KINDS = new Set(['spread', 'cover']);

let folioCounter = 0;
const FOLIOS = PAGES.map(p => {
    const width = TWO_PAGE_KINDS.has(p.kind) ? 2 : 1;
    const start = folioCounter + 1;
    folioCounter += width;
    return { start, width };
});

function renderPage(page) {
    switch (page.kind) {
        case 'cover':
            return coverPage();
        case 'spread':
            return spreadPage(page.chapter, page.beat, page.isFirst);
        case 'reflection':
            return reflectionPage(page.chapter);
        case 'quiz':
            return quizPage();
        case 'end':
            return endPage();
        default:
            return '';
    }
}

let current = 0;
let animating = false;

const spreadEl = document.getElementById('spread');
const prevBtn = document.getElementById('prevBtn');
const nextBtn = document.getElementById('nextBtn');
const indicatorEl = document.getElementById('pageIndicator');
const folioLeftEl = document.getElementById('folioLeft');
const folioRightEl = document.getElementById('folioRight');

function paint() {
    spreadEl.innerHTML = renderPage(PAGES[current]);
    prevBtn.disabled = current === 0;
    nextBtn.disabled = current === PAGES.length - 1;
    indicatorEl.textContent = `${current + 1} / ${PAGES.length}`;

    const folio = FOLIOS[current];
    folioLeftEl.classList.toggle('folio-center', folio.width === 1);
    if (folio.width === 2) {
        folioLeftEl.textContent = folio.start;
        folioRightEl.textContent = folio.start + 1;
        folioLeftEl.hidden = false;
        folioRightEl.hidden = false;
    } else {
        folioLeftEl.textContent = folio.start;
        folioLeftEl.hidden = false;
        folioRightEl.hidden = true;
    }

    spreadEl.querySelectorAll('[data-goto]').forEach(btn => {
        btn.addEventListener('click', () => {
            const chapterNum = Number(btn.dataset.goto);
            const idx = PAGES.findIndex(p => p.kind === 'spread' && p.chapter.num === chapterNum && p.isFirst);
            if (idx >= 0) goTo(idx);
        });
    });
    spreadEl.querySelectorAll('[data-goto-kind]').forEach(btn => {
        btn.addEventListener('click', () => {
            const idx = PAGES.findIndex(p => p.kind === btn.dataset.gotoKind);
            if (idx >= 0) goTo(idx);
        });
    });

    if (PAGES[current].kind === 'quiz') {
        initQuiz();
    }
}

function initQuiz() {
    let answeredCount = 0;
    const progressEl = document.getElementById('quizProgress');

    spreadEl.querySelectorAll('.quiz-item').forEach(item => {
        const qi = Number(item.dataset.qindex);
        const q = QUIZ[qi];
        item.querySelectorAll('.quiz-choice').forEach(btn => {
            btn.addEventListener('click', () => {
                if (item.classList.contains('graded')) return;
                item.classList.add('graded');
                const chosen = Number(btn.dataset.choice);
                item.querySelectorAll('.quiz-choice').forEach(b => {
                    const ci = Number(b.dataset.choice);
                    if (ci === q.answer) b.classList.add('correct');
                    else if (ci === chosen) b.classList.add('incorrect');
                });
                answeredCount++;
                progressEl.textContent = `${answeredCount} / 총 ${QUIZ.length}문항 완료`;
            });
        });
    });
}

function goTo(index) {
    if (animating || index === current || index < 0 || index >= PAGES.length) return;
    animating = true;
    const dir = index > current ? 'flip-next' : 'flip-prev';
    spreadEl.classList.add(dir);
    setTimeout(() => {
        current = index;
        paint();
    }, 230);
    setTimeout(() => {
        spreadEl.classList.remove('flip-next', 'flip-prev');
        animating = false;
    }, 480);
}

prevBtn.addEventListener('click', () => goTo(current - 1));
nextBtn.addEventListener('click', () => goTo(current + 1));

document.getElementById('tocLink').addEventListener('click', () => {
    // 그림책은 차례가 표지에 붙어 있다.
    const idx = PAGES.findIndex(p => p.kind === 'toc');
    goTo(idx >= 0 ? idx : 0);
});

document.addEventListener('keydown', (e) => {
    if (e.key === 'ArrowRight') goTo(current + 1);
    if (e.key === 'ArrowLeft') goTo(current - 1);
});

paint();
