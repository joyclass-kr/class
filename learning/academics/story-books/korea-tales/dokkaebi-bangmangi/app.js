const CHAPTERS = [
    {
        num: 1,
        title: "1장 · 개암 네 알",
        beats: [
            {
                art: "01-brothers.png",
                emoji: "🌰",
                left: [
                    "옛날 어느 마을에 형제가 살았습니다.",
                    "형은 욕심이 많았고, 아우는 마음이 곱고 부지런했지요."
                ],
                right: [
                    "어느 날 아우가 산에 나무를 하러 갔어요.",
                    "낙엽을 헤치는데 발밑에 무언가 데구루루 굴렀습니다.",
                    "반들반들한 개암 한 알이었지요."
                ]
            },
            {
                art: "02-pick.png",
                emoji: "🤲",
                left: [
                    "\"오, 이건 아버지 드려야지.\"",
                    "아우는 개암을 주머니에 넣었어요. 조금 가다 또 한 알을 주웠습니다.",
                    "\"이건 어머니 드려야지.\""
                ],
                right: [
                    "또 한 알.",
                    "\"이건 형님 드려야지.\"",
                    "마지막 한 알을 주우며 아우가 웃었어요.",
                    "\"이건 내가 먹어야겠다.\""
                ]
            },
            {
                art: "03-house.png",
                emoji: "🏚️",
                left: [
                    "나무를 하다 보니 어느새 해가 넘어갔어요. 산속이 캄캄해졌습니다. 다행히 골짜기에 낡은 빈 집이 하나 있었지요."
                ],
                right: [
                    "아우는 그 집 마루에 지게를 내려놓고 몸을 뉘었어요.",
                    "\"하룻밤만 신세 지자.\""
                ]
            },
            {
                art: "04-dokkaebi.png",
                emoji: "👹",
                left: [
                    "한밤중이었어요.",
                    "쿵!",
                    "쿵!",
                    "쿵!"
                ],
                right: [
                    "마당이 시끌벅적해졌습니다. 문틈으로 내다보니 도깨비들이 잔뜩 몰려와 있었어요. 저마다 손에 방망이를 하나씩 들고 있었지요.",
                    "아우는 숨을 죽이고 다락으로 기어 올라갔답니다."
                ]
            }
        ]
    },
    {
        num: 2,
        title: "2장 · 금 나와라 뚝딱",
        beats: [
            {
                art: "05-feast.png",
                emoji: "✨",
                left: [
                    "\"금 나와라, 뚝딱!\"",
                    "방망이를 두드리자 금덩이가 우수수 쏟아졌어요.",
                    "\"은 나와라, 뚝딱!\""
                ],
                right: [
                    "\"떡 나와라, 뚝딱!\"",
                    "도깨비들은 밤새 노래를 부르며 잔치를 벌였습니다."
                ]
            },
            {
                art: "06-hungry.png",
                emoji: "😖",
                left: [
                    "다락 위에서 아우는 배가 고파 견딜 수가 없었어요. 떡 냄새가 코끝을 간질였거든요.",
                    "'저 개암이라도 하나 먹어야겠다.'"
                ],
                right: [
                    "아우는 주머니에서 개암 한 알을 꺼내 입에 넣었습니다.",
                    "그러고는 어금니로 꽉 깨물었지요."
                ]
            },
            {
                art: "07-crack.png",
                emoji: "💥",
                left: [
                    "딱!",
                    "낡은 집 안에서 그 소리가 어찌나 크게 울렸는지 몰라요."
                ],
                right: [
                    "\"으악! 집이 무너진다!\"",
                    "\"기둥이 부러졌다! 도망쳐!\"",
                    "도깨비들이 서로 밟고 넘어지며 문밖으로 쏟아져 나갔습니다."
                ]
            },
            {
                art: "08-left.png",
                emoji: "🔨",
                left: [
                    "날이 밝았어요.",
                    "마당에는 아무도 없었습니다."
                ],
                right: [
                    "다만 도깨비 하나가 허둥대다 떨어뜨린 방망이가 툭 놓여 있었지요. 아우는 그것을 지게에 얹고 집으로 내려왔어요.",
                    "\"금 나와라, 뚝딱!\""
                ]
            }
        ]
    },
    {
        num: 3,
        title: "3장 · 따라 한 형",
        beats: [
            {
                art: "09-brother.png",
                emoji: "😏",
                left: [
                    "아우네 집이 하루아침에 기와집이 되자 형이 달려왔어요.",
                    "\"대체 어찌 된 일이냐? 하나도 빠짐없이 말해 보아라.\""
                ],
                right: [
                    "아우는 있었던 일을 그대로 이야기해 주었습니다. 형의 눈이 점점 커졌지요.",
                    "\"골짜기 빈 집이라… 개암이라…\""
                ]
            },
            {
                art: "10-copy.png",
                emoji: "🌰",
                left: [
                    "이튿날 형은 산으로 달려가 개암을 한 자루나 주웠어요.",
                    "그러고는 그 빈 집에 자리를 잡고 앉았습니다."
                ],
                right: [
                    "밤이 깊자 정말로 도깨비들이 몰려왔지요.",
                    "\"금 나와라, 뚝딱!\"",
                    "형은 침을 꿀꺽 삼켰어요."
                ]
            },
            {
                art: "11-caught.png",
                emoji: "😱",
                left: [
                    "형은 개암을 한 움큼 입에 넣고 힘껏 깨물었어요.",
                    "딱!",
                    "딱!",
                    "따다닥!"
                ],
                right: [
                    "그런데 도깨비들은 달아나지 않았습니다.",
                    "\"저놈이다! 지난번에 우리 방망이를 가져간 놈!\"",
                    "도깨비들이 다락으로 우르르 올라왔지요."
                ]
            },
            {
                art: "12-nose.png",
                emoji: "👃",
                left: [
                    "\"코 늘어나라, 뚝딱!\"",
                    "형의 코가 쭈욱 늘어나 방바닥에 끌렸어요. 도깨비들은 깔깔 웃으며 사라졌답니다."
                ],
                right: [
                    "형은 코를 두 손으로 받쳐 들고 울며 아우를 찾아갔어요.",
                    "아우는 잠자코 방망이를 들었지요.",
                    "\"코 줄어라, 뚝딱!\""
                ]
            }
        ],
        moral: "아우는 개암 네 알을 주우며 세 알을 남에게 줄 생각부터 했어요. 형은 자루 가득 주우면서도 제 몫만 생각했지요. 같은 산에서 같은 것을 주웠지만 손에 남은 것은 달랐답니다.",
        question: "좋은 것이 생겼을 때, 나는 누구를 먼저 떠올리나요?"
    }
];

function artFrame(src, emoji) {
    return `
        <div class="art-frame">
            <img src="images/${src}" alt="" onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';">
            <div class="art-fallback" style="display:none">${emoji}</div>
        </div>`;
}

function coverToc() {
    const item = s => `
        <button type="button" data-goto="${s.num}">
            <span class="toc-num">${s.num}</span>
            <span>${s.title.replace(/^\d+장 · /, '')}</span>
        </button>`;
    return `
        <nav class="cover-toc">
            <h2>차례</h2>
            ${CHAPTERS.map(item).join('')}
            <button type="button" data-goto-kind="quiz">
                <span class="toc-num">❓</span>
                <span>이야기 문제</span>
            </button>
        </nav>`;
}

function coverPage() {
    return `
        <div class="page page-cover">
            <div class="story-page-left story-page-left-full">
                ${artFrame('cover.png', '🔨')}
            </div>
            <div class="story-page-right">
                <h1>도깨비 방망이</h1>
                <p>도깨비 방망이는 지은이가 없는 구전 설화예요. 금 나와라 뚝딱, 은 나와라 뚝딱 하면 무엇이든 나오는 방망이가 나오는 이야기랍니다.</p>
                <p>우리 도깨비는 방망이를 늘 지니고 다녀요. 그 방망이를 두드리면 원하는 것이 나온다고 했지요. 그런데 도깨비는 겁이 아주 많아서, 집이 무너지는 소리나 닭 우는 소리에 놀라 물건을 두고 달아나곤 한답니다.</p>
                <p>개암은 개암나무에 열리는 작은 열매예요. 껍질이 아주 단단해서 이로 깨물면 딱 하고 큰 소리가 나지요. 하필 개암이 이야기에 나오는 것도 그 소리 때문이랍니다.</p>
                ${coverToc()}
            </div>
        </div>`;
}

function spreadPage(chapter, beat, isFirst) {
    const badgeHtml = isFirst ? `<div class="spread-chapter-badge">${chapter.title}</div>` : '';
    const leftHtml = beat.left.map(p => `<p>${p}</p>`).join('');
    const rightHtml = beat.right.map(p => `<p>${p}</p>`).join('');
    return `
        <div class="page page-story">
            <div class="spread-art">
                ${badgeHtml}
                ${artFrame(beat.art, beat.emoji)}
            </div>
            <div class="spread-text">
                <div class="spread-text-left">${leftHtml}</div>
                <div class="spread-text-right">${rightHtml}</div>
            </div>
        </div>`;
}

function reflectionPage(chapter) {
    return `
        <div class="page page-reflection">
            <h2>이야기를 다 읽고</h2>
            <p class="reflection-moral">${chapter.moral}</p>
            <p class="reflection-question">${chapter.question}</p>
        </div>`;
}

const QUIZ = [
    { q: "아우가 산에서 주운 것은 무엇인가요?", choices: ["금덩이 하나", "개암 몇 알", "낡은 방망이"], answer: 1 },
    { q: "아우는 첫 번째 개암을 누구에게 주려 했나요?", choices: ["자기 자신", "이웃 사람", "아버지"], answer: 2 },
    { q: "도깨비들이 방망이로 무엇을 했나요?", choices: ["두드려 금은을 냈다", "서로 때리며 놀았다", "땅을 파고 있었다"], answer: 0 },
    { q: "도깨비들은 무슨 소리에 달아났나요?", choices: ["닭이 우는 소리", "천둥 치는 소리", "개암 깨무는 소리"], answer: 2 },
    { q: "욕심쟁이 형은 어떻게 되었나요?", choices: ["코가 길게 늘어났다", "혹이 하나 더 붙었다", "금을 잔뜩 얻었다"], answer: 0 },
    { q: "아우는 형을 어떻게 했나요?", choices: ["모른 척 돌아섰다", "방망이로 고쳐 주었다", "관아에 알렸다"], answer: 1 }
];

function quizPage() {
    const items = QUIZ.map((item, i) => `
        <div class="quiz-item" data-qindex="${i}">
            <p class="quiz-question">${i + 1}. ${item.q}</p>
            <div class="quiz-choices">
                ${item.choices.map((c, ci) => `<button type="button" class="quiz-choice" data-choice="${ci}">${c}</button>`).join('')}
            </div>
        </div>`).join('');
    return `
        <div class="page page-quiz">
            <h2>이야기 문제</h2>
            <p class="quiz-intro-text" id="quizProgress">0 / 총 ${QUIZ.length}문항 완료</p>
            <div class="quiz-list">${items}</div>
        </div>`;
}

function endPage() {
    return `
        <div class="page page-end">
            ${artFrame('end.png', '🌟')}
            <h2>도깨비 방망이 이야기를 다 읽었어요!</h2>
            <a class="home-btn" href="../../../../../">학습 허브로 돌아가기</a>
        </div>`;
}

const PAGES = [
    { kind: 'cover' },
    ...CHAPTERS.flatMap(chapter => chapter.beats.map((beat, i) => ({ kind: 'spread', chapter, beat, isFirst: i === 0 }))),
    { kind: 'reflection', chapter: CHAPTERS[CHAPTERS.length - 1] },
    { kind: 'quiz' },
    { kind: 'end' }
];

const TWO_PAGE_KINDS = new Set(['spread', 'cover']);

let folioCounter = 0;
const FOLIOS = PAGES.map(p => {
    const width = TWO_PAGE_KINDS.has(p.kind) ? 2 : 1;
    const start = folioCounter + 1;
    folioCounter += width;
    return { start, width };
});

function renderPage(page) {
    switch (page.kind) {
        case 'cover':
            return coverPage();
        case 'spread':
            return spreadPage(page.chapter, page.beat, page.isFirst);
        case 'reflection':
            return reflectionPage(page.chapter);
        case 'quiz':
            return quizPage();
        case 'end':
            return endPage();
        default:
            return '';
    }
}

let current = 0;
let animating = false;

const spreadEl = document.getElementById('spread');
const prevBtn = document.getElementById('prevBtn');
const nextBtn = document.getElementById('nextBtn');
const indicatorEl = document.getElementById('pageIndicator');
const folioLeftEl = document.getElementById('folioLeft');
const folioRightEl = document.getElementById('folioRight');

function paint() {
    spreadEl.innerHTML = renderPage(PAGES[current]);
    prevBtn.disabled = current === 0;
    nextBtn.disabled = current === PAGES.length - 1;
    indicatorEl.textContent = `${current + 1} / ${PAGES.length}`;

    const folio = FOLIOS[current];
    folioLeftEl.classList.toggle('folio-center', folio.width === 1);
    if (folio.width === 2) {
        folioLeftEl.textContent = folio.start;
        folioRightEl.textContent = folio.start + 1;
        folioLeftEl.hidden = false;
        folioRightEl.hidden = false;
    } else {
        folioLeftEl.textContent = folio.start;
        folioLeftEl.hidden = false;
        folioRightEl.hidden = true;
    }

    spreadEl.querySelectorAll('[data-goto]').forEach(btn => {
        btn.addEventListener('click', () => {
            const chapterNum = Number(btn.dataset.goto);
            const idx = PAGES.findIndex(p => p.kind === 'spread' && p.chapter.num === chapterNum && p.isFirst);
            if (idx >= 0) goTo(idx);
        });
    });
    spreadEl.querySelectorAll('[data-goto-kind]').forEach(btn => {
        btn.addEventListener('click', () => {
            const idx = PAGES.findIndex(p => p.kind === btn.dataset.gotoKind);
            if (idx >= 0) goTo(idx);
        });
    });

    if (PAGES[current].kind === 'quiz') {
        initQuiz();
    }
}

function initQuiz() {
    let answeredCount = 0;
    const progressEl = document.getElementById('quizProgress');

    spreadEl.querySelectorAll('.quiz-item').forEach(item => {
        const qi = Number(item.dataset.qindex);
        const q = QUIZ[qi];
        item.querySelectorAll('.quiz-choice').forEach(btn => {
            btn.addEventListener('click', () => {
                if (item.classList.contains('graded')) return;
                item.classList.add('graded');
                const chosen = Number(btn.dataset.choice);
                item.querySelectorAll('.quiz-choice').forEach(b => {
                    const ci = Number(b.dataset.choice);
                    if (ci === q.answer) b.classList.add('correct');
                    else if (ci === chosen) b.classList.add('incorrect');
                });
                answeredCount++;
                progressEl.textContent = `${answeredCount} / 총 ${QUIZ.length}문항 완료`;
            });
        });
    });
}

function goTo(index) {
    if (animating || index === current || index < 0 || index >= PAGES.length) return;
    animating = true;
    const dir = index > current ? 'flip-next' : 'flip-prev';
    spreadEl.classList.add(dir);
    setTimeout(() => {
        current = index;
        paint();
    }, 230);
    setTimeout(() => {
        spreadEl.classList.remove('flip-next', 'flip-prev');
        animating = false;
    }, 480);
}

prevBtn.addEventListener('click', () => goTo(current - 1));
nextBtn.addEventListener('click', () => goTo(current + 1));

document.getElementById('tocLink').addEventListener('click', () => {
    // 그림책은 차례가 표지에 붙어 있다.
    const idx = PAGES.findIndex(p => p.kind === 'toc');
    goTo(idx >= 0 ? idx : 0);
});

document.addEventListener('keydown', (e) => {
    if (e.key === 'ArrowRight') goTo(current + 1);
    if (e.key === 'ArrowLeft') goTo(current - 1);
});

paint();
