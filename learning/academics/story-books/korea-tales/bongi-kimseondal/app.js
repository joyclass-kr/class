const CHAPTERS = [
    {
        num: 1,
        title: "1장 · 돈으로 못 살 게 없다니",
        beats: [
            {
                art: "01-seondal.png",
                emoji: "🧑",
                left: [
                    "옛날 평양에 김선달이라는 사람이 살았습니다.",
                    "가진 것은 없었지만 머리가 비상하고 말재간이 좋았지요."
                ],
                right: [
                    "\"저 사람 한마디면 우는 아이도 웃는다니까.\"",
                    "평양 사람이라면 김선달을 모르는 이가 없었답니다."
                ]
            },
            {
                art: "02-merchants.png",
                emoji: "💰",
                left: [
                    "어느 날 한양에서 큰 상인 셋이 평양에 왔어요.",
                    "비단옷을 입고 엽전 꾸러미를 허리에 주렁주렁 달고서요."
                ],
                right: [
                    "\"평양이 크다더니 별것 없구먼.\"",
                    "\"돈만 있으면 못 살 게 무어람.\"",
                    "\"암, 세상에 돈으로 못 사는 게 어디 있나!\""
                ]
            },
            {
                art: "03-water.png",
                emoji: "🪣",
                left: [
                    "마침 대동강가에서는 물장수들이 물을 긷고 있었어요.",
                    "평양 사람들이 마시는 물은 다 이 강에서 났지요."
                ],
                right: [
                    "그 모습을 보던 김선달의 눈이 반짝였습니다.",
                    "\"돈으로 못 살 게 없다고 하셨소?\"",
                    "김선달은 슬쩍 웃고는 자리를 떴어요."
                ]
            },
            {
                art: "04-plan.png",
                emoji: "🪙",
                left: [
                    "그날 저녁, 김선달이 물장수들을 불러 모았어요.",
                    "그러고는 한 사람씩 엽전을 나누어 주었습니다."
                ],
                right: [
                    "\"내일 물을 길으러 오거든 나에게 인사를 하시오.\"",
                    "\"그리고 이 돈을 그대로 내 앞에 내놓으면 됩니다.\"",
                    "물장수들은 영문을 몰랐지만 고개를 끄덕였지요."
                ]
            }
        ]
    },
    {
        num: 2,
        title: "2장 · 대동강을 팔다",
        beats: [
            {
                art: "05-collect.png",
                emoji: "🌅",
                left: [
                    "이튿날 아침, 김선달은 강가 바위에 떡 앉았어요.",
                    "마침 상인 셋이 그 앞을 지나갔습니다."
                ],
                right: [
                    "그때 물장수 하나가 다가와 허리를 굽혔지요.",
                    "\"어르신, 오늘 물값입니다.\"",
                    "짤랑— 엽전이 김선달 앞에 놓였어요."
                ]
            },
            {
                art: "06-line.png",
                emoji: "😲",
                left: [
                    "한 사람이 지나가자 또 한 사람.",
                    "물장수들이 줄줄이 다가와 돈을 내놓았습니다."
                ],
                right: [
                    "상인들의 눈이 점점 커졌어요.",
                    "\"이보시오. 저 사람들이 왜 당신에게 돈을 내는 거요?\"",
                    "김선달은 못 들은 척 엽전만 세었지요."
                ]
            },
            {
                art: "07-claim.png",
                emoji: "🌊",
                left: [
                    "\"설마 저 강이 당신 것이오?\"",
                    "김선달이 그제야 고개를 들었어요."
                ],
                right: [
                    "\"조상 대대로 물려받은 것이지요.\"",
                    "\"물 한 지게에 얼마씩 받고 있소.\"",
                    "\"허, 이런 노른자위 땅이 다 있나!\""
                ]
            },
            {
                art: "08-sell.png",
                emoji: "📜",
                left: [
                    "상인들이 달라붙어 조르기 시작했어요.",
                    "\"우리에게 파시오! 값은 달라는 대로 주겠소!\""
                ],
                right: [
                    "김선달은 한참을 못 이기는 척하다가 마지못해 고개를 끄덕였지요.",
                    "상인들은 엽전 궤짝을 통째로 내밀었습니다.",
                    "\"이제 이 강은 우리 것이오!\""
                ]
            }
        ]
    },
    {
        num: 3,
        title: "3장 · 아무도 못 사는 것",
        beats: [
            {
                art: "09-morning.png",
                emoji: "🪣",
                left: [
                    "이튿날 아침, 상인 셋이 강가 바위에 나란히 앉았어요.",
                    "물장수들이 하나둘 물을 길으러 나왔습니다."
                ],
                right: [
                    "\"자, 물값을 내시오!\"",
                    "물장수들이 서로를 쳐다보았지요.",
                    "\"…무슨 물값이요?\""
                ]
            },
            {
                art: "10-refuse.png",
                emoji: "😠",
                left: [
                    "\"이 강은 이제 우리 것이오!\"",
                    "\"허허, 별 우스운 소리를 다 듣겠네.\""
                ],
                right: [
                    "\"강물이 어떻게 누구 것이 됩니까?\"",
                    "물장수들은 웃으며 물지게를 지고 가 버렸어요.",
                    "상인들의 얼굴이 파랗게 질렸습니다."
                ]
            },
            {
                art: "11-return.png",
                emoji: "🪙",
                left: [
                    "상인들이 씩씩거리며 김선달을 찾아갔어요.",
                    "그런데 김선달은 달아나지 않고 그대로 앉아 있었지요."
                ],
                right: [
                    "김선달은 엽전 궤짝을 그대로 밀어 놓았습니다.",
                    "\"한 푼도 안 썼소. 도로 가져가시오.\"",
                    "상인들이 오히려 어리둥절해졌어요."
                ]
            },
            {
                art: "12-lesson.png",
                emoji: "🌊",
                left: [
                    "\"그럼 대체 왜 이런 짓을 한 거요?\"",
                    "김선달이 강물을 가리켰어요."
                ],
                right: [
                    "\"돈으로 못 살 게 없다고 하셨지요.\"",
                    "\"그런데 저 강물은 아무도 못 삽니다. 나도, 당신들도요.\"",
                    "상인 셋은 아무 말도 못 하고 돌아갔답니다."
                ]
            }
        ],
        moral: "김선달은 상인들의 돈을 노린 것이 아니라 그 말을 노렸어요. 돈으로 못 살 게 없다는 말은, 돈으로 살 수 없는 것을 한 번도 생각해 보지 않아서 나오는 말이랍니다.",
        question: "돈으로는 살 수 없는 것에는 무엇이 있을까요?"
    }
];

function artFrame(src, emoji) {
    return `
        <div class="art-frame">
            <img src="images/${src}" alt="" onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';">
            <div class="art-fallback" style="display:none">${emoji}</div>
        </div>`;
}

function coverPage() {
    return `
        <div class="page page-cover">
            <div class="story-page-left story-page-left-full">
                ${artFrame('cover.png', '💧')}
            </div>
            <div class="story-page-right">
                <h1>봉이 김선달</h1>
                <p>봉이 김선달은 조선 후기에 평양 지방을 무대로 전해 내려온 이야기 속 인물이에요. 실제로 그런 사람이 있었는지는 확실하지 않고, 여러 사람의 이야기가 한 사람 이름으로 모인 것으로 보는 학자가 많답니다.</p>
                <p>김선달 이야기 가운데 가장 널리 알려진 것이 대동강 물을 팔았다는 이야기예요. 지금도 말이 안 되는 장사를 두고 대동강 물 팔아먹는 소리라고 하지요.</p>
                <p>대동강은 평양을 가로지르는 큰 강이에요. 옛날 평양 사람들은 이 강물을 길어다 먹었고, 물을 길어 파는 물장수라는 직업도 따로 있었답니다. 강물이 곧 사람들의 살림이었던 셈이지요.</p>
            </div>
        </div>`;
}

function tocPage() {
    const itemHtml = s => `
        <li>
            <button type="button" data-goto="${s.num}">
                <span class="toc-num">${s.num}</span>
                <span>
                    <strong>${s.title.replace(/^\d+장 · /, '')}</strong>
                </span>
            </button>
        </li>`;
    const quizItemHtml = `
        <li>
            <button type="button" data-goto-kind="quiz">
                <span class="toc-num">❓</span>
                <span>
                    <strong>이야기 문제</strong>
                </span>
            </button>
        </li>`;
    const half = Math.ceil(CHAPTERS.length / 2);
    const leftItems = CHAPTERS.slice(0, half).map(itemHtml).join('');
    const rightItems = CHAPTERS.slice(half).map(itemHtml).join('') + quizItemHtml;
    return `
        <div class="page page-toc">
            <div class="story-page-left">
                <h2>차례</h2>
                <ul class="toc-list">${leftItems}</ul>
            </div>
            <div class="story-page-right">
                <ul class="toc-list">${rightItems}</ul>
            </div>
        </div>`;
}

function spreadPage(chapter, beat, isFirst) {
    const badgeHtml = isFirst ? `<div class="spread-chapter-badge">${chapter.title}</div>` : '';
    const leftHtml = beat.left.map(p => `<p>${p}</p>`).join('');
    const rightHtml = beat.right.map(p => `<p>${p}</p>`).join('');
    return `
        <div class="page page-story">
            <div class="spread-art">
                ${badgeHtml}
                ${artFrame(beat.art, beat.emoji)}
            </div>
            <div class="spread-text">
                <div class="spread-text-left">${leftHtml}</div>
                <div class="spread-text-right">${rightHtml}</div>
            </div>
        </div>`;
}

function reflectionPage(chapter) {
    return `
        <div class="page page-reflection">
            <h2>이야기를 다 읽고</h2>
            <p class="reflection-moral">${chapter.moral}</p>
            <p class="reflection-question">${chapter.question}</p>
        </div>`;
}

const QUIZ = [
    { q: "한양 상인들이 자랑한 것은 무엇인가요?", choices: ["힘이 제일 세다고", "돈으로 못 살 게 없다고", "글을 제일 잘 안다고"], answer: 1 },
    { q: "김선달은 물장수들에게 무엇을 했나요?", choices: ["물을 대신 길어 줬다", "물값을 올려 줬다", "미리 엽전을 나눠 줬다"], answer: 2 },
    { q: "상인들은 무엇을 보고 속았나요?", choices: ["물장수가 돈을 내는 것", "강가에 세워 둔 팻말", "관아에서 받은 문서"], answer: 0 },
    { q: "상인들이 사겠다고 한 것은 무엇인가요?", choices: ["평양의 집", "배 한 척", "대동강"], answer: 2 },
    { q: "김선달은 받은 돈을 어떻게 했나요?", choices: ["그대로 돌려주었다", "몰래 챙겨 달아났다", "물장수에게 나눠 줬다"], answer: 0 },
    { q: "김선달이 마지막에 한 말은 무엇인가요?", choices: ["돈이 세상에서 최고라고", "강물은 아무도 못 산다고", "다시는 오지 말라고"], answer: 1 }
];

function quizPage() {
    const items = QUIZ.map((item, i) => `
        <div class="quiz-item" data-qindex="${i}">
            <p class="quiz-question">${i + 1}. ${item.q}</p>
            <div class="quiz-choices">
                ${item.choices.map((c, ci) => `<button type="button" class="quiz-choice" data-choice="${ci}">${c}</button>`).join('')}
            </div>
        </div>`).join('');
    return `
        <div class="page page-quiz">
            <h2>이야기 문제</h2>
            <p class="quiz-intro-text" id="quizProgress">0 / 총 ${QUIZ.length}문항 완료</p>
            <div class="quiz-list">${items}</div>
        </div>`;
}

function endPage() {
    return `
        <div class="page page-end">
            ${artFrame('end.png', '🌟')}
            <h2>봉이 김선달 이야기를 다 읽었어요!</h2>
            <a class="home-btn" href="../../../../../">학습 허브로 돌아가기</a>
        </div>`;
}

const PAGES = [
    { kind: 'cover' },
    { kind: 'toc' },
    ...CHAPTERS.flatMap(chapter => chapter.beats.map((beat, i) => ({ kind: 'spread', chapter, beat, isFirst: i === 0 }))),
    { kind: 'reflection', chapter: CHAPTERS[CHAPTERS.length - 1] },
    { kind: 'quiz' },
    { kind: 'end' }
];

const TWO_PAGE_KINDS = new Set(['spread', 'toc', 'cover']);

let folioCounter = 0;
const FOLIOS = PAGES.map(p => {
    const width = TWO_PAGE_KINDS.has(p.kind) ? 2 : 1;
    const start = folioCounter + 1;
    folioCounter += width;
    return { start, width };
});

function renderPage(page) {
    switch (page.kind) {
        case 'cover':
            return coverPage();
        case 'toc':
            return tocPage();
        case 'spread':
            return spreadPage(page.chapter, page.beat, page.isFirst);
        case 'reflection':
            return reflectionPage(page.chapter);
        case 'quiz':
            return quizPage();
        case 'end':
            return endPage();
        default:
            return '';
    }
}

let current = 0;
let animating = false;

const spreadEl = document.getElementById('spread');
const prevBtn = document.getElementById('prevBtn');
const nextBtn = document.getElementById('nextBtn');
const indicatorEl = document.getElementById('pageIndicator');
const folioLeftEl = document.getElementById('folioLeft');
const folioRightEl = document.getElementById('folioRight');

function paint() {
    spreadEl.innerHTML = renderPage(PAGES[current]);
    prevBtn.disabled = current === 0;
    nextBtn.disabled = current === PAGES.length - 1;
    indicatorEl.textContent = `${current + 1} / ${PAGES.length}`;

    const folio = FOLIOS[current];
    folioLeftEl.classList.toggle('folio-center', folio.width === 1);
    if (folio.width === 2) {
        folioLeftEl.textContent = folio.start;
        folioRightEl.textContent = folio.start + 1;
        folioLeftEl.hidden = false;
        folioRightEl.hidden = false;
    } else {
        folioLeftEl.textContent = folio.start;
        folioLeftEl.hidden = false;
        folioRightEl.hidden = true;
    }

    spreadEl.querySelectorAll('[data-goto]').forEach(btn => {
        btn.addEventListener('click', () => {
            const chapterNum = Number(btn.dataset.goto);
            const idx = PAGES.findIndex(p => p.kind === 'spread' && p.chapter.num === chapterNum && p.isFirst);
            if (idx >= 0) goTo(idx);
        });
    });
    spreadEl.querySelectorAll('[data-goto-kind]').forEach(btn => {
        btn.addEventListener('click', () => {
            const idx = PAGES.findIndex(p => p.kind === btn.dataset.gotoKind);
            if (idx >= 0) goTo(idx);
        });
    });

    if (PAGES[current].kind === 'quiz') {
        initQuiz();
    }
}

function initQuiz() {
    let answeredCount = 0;
    const progressEl = document.getElementById('quizProgress');

    spreadEl.querySelectorAll('.quiz-item').forEach(item => {
        const qi = Number(item.dataset.qindex);
        const q = QUIZ[qi];
        item.querySelectorAll('.quiz-choice').forEach(btn => {
            btn.addEventListener('click', () => {
                if (item.classList.contains('graded')) return;
                item.classList.add('graded');
                const chosen = Number(btn.dataset.choice);
                item.querySelectorAll('.quiz-choice').forEach(b => {
                    const ci = Number(b.dataset.choice);
                    if (ci === q.answer) b.classList.add('correct');
                    else if (ci === chosen) b.classList.add('incorrect');
                });
                answeredCount++;
                progressEl.textContent = `${answeredCount} / 총 ${QUIZ.length}문항 완료`;
            });
        });
    });
}

function goTo(index) {
    if (animating || index === current || index < 0 || index >= PAGES.length) return;
    animating = true;
    const dir = index > current ? 'flip-next' : 'flip-prev';
    spreadEl.classList.add(dir);
    setTimeout(() => {
        current = index;
        paint();
    }, 230);
    setTimeout(() => {
        spreadEl.classList.remove('flip-next', 'flip-prev');
        animating = false;
    }, 480);
}

prevBtn.addEventListener('click', () => goTo(current - 1));
nextBtn.addEventListener('click', () => goTo(current + 1));

document.getElementById('tocLink').addEventListener('click', () => {
    const idx = PAGES.findIndex(p => p.kind === 'toc');
    if (idx >= 0) goTo(idx);
});

document.addEventListener('keydown', (e) => {
    if (e.key === 'ArrowRight') goTo(current + 1);
    if (e.key === 'ArrowLeft') goTo(current - 1);
});

paint();
