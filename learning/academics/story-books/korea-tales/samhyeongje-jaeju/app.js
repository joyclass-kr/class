const CHAPTERS = [
    {
        num: 1,
        title: "1장 · 각자 배우러 떠나다",
        beats: [
            {
                art: "01-brothers.png",
                emoji: "👨‍👨‍👦",
                left: [
                    "옛날 어느 산골에 삼형제가 살았습니다.",
                    "집이 가난해 물려받을 것이라고는 아무것도 없었지요."
                ],
                right: [
                    "어느 날 아버지가 세 아들을 불러 앉혔어요.",
                    "\"땅도 돈도 없으니, 대신 재주를 하나씩 배워 오너라.\"",
                    "\"삼 년 뒤에 여기서 다시 만나자.\""
                ]
            },
            {
                art: "02-depart.png",
                emoji: "🛤️",
                left: [
                    "이튿날 아침, 삼형제는 갈림길에 섰어요.",
                    "\"형님, 몸조심하십시오.\"",
                    "\"너희도.\""
                ],
                right: [
                    "첫째는 동쪽으로, 둘째는 서쪽으로, 셋째는 남쪽으로 갔습니다. 세 사람의 등이 각각 다른 길로 멀어져 갔지요."
                ]
            },
            {
                art: "03-return.png",
                emoji: "🏡",
                left: [
                    "삼 년이 흘러 삼형제가 집에 모였어요.",
                    "\"무슨 재주를 배웠느냐?\"",
                    "첫째가 먼저 나섰습니다."
                ],
                right: [
                    "\"저는 귀를 배웠습니다. 백 리 밖 소리도 들립니다.\"",
                    "첫째가 땅에 귀를 대자 눈이 스르르 감겼어요."
                ]
            },
            {
                art: "04-skills.png",
                emoji: "🏹",
                left: [
                    "\"저는 활을 배웠습니다.\"",
                    "둘째가 시위를 당기자 백 걸음 밖 나뭇잎이 딱 하고 떨어졌어요."
                ],
                right: [
                    "\"저는… 받는 재주를 배웠습니다.\"",
                    "셋째의 말에 형들이 웃음을 터뜨렸지요.",
                    "\"받는 재주? 그게 무슨 재주냐!\""
                ]
            }
        ]
    },
    {
        num: 2,
        title: "2장 · 백 리 밖의 울음소리",
        beats: [
            {
                art: "05-hear.png",
                emoji: "👂",
                left: [
                    "그때 첫째가 갑자기 손을 들어 올렸어요.",
                    "\"쉿. 조용히 해 보아라.\""
                ],
                right: [
                    "첫째는 눈을 감고 한참을 귀 기울였습니다.",
                    "\"아이 울음소리다.\"",
                    "\"아주 높은 데서 들린다. 백 리 밖 절벽 위야.\""
                ]
            },
            {
                art: "06-village.png",
                emoji: "😰",
                left: [
                    "마침 마을에는 큰일이 나 있었어요.",
                    "커다란 이무기가 원님댁 아이를 채어 갔다는 것이었지요."
                ],
                right: [
                    "\"어디로 갔는지도 모른답니다.\"",
                    "\"찾아 주는 이에게는 무엇이든 주겠다고 하셨대요.\"",
                    "삼형제는 서로를 쳐다보았습니다."
                ]
            },
            {
                art: "07-run.png",
                emoji: "🏃",
                left: [
                    "삼형제는 그길로 달렸어요. 첫째가 앞장서서 방향을 짚었습니다.",
                    "\"조금 더 왼쪽. 이제 곧장.\""
                ],
                right: [
                    "산을 넘고 골짜기를 건너 반나절을 달렸지요.",
                    "마침내 하늘을 찌를 듯한 절벽 아래에 닿았어요."
                ]
            },
            {
                art: "08-cliff.png",
                emoji: "🐉",
                left: [
                    "고개를 젖혀 올려다보니 아득한 절벽 꼭대기였습니다.",
                    "커다란 이무기가 똬리를 틀고 있었고, 그 한가운데 아이가 울고 있었어요."
                ],
                right: [
                    "\"저기 있다!\"",
                    "그러나 사람의 힘으로는 올라갈 수 없는 높이였지요."
                ]
            }
        ]
    },
    {
        num: 3,
        title: "3장 · 셋이라야 되는 일",
        beats: [
            {
                art: "09-aim.png",
                emoji: "🎯",
                left: [
                    "둘째가 활을 들었어요.",
                    "\"내가 저놈을 맞히마.\"",
                    "\"안 됩니다, 형님!\""
                ],
                right: [
                    "첫째가 팔을 붙잡았습니다.",
                    "\"이무기가 놀라 몸을 풀면 아이가 그대로 떨어진다.\"",
                    "\"…그럼 어쩌자는 말이오?\""
                ]
            },
            {
                art: "10-ready.png",
                emoji: "🤲",
                left: [
                    "셋째가 조용히 앞으로 나섰어요.",
                    "\"형님, 쏘십시오.\"",
                    "\"떨어지는 것은 제가 받겠습니다.\""
                ],
                right: [
                    "셋째는 절벽 아래 한가운데에 자리를 잡고 두 팔을 벌렸습니다.",
                    "첫째가 눈을 감고 귀를 기울였어요.",
                    "\"바람이 오른쪽에서 분다. 반 뼘만 왼쪽으로.\""
                ]
            },
            {
                art: "11-catch.png",
                emoji: "💫",
                left: [
                    "피융—",
                    "화살이 아득한 절벽 꼭대기로 날아올랐어요. 이무기가 몸을 크게 뒤척였습니다."
                ],
                right: [
                    "아이가 절벽 아래로 떨어졌어요. 셋째가 두 팔을 벌린 채 서너 걸음 옮겼습니다. 폭—  아이는 셋째의 품에 사뿐히 안겼답니다."
                ]
            },
            {
                art: "12-reward.png",
                emoji: "🏅",
                left: [
                    "원님은 아이를 끌어안고 한참을 울었어요.",
                    "\"누구의 공이 가장 크냐? 상을 내려야겠다.\"",
                    "삼형제는 서로를 쳐다보다가 함께 고개를 저었습니다."
                ],
                right: [
                    "\"셋 중 하나만 빠졌어도 아이를 못 구했을 겁니다.\"",
                    "원님은 셋에게 똑같이 상을 내렸답니다."
                ]
            }
        ],
        moral: "형들은 셋째의 재주를 듣고 웃었어요. 그런데 그날 아이를 받아 낸 것은 그 웃음거리였던 재주였지요. 쓸모없어 보이는 재주는 아직 쓰일 자리를 만나지 못한 것일 뿐이랍니다.",
        question: "남들이 대수롭지 않게 여기는, 내가 잘하는 일은 무엇인가요?"
    }
];

function artFrame(src, emoji) {
    return `
        <div class="art-frame">
            <img src="images/${src}" alt="" onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';">
            <div class="art-fallback" style="display:none">${emoji}</div>
        </div>`;
}

function coverToc() {
    const item = s => `
        <button type="button" data-goto="${s.num}">
            <span class="toc-num">${s.num}</span>
            <span>${s.title.replace(/^\d+장 · /, '')}</span>
        </button>`;
    return `
        <nav class="cover-toc">
            <h2>차례</h2>
            ${CHAPTERS.map(item).join('')}
            <button type="button" data-goto-kind="quiz">
                <span class="toc-num">❓</span>
                <span>이야기 문제</span>
            </button>
        </nav>`;
}

function coverPage() {
    return `
        <div class="page page-cover">
            <div class="story-page-left story-page-left-full">
                ${artFrame('cover.png', '🏹')}
            </div>
            <div class="story-page-right">
                <h1>삼형제의 재주</h1>
                <p>삼형제의 재주는 지은이가 없는 구전 설화예요. 형제가 각자 다른 재주를 익혀 와서, 그 셋을 모두 써야만 풀리는 일을 해내는 이야기랍니다.</p>
                <p>이런 짜임의 이야기는 세계 곳곳에 있어요. 그림 형제가 모은 독일 이야기에도, 아라비아의 옛이야기에도 재주가 다른 형제들이 나오지요. 어느 나라 이야기든 마지막에 하는 말은 같아요. 누구의 재주가 가장 훌륭했는지는 정할 수 없다는 것이지요.</p>
                <p>셋이라는 수가 여기서도 쓰였어요. 재주가 둘이면 겨루기가 되고, 넷이면 복잡해지지요. 셋일 때에만 서로 물고 물리며 어느 하나도 뺄 수 없는 이야기가 된답니다.</p>
                ${coverToc()}
            </div>
        </div>`;
}

function spreadPage(chapter, beat, isFirst) {
    const badgeHtml = isFirst ? `<div class="spread-chapter-badge">${chapter.title}</div>` : '';
    const leftHtml = beat.left.map(p => `<p>${p}</p>`).join('');
    const rightHtml = beat.right.map(p => `<p>${p}</p>`).join('');
    return `
        <div class="page page-story">
            <div class="spread-art">
                ${badgeHtml}
                ${artFrame(beat.art, beat.emoji)}
            </div>
            <div class="spread-text">
                <div class="spread-text-left">${leftHtml}</div>
                <div class="spread-text-right">${rightHtml}</div>
            </div>
        </div>`;
}

function reflectionPage(chapter) {
    return `
        <div class="page page-reflection">
            <h2>이야기를 다 읽고</h2>
            <p class="reflection-moral">${chapter.moral}</p>
            <p class="reflection-question">${chapter.question}</p>
        </div>`;
}

const QUIZ = [
    { q: "첫째가 익혀 온 재주는 무엇인가요?", choices: ["아주 멀리 보는 눈", "아주 멀리 듣는 귀", "아주 빨리 뛰는 발"], answer: 1 },
    { q: "둘째가 익혀 온 재주는 무엇인가요?", choices: ["무엇이든 고치는 손", "무엇이든 아는 머리", "백발백중 활 솜씨"], answer: 2 },
    { q: "셋째가 익혀 온 재주는 무엇인가요?", choices: ["떨어지는 것 받기", "무거운 것 들기", "오래 숨 참기"], answer: 0 },
    { q: "삼형제가 구하러 나선 사람은 누구인가요?", choices: ["길을 잃은 나그네", "물에 빠진 어부", "이무기가 데려간 아이"], answer: 2 },
    { q: "원님은 삼형제에게 상을 어떻게 내렸나요?", choices: ["셋째에게만 주었다", "셋에게 똑같이 주었다", "첫째에게만 주었다"], answer: 1 },
    { q: "원님은 누구의 재주가 으뜸이라고 했나요?", choices: ["첫째가 가장 훌륭하다고", "셋 다 없으면 안 된다고", "둘째가 가장 훌륭하다고"], answer: 1 }
];

function quizPage() {
    const items = QUIZ.map((item, i) => `
        <div class="quiz-item" data-qindex="${i}">
            <p class="quiz-question">${i + 1}. ${item.q}</p>
            <div class="quiz-choices">
                ${item.choices.map((c, ci) => `<button type="button" class="quiz-choice" data-choice="${ci}">${c}</button>`).join('')}
            </div>
        </div>`).join('');
    return `
        <div class="page page-quiz">
            <h2>이야기 문제</h2>
            <p class="quiz-intro-text" id="quizProgress">0 / 총 ${QUIZ.length}문항 완료</p>
            <div class="quiz-list">${items}</div>
        </div>`;
}

function endPage() {
    return `
        <div class="page page-end">
            ${artFrame('end.png', '🌟')}
            <h2>삼형제의 재주 이야기를 다 읽었어요!</h2>
            <a class="home-btn" href="../../../../../">학습 허브로 돌아가기</a>
        </div>`;
}

const PAGES = [
    { kind: 'cover' },
    ...CHAPTERS.flatMap(chapter => chapter.beats.map((beat, i) => ({ kind: 'spread', chapter, beat, isFirst: i === 0 }))),
    { kind: 'reflection', chapter: CHAPTERS[CHAPTERS.length - 1] },
    { kind: 'quiz' },
    { kind: 'end' }
];

const TWO_PAGE_KINDS = new Set(['spread', 'cover']);

let folioCounter = 0;
const FOLIOS = PAGES.map(p => {
    const width = TWO_PAGE_KINDS.has(p.kind) ? 2 : 1;
    const start = folioCounter + 1;
    folioCounter += width;
    return { start, width };
});

function renderPage(page) {
    switch (page.kind) {
        case 'cover':
            return coverPage();
        case 'spread':
            return spreadPage(page.chapter, page.beat, page.isFirst);
        case 'reflection':
            return reflectionPage(page.chapter);
        case 'quiz':
            return quizPage();
        case 'end':
            return endPage();
        default:
            return '';
    }
}

let current = 0;
let animating = false;

const spreadEl = document.getElementById('spread');
const prevBtn = document.getElementById('prevBtn');
const nextBtn = document.getElementById('nextBtn');
const indicatorEl = document.getElementById('pageIndicator');
const folioLeftEl = document.getElementById('folioLeft');
const folioRightEl = document.getElementById('folioRight');

function paint() {
    spreadEl.innerHTML = renderPage(PAGES[current]);
    prevBtn.disabled = current === 0;
    nextBtn.disabled = current === PAGES.length - 1;
    indicatorEl.textContent = `${current + 1} / ${PAGES.length}`;

    const folio = FOLIOS[current];
    folioLeftEl.classList.toggle('folio-center', folio.width === 1);
    if (folio.width === 2) {
        folioLeftEl.textContent = folio.start;
        folioRightEl.textContent = folio.start + 1;
        folioLeftEl.hidden = false;
        folioRightEl.hidden = false;
    } else {
        folioLeftEl.textContent = folio.start;
        folioLeftEl.hidden = false;
        folioRightEl.hidden = true;
    }

    spreadEl.querySelectorAll('[data-goto]').forEach(btn => {
        btn.addEventListener('click', () => {
            const chapterNum = Number(btn.dataset.goto);
            const idx = PAGES.findIndex(p => p.kind === 'spread' && p.chapter.num === chapterNum && p.isFirst);
            if (idx >= 0) goTo(idx);
        });
    });
    spreadEl.querySelectorAll('[data-goto-kind]').forEach(btn => {
        btn.addEventListener('click', () => {
            const idx = PAGES.findIndex(p => p.kind === btn.dataset.gotoKind);
            if (idx >= 0) goTo(idx);
        });
    });

    if (PAGES[current].kind === 'quiz') {
        initQuiz();
    }
}

function initQuiz() {
    let answeredCount = 0;
    const progressEl = document.getElementById('quizProgress');

    spreadEl.querySelectorAll('.quiz-item').forEach(item => {
        const qi = Number(item.dataset.qindex);
        const q = QUIZ[qi];
        item.querySelectorAll('.quiz-choice').forEach(btn => {
            btn.addEventListener('click', () => {
                if (item.classList.contains('graded')) return;
                item.classList.add('graded');
                const chosen = Number(btn.dataset.choice);
                item.querySelectorAll('.quiz-choice').forEach(b => {
                    const ci = Number(b.dataset.choice);
                    if (ci === q.answer) b.classList.add('correct');
                    else if (ci === chosen) b.classList.add('incorrect');
                });
                answeredCount++;
                progressEl.textContent = `${answeredCount} / 총 ${QUIZ.length}문항 완료`;
            });
        });
    });
}

function goTo(index) {
    if (animating || index === current || index < 0 || index >= PAGES.length) return;
    animating = true;
    const dir = index > current ? 'flip-next' : 'flip-prev';
    spreadEl.classList.add(dir);
    setTimeout(() => {
        current = index;
        paint();
    }, 230);
    setTimeout(() => {
        spreadEl.classList.remove('flip-next', 'flip-prev');
        animating = false;
    }, 480);
}

prevBtn.addEventListener('click', () => goTo(current - 1));
nextBtn.addEventListener('click', () => goTo(current + 1));

document.getElementById('tocLink').addEventListener('click', () => {
    // 그림책은 차례가 표지에 붙어 있다.
    const idx = PAGES.findIndex(p => p.kind === 'toc');
    goTo(idx >= 0 ? idx : 0);
});

document.addEventListener('keydown', (e) => {
    if (e.key === 'ArrowRight') goTo(current + 1);
    if (e.key === 'ArrowLeft') goTo(current - 1);
});

paint();
