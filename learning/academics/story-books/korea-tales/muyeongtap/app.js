const CHAPTERS = [
    {
        num: 1,
        title: "1장 · 서라벌로 간 석수",
        beats: [
            {
                art: "01-asadal.png",
                emoji: "🔨",
                left: [
                    "옛날 백제 땅에 아사달이라는 석수가 살았습니다.",
                    "돌 다루는 솜씨가 나라에서 으뜸이었지요."
                ],
                right: [
                    "\"저 사람 손에 걸리면 돌도 숨을 쉰다니까.\"",
                    "아내 아사녀는 그 말을 들을 때마다 어깨가 으쓱했어요."
                ]
            },
            {
                art: "02-farewell.png",
                emoji: "🌾",
                left: [
                    "어느 날 신라에서 사람이 찾아왔습니다.",
                    "\"서라벌에 큰 절을 짓소. 탑을 세워 주시오.\""
                ],
                right: [
                    "아사달은 연장 자루를 챙겨 등에 졌어요.",
                    "\"탑이 다 서면 곧바로 돌아오리다.\"",
                    "아사녀는 마을 어귀까지 따라 나와 손을 흔들었지요."
                ]
            },
            {
                art: "03-bulguksa.png",
                emoji: "⛰️",
                left: [
                    "불국사 공사판은 아침부터 밤까지 시끌시끌했어요.",
                    "돌 깨는 소리, 흙 지는 소리."
                ],
                right: [
                    "아사달은 아무와도 말을 섞지 않고 돌만 쪼았습니다.",
                    "땅땅. 땅땅.",
                    "그 소리만 새벽까지 이어졌지요."
                ]
            },
            {
                art: "04-years.png",
                emoji: "🍂",
                left: [
                    "한 해가 가고 두 해가 갔어요.",
                    "탑은 좀처럼 올라가지 않았습니다."
                ],
                right: [
                    "돌 하나를 다듬는 데만 몇 달이 걸렸거든요.",
                    "\"조금만 더. 조금만 더.\"",
                    "아사달의 머리에도 흰 것이 섞이기 시작했어요."
                ]
            },
            {
                art: "05-asanyeo.png",
                emoji: "👣",
                left: [
                    "기다리다 못한 아사녀가 길을 나섰습니다.",
                    "짚신이 몇 켤레나 닳도록 걷고 또 걸었지요."
                ],
                right: [
                    "마침내 저 멀리 불국사 지붕이 보였어요.",
                    "아사녀는 치마를 걷어붙이고 뛰었습니다.",
                    "\"저 안에 그이가 있구나!\""
                ]
            }
        ]
    },
    {
        num: 2,
        title: "2장 · 그림자 연못",
        beats: [
            {
                art: "06-refused.png",
                emoji: "🚧",
                left: [
                    "그런데 절 문 앞에서 막히고 말았어요.",
                    "\"탑이 끝나기 전에는 아무도 못 들어갑니다.\""
                ],
                right: [
                    "\"백제에서 여기까지 걸어왔습니다!\"",
                    "\"…그래도 안 됩니다.\"",
                    "문지기는 끝내 고개를 저었지요."
                ]
            },
            {
                art: "07-monk.png",
                emoji: "🧎",
                left: [
                    "그때 노스님이 다가와 아래쪽을 가리켰어요.",
                    "산 밑에 못이 하나 있었습니다."
                ],
                right: [
                    "\"저 못을 영지라 하오.\"",
                    "\"탑이 다 서면 저 물에 탑 그림자가 비칠 것이오.\"",
                    "\"그림자가 보이거든 그때 오시오.\""
                ]
            },
            {
                art: "08-wait.png",
                emoji: "💧",
                left: [
                    "그날부터 아사녀는 못가에 앉았어요.",
                    "물만 들여다보면서요."
                ],
                right: [
                    "구름이 지나가면 벌떡 일어섰지요.",
                    "\"저건가?\"",
                    "구름이었어요. 늘 구름이었어요."
                ]
            },
            {
                art: "09-seasons.png",
                emoji: "🌸",
                left: [
                    "못가에 진달래가 피었다 졌어요.",
                    "물잠자리가 날았다 사라졌지요."
                ],
                right: [
                    "갈대가 누렇게 세고, 못이 꽁꽁 얼었다가 다시 풀렸습니다.",
                    "아사녀는 그 자리에 그대로 있었어요. 마을 사람들이 밥을 날라다 주었답니다."
                ]
            },
            {
                art: "10-nothing.png",
                emoji: "🌫️",
                left: [
                    "탑은 차츰차츰 모습을 갖추어 갔어요.",
                    "그런데 이상한 일이었습니다."
                ],
                right: [
                    "탑이 아무리 높아져도 못에는 아무것도 비치지 않았지요.",
                    "산이 비치고, 하늘이 비치고, 새가 비쳤지만",
                    "탑만은 끝내 비치지 않았어요."
                ]
            }
        ]
    },
    {
        num: 3,
        title: "3장 · 그림자 없는 탑",
        beats: [
            {
                art: "11-done.png",
                emoji: "🗿",
                left: [
                    "그리고 어느 밤, 아사달이 마지막 돌을 얹었습니다.",
                    "땅— 하고 마지막 정 소리가 산에 울렸어요."
                ],
                right: [
                    "달빛 아래 탑이 우뚝 섰지요.",
                    "군더더기 하나 없이 반듯한 삼층 석탑이었습니다.",
                    "\"…다 됐다.\""
                ]
            },
            {
                art: "12-run.png",
                emoji: "🏃",
                left: [
                    "아사달은 연장도 팽개치고 산을 뛰어 내려갔어요.",
                    "누가 못가에 앉아 있다는 말을 그제야 들었거든요."
                ],
                right: [
                    "\"아사녀!\"",
                    "돌부리에 채여 넘어지고, 다시 일어나 뛰었지요.",
                    "새벽 물안개가 자욱했습니다."
                ]
            },
            {
                art: "13-meet.png",
                emoji: "🌅",
                left: [
                    "못가 바위에 아사녀가 웅크리고 잠들어 있었어요.",
                    "무릎에는 마르지 않은 진달래 한 줌."
                ],
                right: [
                    "아사달이 그 어깨를 조심조심 흔들었습니다. 아사녀가 눈을 떴어요.",
                    "둘은 한참 동안 아무 말도 못 했지요."
                ]
            },
            {
                art: "14-tower.png",
                emoji: "🪞",
                left: [
                    "\"그림자가 끝내 안 왔어요.\"",
                    "\"…미안하오.\"",
                    "아사녀가 픽 웃었어요."
                ],
                right: [
                    "\"대신 이게 비치네요.\"",
                    "못에는 두 사람의 얼굴이 나란히 비치고 있었습니다.",
                    "그 뒤로 사람들은 그 탑을 무영탑이라 불렀대요."
                ]
            }
        ],
        moral: "아사녀가 못가에서 본 것은 탑이 아니었어요. 몇 해를 기다린 끝에 물에 비친 것은 결국 사람이었지요. 기다림이 향하는 곳은 늘 물건이 아니라 사람이랍니다.",
        question: "내가 오래 기다려 본 것은 무엇인가요?"
    }
];

function artFrame(src, emoji) {
    return `
        <div class="art-frame">
            <img src="images/${src}" alt="" onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';">
            <div class="art-fallback" style="display:none">${emoji}</div>
        </div>`;
}

function coverToc() {
    const item = s => `
        <button type="button" data-goto="${s.num}">
            <span class="toc-num">${s.num}</span>
            <span>${s.title.replace(/^\d+장 · /, '')}</span>
        </button>`;
    return `
        <nav class="cover-toc">
            <h2>차례</h2>
            ${CHAPTERS.map(item).join('')}
            <button type="button" data-goto-kind="quiz">
                <span class="toc-num">❓</span>
                <span>이야기 문제</span>
            </button>
        </nav>`;
}

function coverPage() {
    return `
        <div class="page page-cover">
            <div class="story-page-left story-page-left-full">
                ${artFrame('cover.png', '🗿')}
            </div>
            <div class="story-page-right">
                <h1>무영탑</h1>
                <p>무영탑은 경주 불국사에 있는 석가탑에 얽혀 전해 내려온 이야기예요. 무영탑은 그림자가 없는 탑이라는 뜻이랍니다.</p>
                <p>이야기 속 석수 아사달은 백제 사람으로 나와요. 실제로 신라가 큰 절을 지을 때 백제와 당나라의 기술자를 불러다 썼다는 기록이 남아 있지요.</p>
                <p>불국사 아래에는 영지라는 못이 있어요. 못가에는 얼굴이 뭉개진 오래된 돌부처가 지금도 앉아 있는데, 사람들은 그 돌부처를 이 이야기와 이어서 말한답니다.</p>
                <p>석가탑은 지금도 국보로 남아 있어요. 1966년에 탑을 수리하다가 그 안에서 세상에서 가장 오래된 목판 인쇄물인 무구정광대다라니경이 나와 온 나라가 놀랐지요.</p>
                ${coverToc()}
            </div>
        </div>`;
}

function spreadPage(chapter, beat, isFirst) {
    const badgeHtml = isFirst ? `<div class="spread-chapter-badge">${chapter.title}</div>` : '';
    const leftHtml = beat.left.map(p => `<p>${p}</p>`).join('');
    const rightHtml = beat.right.map(p => `<p>${p}</p>`).join('');
    return `
        <div class="page page-story">
            <div class="spread-art">
                ${badgeHtml}
                ${artFrame(beat.art, beat.emoji)}
            </div>
            <div class="spread-text">
                <div class="spread-text-left">${leftHtml}</div>
                <div class="spread-text-right">${rightHtml}</div>
            </div>
        </div>`;
}

function reflectionPage(chapter) {
    return `
        <div class="page page-reflection">
            <h2>이야기를 다 읽고</h2>
            <p class="reflection-moral">${chapter.moral}</p>
            <p class="reflection-question">${chapter.question}</p>
        </div>`;
}

const QUIZ = [
    { q: "아사달은 무엇을 하는 사람인가요?", choices: ["나무를 깎는 목수", "돌을 다듬는 석수", "그림을 그리는 화가"], answer: 1 },
    { q: "아사달은 어디로 불려 갔나요?", choices: ["한양의 궁궐", "바닷가 마을", "서라벌 불국사"], answer: 2 },
    { q: "아사녀는 왜 절 안에 들어가지 못했나요?", choices: ["탑이 완성되기 전이라서", "오는 길을 잃어버려서", "가진 돈이 하나도 없어서"], answer: 0 },
    { q: "스님이 아사녀에게 알려 준 것은 무엇인가요?", choices: ["절 뒷문으로 들어오라고", "다음 봄에 오라고", "못에 탑 그림자가 비친다고"], answer: 2 },
    { q: "못에 탑 그림자가 비쳤나요?", choices: ["끝내 비치지 않았다", "첫날에 바로 비쳤다", "겨울에만 비쳤다"], answer: 0 },
    { q: "그래서 이 탑을 무엇이라 부르게 되었나요?", choices: ["물에 비친 탑", "그림자 없는 탑", "백제의 탑"], answer: 1 }
];

function quizPage() {
    const items = QUIZ.map((item, i) => `
        <div class="quiz-item" data-qindex="${i}">
            <p class="quiz-question">${i + 1}. ${item.q}</p>
            <div class="quiz-choices">
                ${item.choices.map((c, ci) => `<button type="button" class="quiz-choice" data-choice="${ci}">${c}</button>`).join('')}
            </div>
        </div>`).join('');
    return `
        <div class="page page-quiz">
            <h2>이야기 문제</h2>
            <p class="quiz-intro-text" id="quizProgress">0 / 총 ${QUIZ.length}문항 완료</p>
            <div class="quiz-list">${items}</div>
        </div>`;
}

function endPage() {
    return `
        <div class="page page-end">
            ${artFrame('end.png', '🌟')}
            <h2>무영탑 이야기를 다 읽었어요!</h2>
            <a class="home-btn" href="../../../../../">학습 허브로 돌아가기</a>
        </div>`;
}

const PAGES = [
    { kind: 'cover' },
    ...CHAPTERS.flatMap(chapter => chapter.beats.map((beat, i) => ({ kind: 'spread', chapter, beat, isFirst: i === 0 }))),
    { kind: 'reflection', chapter: CHAPTERS[CHAPTERS.length - 1] },
    { kind: 'quiz' },
    { kind: 'end' }
];

const TWO_PAGE_KINDS = new Set(['spread', 'cover']);

let folioCounter = 0;
const FOLIOS = PAGES.map(p => {
    const width = TWO_PAGE_KINDS.has(p.kind) ? 2 : 1;
    const start = folioCounter + 1;
    folioCounter += width;
    return { start, width };
});

function renderPage(page) {
    switch (page.kind) {
        case 'cover':
            return coverPage();
        case 'spread':
            return spreadPage(page.chapter, page.beat, page.isFirst);
        case 'reflection':
            return reflectionPage(page.chapter);
        case 'quiz':
            return quizPage();
        case 'end':
            return endPage();
        default:
            return '';
    }
}

let current = 0;
let animating = false;

const spreadEl = document.getElementById('spread');
const prevBtn = document.getElementById('prevBtn');
const nextBtn = document.getElementById('nextBtn');
const indicatorEl = document.getElementById('pageIndicator');
const folioLeftEl = document.getElementById('folioLeft');
const folioRightEl = document.getElementById('folioRight');

function paint() {
    spreadEl.innerHTML = renderPage(PAGES[current]);
    prevBtn.disabled = current === 0;
    nextBtn.disabled = current === PAGES.length - 1;
    indicatorEl.textContent = `${current + 1} / ${PAGES.length}`;

    const folio = FOLIOS[current];
    folioLeftEl.classList.toggle('folio-center', folio.width === 1);
    if (folio.width === 2) {
        folioLeftEl.textContent = folio.start;
        folioRightEl.textContent = folio.start + 1;
        folioLeftEl.hidden = false;
        folioRightEl.hidden = false;
    } else {
        folioLeftEl.textContent = folio.start;
        folioLeftEl.hidden = false;
        folioRightEl.hidden = true;
    }

    spreadEl.querySelectorAll('[data-goto]').forEach(btn => {
        btn.addEventListener('click', () => {
            const chapterNum = Number(btn.dataset.goto);
            const idx = PAGES.findIndex(p => p.kind === 'spread' && p.chapter.num === chapterNum && p.isFirst);
            if (idx >= 0) goTo(idx);
        });
    });
    spreadEl.querySelectorAll('[data-goto-kind]').forEach(btn => {
        btn.addEventListener('click', () => {
            const idx = PAGES.findIndex(p => p.kind === btn.dataset.gotoKind);
            if (idx >= 0) goTo(idx);
        });
    });

    if (PAGES[current].kind === 'quiz') {
        initQuiz();
    }
}

function initQuiz() {
    let answeredCount = 0;
    const progressEl = document.getElementById('quizProgress');

    spreadEl.querySelectorAll('.quiz-item').forEach(item => {
        const qi = Number(item.dataset.qindex);
        const q = QUIZ[qi];
        item.querySelectorAll('.quiz-choice').forEach(btn => {
            btn.addEventListener('click', () => {
                if (item.classList.contains('graded')) return;
                item.classList.add('graded');
                const chosen = Number(btn.dataset.choice);
                item.querySelectorAll('.quiz-choice').forEach(b => {
                    const ci = Number(b.dataset.choice);
                    if (ci === q.answer) b.classList.add('correct');
                    else if (ci === chosen) b.classList.add('incorrect');
                });
                answeredCount++;
                progressEl.textContent = `${answeredCount} / 총 ${QUIZ.length}문항 완료`;
            });
        });
    });
}

function goTo(index) {
    if (animating || index === current || index < 0 || index >= PAGES.length) return;
    animating = true;
    const dir = index > current ? 'flip-next' : 'flip-prev';
    spreadEl.classList.add(dir);
    setTimeout(() => {
        current = index;
        paint();
    }, 230);
    setTimeout(() => {
        spreadEl.classList.remove('flip-next', 'flip-prev');
        animating = false;
    }, 480);
}

prevBtn.addEventListener('click', () => goTo(current - 1));
nextBtn.addEventListener('click', () => goTo(current + 1));

document.getElementById('tocLink').addEventListener('click', () => {
    // 그림책은 차례가 표지에 붙어 있다.
    const idx = PAGES.findIndex(p => p.kind === 'toc');
    goTo(idx >= 0 ? idx : 0);
});

document.addEventListener('keydown', (e) => {
    if (e.key === 'ArrowRight') goTo(current + 1);
    if (e.key === 'ArrowLeft') goTo(current - 1);
});

paint();
