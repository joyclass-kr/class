const CHAPTERS = [
    {
        num: 1,
        title: "1장 · 한겨울의 심부름",
        beats: [
            {
                art: "01-yeoni.png",
                emoji: "❄️",
                left: [
                    "옛날 어느 마을에 연이라는 아이가 살았습니다. 어머니를 일찍 여의고 새어머니와 함께 살았지요. 새어머니는 연이에게만 일을 시켰어요."
                ],
                right: [
                    "물 긷기, 빨래, 불 때기.",
                    "손이 얼어 터져도 쉬는 날이 없었답니다."
                ]
            },
            {
                art: "02-order.png",
                emoji: "🥬",
                left: [
                    "눈이 무릎까지 쌓인 섣달이었어요. 새어머니가 소쿠리를 툭 던졌습니다.",
                    "\"산에 가서 봄나물을 뜯어 오너라.\""
                ],
                right: [
                    "\"…한겨울에 나물이 어디 있어요?\"",
                    "\"못 뜯으면 들어올 생각도 마라.\""
                ]
            },
            {
                art: "03-snow.png",
                emoji: "🏔️",
                left: [
                    "연이는 소쿠리를 안고 눈 덮인 산을 올랐어요. 아무리 헤쳐도 나오는 건 마른 풀뿌리뿐이었지요.",
                    "손끝이 새빨개지고 눈물이 볼에서 얼어붙었습니다."
                ],
                right: [
                    "'이대로 얼어 죽는구나.'",
                    "연이는 큰 버드나무 아래 털썩 주저앉았어요."
                ]
            },
            {
                art: "04-door.png",
                emoji: "🚪",
                left: [
                    "그때였어요.",
                    "드르륵—",
                    "버드나무 뒤 바위가 옆으로 밀려났습니다."
                ],
                right: [
                    "그 틈으로 따뜻한 바람이 훅 끼쳐 나왔어요.",
                    "\"춥지? 어서 들어와.\"",
                    "문 안에서 웬 남자아이가 손을 내밀었지요."
                ]
            },
            {
                art: "05-garden.png",
                emoji: "🌸",
                left: [
                    "연이는 눈을 몇 번이나 비볐어요. 바위 안은 온통 봄이었습니다. 냇물이 졸졸 흐르고 나비가 날아다녔지요."
                ],
                right: [
                    "발밑에는 파릇파릇한 나물이 지천이었어요.",
                    "\"나는 버들 도령이라고 해.\""
                ]
            }
        ]
    },
    {
        num: 2,
        title: "2장 · 봄이 있는 문",
        beats: [
            {
                art: "06-doryeong.png",
                emoji: "🧺",
                left: [
                    "버들 도령은 연이의 소쿠리를 나물로 가득 채워 주었어요.",
                    "\"가져가. 얼마든지 있으니까.\""
                ],
                right: [
                    "\"고마워. 그런데 여긴 대체 어디야?\"",
                    "버들 도령은 씩 웃기만 했지요.",
                    "\"추우면 또 와.\""
                ]
            },
            {
                art: "07-again.png",
                emoji: "🌿",
                left: [
                    "그날부터 연이는 몰래몰래 그 문을 찾아갔어요. 둘은 냇물에 발을 담그고 이야기를 나누었습니다. 연이가 웃는 법을 다시 배운 것도 그 안에서였지요."
                ],
                right: [
                    "\"밖은 아직도 겨울이야?\"",
                    "\"응. 여기만 봄이야.\""
                ]
            },
            {
                art: "08-suspect.png",
                emoji: "🤨",
                left: [
                    "한겨울에 파란 나물이 매일같이 들어오니 새어머니가 이상했어요.",
                    "\"이걸 대체 어디서 뜯어 오느냐?\""
                ],
                right: [
                    "연이는 고개를 저었습니다.",
                    "\"…그냥 산에서요.\"",
                    "새어머니의 눈이 가늘어졌지요."
                ]
            },
            {
                art: "09-follow.png",
                emoji: "👣",
                left: [
                    "이튿날 새어머니는 연이의 뒤를 살금살금 밟았어요.",
                    "눈 위에 난 발자국을 따라서요."
                ],
                right: [
                    "버드나무 아래에서 바위가 스르르 열리는 것을 보고 말았지요.",
                    "그 안에 봄이 가득한 것도요.",
                    "'저걸 다 내 것으로 만들어야겠다.'"
                ]
            }
        ]
    },
    {
        num: 3,
        title: "3장 · 세 송이 꽃",
        beats: [
            {
                art: "10-strike.png",
                emoji: "😨",
                left: [
                    "다음 날, 새어머니가 먼저 그 문 앞에 섰어요.",
                    "\"문 열어라!\"",
                    "문이 열리자 새어머니는 나물이며 꽃이며 마구 쓸어 담았습니다."
                ],
                right: [
                    "\"그러시면 안 돼요!\"",
                    "말리던 버들 도령이 그만 풀밭에 쓰러지고 말았어요."
                ]
            },
            {
                art: "11-flee.png",
                emoji: "💨",
                left: [
                    "쿠르릉—",
                    "바위 문이 무서운 소리를 내며 닫히기 시작했어요. 새어머니는 보따리를 팽개치고 허둥지둥 빠져나갔습니다."
                ],
                right: [
                    "\"으아악!\"",
                    "그러고는 다시는 그 산에 오지 않았지요."
                ]
            },
            {
                art: "12-flowers.png",
                emoji: "🌺",
                left: [
                    "뒤늦게 달려온 연이가 버들 도령을 흔들었어요.",
                    "\"일어나! 눈 좀 떠 봐!\""
                ],
                right: [
                    "그때 냇가에 못 보던 꽃 세 송이가 피어 있었습니다.",
                    "새하얀 꽃, 발그레한 꽃, 새빨간 꽃.",
                    "연이는 세 송이를 조심조심 꺾었어요."
                ]
            },
            {
                art: "13-revive.png",
                emoji: "✨",
                left: [
                    "하얀 꽃을 얹자 몸이 반듯해졌어요. 발그레한 꽃을 얹자 볼에 살이 돌았지요.",
                    "빨간 꽃을 얹자—"
                ],
                right: [
                    "\"에취!\"",
                    "버들 도령이 벌떡 일어나 앉았습니다."
                ]
            },
            {
                art: "14-spring.png",
                emoji: "🌷",
                left: [
                    "\"나 얼마나 잤어?\"",
                    "\"몰라! 이 바보야!\"",
                    "연이가 울면서 웃었어요."
                ],
                right: [
                    "그해 겨울, 바위 문이 활짝 열린 채 닫히지 않았답니다.",
                    "봄이 그 문으로 걸어 나와 온 산에 퍼졌지요. 지금도 봄은 거기서 나온대요."
                ]
            }
        ],
        moral: "새어머니는 봄을 통째로 가지려다 하나도 얻지 못했어요. 연이는 소쿠리 하나만 받아 왔는데 봄이 온 산으로 퍼져 나갔지요. 나눠 받은 것과 빼앗은 것은 그렇게 다르답니다.",
        question: "누군가 나에게 아무 대가 없이 내준 것이 있나요?"
    }
];

function artFrame(src, emoji) {
    return `
        <div class="art-frame">
            <img src="images/${src}" alt="" onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';">
            <div class="art-fallback" style="display:none">${emoji}</div>
        </div>`;
}

function coverToc() {
    const item = s => `
        <button type="button" data-goto="${s.num}">
            <span class="toc-num">${s.num}</span>
            <span>${s.title.replace(/^\d+장 · /, '')}</span>
        </button>`;
    return `
        <nav class="cover-toc">
            <h2>차례</h2>
            ${CHAPTERS.map(item).join('')}
            <button type="button" data-goto-kind="quiz">
                <span class="toc-num">❓</span>
                <span>이야기 문제</span>
            </button>
        </nav>`;
}

function coverPage() {
    return `
        <div class="page page-cover">
            <div class="story-page-left story-page-left-full">
                ${artFrame('cover.png', '🌱')}
            </div>
            <div class="story-page-right">
                <h1>연이와 버들 도령</h1>
                <p>연이와 버들 도령은 지은이가 없는 구전 설화예요. 한겨울에 봄나물을 뜯어 오라는 심부름에서 이야기가 시작된답니다.</p>
                <p>이 이야기에는 뼈살이꽃, 살살이꽃, 피살이꽃이라는 세 송이 꽃이 나와요. 쓰러진 사람을 되살리는 꽃이지요. 같은 세 꽃이 해와 달이 된 오누이 이야기 같은 다른 옛이야기에도 똑같이 나온답니다.</p>
                <p>바위 문을 열면 그 안에 봄이 들어 있다는 생각은 우리 옛이야기에 자주 나와요. 겨울이 아무리 길어도 어딘가에 봄이 갇혀 있을 뿐이라고 여긴 것이지요.</p>
                ${coverToc()}
            </div>
        </div>`;
}

function spreadPage(chapter, beat, isFirst) {
    const badgeHtml = isFirst ? `<div class="spread-chapter-badge">${chapter.title}</div>` : '';
    const leftHtml = beat.left.map(p => `<p>${p}</p>`).join('');
    const rightHtml = beat.right.map(p => `<p>${p}</p>`).join('');
    return `
        <div class="page page-story">
            <div class="spread-art">
                ${badgeHtml}
                ${artFrame(beat.art, beat.emoji)}
            </div>
            <div class="spread-text">
                <div class="spread-text-left">${leftHtml}</div>
                <div class="spread-text-right">${rightHtml}</div>
            </div>
        </div>`;
}

function reflectionPage(chapter) {
    return `
        <div class="page page-reflection">
            <h2>이야기를 다 읽고</h2>
            <p class="reflection-moral">${chapter.moral}</p>
            <p class="reflection-question">${chapter.question}</p>
        </div>`;
}

const QUIZ = [
    { q: "계모가 한겨울에 시킨 심부름은 무엇인가요?", choices: ["물을 길어 오라고", "봄나물을 뜯어 오라고", "나무를 해 오라고"], answer: 1 },
    { q: "연이가 산에서 만난 것은 무엇인가요?", choices: ["말하는 호랑이", "얼어붙은 강", "열리는 바위 문"], answer: 2 },
    { q: "바위 문 안은 어떤 곳이었나요?", choices: ["봄이 한창인 곳", "캄캄한 굴속", "금은이 쌓인 곳"], answer: 0 },
    { q: "계모는 연이를 어떻게 했나요?", choices: ["혼자 산에 갔다", "심부름을 그만두게 했다", "몰래 뒤를 따라갔다"], answer: 2 },
    { q: "연이가 찾아낸 것은 무엇인가요?", choices: ["세 송이 꽃", "약초 한 뿌리", "맑은 샘물"], answer: 0 },
    { q: "그 뒤로 봄은 어디에서 나온다고 했나요?", choices: ["남쪽 바다에서", "그 바위 문에서", "하늘 위에서"], answer: 1 }
];

function quizPage() {
    const items = QUIZ.map((item, i) => `
        <div class="quiz-item" data-qindex="${i}">
            <p class="quiz-question">${i + 1}. ${item.q}</p>
            <div class="quiz-choices">
                ${item.choices.map((c, ci) => `<button type="button" class="quiz-choice" data-choice="${ci}">${c}</button>`).join('')}
            </div>
        </div>`).join('');
    return `
        <div class="page page-quiz">
            <h2>이야기 문제</h2>
            <p class="quiz-intro-text" id="quizProgress">0 / 총 ${QUIZ.length}문항 완료</p>
            <div class="quiz-list">${items}</div>
        </div>`;
}

function endPage() {
    return `
        <div class="page page-end">
            ${artFrame('end.png', '🌟')}
            <h2>연이와 버들 도령 이야기를 다 읽었어요!</h2>
            <a class="home-btn" href="../../../../../">학습 허브로 돌아가기</a>
        </div>`;
}

const PAGES = [
    { kind: 'cover' },
    ...CHAPTERS.flatMap(chapter => chapter.beats.map((beat, i) => ({ kind: 'spread', chapter, beat, isFirst: i === 0 }))),
    { kind: 'reflection', chapter: CHAPTERS[CHAPTERS.length - 1] },
    { kind: 'quiz' },
    { kind: 'end' }
];

const TWO_PAGE_KINDS = new Set(['spread', 'cover']);

let folioCounter = 0;
const FOLIOS = PAGES.map(p => {
    const width = TWO_PAGE_KINDS.has(p.kind) ? 2 : 1;
    const start = folioCounter + 1;
    folioCounter += width;
    return { start, width };
});

function renderPage(page) {
    switch (page.kind) {
        case 'cover':
            return coverPage();
        case 'spread':
            return spreadPage(page.chapter, page.beat, page.isFirst);
        case 'reflection':
            return reflectionPage(page.chapter);
        case 'quiz':
            return quizPage();
        case 'end':
            return endPage();
        default:
            return '';
    }
}

let current = 0;
let animating = false;

const spreadEl = document.getElementById('spread');
const prevBtn = document.getElementById('prevBtn');
const nextBtn = document.getElementById('nextBtn');
const indicatorEl = document.getElementById('pageIndicator');
const folioLeftEl = document.getElementById('folioLeft');
const folioRightEl = document.getElementById('folioRight');

function paint() {
    spreadEl.innerHTML = renderPage(PAGES[current]);
    prevBtn.disabled = current === 0;
    nextBtn.disabled = current === PAGES.length - 1;
    indicatorEl.textContent = `${current + 1} / ${PAGES.length}`;

    const folio = FOLIOS[current];
    folioLeftEl.classList.toggle('folio-center', folio.width === 1);
    if (folio.width === 2) {
        folioLeftEl.textContent = folio.start;
        folioRightEl.textContent = folio.start + 1;
        folioLeftEl.hidden = false;
        folioRightEl.hidden = false;
    } else {
        folioLeftEl.textContent = folio.start;
        folioLeftEl.hidden = false;
        folioRightEl.hidden = true;
    }

    spreadEl.querySelectorAll('[data-goto]').forEach(btn => {
        btn.addEventListener('click', () => {
            const chapterNum = Number(btn.dataset.goto);
            const idx = PAGES.findIndex(p => p.kind === 'spread' && p.chapter.num === chapterNum && p.isFirst);
            if (idx >= 0) goTo(idx);
        });
    });
    spreadEl.querySelectorAll('[data-goto-kind]').forEach(btn => {
        btn.addEventListener('click', () => {
            const idx = PAGES.findIndex(p => p.kind === btn.dataset.gotoKind);
            if (idx >= 0) goTo(idx);
        });
    });

    if (PAGES[current].kind === 'quiz') {
        initQuiz();
    }
}

function initQuiz() {
    let answeredCount = 0;
    const progressEl = document.getElementById('quizProgress');

    spreadEl.querySelectorAll('.quiz-item').forEach(item => {
        const qi = Number(item.dataset.qindex);
        const q = QUIZ[qi];
        item.querySelectorAll('.quiz-choice').forEach(btn => {
            btn.addEventListener('click', () => {
                if (item.classList.contains('graded')) return;
                item.classList.add('graded');
                const chosen = Number(btn.dataset.choice);
                item.querySelectorAll('.quiz-choice').forEach(b => {
                    const ci = Number(b.dataset.choice);
                    if (ci === q.answer) b.classList.add('correct');
                    else if (ci === chosen) b.classList.add('incorrect');
                });
                answeredCount++;
                progressEl.textContent = `${answeredCount} / 총 ${QUIZ.length}문항 완료`;
            });
        });
    });
}

function goTo(index) {
    if (animating || index === current || index < 0 || index >= PAGES.length) return;
    animating = true;
    const dir = index > current ? 'flip-next' : 'flip-prev';
    spreadEl.classList.add(dir);
    setTimeout(() => {
        current = index;
        paint();
    }, 230);
    setTimeout(() => {
        spreadEl.classList.remove('flip-next', 'flip-prev');
        animating = false;
    }, 480);
}

prevBtn.addEventListener('click', () => goTo(current - 1));
nextBtn.addEventListener('click', () => goTo(current + 1));

document.getElementById('tocLink').addEventListener('click', () => {
    // 그림책은 차례가 표지에 붙어 있다.
    const idx = PAGES.findIndex(p => p.kind === 'toc');
    goTo(idx >= 0 ? idx : 0);
});

document.addEventListener('keydown', (e) => {
    if (e.key === 'ArrowRight') goTo(current + 1);
    if (e.key === 'ArrowLeft') goTo(current - 1);
});

paint();
