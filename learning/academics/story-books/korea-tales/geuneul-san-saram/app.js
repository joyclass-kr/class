const CHAPTERS = [
    {
        num: 1,
        title: "1장 · 그늘을 사겠소",
        beats: [
            {
                art: "01-tree.png",
                emoji: "🌳",
                left: [
                    "어느 마을 어귀에 아름드리 느티나무 한 그루가 서 있었습니다.",
                    "어른 다섯이 팔을 벌려야 겨우 안기는 나무였지요.",
                    "가지가 넓게 퍼져 한여름에도 그 아래는 서늘했지요.",
                    "여름이면 온 마을 사람이 그 아래에 모였어요."
                ],
                right: [
                    "그런데 그 나무는 마을에서 제일 부자인 영감의 집 담 옆에 있었어요. 부자는 늘 그 나무를 제 것이라 여겼답니다.",
                    "나무는 마을 것인지 부자 것인지 아무도 몰랐어요.",
                    "누가 쉬고 있으면 눈을 흘겼지요."
                ]
            },
            {
                art: "02-rest.png",
                emoji: "😌",
                left: [
                    "몹시 무더운 날이었어요.",
                    "숨이 턱턱 막히는 날이었어요.",
                    "먼 길을 걷던 나그네가 그 그늘에 짐을 내려놓았습니다.",
                    "멀리서부터 그 느티나무가 보였지요."
                ],
                right: [
                    "\"어이구, 살 것 같구나.\"",
                    "나그네가 두 팔을 쭉 뻗었어요.",
                    "나그네는 부채를 부치며 다리를 쭉 폈어요. 매미 소리가 요란했지요.",
                    "금세 눈이 스르르 감겼답니다."
                ]
            },
            {
                art: "03-scold.png",
                emoji: "😠",
                left: [
                    "그때 대문이 벌컥 열렸어요. 부자가 성큼성큼 걸어 나왔습니다.",
                    "부자가 마당에서 그 모습을 본 것이었어요.",
                    "\"여기가 어디라고 함부로 눕는 게냐!\"",
                    "얼굴이 벌겋게 달아올라 있었지요."
                ],
                right: [
                    "\"이 나무는 내 것이고, 이 그늘도 내 것이다!\"",
                    "부자가 지팡이로 땅을 쿵쿵 찍었어요.",
                    "\"당장 나가지 못할까!\"",
                    "나그네는 눈을 뜨고 부자를 올려다보았지요."
                ]
            },
            {
                art: "04-buy.png",
                emoji: "💰",
                left: [
                    "나그네는 천천히 몸을 일으켰어요. 그러고는 허리춤에서 엽전 꾸러미를 꺼냈습니다.",
                    "화를 내기는커녕 얼굴빛 하나 달라지지 않았지요.",
                    "\"그렇다면 이 그늘을 제게 파시지요.\"",
                    "엽전이 짤랑짤랑 소리를 냈지요."
                ],
                right: [
                    "\"…뭐라고?\"",
                    "부자가 눈을 껌뻑였어요.",
                    "\"값은 넉넉히 드리겠습니다.\"",
                    "나그네가 엽전을 내밀었답니다."
                ]
            }
        ]
    },
    {
        num: 2,
        title: "2장 · 그늘이 옮겨 가다",
        beats: [
            {
                art: "05-deal.png",
                emoji: "🤝",
                left: [
                    "부자는 속으로 쾌재를 불렀어요.",
                    "세상에 그런 횡재가 없었거든요.",
                    "'허, 그늘을 돈 주고 사는 바보가 다 있구나.'",
                    "\"좋다. 오늘부터 저 그늘은 네 것이다.\"",
                    "부자가 얼른 손을 내밀었어요."
                ],
                right: [
                    "부자는 엽전을 세어 품에 넣고 흐뭇하게 돌아섰지요.",
                    "부자는 콧노래까지 흥얼거렸어요.",
                    "나그네는 빙그레 웃기만 했답니다.",
                    "아무 말도 하지 않았지요."
                ]
            },
            {
                art: "06-move.png",
                emoji: "🌇",
                left: [
                    "해가 서쪽으로 기울기 시작했어요.",
                    "나무 그림자가 길게 늘어졌어요.",
                    "그러자 나무 그늘이 조금씩 자리를 옮겼습니다.",
                    "처음에는 아무도 몰랐지요.",
                    "길에서 담 쪽으로,"
                ],
                right: [
                    "담에서 담을 넘어, 마침내 부잣집 마당 한복판으로 들어갔지요.",
                    "그늘이 담을 넘어 들어갔어요.",
                    "부잣집 마당이 온통 시원해졌지요."
                ]
            },
            {
                art: "07-enter.png",
                emoji: "🚶",
                left: [
                    "나그네는 짐을 챙겨 들고 그늘을 따라 걸었어요. 대문을 지나 마당 한가운데에 멍석을 폈습니다.",
                    "나그네가 천천히 일어섰어요.",
                    "\"아니, 이게 무슨 짓이오!\"",
                    "대문이 활짝 열려 있었지요."
                ],
                right: [
                    "\"제 그늘이 여기로 왔으니 저도 따라온 것뿐입니다.\"",
                    "부자가 버선발로 뛰어나왔어요.",
                    "부자는 말문이 막히고 말았지요.",
                    "나그네는 태연하게 멍석에 누웠답니다."
                ]
            },
            {
                art: "08-friends.png",
                emoji: "👥",
                left: [
                    "이튿날, 나그네는 길에서 만난 사람들을 데려왔어요.",
                    "나그네는 이른 아침부터 길목에 서 있었어요.",
                    "\"제 그늘입니다. 편히 쉬시지요.\"",
                    "열 사람도 넘었지요."
                ],
                right: [
                    "사람들이 마당에 둘러앉아 부채질을 하며 이야기꽃을 피웠습니다.",
                    "부자네 마당이 잔칫집처럼 시끌시끌했어요.",
                    "부자는 방 안에서 이를 갈았지만 어쩔 도리가 없었어요.",
                    "제가 판 그늘이었으니까요."
                ]
            }
        ]
    },
    {
        num: 3,
        title: "3장 · 누구의 것도 아닌 것",
        beats: [
            {
                art: "09-porch.png",
                emoji: "🏠",
                left: [
                    "날이 갈수록 그늘은 더 깊이 들어왔어요.",
                    "여름이 깊어질수록 해가 낮아졌거든요.",
                    "어느 날에는 대청마루까지 그늘이 닿았습니다. 사람들이 마루에 걸터앉아 낮잠을 자고 국수를 말아 먹었지요.",
                    "마루가 서늘해졌지요."
                ],
                right: [
                    "부자는 제 집 마루 한구석에서 눈치를 보게 되었답니다.",
                    "앉을 자리가 없어 툇마루 끝에 걸터앉았어요.",
                    "제 집인데도 손님 같았답니다."
                ]
            },
            {
                art: "10-beg.png",
                emoji: "🙏",
                left: [
                    "마침내 부자가 나그네를 찾아왔어요.",
                    "며칠을 벼르다 겨우 입을 뗐어요.",
                    "\"이보시오. 돈을 곱절로 돌려줄 테니 무릅시다.\"",
                    "목소리에 힘이 하나도 없었지요."
                ],
                right: [
                    "\"이 집이 내 집인지 저잣거리인지 알 수가 없구려.\"",
                    "부자가 한숨을 푹 쉬었어요.",
                    "부자의 어깨가 축 처져 있었습니다. 나그네는 한참 동안 말이 없었어요.",
                    "나그네는 나무만 올려다보았지요."
                ]
            },
            {
                art: "11-return.png",
                emoji: "🍃",
                left: [
                    "\"돈은 도로 가져가십시오.\"",
                    "나그네가 엽전 꾸러미를 도로 내밀었어요.",
                    "\"…무르겠다는 말이오?\"",
                    "\"아닙니다.\"",
                    "부자가 눈을 크게 떴지요."
                ],
                right: [
                    "\"이 그늘은 이제 제 것이니, 제 뜻대로 하겠습니다.\"",
                    "나그네가 마당에 모인 사람들을 둘러보았어요.",
                    "\"오늘부터 이 그늘은 마을 사람 모두의 것입니다.\"",
                    "부자의 얼굴이 붉어졌지요.",
                    "\"그늘은 본디 그런 것이니까요.\""
                ]
            },
            {
                art: "12-village.png",
                emoji: "🎋",
                left: [
                    "나그네는 그날로 길을 떠났어요.",
                    "뒤도 돌아보지 않고 고개를 넘어갔어요.",
                    "부자는 한참 동안 나무를 올려다보고 서 있었습니다.",
                    "해가 질 때까지 그러고 있었지요."
                ],
                right: [
                    "이듬해 여름, 느티나무 아래에는 멍석이 깔려 있었어요. 부자도 그 한쪽에 앉아 부채를 부치고 있었지요.",
                    "아이들이 그 위에서 뒹굴었어요.",
                    "이제 아무도 그늘을 제 것이라 하지 않았답니다.",
                    "느티나무는 해마다 더 넓게 그늘을 드리웠지요."
                ]
            }
        ],
        moral: "부자는 그늘을 팔아 돈을 벌었다고 생각했어요. 그런데 판 것은 그늘이 아니라 제 집 마당이었지요. 값을 매길 수 없는 것에 값을 붙이면, 잃는 쪽은 대개 값을 붙인 사람이랍니다.",
        question: "돈으로 사고팔 수 없는 것에는 무엇이 있을까요?"
    }
];

function artFrame(src, emoji) {
    return `
        <div class="art-frame">
            <img src="images/${src}" alt="" onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';">
            <div class="art-fallback" style="display:none">${emoji}</div>
        </div>`;
}

function coverToc() {
    const item = s => `
        <button type="button" data-goto="${s.num}">
            <span class="toc-num">${s.num}</span>
            <span>${s.title.replace(/^\d+장 · /, '')}</span>
        </button>`;
    return `
        <nav class="cover-toc">
            <h2>차례</h2>
            ${CHAPTERS.map(item).join('')}
            <button type="button" data-goto-kind="quiz">
                <span class="toc-num">❓</span>
                <span>이야기 문제</span>
            </button>
        </nav>`;
}

function coverPage() {
    return `
        <div class="page page-cover">
            <div class="story-page-left story-page-left-full">
                ${artFrame('cover.png', '🌳')}
            </div>
            <div class="story-page-right">
                <h1>그늘을 산 사람</h1>
                <p>그늘을 산 사람은 지은이가 없는 구전 설화예요. 그늘처럼 아무도 값을 매기지 않던 것을 사고팔면 어떻게 되는지를 다룬 이야기랍니다.</p>
                <p>마을 어귀의 큰 느티나무는 옛날 우리 마을마다 있던 것이에요. 정자나무라고 불렀지요. 여름이면 그 아래에 멍석을 깔고 사람들이 모여 쉬고, 이야기하고, 낮잠을 잤어요. 나무 한 그루가 마을의 사랑방 노릇을 한 셈이랍니다.</p>
                <p>그늘은 원래 누구의 것도 아니에요. 그런 것에 값을 붙이면 어떻게 되는지를 다룬 이야기는 세계 곳곳에 있답니다. 우리 이야기는 그 가운데서도 유난히 짧고 매섭지요.</p>
                ${coverToc()}
            </div>
        </div>`;
}

function spreadPage(chapter, beat, isFirst) {
    const badgeHtml = isFirst ? `<div class="spread-chapter-badge">${chapter.title}</div>` : '';
    const leftHtml = beat.left.map(p => `<p>${p}</p>`).join('');
    const rightHtml = beat.right.map(p => `<p>${p}</p>`).join('');
    return `
        <div class="page page-story">
            <div class="spread-art">
                ${badgeHtml}
                ${artFrame(beat.art, beat.emoji)}
            </div>
            <div class="spread-text">
                <div class="spread-text-left">${leftHtml}</div>
                <div class="spread-text-right">${rightHtml}</div>
            </div>
        </div>`;
}


const QUIZ = [
    { q: "나그네가 쉬던 곳은 어디인가요?", choices: ["주막의 마루", "느티나무 그늘", "냇가의 바위"], answer: 1 },
    { q: "부자는 나그네에게 무엇이라고 했나요?", choices: ["물값을 내고 가라고", "짐을 두고 가라고", "내 그늘이니 나가라고"], answer: 2 },
    { q: "나그네는 어떻게 했나요?", choices: ["그늘을 돈 주고 샀다", "말없이 자리를 떴다", "관아에 알렸다"], answer: 0 },
    { q: "해가 기울자 그늘은 어디로 갔나요?", choices: ["길 건너 논밭으로", "나무 뒤 숲으로", "부잣집 마당 안으로"], answer: 2 },
    { q: "부자가 견디다 못해 한 말은 무엇인가요?", choices: ["돈을 돌려줄 테니 무르자", "그늘을 더 사고 싶다", "나무를 베어 버리겠다"], answer: 0 },
    { q: "나그네는 그늘을 어떻게 했나요?", choices: ["혼자 계속 차지했다", "마을 모두에게 돌려줬다", "값을 올려 되팔았다"], answer: 1 }
];

function quizPage() {
    const items = QUIZ.map((item, i) => `
        <div class="quiz-item" data-qindex="${i}">
            <p class="quiz-question">${i + 1}. ${item.q}</p>
            <div class="quiz-choices">
                ${item.choices.map((c, ci) => `<button type="button" class="quiz-choice" data-choice="${ci}">${c}</button>`).join('')}
            </div>
        </div>`).join('');
    return `
        <div class="page page-quiz">
            <h2>이야기 문제</h2>
            <p class="quiz-intro-text" id="quizProgress">0 / 총 ${QUIZ.length}문항 완료</p>
            <div class="quiz-list">${items}</div>
        </div>`;
}


/* 읽고 나서 — 세계명작 트랙과 같은 형식이다. 동화틀은 쪽을 재서 나누지 않으므로
   펼침면마다 왼쪽·오른쪽 글을 손으로 나누어 둔다. */
const AFTERWORD = {
    title: '읽고 나서',
    emoji: '🌳',
    spreads: [
        {
            art: 'end.png',
            left: [
                "다만 방향이 반대입니다. 김선달은 팔 수 없는 것을 팔아 넘겼고, 여기서는 부자가 팔 수 없는 것을 스스로 팔았습니다.",
                "다시 보면 부자가 판 것은 그늘이 아니었습니다. 그늘은 해를 따라 하루 종일 움직입니다. 아침에는 마당에 있다가 저녁에는 안방까지 들어옵니다.",
                "그러니 그늘을 판다는 것은 집을 조금씩 파는 일이었습니다. 부자는 그 셈을 하지 않고 그날 손에 쥔 돈만 보았습니다.",
                "나그네는 억지를 부린 적이 없습니다. 산 것을 그대로 썼을 뿐입니다. 잘못은 팔지 못할 것을 판 쪽에 있었습니다."
            ],
            right: [
                "부자는 그늘을 팔아 돈을 벌었다고 여겼습니다. 그런데 판 것은 그늘이 아니라 제 집 마당이었습니다. 값을 매길 수 없는 것에 값을 붙이면 잃는 쪽은 대개 값을 붙인 사람입니다.",
                "돈으로 사고팔 수 없는 것에 무엇이 있을까요?"
            ]
        }
    ]
};

function afterPage(spread, isFirst) {
    const head = isFirst ? `<h2>${AFTERWORD.title}</h2>` : '';
    // 그림은 오른쪽 칸 맨 위 모서리에 꽉 붙인다. 학습 허브로 가는 길은 그 칸 맨 아래에 둔다.
    const art = spread.art ? `<div class="after-art">${artFrame(spread.art, AFTERWORD.emoji)}</div>` : '';
    const foot = spread.last ? `<p class="after-home"><a class="home-btn" href="../../../../../">학습 허브로 돌아가기</a></p>` : '';
    const col = (ps) => ps.map(t => `<p>${t}</p>`).join('');
    return `
        <div class="page page-after">
            <div class="after-col after-col-left">
                ${head}
                ${col(spread.left)}
            </div>
            <div class="after-col after-col-right${spread.art ? ' after-col-image' : ''}">
                ${art}
                ${col(spread.right)}
                ${foot}
            </div>
        </div>`;
}

const PAGES = [
    { kind: 'cover' },
    ...CHAPTERS.flatMap(chapter => chapter.beats.map((beat, i) => ({ kind: 'spread', chapter, beat, isFirst: i === 0 }))),
    { kind: 'quiz' },
    ...AFTERWORD.spreads.map((spread, i) => ({ kind: 'after', spread, isFirst: i === 0, last: i === AFTERWORD.spreads.length - 1 })),
];

const TWO_PAGE_KINDS = new Set(['spread', 'cover', 'after']);

let folioCounter = 0;
const FOLIOS = PAGES.map(p => {
    const width = TWO_PAGE_KINDS.has(p.kind) ? 2 : 1;
    const start = folioCounter + 1;
    folioCounter += width;
    return { start, width };
});

function renderPage(page) {
    switch (page.kind) {
        case 'cover':
            return coverPage();
        case 'spread':
            return spreadPage(page.chapter, page.beat, page.isFirst);
        case 'after':
            return afterPage({ ...page.spread, last: page.last }, page.isFirst);
        case 'quiz':
            return quizPage();
        default:
            return '';
    }
}

let current = 0;
let animating = false;

const spreadEl = document.getElementById('spread');
const prevBtn = document.getElementById('prevBtn');
const nextBtn = document.getElementById('nextBtn');
const indicatorEl = document.getElementById('pageIndicator');
const folioLeftEl = document.getElementById('folioLeft');
const folioRightEl = document.getElementById('folioRight');

function paint() {
    spreadEl.innerHTML = renderPage(PAGES[current]);
    prevBtn.disabled = current === 0;
    nextBtn.disabled = current === PAGES.length - 1;
    indicatorEl.textContent = `${current + 1} / ${PAGES.length}`;

    const folio = FOLIOS[current];
    folioLeftEl.classList.toggle('folio-center', folio.width === 1);
    if (folio.width === 2) {
        folioLeftEl.textContent = folio.start;
        folioRightEl.textContent = folio.start + 1;
        folioLeftEl.hidden = false;
        folioRightEl.hidden = false;
    } else {
        folioLeftEl.textContent = folio.start;
        folioLeftEl.hidden = false;
        folioRightEl.hidden = true;
    }

    spreadEl.querySelectorAll('[data-goto]').forEach(btn => {
        btn.addEventListener('click', () => {
            const chapterNum = Number(btn.dataset.goto);
            const idx = PAGES.findIndex(p => p.kind === 'spread' && p.chapter.num === chapterNum && p.isFirst);
            if (idx >= 0) goTo(idx);
        });
    });
    spreadEl.querySelectorAll('[data-goto-kind]').forEach(btn => {
        btn.addEventListener('click', () => {
            const idx = PAGES.findIndex(p => p.kind === btn.dataset.gotoKind);
            if (idx >= 0) goTo(idx);
        });
    });

    if (PAGES[current].kind === 'quiz') {
        initQuiz();
    }
}

function initQuiz() {
    let answeredCount = 0;
    const progressEl = document.getElementById('quizProgress');

    spreadEl.querySelectorAll('.quiz-item').forEach(item => {
        const qi = Number(item.dataset.qindex);
        const q = QUIZ[qi];
        item.querySelectorAll('.quiz-choice').forEach(btn => {
            btn.addEventListener('click', () => {
                if (item.classList.contains('graded')) return;
                item.classList.add('graded');
                const chosen = Number(btn.dataset.choice);
                item.querySelectorAll('.quiz-choice').forEach(b => {
                    const ci = Number(b.dataset.choice);
                    if (ci === q.answer) b.classList.add('correct');
                    else if (ci === chosen) b.classList.add('incorrect');
                });
                answeredCount++;
                progressEl.textContent = `${answeredCount} / 총 ${QUIZ.length}문항 완료`;
            });
        });
    });
}

function goTo(index) {
    if (animating || index === current || index < 0 || index >= PAGES.length) return;
    animating = true;
    const dir = index > current ? 'flip-next' : 'flip-prev';
    spreadEl.classList.add(dir);
    setTimeout(() => {
        current = index;
        paint();
    }, 230);
    setTimeout(() => {
        spreadEl.classList.remove('flip-next', 'flip-prev');
        animating = false;
    }, 480);
}

prevBtn.addEventListener('click', () => goTo(current - 1));
nextBtn.addEventListener('click', () => goTo(current + 1));

document.getElementById('tocLink').addEventListener('click', () => {
    // 그림책은 차례가 표지에 붙어 있다.
    const idx = PAGES.findIndex(p => p.kind === 'toc');
    goTo(idx >= 0 ? idx : 0);
});

document.addEventListener('keydown', (e) => {
    if (e.key === 'ArrowRight') goTo(current + 1);
    if (e.key === 'ArrowLeft') goTo(current - 1);
});

paint();
