const CHAPTERS = [
    {
        num: 1,
        title: "1장 · 하늘에서 내려온 환웅",
        beats: [
            {
                art: "01-heaven.png",
                emoji: "☁️",
                left: [
                    "아주 먼 옛날, 하늘나라에 환인이라는 임금이 있었습니다.",
                    "그에게는 환웅이라는 아들이 있었지요."
                ],
                right: [
                    "환웅은 날마다 구름 사이로 아래를 내려다보았어요.",
                    "\"아버지, 저는 저 아래로 내려가고 싶습니다.\"",
                    "\"사람들 사는 곳이 그리 좋으냐?\""
                ]
            },
            {
                art: "02-descend.png",
                emoji: "⛰️",
                left: [
                    "환인은 아래 세상을 오래 살펴보았습니다. 그러고는 태백산을 짚었어요.",
                    "\"저곳이라면 사람을 이롭게 할 만하구나.\""
                ],
                right: [
                    "환웅은 무리 삼천을 이끌고 산꼭대기로 내려왔지요.",
                    "바람과 비와 구름을 맡은 셋도 함께였어요."
                ]
            },
            {
                art: "03-sinsi.png",
                emoji: "🌳",
                left: [
                    "산꼭대기에는 커다란 나무 한 그루가 서 있었습니다.",
                    "사람들이 신단수라 부르던 나무였지요."
                ],
                right: [
                    "환웅은 그 아래에 터를 열었어요.",
                    "\"이곳을 신시라 하겠다.\"",
                    "그날부터 산 아래 사람들이 모여들기 시작했답니다."
                ]
            },
            {
                art: "04-rule.png",
                emoji: "🌾",
                left: [
                    "환웅은 사람들에게 곡식 기르는 법을 가르쳤어요. 병을 고치는 법도, 옳고 그름을 가리는 법도요."
                ],
                right: [
                    "\"널리 사람을 이롭게 하라.\"",
                    "그것이 환웅이 늘 하던 말이었지요. 신시에는 웃음소리가 끊이지 않았습니다."
                ]
            },
            {
                art: "05-two.png",
                emoji: "🐻",
                left: [
                    "어느 날 곰 한 마리와 호랑이 한 마리가 찾아왔어요. 둘은 신단수 아래에 나란히 엎드렸습니다.",
                    "\"사람이 되고 싶습니다.\""
                ],
                right: [
                    "\"저희도 저들처럼 살고 싶습니다.\"",
                    "환웅은 한참 동안 둘을 내려다보았어요."
                ]
            }
        ]
    },
    {
        num: 2,
        title: "2장 · 동굴 속의 백일",
        beats: [
            {
                art: "06-give.png",
                emoji: "🌿",
                left: [
                    "환웅이 쑥 한 다발과 마늘 스무 개를 내주었어요.",
                    "\"이것만 먹고 백 일을 견디어라.\"",
                    "\"그동안 햇빛을 보아서는 안 된다.\""
                ],
                right: [
                    "\"그것뿐입니까?\"",
                    "호랑이가 코웃음을 쳤지요.",
                    "\"그쯤이야!\""
                ]
            },
            {
                art: "07-cave.png",
                emoji: "🕳️",
                left: [
                    "둘은 깊은 동굴로 들어갔습니다. 빛이라고는 입구에 한 줄기뿐이었어요.",
                    "쑥은 쓰고 마늘은 매웠지요."
                ],
                right: [
                    "\"아이고, 써라.\"",
                    "\"…참자.\""
                ]
            },
            {
                art: "08-tiger.png",
                emoji: "🐯",
                left: [
                    "사흘이 지나자 호랑이가 안절부절못했어요. 이레가 지나자 벌떡 일어섰습니다."
                ],
                right: [
                    "\"못 하겠다! 나는 이렇게는 못 산다!\"",
                    "호랑이는 동굴 밖으로 뛰쳐나갔어요. 곰은 그 뒷모습을 물끄러미 바라보았지요."
                ]
            },
            {
                art: "09-bear.png",
                emoji: "🌑",
                left: [
                    "곰은 혼자 남았어요. 쑥을 씹고, 마늘을 씹고, 또 씹었습니다. 발톱이 무디어지고 털이 푸석해졌지요."
                ],
                right: [
                    "'조금만 더. 조금만 더.'",
                    "동굴 밖에서 계절이 바뀌는 소리가 들려왔어요."
                ]
            },
            {
                art: "10-woman.png",
                emoji: "✨",
                left: [
                    "삼칠일, 그러니까 스무하루가 되던 날 아침이었어요.",
                    "동굴 안이 눈이 부시도록 환해졌습니다."
                ],
                right: [
                    "곰의 몸에서 검은 털이 눈처럼 흩날렸지요. 빛이 걷히자 그 자리에 한 여인이 서 있었어요. 여인은 제 두 손을 한참이나 들여다보았습니다.",
                    "사람들은 그를 웅녀라 불렀답니다."
                ]
            }
        ]
    },
    {
        num: 3,
        title: "3장 · 아사달의 아침",
        beats: [
            {
                art: "11-pray.png",
                emoji: "🙏",
                left: [
                    "웅녀는 사람이 되었지만 곁에 아무도 없었어요. 날마다 신단수 아래에 앉아 빌었습니다."
                ],
                right: [
                    "\"아이를 하나만 갖게 해 주십시오.\"",
                    "그 소리가 어찌나 간절했던지 나뭇잎이 다 떨어지도록 이어졌지요."
                ]
            },
            {
                art: "12-birth.png",
                emoji: "👶",
                left: [
                    "환웅이 그 앞에 내려섰어요. 둘은 부부가 되었습니다.",
                    "이듬해, 신단수 아래에서 아이 울음소리가 터졌지요."
                ],
                right: [
                    "\"응애— 응애—\"",
                    "그 아이의 이름이 단군왕검이었어요."
                ]
            },
            {
                art: "13-asadal.png",
                emoji: "🌄",
                left: [
                    "자라난 단군은 새 터를 찾아 길을 나섰습니다. 강이 넓고 들이 기름진 곳이었지요."
                ],
                right: [
                    "\"이곳을 아사달이라 하겠다.\"",
                    "아사달은 아침 땅이라는 뜻이었어요. 사람들이 그 말을 따라 옮겨 왔답니다."
                ]
            },
            {
                art: "14-joseon.png",
                emoji: "🏞️",
                left: [
                    "단군은 성을 쌓고 나라를 열었어요.",
                    "\"나라 이름은 조선이다.\"",
                    "조선 역시 아침이라는 뜻이었지요."
                ],
                right: [
                    "곰이 깊은 어둠 속에서 견뎌 얻은 것이 결국 아침이었던 셈이에요. 그것이 우리 땅에 선 첫 번째 나라였답니다."
                ]
            }
        ],
        moral: "호랑이는 첫날 가장 자신 있어 했고, 곰은 아무 말 없이 앉아 있었어요. 끝까지 남는 사람은 큰소리치던 쪽이 아니라 조용히 견디던 쪽인 경우가 많답니다.",
        question: "당장은 재미없지만 오래 해 볼 만한 일이 나에게도 있을까요?"
    }
];

function artFrame(src, emoji) {
    return `
        <div class="art-frame">
            <img src="images/${src}" alt="" onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';">
            <div class="art-fallback" style="display:none">${emoji}</div>
        </div>`;
}

function coverToc() {
    const item = s => `
        <button type="button" data-goto="${s.num}">
            <span class="toc-num">${s.num}</span>
            <span>${s.title.replace(/^\d+장 · /, '')}</span>
        </button>`;
    return `
        <nav class="cover-toc">
            <h2>차례</h2>
            ${CHAPTERS.map(item).join('')}
            <button type="button" data-goto-kind="quiz">
                <span class="toc-num">❓</span>
                <span>이야기 문제</span>
            </button>
        </nav>`;
}

function coverPage() {
    return `
        <div class="page page-cover">
            <div class="story-page-left story-page-left-full">
                ${artFrame('cover.png', '🐻')}
            </div>
            <div class="story-page-right">
                <h1>단군</h1>
                <p>단군 이야기는 고려 때 승려 일연이 쓴 삼국유사에 실려 전해져요. 비슷한 시기에 이승휴가 쓴 제왕운기에도 함께 실려 있답니다.</p>
                <p>이야기 속에서 곰과 호랑이가 사람이 되기를 바라는 대목은 오래된 북방 이야기들과 닮았어요. 곰을 조상으로 모시는 이야기는 시베리아와 만주 여러 곳에 남아 있지요.</p>
                <p>환웅이 데리고 내려온 풍백, 우사, 운사는 바람과 비와 구름을 맡은 이들이에요. 농사에 가장 중요한 세 가지를 이름으로 삼은 것이랍니다.</p>
                ${coverToc()}
            </div>
        </div>`;
}

function spreadPage(chapter, beat, isFirst) {
    const badgeHtml = isFirst ? `<div class="spread-chapter-badge">${chapter.title}</div>` : '';
    const leftHtml = beat.left.map(p => `<p>${p}</p>`).join('');
    const rightHtml = beat.right.map(p => `<p>${p}</p>`).join('');
    return `
        <div class="page page-story">
            <div class="spread-art">
                ${badgeHtml}
                ${artFrame(beat.art, beat.emoji)}
            </div>
            <div class="spread-text">
                <div class="spread-text-left">${leftHtml}</div>
                <div class="spread-text-right">${rightHtml}</div>
            </div>
        </div>`;
}

function reflectionPage(chapter) {
    return `
        <div class="page page-reflection">
            <h2>이야기를 다 읽고</h2>
            <p class="reflection-moral">${chapter.moral}</p>
            <p class="reflection-question">${chapter.question}</p>
        </div>`;
}

const QUIZ = [
    { q: "환웅이 하늘에서 데리고 내려온 이들은 누구인가요?", choices: ["곰과 호랑이와 사슴을 맡은 이", "바람과 비와 구름을 맡은 이", "해와 달과 별을 맡은 이"], answer: 1 },
    { q: "환웅이 내려와 연 곳의 이름은 무엇인가요?", choices: ["아사달", "평양", "신시"], answer: 2 },
    { q: "곰과 호랑이가 받은 것은 무엇인가요?", choices: ["쑥과 마늘", "쌀과 보리", "약과 물"], answer: 0 },
    { q: "호랑이는 어떻게 되었나요?", choices: ["곰보다 먼저 사람이 되었다", "동굴에서 잠들었다", "못 참고 뛰쳐나갔다"], answer: 2 },
    { q: "사람이 된 곰을 무엇이라 불렀나요?", choices: ["웅녀", "환인", "왕검"], answer: 0 },
    { q: "단군이 도읍으로 정한 곳은 어디인가요?", choices: ["신시", "아사달", "태백산"], answer: 1 }
];

function quizPage() {
    const items = QUIZ.map((item, i) => `
        <div class="quiz-item" data-qindex="${i}">
            <p class="quiz-question">${i + 1}. ${item.q}</p>
            <div class="quiz-choices">
                ${item.choices.map((c, ci) => `<button type="button" class="quiz-choice" data-choice="${ci}">${c}</button>`).join('')}
            </div>
        </div>`).join('');
    return `
        <div class="page page-quiz">
            <h2>이야기 문제</h2>
            <p class="quiz-intro-text" id="quizProgress">0 / 총 ${QUIZ.length}문항 완료</p>
            <div class="quiz-list">${items}</div>
        </div>`;
}

function endPage() {
    return `
        <div class="page page-end">
            ${artFrame('end.png', '🌟')}
            <h2>단군 이야기를 다 읽었어요!</h2>
            <a class="home-btn" href="../../../../../">학습 허브로 돌아가기</a>
        </div>`;
}

const PAGES = [
    { kind: 'cover' },
    ...CHAPTERS.flatMap(chapter => chapter.beats.map((beat, i) => ({ kind: 'spread', chapter, beat, isFirst: i === 0 }))),
    { kind: 'reflection', chapter: CHAPTERS[CHAPTERS.length - 1] },
    { kind: 'quiz' },
    { kind: 'end' }
];

const TWO_PAGE_KINDS = new Set(['spread', 'cover']);

let folioCounter = 0;
const FOLIOS = PAGES.map(p => {
    const width = TWO_PAGE_KINDS.has(p.kind) ? 2 : 1;
    const start = folioCounter + 1;
    folioCounter += width;
    return { start, width };
});

function renderPage(page) {
    switch (page.kind) {
        case 'cover':
            return coverPage();
        case 'spread':
            return spreadPage(page.chapter, page.beat, page.isFirst);
        case 'reflection':
            return reflectionPage(page.chapter);
        case 'quiz':
            return quizPage();
        case 'end':
            return endPage();
        default:
            return '';
    }
}

let current = 0;
let animating = false;

const spreadEl = document.getElementById('spread');
const prevBtn = document.getElementById('prevBtn');
const nextBtn = document.getElementById('nextBtn');
const indicatorEl = document.getElementById('pageIndicator');
const folioLeftEl = document.getElementById('folioLeft');
const folioRightEl = document.getElementById('folioRight');

function paint() {
    spreadEl.innerHTML = renderPage(PAGES[current]);
    prevBtn.disabled = current === 0;
    nextBtn.disabled = current === PAGES.length - 1;
    indicatorEl.textContent = `${current + 1} / ${PAGES.length}`;

    const folio = FOLIOS[current];
    folioLeftEl.classList.toggle('folio-center', folio.width === 1);
    if (folio.width === 2) {
        folioLeftEl.textContent = folio.start;
        folioRightEl.textContent = folio.start + 1;
        folioLeftEl.hidden = false;
        folioRightEl.hidden = false;
    } else {
        folioLeftEl.textContent = folio.start;
        folioLeftEl.hidden = false;
        folioRightEl.hidden = true;
    }

    spreadEl.querySelectorAll('[data-goto]').forEach(btn => {
        btn.addEventListener('click', () => {
            const chapterNum = Number(btn.dataset.goto);
            const idx = PAGES.findIndex(p => p.kind === 'spread' && p.chapter.num === chapterNum && p.isFirst);
            if (idx >= 0) goTo(idx);
        });
    });
    spreadEl.querySelectorAll('[data-goto-kind]').forEach(btn => {
        btn.addEventListener('click', () => {
            const idx = PAGES.findIndex(p => p.kind === btn.dataset.gotoKind);
            if (idx >= 0) goTo(idx);
        });
    });

    if (PAGES[current].kind === 'quiz') {
        initQuiz();
    }
}

function initQuiz() {
    let answeredCount = 0;
    const progressEl = document.getElementById('quizProgress');

    spreadEl.querySelectorAll('.quiz-item').forEach(item => {
        const qi = Number(item.dataset.qindex);
        const q = QUIZ[qi];
        item.querySelectorAll('.quiz-choice').forEach(btn => {
            btn.addEventListener('click', () => {
                if (item.classList.contains('graded')) return;
                item.classList.add('graded');
                const chosen = Number(btn.dataset.choice);
                item.querySelectorAll('.quiz-choice').forEach(b => {
                    const ci = Number(b.dataset.choice);
                    if (ci === q.answer) b.classList.add('correct');
                    else if (ci === chosen) b.classList.add('incorrect');
                });
                answeredCount++;
                progressEl.textContent = `${answeredCount} / 총 ${QUIZ.length}문항 완료`;
            });
        });
    });
}

function goTo(index) {
    if (animating || index === current || index < 0 || index >= PAGES.length) return;
    animating = true;
    const dir = index > current ? 'flip-next' : 'flip-prev';
    spreadEl.classList.add(dir);
    setTimeout(() => {
        current = index;
        paint();
    }, 230);
    setTimeout(() => {
        spreadEl.classList.remove('flip-next', 'flip-prev');
        animating = false;
    }, 480);
}

prevBtn.addEventListener('click', () => goTo(current - 1));
nextBtn.addEventListener('click', () => goTo(current + 1));

document.getElementById('tocLink').addEventListener('click', () => {
    // 그림책은 차례가 표지에 붙어 있다.
    const idx = PAGES.findIndex(p => p.kind === 'toc');
    goTo(idx >= 0 ? idx : 0);
});

document.addEventListener('keydown', (e) => {
    if (e.key === 'ArrowRight') goTo(current + 1);
    if (e.key === 'ArrowLeft') goTo(current - 1);
});

paint();
