const CHAPTERS = [
    {
        num: 1,
        title: "1장 · 나란히 지은 농사",
        beats: [
            {
                art: "01-brothers.webp",
                emoji: "👬",
                left: [
                    "옛날 어느 마을에 형제가 살았습니다.",
                    "논도 나란히, 집도 나란히.",
                    "어릴 때부터 한 번도 다툰 적이 없는 형제였어요. 밭을 매도 나란히, 물을 대도 나란히였지요."
                ],
                right: [
                    "\"형님, 오늘은 형님 논부터 물을 대시지요.\"",
                    "\"아니다, 네 논이 더 마르지 않았느냐.\"",
                    "서로 양보하느라 도랑 앞에서 한참을 서 있기도 했습니다. 동네 사람들은 두 사람을 의좋은 형제라고 불렀답니다."
                ]
            },
            {
                art: "02-harvest.webp",
                emoji: "🌾",
                left: [
                    "가을이 왔어요. 온 들판이 누렇게 익어 고개를 숙였지요.",
                    "형제는 나란히 서서 벼를 베고 볏단을 쌓았습니다.",
                    "\"자, 똑같이 나누자.\"",
                    "\"예, 형님.\""
                ],
                right: [
                    "두 사람은 볏단을 하나하나 세어 가며 똑같이 나눴어요.",
                    "형네 논에 한 무더기, 동생네 논에 한 무더기.",
                    "높이까지 꼭 같았지요. 둘은 흐뭇하게 웃으며 집으로 돌아갔습니다."
                ]
            },
            {
                art: "03-elder-thinks.webp",
                emoji: "🌙",
                left: [
                    "그날 밤, 형은 도무지 잠이 오지 않았어요. 이리 뒤척이고 저리 뒤척여도 마찬가지였지요.",
                    "'동생은 올봄에 살림을 새로 차렸지.'",
                    "'솥도 사야 하고 이불도 사야 할 텐데.'"
                ],
                right: [
                    "형은 자리에서 슬며시 일어났습니다.",
                    "'똑같이 나눈 건 아무래도 아니야.'",
                    "그러고는 식구들이 깨지 않게 문을 살살 밀고 논으로 나갔답니다."
                ]
            }
        ]
    },
    {
        num: 2,
        title: "2장 · 밤마다 오가는 볏단",
        beats: [
            {
                art: "04-elder-carries.webp",
                emoji: "🌾",
                left: [
                    "형은 자기 볏단을 지게에 얹었어요.",
                    "영차, 영차.",
                    "달빛을 밟으며 동생네 논으로 갔지요. 논둑길에 형의 그림자가 길게 늘어졌습니다."
                ],
                right: [
                    "볏단을 동생네 무더기에 슬쩍 올려놓고 형은 서둘러 돌아왔어요.",
                    "'동생이 알면 안 받으려 할 테니, 모른 척해야지.'",
                    "이불 속에 들어가서야 형은 겨우 잠이 들었답니다."
                ]
            },
            {
                art: "05-younger-thinks.webp",
                emoji: "🤔",
                left: [
                    "그런데 바로 그 밤, 동생도 잠이 오지 않았습니다.",
                    "'형님네는 식구가 많잖아.'",
                    "'조카들이 다섯이나 되는데. 겨울에 먹을 것이 모자라면 어쩌지.'"
                ],
                right: [
                    "동생도 자리를 박차고 일어났어요.",
                    "'똑같이 나눈 건 아무래도 아니지.'",
                    "그러고는 아내가 깰까 봐 발끝으로 살금살금 논으로 나갔답니다."
                ]
            },
            {
                art: "06-younger-carries.webp",
                emoji: "🌾",
                left: [
                    "동생도 자기 볏단을 지게에 얹었어요.",
                    "영차, 영차.",
                    "형이 다녀간 그 길을 이번에는 반대로 걸었지요. 서로 엇갈린 줄도 모르고요."
                ],
                right: [
                    "동생은 형네 무더기에 볏단을 올려놓고 얼른 돌아왔습니다.",
                    "'형님이 알면 도로 가져가라 하실 테니, 입 꼭 다물어야지.'"
                ]
            },
            {
                art: "07-morning.webp",
                emoji: "😮",
                left: [
                    "이튿날 아침.",
                    "형이 논에 나가 보고 눈을 끔뻑였어요. 몇 번을 세어 보아도 마찬가지였지요.",
                    "\"어라? 어제랑 똑같잖아.\""
                ],
                right: [
                    "동생도 자기 논 앞에서 고개를 갸웃했습니다.",
                    "\"이상하다. 분명히 한 지게 덜어 냈는데.\"",
                    "두 사람 다 영문을 몰라 뒤통수만 긁적였답니다."
                ]
            }
        ]
    },
    {
        num: 3,
        title: "3장 · 달빛 아래에서",
        beats: [
            {
                art: "08-again.webp",
                emoji: "😵",
                left: [
                    "그날 밤에도 형은 볏단을 옮겼어요. 동생도 볏단을 옮겼고요.",
                    "이튿날 아침, 볏단은 또 그대로였습니다. 한 단도 늘지 않고 한 단도 줄지 않았지요."
                ],
                right: [
                    "\"허, 이거 귀신이 곡할 노릇이네.\"",
                    "\"밤새 누가 도로 갖다 놓는 것도 아니고…\"",
                    "형도 동생도 어리둥절할 뿐이었답니다."
                ]
            },
            {
                art: "09-meet.webp",
                emoji: "🌕",
                left: [
                    "사흘째 되는 밤.",
                    "형이 지게를 지고 논둑길을 걸어갔어요. 저쪽에서도 누군가 지게를 지고 오고 있었지요.",
                    "달빛 아래에서 두 사람은 딱 마주쳤습니다."
                ],
                right: [
                    "\"형님?\"",
                    "\"너, 너는…\"",
                    "두 지게에는 똑같이 볏단이 실려 있었어요. 그제야 사흘 동안의 수수께끼가 풀린 것이지요."
                ]
            },
            {
                art: "10-laugh.webp",
                emoji: "😄",
                left: [
                    "형이 먼저 웃음을 터뜨렸습니다.",
                    "\"이 녀석, 너였구나!\"",
                    "\"형님이야말로요!\""
                ],
                right: [
                    "두 형제는 지게를 내려놓고 논둑에 나란히 앉아 한참을 웃었어요. 웃음소리가 달빛 아래 논을 건너갔지요.",
                    "그 뒤로도 볏단은 늘 똑같았지만, 두 사람은 더 이상 궁금해하지 않았답니다."
                ]
            }
        ],
        moral: "고마운 마음은 말보다 행동에 먼저 나타나요. 두 형제는 사흘 동안 서로에게 한마디도 하지 않았지만, 볏단은 밤마다 오갔답니다.",
        question: "누군가를 위해 티 내지 않고 무언가를 해 준 적이 있나요?"
    }
];

function artFrame(src, emoji) {
    return `
        <div class="art-frame">
            <img src="images/${src}" alt="" onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';">
            <div class="art-fallback" style="display:none">${emoji}</div>
        </div>`;
}

function coverToc() {
    const item = s => `
        <button type="button" data-goto="${s.num}">
            <span class="toc-num">${s.num}</span>
            <span>${s.title.replace(/^\d+장 · /, '')}</span>
        </button>`;
    return `
        <nav class="cover-toc">
            <h2>차례</h2>
            ${CHAPTERS.map(item).join('')}
            <button type="button" data-goto-kind="quiz">
                <span class="toc-num">❓</span>
                <span>이야기 문제</span>
            </button>
        </nav>`;
}

function coverPage() {
    return `
        <div class="page page-cover">
            <div class="story-page-left story-page-left-full">
                ${artFrame('cover.webp', '👬')}
            </div>
            <div class="story-page-right">
                <h1>의좋은 형제</h1>
                <p>의좋은 형제도 지은이가 없는 구전 설화예요. 우리나라 어느 고장에서나 전해지는 이야기라 마을마다 조금씩 다른 모습으로 남아 있답니다.</p>
                <p>충청남도 예산에는 이 이야기의 주인공으로 전해지는 이성만·이순 형제를 기리는 비석이 실제로 서 있어요. 조선 시대에 나라에서 두 형제의 우애를 널리 알리려고 세운 것이랍니다.</p>
                <p>우리 옛이야기에는 형제가 다투는 이야기와 형제가 서로 아끼는 이야기가 나란히 전해져요. 흥부와 놀부가 앞의 갈래라면 의좋은 형제는 뒤의 갈래지요. 두 갈래를 함께 읽으면 옛사람들이 무엇을 좋게 여기고 무엇을 부끄러워했는지가 보인답니다.</p>
                ${coverToc()}
            </div>
        </div>`;
}

function spreadPage(chapter, beat, isFirst) {
    const badgeHtml = isFirst ? `<div class="spread-chapter-badge">${chapter.title}</div>` : '';
    const leftHtml = beat.left.map(p => `<p>${p}</p>`).join('');
    const rightHtml = beat.right.map(p => `<p>${p}</p>`).join('');
    return `
        <div class="page page-story">
            <div class="spread-art">
                ${badgeHtml}
                ${artFrame(beat.art, beat.emoji)}
            </div>
            <div class="spread-text">
                <div class="spread-text-left">${leftHtml}</div>
                <div class="spread-text-right">${rightHtml}</div>
            </div>
        </div>`;
}

function reflectionPage(chapter) {
    return `
        <div class="page page-reflection">
            <h2>이야기를 다 읽고</h2>
            <p class="reflection-moral">${chapter.moral}</p>
            <p class="reflection-question">${chapter.question}</p>
        </div>`;
}

const QUIZ = [
    { q: "두 형제의 논은 어떻게 있었나요?", choices: ["멀리 떨어져 있었다", "나란히 붙어 있었다", "산 너머에 있었다"], answer: 1 },
    { q: "가을에 두 형제는 볏단을 어떻게 나눴나요?", choices: ["형이 더 가졌다", "동생이 더 가졌다", "똑같이 나눴다"], answer: 2 },
    { q: "형은 왜 동생에게 볏단을 옮겼나요?", choices: ["살림을 새로 차려서", "농사를 못 지어서", "몸이 아파서"], answer: 0 },
    { q: "동생은 왜 형에게 볏단을 옮겼나요?", choices: ["논이 좁아서", "빚이 있어서", "식구가 많아서"], answer: 2 },
    { q: "아침마다 볏단은 어떻게 되어 있었나요?", choices: ["그대로였다", "줄어 있었다", "없어졌다"], answer: 0 },
    { q: "두 형제는 어디에서 마주쳤나요?", choices: ["마을 장터에서", "밤길 논둑에서", "서로의 집에서"], answer: 1 }
];

function quizPage() {
    const items = QUIZ.map((item, i) => `
        <div class="quiz-item" data-qindex="${i}">
            <p class="quiz-question">${i + 1}. ${item.q}</p>
            <div class="quiz-choices">
                ${item.choices.map((c, ci) => `<button type="button" class="quiz-choice" data-choice="${ci}">${c}</button>`).join('')}
            </div>
        </div>`).join('');
    return `
        <div class="page page-quiz">
            <h2>이야기 문제</h2>
            <p class="quiz-intro-text" id="quizProgress">0 / 총 ${QUIZ.length}문항 완료</p>
            <div class="quiz-list">${items}</div>
        </div>`;
}

function endPage() {
    return `
        <div class="page page-end">
            ${artFrame('end.webp', '🌟')}
            <h2>의좋은 형제 이야기를 다 읽었어요!</h2>
            <a class="home-btn" href="../../../../../">학습 허브로 돌아가기</a>
        </div>`;
}

const PAGES = [
    { kind: 'cover' },
    ...CHAPTERS.flatMap(chapter => chapter.beats.map((beat, i) => ({ kind: 'spread', chapter, beat, isFirst: i === 0 }))),
    { kind: 'reflection', chapter: CHAPTERS[CHAPTERS.length - 1] },
    { kind: 'quiz' },
    { kind: 'end' }
];

const TWO_PAGE_KINDS = new Set(['spread', 'cover']);

let folioCounter = 0;
const FOLIOS = PAGES.map(p => {
    const width = TWO_PAGE_KINDS.has(p.kind) ? 2 : 1;
    const start = folioCounter + 1;
    folioCounter += width;
    return { start, width };
});

function renderPage(page) {
    switch (page.kind) {
        case 'cover':
            return coverPage();
        case 'spread':
            return spreadPage(page.chapter, page.beat, page.isFirst);
        case 'reflection':
            return reflectionPage(page.chapter);
        case 'quiz':
            return quizPage();
        case 'end':
            return endPage();
        default:
            return '';
    }
}

let current = 0;
let animating = false;

const spreadEl = document.getElementById('spread');
const prevBtn = document.getElementById('prevBtn');
const nextBtn = document.getElementById('nextBtn');
const indicatorEl = document.getElementById('pageIndicator');
const folioLeftEl = document.getElementById('folioLeft');
const folioRightEl = document.getElementById('folioRight');

function paint() {
    spreadEl.innerHTML = renderPage(PAGES[current]);
    prevBtn.disabled = current === 0;
    nextBtn.disabled = current === PAGES.length - 1;
    indicatorEl.textContent = `${current + 1} / ${PAGES.length}`;

    const folio = FOLIOS[current];
    folioLeftEl.classList.toggle('folio-center', folio.width === 1);
    if (folio.width === 2) {
        folioLeftEl.textContent = folio.start;
        folioRightEl.textContent = folio.start + 1;
        folioLeftEl.hidden = false;
        folioRightEl.hidden = false;
    } else {
        folioLeftEl.textContent = folio.start;
        folioLeftEl.hidden = false;
        folioRightEl.hidden = true;
    }

    spreadEl.querySelectorAll('[data-goto]').forEach(btn => {
        btn.addEventListener('click', () => {
            const chapterNum = Number(btn.dataset.goto);
            const idx = PAGES.findIndex(p => p.kind === 'spread' && p.chapter.num === chapterNum && p.isFirst);
            if (idx >= 0) goTo(idx);
        });
    });
    spreadEl.querySelectorAll('[data-goto-kind]').forEach(btn => {
        btn.addEventListener('click', () => {
            const idx = PAGES.findIndex(p => p.kind === btn.dataset.gotoKind);
            if (idx >= 0) goTo(idx);
        });
    });

    if (PAGES[current].kind === 'quiz') {
        initQuiz();
    }
}

function initQuiz() {
    let answeredCount = 0;
    const progressEl = document.getElementById('quizProgress');

    spreadEl.querySelectorAll('.quiz-item').forEach(item => {
        const qi = Number(item.dataset.qindex);
        const q = QUIZ[qi];
        item.querySelectorAll('.quiz-choice').forEach(btn => {
            btn.addEventListener('click', () => {
                if (item.classList.contains('graded')) return;
                item.classList.add('graded');
                const chosen = Number(btn.dataset.choice);
                item.querySelectorAll('.quiz-choice').forEach(b => {
                    const ci = Number(b.dataset.choice);
                    if (ci === q.answer) b.classList.add('correct');
                    else if (ci === chosen) b.classList.add('incorrect');
                });
                answeredCount++;
                progressEl.textContent = `${answeredCount} / 총 ${QUIZ.length}문항 완료`;
            });
        });
    });
}

function goTo(index) {
    if (animating || index === current || index < 0 || index >= PAGES.length) return;
    animating = true;
    const dir = index > current ? 'flip-next' : 'flip-prev';
    spreadEl.classList.add(dir);
    setTimeout(() => {
        current = index;
        paint();
    }, 230);
    setTimeout(() => {
        spreadEl.classList.remove('flip-next', 'flip-prev');
        animating = false;
    }, 480);
}

prevBtn.addEventListener('click', () => goTo(current - 1));
nextBtn.addEventListener('click', () => goTo(current + 1));

document.getElementById('tocLink').addEventListener('click', () => {
    // 그림책은 차례가 표지에 붙어 있다.
    const idx = PAGES.findIndex(p => p.kind === 'toc');
    goTo(idx >= 0 ? idx : 0);
});

document.addEventListener('keydown', (e) => {
    if (e.key === 'ArrowRight') goTo(current + 1);
    if (e.key === 'ArrowLeft') goTo(current - 1);
});

paint();
