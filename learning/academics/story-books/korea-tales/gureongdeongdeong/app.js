const CHAPTERS = [
    {
        num: 1,
        title: "1장 · 구렁덩덩 신선비네",
        beats: [
            {
                art: "01-birth.png",
                emoji: "🐍",
                left: [
                    "옛날 어느 마을에 아이 없는 할머니가 살았습니다.",
                    "늘그막에 아이를 하나 낳았는데 사람들이 다 놀랐어요."
                ],
                right: [
                    "커다란 구렁이였거든요.",
                    "그래도 할머니는 아이를 이불에 곱게 싸서 아랫목에 뉘었습니다.",
                    "\"내가 낳았으니 내 자식이지.\""
                ]
            },
            {
                art: "02-neighbors.png",
                emoji: "👀",
                left: [
                    "소문을 듣고 이웃집 세 딸이 구경을 왔어요.",
                    "문틈으로 방 안을 들여다보았지요."
                ],
                right: [
                    "\"으악! 징그러워!\"",
                    "\"저게 무슨 아이야, 뱀이지!\"",
                    "첫째와 둘째가 침을 뱉고 달아났습니다."
                ]
            },
            {
                art: "03-third.png",
                emoji: "🌸",
                left: [
                    "그런데 셋째 딸만은 그 자리에 남았어요.",
                    "한참을 들여다보더니 이렇게 말했지요."
                ],
                right: [
                    "\"어머, 구렁덩덩 신선비를 낳으셨네요.\"",
                    "그 말을 들은 구렁이가 고개를 슬며시 들었습니다.",
                    "할머니의 눈에 눈물이 핑 돌았어요."
                ]
            },
            {
                art: "04-marry.png",
                emoji: "🎊",
                left: [
                    "그날 밤 구렁이가 할머니에게 말했어요.",
                    "\"어머니, 저 집 셋째 딸에게 장가들고 싶습니다.\""
                ],
                right: [
                    "이웃집에서는 첫째도 둘째도 펄쩍 뛰었지요.",
                    "그런데 셋째가 조용히 말했습니다.",
                    "\"제가 가겠습니다.\""
                ]
            }
        ]
    },
    {
        num: 2,
        title: "2장 · 밤에 벗어 둔 허물",
        beats: [
            {
                art: "05-shed.png",
                emoji: "✨",
                left: [
                    "혼례를 올린 그날 밤이었어요.",
                    "신부가 눈을 떠 보니 방 안이 환했습니다."
                ],
                right: [
                    "구렁이 허물이 방 한쪽에 벗어져 있고,",
                    "그 옆에 훤칠한 젊은이가 앉아 있었지요.",
                    "\"놀라지 마시오. 나요.\""
                ]
            },
            {
                art: "06-promise.png",
                emoji: "🤝",
                left: [
                    "신선비는 낮에는 구렁이로, 밤에는 사람으로 지냈어요.",
                    "그러던 어느 날 과거를 보러 한양에 가게 되었습니다."
                ],
                right: [
                    "떠나기 전날 밤, 신선비가 허물을 곱게 접어 건넸어요.",
                    "\"이것을 아무도 못 보게 간수해 주시오.\"",
                    "\"불에 닿으면 나는 돌아오지 못하오.\""
                ]
            },
            {
                art: "07-burn.png",
                emoji: "🔥",
                left: [
                    "그런데 언니 둘이 그 이야기를 엿듣고 말았어요.",
                    "\"허물만 없으면 그 잘생긴 사람이 우리 것이 되지 않을까?\""
                ],
                right: [
                    "둘은 동생이 물을 길으러 간 사이에 방으로 들어갔습니다.",
                    "그러고는 허물을 찾아내 아궁이에 던져 버렸지요.",
                    "매캐한 연기가 굴뚝으로 피어올랐어요."
                ]
            },
            {
                art: "08-gone.png",
                emoji: "🌫️",
                left: [
                    "그날 밤 신선비는 돌아오지 않았어요.",
                    "이튿날도, 그다음 날도요."
                ],
                right: [
                    "빈 방에 편지 한 장만 놓여 있었습니다.",
                    "「나를 찾으려거든 해 지는 쪽으로 오시오.」",
                    "아내는 그 종이를 품에 넣고 짚신을 챙겼지요."
                ]
            }
        ]
    },
    {
        num: 3,
        title: "3장 · 신을 몇 켤레나 닳려",
        beats: [
            {
                art: "09-magpie.png",
                emoji: "🐦",
                left: [
                    "며칠을 걷다가 까치 떼를 만났어요.",
                    "\"신선비가 간 길을 아니?\""
                ],
                right: [
                    "\"알지. 그런데 우리는 지금 춥단다.\"",
                    "아내는 겉옷을 벗어 까치들에게 덮어 주었어요.",
                    "까치들이 서쪽을 가리켰지요."
                ]
            },
            {
                art: "10-boar.png",
                emoji: "🐗",
                left: [
                    "다음에는 밭을 뒤지는 멧돼지를 만났어요.",
                    "\"신선비가 간 길을 아니?\""
                ],
                right: [
                    "\"알지. 그런데 이 밭이 너무 넓구나.\"",
                    "아내는 함께 밭을 뒤집어 주었습니다.",
                    "손톱이 다 깨졌지만 밤이 새도록 멈추지 않았어요."
                ]
            },
            {
                art: "11-washer.png",
                emoji: "🧺",
                left: [
                    "마지막으로 냇가에서 빨래하는 할머니를 만났어요.",
                    "아내의 짚신은 이미 네 켤레째 닳아 있었지요."
                ],
                right: [
                    "빨래를 다 밟아 주자 할머니가 말했습니다.",
                    "\"저 고개만 넘으면 큰 기와집이 있다.\"",
                    "\"오늘이 그 집 혼삿날이라더구나.\""
                ]
            },
            {
                art: "12-reunion.png",
                emoji: "💛",
                left: [
                    "아내는 그 집 담 밖에서 노래를 불렀어요.",
                    "\"구렁덩덩 신선비, 구렁덩덩 신선비.\""
                ],
                right: [
                    "잔칫상 앞에 앉아 있던 신선비가 벌떡 일어섰습니다.",
                    "그 이름으로 불러 준 사람은 세상에 하나뿐이었거든요.",
                    "둘은 그 자리에서 서로를 알아보았답니다."
                ]
            }
        ],
        moral: "언니들은 허물이 흉하다고 침을 뱉었고, 셋째는 그것을 구렁덩덩 신선비라고 불러 주었어요. 같은 것을 보고도 어떻게 부르느냐에 따라 그 뒤가 달라졌지요. 부르는 말에는 그렇게 힘이 있답니다.",
        question: "남이 흉하다고 한 것을 나는 다르게 본 적이 있나요?"
    }
];

function artFrame(src, emoji) {
    return `
        <div class="art-frame">
            <img src="images/${src}" alt="" onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';">
            <div class="art-fallback" style="display:none">${emoji}</div>
        </div>`;
}

function coverPage() {
    return `
        <div class="page page-cover">
            <div class="story-page-left story-page-left-full">
                ${artFrame('cover.png', '🐍')}
            </div>
            <div class="story-page-right">
                <h1>구렁덩덩 신선비</h1>
                <p>구렁덩덩 신선비는 지은이가 없는 구전 설화예요. 구렁이로 태어난 신랑과 그를 찾아 나선 아내의 이야기랍니다.</p>
                <p>신선비는 새신랑을 높여 부르던 말이에요. 구렁덩덩은 구렁이를 다정하게 부르는 소리고요. 이름 자체가 무서운 것을 곱게 불러 준 말인 셈이지요.</p>
                <p>짐승 모습으로 태어난 신랑이 밤에는 사람이 되고, 금기를 어겨 사라지고, 아내가 먼 길을 걸어 되찾는 짜임은 세계 곳곳에 있어요. 유럽의 미녀와 야수, 큐피드와 프시케 이야기가 같은 갈래랍니다.</p>
                <p>우리 판본에서 눈여겨볼 것은 아내가 스스로 걸어서 찾아간다는 점이에요. 누가 구해 주기를 기다리지 않고, 신을 몇 켤레나 닳려 가며 제 발로 갑니다.</p>
            </div>
        </div>`;
}

function tocPage() {
    const itemHtml = s => `
        <li>
            <button type="button" data-goto="${s.num}">
                <span class="toc-num">${s.num}</span>
                <span>
                    <strong>${s.title.replace(/^\d+장 · /, '')}</strong>
                </span>
            </button>
        </li>`;
    const quizItemHtml = `
        <li>
            <button type="button" data-goto-kind="quiz">
                <span class="toc-num">❓</span>
                <span>
                    <strong>이야기 문제</strong>
                </span>
            </button>
        </li>`;
    const half = Math.ceil(CHAPTERS.length / 2);
    const leftItems = CHAPTERS.slice(0, half).map(itemHtml).join('');
    const rightItems = CHAPTERS.slice(half).map(itemHtml).join('') + quizItemHtml;
    return `
        <div class="page page-toc">
            <div class="story-page-left">
                <h2>차례</h2>
                <ul class="toc-list">${leftItems}</ul>
            </div>
            <div class="story-page-right">
                <ul class="toc-list">${rightItems}</ul>
            </div>
        </div>`;
}

function spreadPage(chapter, beat, isFirst) {
    const badgeHtml = isFirst ? `<div class="spread-chapter-badge">${chapter.title}</div>` : '';
    const leftHtml = beat.left.map(p => `<p>${p}</p>`).join('');
    const rightHtml = beat.right.map(p => `<p>${p}</p>`).join('');
    return `
        <div class="page page-story">
            <div class="spread-art">
                ${badgeHtml}
                ${artFrame(beat.art, beat.emoji)}
            </div>
            <div class="spread-text">
                <div class="spread-text-left">${leftHtml}</div>
                <div class="spread-text-right">${rightHtml}</div>
            </div>
        </div>`;
}

function reflectionPage(chapter) {
    return `
        <div class="page page-reflection">
            <h2>이야기를 다 읽고</h2>
            <p class="reflection-moral">${chapter.moral}</p>
            <p class="reflection-question">${chapter.question}</p>
        </div>`;
}

const QUIZ = [
    { q: "할머니가 낳은 아이는 어떤 모습이었나요?", choices: ["조그만 두꺼비", "커다란 구렁이", "몸이 반쪽인 아이"], answer: 1 },
    { q: "이웃집 세 딸 가운데 셋째만 무엇을 했나요?", choices: ["선물을 가져왔다", "가까이 다가갔다", "곱게 불러 주었다"], answer: 2 },
    { q: "밤이 되자 구렁이는 어떻게 되었나요?", choices: ["허물을 벗고 신선비가 되었다", "몸이 더 크게 자라났다", "사람 말을 하기 시작했다"], answer: 0 },
    { q: "신선비가 아내에게 당부한 것은 무엇인가요?", choices: ["문을 열지 말라고", "사흘을 기다리라고", "허물을 잘 간수하라고"], answer: 2 },
    { q: "언니들은 허물을 어떻게 했나요?", choices: ["아궁이에 넣어 태웠다", "몰래 감추어 두었다", "냇물에 던져 버렸다"], answer: 0 },
    { q: "아내는 신선비를 어떻게 찾아 나섰나요?", choices: ["말을 타고 달려갔다", "걸어서 물어물어 갔다", "배를 타고 건너갔다"], answer: 1 },
    { q: "까치는 무엇을 주면 길을 알려 준다고 했나요?", choices: ["가지고 있던 돈을", "입고 있던 옷을", "머리에 꽂은 비녀를"], answer: 1 },
    { q: "둘은 마지막에 어떻게 되었나요?", choices: ["끝내 헤어지고 말았다", "하늘로 올라갔다", "다시 만나 함께 살았다"], answer: 2 }
];

function quizPage() {
    const items = QUIZ.map((item, i) => `
        <div class="quiz-item" data-qindex="${i}">
            <p class="quiz-question">${i + 1}. ${item.q}</p>
            <div class="quiz-choices">
                ${item.choices.map((c, ci) => `<button type="button" class="quiz-choice" data-choice="${ci}">${c}</button>`).join('')}
            </div>
        </div>`).join('');
    return `
        <div class="page page-quiz">
            <h2>이야기 문제</h2>
            <p class="quiz-intro-text" id="quizProgress">0 / 총 ${QUIZ.length}문항 완료</p>
            <div class="quiz-list">${items}</div>
        </div>`;
}

function endPage() {
    return `
        <div class="page page-end">
            ${artFrame('end.png', '🌟')}
            <h2>구렁덩덩 신선비 이야기를 다 읽었어요!</h2>
            <a class="home-btn" href="../../../../../">학습 허브로 돌아가기</a>
        </div>`;
}

const PAGES = [
    { kind: 'cover' },
    { kind: 'toc' },
    ...CHAPTERS.flatMap(chapter => chapter.beats.map((beat, i) => ({ kind: 'spread', chapter, beat, isFirst: i === 0 }))),
    { kind: 'reflection', chapter: CHAPTERS[CHAPTERS.length - 1] },
    { kind: 'quiz' },
    { kind: 'end' }
];

const TWO_PAGE_KINDS = new Set(['spread', 'toc', 'cover']);

let folioCounter = 0;
const FOLIOS = PAGES.map(p => {
    const width = TWO_PAGE_KINDS.has(p.kind) ? 2 : 1;
    const start = folioCounter + 1;
    folioCounter += width;
    return { start, width };
});

function renderPage(page) {
    switch (page.kind) {
        case 'cover':
            return coverPage();
        case 'toc':
            return tocPage();
        case 'spread':
            return spreadPage(page.chapter, page.beat, page.isFirst);
        case 'reflection':
            return reflectionPage(page.chapter);
        case 'quiz':
            return quizPage();
        case 'end':
            return endPage();
        default:
            return '';
    }
}

let current = 0;
let animating = false;

const spreadEl = document.getElementById('spread');
const prevBtn = document.getElementById('prevBtn');
const nextBtn = document.getElementById('nextBtn');
const indicatorEl = document.getElementById('pageIndicator');
const folioLeftEl = document.getElementById('folioLeft');
const folioRightEl = document.getElementById('folioRight');

function paint() {
    spreadEl.innerHTML = renderPage(PAGES[current]);
    prevBtn.disabled = current === 0;
    nextBtn.disabled = current === PAGES.length - 1;
    indicatorEl.textContent = `${current + 1} / ${PAGES.length}`;

    const folio = FOLIOS[current];
    folioLeftEl.classList.toggle('folio-center', folio.width === 1);
    if (folio.width === 2) {
        folioLeftEl.textContent = folio.start;
        folioRightEl.textContent = folio.start + 1;
        folioLeftEl.hidden = false;
        folioRightEl.hidden = false;
    } else {
        folioLeftEl.textContent = folio.start;
        folioLeftEl.hidden = false;
        folioRightEl.hidden = true;
    }

    spreadEl.querySelectorAll('[data-goto]').forEach(btn => {
        btn.addEventListener('click', () => {
            const chapterNum = Number(btn.dataset.goto);
            const idx = PAGES.findIndex(p => p.kind === 'spread' && p.chapter.num === chapterNum && p.isFirst);
            if (idx >= 0) goTo(idx);
        });
    });
    spreadEl.querySelectorAll('[data-goto-kind]').forEach(btn => {
        btn.addEventListener('click', () => {
            const idx = PAGES.findIndex(p => p.kind === btn.dataset.gotoKind);
            if (idx >= 0) goTo(idx);
        });
    });

    if (PAGES[current].kind === 'quiz') {
        initQuiz();
    }
}

function initQuiz() {
    let answeredCount = 0;
    const progressEl = document.getElementById('quizProgress');

    spreadEl.querySelectorAll('.quiz-item').forEach(item => {
        const qi = Number(item.dataset.qindex);
        const q = QUIZ[qi];
        item.querySelectorAll('.quiz-choice').forEach(btn => {
            btn.addEventListener('click', () => {
                if (item.classList.contains('graded')) return;
                item.classList.add('graded');
                const chosen = Number(btn.dataset.choice);
                item.querySelectorAll('.quiz-choice').forEach(b => {
                    const ci = Number(b.dataset.choice);
                    if (ci === q.answer) b.classList.add('correct');
                    else if (ci === chosen) b.classList.add('incorrect');
                });
                answeredCount++;
                progressEl.textContent = `${answeredCount} / 총 ${QUIZ.length}문항 완료`;
            });
        });
    });
}

function goTo(index) {
    if (animating || index === current || index < 0 || index >= PAGES.length) return;
    animating = true;
    const dir = index > current ? 'flip-next' : 'flip-prev';
    spreadEl.classList.add(dir);
    setTimeout(() => {
        current = index;
        paint();
    }, 230);
    setTimeout(() => {
        spreadEl.classList.remove('flip-next', 'flip-prev');
        animating = false;
    }, 480);
}

prevBtn.addEventListener('click', () => goTo(current - 1));
nextBtn.addEventListener('click', () => goTo(current + 1));

document.getElementById('tocLink').addEventListener('click', () => {
    const idx = PAGES.findIndex(p => p.kind === 'toc');
    if (idx >= 0) goTo(idx);
});

document.addEventListener('keydown', (e) => {
    if (e.key === 'ArrowRight') goTo(current + 1);
    if (e.key === 'ArrowLeft') goTo(current - 1);
});

paint();
