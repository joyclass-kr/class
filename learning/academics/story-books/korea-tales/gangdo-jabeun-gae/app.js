const CHAPTERS = [
    {
        num: 1,
        title: "1장 · 장에서 돌아오는 길",
        beats: [
            {
                art: "01-dog.webp",
                emoji: "🐕",
                left: [
                    "옛날에 봇짐을 지고 이 장 저 장 떠도는 장수가 하나 있었습니다.",
                    "장수에게는 개 한 마리가 있었어요. 등에 까만 점이 박혀 있어서 이름은 바둑이였지요.",
                    "몇 해 전 눈 오는 날, 길가에서 떨고 있던 것을 품에 안고 온 개였답니다.",
                    "그때부터 둘은 한 번도 떨어진 적이 없었어요."
                ],
                right: [
                    "\"바둑아, 가자!\"",
                    "장수가 부르면 바둑이는 꼬리를 세차게 흔들며 달려왔습니다. 부르기가 무섭게 발밑에 와 있었지요.",
                    "바둑이는 늘 장수의 뒤를 졸졸 따라다녔어요.",
                    "장수가 국밥을 먹으면 바둑이도 국밥을 먹었답니다. 장수가 잠들면 그 발치에서 잤지요."
                ]
            },
            {
                art: "02-market.webp",
                emoji: "💰",
                left: [
                    "그날은 장이 아주 잘됐어요. 해가 지기도 전에 봇짐이 텅 비도록 물건이 다 팔렸지요.",
                    "\"허허, 오늘은 운수가 좋구먼.\"",
                    "\"바둑아, 오늘은 고기라도 사 먹자꾸나.\"",
                    "바둑이가 알아듣는다는 듯 컹 하고 짖었습니다."
                ],
                right: [
                    "장수는 두둑해진 돈주머니를 허리춤에 단단히 매었어요. 한 해를 벌어 모은 돈이 다 거기 들어 있었지요.",
                    "이 돈이면 겨울을 넉넉히 날 수 있었습니다.",
                    "바둑이가 꼬리를 흔들며 앞장을 섰어요.",
                    "장수의 발걸음이 그렇게 가벼울 수가 없었답니다."
                ]
            },
            {
                art: "03-inn.webp",
                emoji: "🍲",
                left: [
                    "해가 저물어 장수는 길가 주막에 들었습니다. 마루에 앉으니 다리가 저릿저릿했지요.",
                    "\"여기 국밥 두 그릇 주시오.\"",
                    "장수는 한 그릇을 바둑이 앞에 놓아 주었어요.",
                    "바둑이가 후룩후룩 소리를 내며 맛있게 먹었답니다."
                ],
                right: [
                    "그런데 옆자리에 앉은 사내가 자꾸 이쪽을 흘끔거렸어요. 장수의 허리춤을, 정확히는 그 돈주머니를요.",
                    "눈빛이 예사롭지 않았지요.",
                    "바둑이가 국밥을 먹다 말고 슬며시 고개를 들었습니다.",
                    "그러고는 그 사내를 노려보며 낮게 으르렁거렸어요."
                ]
            }
        ]
    },
    {
        num: 2,
        title: "2장 · 안개 낀 고갯길",
        beats: [
            {
                art: "04-dawn.webp",
                emoji: "🌫️",
                left: [
                    "이튿날 새벽, 장수는 남들보다 일찍 길을 나섰습니다. 고갯길에는 안개가 자욱하게 깔려 있었지요.",
                    "한 치 앞도 잘 보이지 않았어요.",
                    "\"오늘 안에 집에 닿아야 할 텐데.\"",
                    "장수는 걸음을 재촉했답니다."
                ],
                right: [
                    "그런데 바둑이가 자꾸 뒤를 돌아보며 걸음을 늦추었어요. 귀를 쫑긋 세우고 코를 킁킁거렸지요.",
                    "무언가 마음에 걸리는 모양이었습니다.",
                    "\"왜 그러느냐? 어서 가자.\"",
                    "장수가 재촉해도 바둑이는 자꾸만 뒤가 궁금한 모양이었어요."
                ]
            },
            {
                art: "05-robbed.webp",
                emoji: "😱",
                left: [
                    "고갯마루에 이르렀을 때였습니다.",
                    "안개 속에서 누군가 불쑥 튀어나왔어요. 장수는 그 자리에서 우뚝 멈춰 섰지요.",
                    "어젯밤 주막에서 흘끔거리던 바로 그 사내였습니다.",
                    "손에는 몽둥이가 들려 있었어요."
                ],
                right: [
                    "사내는 장수를 힘껏 밀치고 돈주머니를 낚아챘습니다. 장수는 그대로 나동그라졌지요.",
                    "눈 깜짝할 사이의 일이었어요.",
                    "사내는 숲속으로 쏜살같이 사라졌습니다.",
                    "\"도, 도둑이야! 사람 살려!\""
                ]
            },
            {
                art: "06-lost.webp",
                emoji: "😞",
                left: [
                    "장수는 벌떡 일어나 숲으로 뛰어들었어요. 하지만 안개가 워낙 짙어 이내 방향을 잃고 말았습니다.",
                    "이리 헤매고 저리 헤매도 사내의 그림자조차 보이지 않았지요.",
                    "나뭇가지에 얼굴만 잔뜩 긁혔답니다."
                ],
                right: [
                    "\"한 해를 벌어 모은 돈인데…\"",
                    "\"이걸 어떻게 집에 가서 말한담.\"",
                    "장수는 그만 길바닥에 털썩 주저앉았어요. 눈앞이 캄캄했지요.",
                    "바둑이가 그 곁에서 땅에 코를 대고 킁킁거렸답니다."
                ]
            }
        ]
    },
    {
        num: 3,
        title: "3장 · 바둑이가 달렸다",
        beats: [
            {
                art: "07-scent.webp",
                emoji: "👃",
                left: [
                    "그때 바둑이가 갑자기 귀를 쫑긋 세웠습니다. 무언가를 찾아낸 모양이었어요.",
                    "바둑이는 한 방향을 향해 컹! 하고 크게 짖었지요.",
                    "그러고는 장수를 한 번 돌아보았습니다.",
                    "마치 여기라고 말하는 것 같았어요."
                ],
                right: [
                    "\"바둑아! 어디 가느냐!\"",
                    "바둑이는 대답 대신 안개 속으로 뛰어들었습니다.",
                    "네 다리가 보이지 않을 만큼 빨랐어요. 장수가 아무리 불러도 돌아오지 않았지요.",
                    "짖는 소리만 점점 멀어져 갔답니다."
                ]
            },
            {
                art: "08-chase.webp",
                emoji: "🌲",
                left: [
                    "바둑이는 나무 사이를 가로질러 달렸어요.",
                    "코를 땅에 대고, 다시 들고, 또 달리고.",
                    "가시덤불에 걸려 털이 뜯겨도 멈추지 않았습니다. 냄새는 갈수록 진해졌지요."
                ],
                right: [
                    "골짜기 하나를 넘었을 때였어요.",
                    "커다란 바위 뒤에 웅크리고 앉은 사내가 보였습니다. 돈주머니를 품에 꼭 안고 숨을 고르고 있었지요.",
                    "이만하면 따돌렸다고 여기는 얼굴이었어요.",
                    "바둑이는 걸음을 멈추고 몸을 낮췄답니다."
                ]
            },
            {
                art: "09-bark.webp",
                emoji: "🗣️",
                left: [
                    "\"컹! 컹! 컹컹컹!\"",
                    "바둑이가 온 산이 울리도록 짖었어요. 목이 쉬도록 짖고 또 짖었지요.",
                    "사내는 화들짝 놀라 벌떡 일어났습니다.",
                    "\"저리 가! 저리 가란 말이야!\""
                ],
                right: [
                    "사내가 발을 구르고 몽둥이를 휘둘러도 바둑이는 물러서지 않았어요.",
                    "오히려 달려들어 바짓자락을 꽉 물고 늘어졌지요.",
                    "사내는 바둑이를 떼어 내느라 달아나지도 못했습니다.",
                    "그 소리를 듣고 나무하러 온 사람들이 우르르 달려왔답니다."
                ]
            },
            {
                art: "10-return.webp",
                emoji: "🍖",
                left: [
                    "사내는 그 자리에서 붙잡혔어요. 돈주머니는 한 푼도 축나지 않고 고스란히 돌아왔지요.",
                    "장수는 바둑이를 끌어안고 한참을 놓지 못했습니다.",
                    "\"네가 아니었으면 어쩔 뻔했느냐.\""
                ],
                right: [
                    "바둑이는 그저 꼬리만 흔들었어요. 다리에 상처가 여러 군데 나 있었지요.",
                    "장수는 그 상처를 하나하나 닦아 주었습니다.",
                    "그날부터 바둑이의 밥그릇에는 늘 고기 한 점이 놓였답니다.",
                    "\"우리 바둑이는 개가 아니라 식구야.\""
                ]
            }
        ],
        moral: "바둑이는 눈 오는 날 길가에서 구해진 개였어요. 그때 장수가 그냥 지나쳤다면 이날의 일도 없었겠지요. 베푼 마음은 잊고 지내도 어딘가에 남아 있답니다.",
        question: "내가 무심코 베푼 친절이 누군가에게 크게 남은 적이 있을까요?"
    }
];

function artFrame(src, emoji) {
    return `
        <div class="art-frame">
            <img src="images/${src}" alt="" onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';">
            <div class="art-fallback" style="display:none">${emoji}</div>
        </div>`;
}

function coverToc() {
    const item = s => `
        <button type="button" data-goto="${s.num}">
            <span class="toc-num">${s.num}</span>
            <span>${s.title.replace(/^\d+장 · /, '')}</span>
        </button>`;
    return `
        <nav class="cover-toc">
            <h2>차례</h2>
            ${CHAPTERS.map(item).join('')}
            <button type="button" data-goto-kind="quiz">
                <span class="toc-num">❓</span>
                <span>이야기 문제</span>
            </button>
        </nav>`;
}

function coverPage() {
    return `
        <div class="page page-cover">
            <div class="story-page-left story-page-left-full">
                ${artFrame('cover.webp', '🐕')}
            </div>
            <div class="story-page-right">
                <h1>강도를 잡은 개</h1>
                <p>강도를 잡은 개는 지은이가 없는 구전 설화예요. 의로운 개가 주인공인 이야기를 의견 설화라고 부르는데, 그 갈래에 속한답니다.</p>
                <p>우리나라에는 개를 기리는 비석과 무덤이 실제로 여러 곳에 남아 있어요. 전라북도 임실의 오수라는 곳에는 잠든 주인을 불에서 구해 낸 개의 무덤이 있는데, 그 이야기는 고려 시대 책에도 실려 있지요.</p>
                <p>옛날 시골에서 개는 귀여워하는 짐승이 아니라 함께 일하는 식구였어요. 집을 지키고, 짐승을 몰고, 먼 길에 주인을 따라나섰지요. 그래서 개가 주인공인 옛이야기가 유난히 많답니다.</p>
                ${coverToc()}
            </div>
        </div>`;
}

function spreadPage(chapter, beat, isFirst) {
    const badgeHtml = isFirst ? `<div class="spread-chapter-badge">${chapter.title}</div>` : '';
    const leftHtml = beat.left.map(p => `<p>${p}</p>`).join('');
    const rightHtml = beat.right.map(p => `<p>${p}</p>`).join('');
    return `
        <div class="page page-story">
            <div class="spread-art">
                ${badgeHtml}
                ${artFrame(beat.art, beat.emoji)}
            </div>
            <div class="spread-text">
                <div class="spread-text-left">${leftHtml}</div>
                <div class="spread-text-right">${rightHtml}</div>
            </div>
        </div>`;
}


const QUIZ = [
    { q: "장수는 바둑이를 어떻게 만났나요?", choices: ["장에서 사 와서", "눈 오는 날 주워서", "이웃이 주어서"], answer: 1 },
    { q: "주막에서 사내가 흘끔거린 것은 무엇인가요?", choices: ["장수의 빈 봇짐", "장수의 국밥그릇", "장수의 돈주머니"], answer: 2 },
    { q: "사내는 어디에서 돈주머니를 빼앗았나요?", choices: ["안개 낀 고갯마루", "주막의 방 안에서", "강가 나루터에서"], answer: 0 },
    { q: "바둑이는 무엇으로 사내를 찾아냈나요?", choices: ["발자국을 보고", "소리를 들어서", "냄새를 맡아서"], answer: 2 },
    { q: "바둑이는 사내를 찾아내 어떻게 했나요?", choices: ["짖으며 물고 늘어졌다", "조용히 지켜보았다", "주인에게 돌아갔다"], answer: 0 },
    { q: "사람들이 달려온 까닭은 무엇인가요?", choices: ["장수가 소리쳐 불러서", "개 짖는 소리 때문에", "연기가 크게 나서"], answer: 1 }
];

function quizPage() {
    const items = QUIZ.map((item, i) => `
        <div class="quiz-item" data-qindex="${i}">
            <p class="quiz-question">${i + 1}. ${item.q}</p>
            <div class="quiz-choices">
                ${item.choices.map((c, ci) => `<button type="button" class="quiz-choice" data-choice="${ci}">${c}</button>`).join('')}
            </div>
        </div>`).join('');
    return `
        <div class="page page-quiz">
            <h2>이야기 문제</h2>
            <p class="quiz-intro-text" id="quizProgress">0 / 총 ${QUIZ.length}문항 완료</p>
            <div class="quiz-list">${items}</div>
        </div>`;
}


/* 읽고 나서 — 세계명작 트랙과 같은 형식이다. 동화틀은 쪽을 재서 나누지 않으므로
   펼침면마다 왼쪽·오른쪽 글을 손으로 나누어 둔다. */
const AFTERWORD = {
    title: '읽고 나서',
    emoji: '🐕',
    spreads: [
        {
            art: 'end.webp',
            left: [
                "주인을 구한 개 이야기는 우리나라에 아주 많습니다. 그런 개를 의로운 개라 하여 의견이라 불렀고, 무덤을 만들어 주고 비석까지 세운 곳도 있습니다.",
                "전라북도 임실에는 오수라는 곳이 있는데, 마을 이름 자체가 개를 기린 데서 왔다고 전해 옵니다. 그곳에는 지금도 개를 기리는 비석이 서 있습니다.",
                "그때 장수는 무엇을 바라고 데려온 것이 아닙니다. 그러니 이 이야기는 갚음을 받은 이야기가 아니라, 그날의 일이 어디까지 갔는지를 보여 주는 이야기입니다.",
                "말을 못 하는 짐승이 사람을 구하는 이야기가 이토록 많이 남은 것은, 사람들이 그 곁을 오래 지켜보았다는 뜻이기도 합니다."
            ],
            right: [
                "바둑이는 눈 오는 날 길가에서 구해진 개였습니다. 그때 장수가 그냥 지나쳤다면 이날의 일도 없었을 것입니다. 베푼 마음은 잊고 지내도 어딘가에 남아 있습니다.",
                "내가 무심코 베푼 친절이 누군가에게 크게 남은 적이 있을까요?"
            ]
        }
    ]
};

function afterPage(spread, isFirst) {
    const head = isFirst ? `<h2>${AFTERWORD.title}</h2>` : '';
    // 그림은 오른쪽 칸 맨 위 모서리에 꽉 붙인다. 학습 허브로 가는 길은 그 칸 맨 아래에 둔다.
    const art = spread.art ? `<div class="after-art">${artFrame(spread.art, AFTERWORD.emoji)}</div>` : '';
    const foot = spread.last ? `<p class="after-home"><a class="home-btn" href="../../../../../">학습 허브로 돌아가기</a></p>` : '';
    const col = (ps) => ps.map(t => `<p>${t}</p>`).join('');
    return `
        <div class="page page-after">
            <div class="after-col after-col-left">
                ${head}
                ${col(spread.left)}
            </div>
            <div class="after-col after-col-right${spread.art ? ' after-col-image' : ''}">
                ${art}
                ${col(spread.right)}
                ${foot}
            </div>
        </div>`;
}

const PAGES = [
    { kind: 'cover' },
    ...CHAPTERS.flatMap(chapter => chapter.beats.map((beat, i) => ({ kind: 'spread', chapter, beat, isFirst: i === 0 }))),
    { kind: 'quiz' },
    ...AFTERWORD.spreads.map((spread, i) => ({ kind: 'after', spread, isFirst: i === 0, last: i === AFTERWORD.spreads.length - 1 })),
];

const TWO_PAGE_KINDS = new Set(['spread', 'cover', 'after']);

let folioCounter = 0;
const FOLIOS = PAGES.map(p => {
    const width = TWO_PAGE_KINDS.has(p.kind) ? 2 : 1;
    const start = folioCounter + 1;
    folioCounter += width;
    return { start, width };
});

function renderPage(page) {
    switch (page.kind) {
        case 'cover':
            return coverPage();
        case 'spread':
            return spreadPage(page.chapter, page.beat, page.isFirst);
        case 'after':
            return afterPage({ ...page.spread, last: page.last }, page.isFirst);
        case 'quiz':
            return quizPage();
        default:
            return '';
    }
}

let current = 0;
let animating = false;

const spreadEl = document.getElementById('spread');
const prevBtn = document.getElementById('prevBtn');
const nextBtn = document.getElementById('nextBtn');
const indicatorEl = document.getElementById('pageIndicator');
const folioLeftEl = document.getElementById('folioLeft');
const folioRightEl = document.getElementById('folioRight');

function paint() {
    spreadEl.innerHTML = renderPage(PAGES[current]);
    prevBtn.disabled = current === 0;
    nextBtn.disabled = current === PAGES.length - 1;
    indicatorEl.textContent = `${current + 1} / ${PAGES.length}`;

    const folio = FOLIOS[current];
    folioLeftEl.classList.toggle('folio-center', folio.width === 1);
    if (folio.width === 2) {
        folioLeftEl.textContent = folio.start;
        folioRightEl.textContent = folio.start + 1;
        folioLeftEl.hidden = false;
        folioRightEl.hidden = false;
    } else {
        folioLeftEl.textContent = folio.start;
        folioLeftEl.hidden = false;
        folioRightEl.hidden = true;
    }

    spreadEl.querySelectorAll('[data-goto]').forEach(btn => {
        btn.addEventListener('click', () => {
            const chapterNum = Number(btn.dataset.goto);
            const idx = PAGES.findIndex(p => p.kind === 'spread' && p.chapter.num === chapterNum && p.isFirst);
            if (idx >= 0) goTo(idx);
        });
    });
    spreadEl.querySelectorAll('[data-goto-kind]').forEach(btn => {
        btn.addEventListener('click', () => {
            const idx = PAGES.findIndex(p => p.kind === btn.dataset.gotoKind);
            if (idx >= 0) goTo(idx);
        });
    });

    if (PAGES[current].kind === 'quiz') {
        initQuiz();
    }
}

function initQuiz() {
    let answeredCount = 0;
    const progressEl = document.getElementById('quizProgress');

    spreadEl.querySelectorAll('.quiz-item').forEach(item => {
        const qi = Number(item.dataset.qindex);
        const q = QUIZ[qi];
        item.querySelectorAll('.quiz-choice').forEach(btn => {
            btn.addEventListener('click', () => {
                if (item.classList.contains('graded')) return;
                item.classList.add('graded');
                const chosen = Number(btn.dataset.choice);
                item.querySelectorAll('.quiz-choice').forEach(b => {
                    const ci = Number(b.dataset.choice);
                    if (ci === q.answer) b.classList.add('correct');
                    else if (ci === chosen) b.classList.add('incorrect');
                });
                answeredCount++;
                progressEl.textContent = `${answeredCount} / 총 ${QUIZ.length}문항 완료`;
            });
        });
    });
}

function goTo(index) {
    if (animating || index === current || index < 0 || index >= PAGES.length) return;
    animating = true;
    const dir = index > current ? 'flip-next' : 'flip-prev';
    spreadEl.classList.add(dir);
    setTimeout(() => {
        current = index;
        paint();
    }, 230);
    setTimeout(() => {
        spreadEl.classList.remove('flip-next', 'flip-prev');
        animating = false;
    }, 480);
}

prevBtn.addEventListener('click', () => goTo(current - 1));
nextBtn.addEventListener('click', () => goTo(current + 1));

document.getElementById('tocLink').addEventListener('click', () => {
    // 그림책은 차례가 표지에 붙어 있다.
    const idx = PAGES.findIndex(p => p.kind === 'toc');
    goTo(idx >= 0 ? idx : 0);
});

document.addEventListener('keydown', (e) => {
    if (e.key === 'ArrowRight') goTo(current + 1);
    if (e.key === 'ArrowLeft') goTo(current - 1);
});

paint();
