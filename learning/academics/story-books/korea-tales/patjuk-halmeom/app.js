const CHAPTERS = [
    {
        num: 1,
        title: "1장 · 가을에 오시오",
        beats: [
            {
                art: "01-field.png",
                emoji: "🌱",
                left: [
                    "옛날 어느 산 밑에 할머니가 혼자 살았습니다.",
                    "밭이라고는 팥밭 한 뙈기가 전부였지요."
                ],
                right: [
                    "그날도 할머니는 호미를 들고 팥밭을 매고 있었어요.",
                    "\"올해는 팥이 잘 여물어야 할 텐데.\""
                ]
            },
            {
                art: "02-tiger.png",
                emoji: "🐯",
                left: [
                    "그때 등 뒤에서 서늘한 바람이 불었어요.",
                    "\"어흥!\"",
                    "커다란 호랑이가 떡 버티고 서 있었습니다."
                ],
                right: [
                    "\"할멈, 오늘 자네를 잡아먹어야겠다.\"",
                    "할머니는 호미를 떨어뜨렸지요.",
                    "다리가 후들후들 떨렸어요."
                ]
            },
            {
                art: "03-deal.png",
                emoji: "🥣",
                left: [
                    "\"자, 잠깐만!\"",
                    "할머니가 손을 번쩍 들었어요.",
                    "\"지금 나를 먹어 봐야 뼈밖에 없소.\""
                ],
                right: [
                    "\"가을에 팥이 여물면 팥죽을 한 솥 쑤어 드리리다.\"",
                    "\"배불리 자시고 나서 나를 잡아먹으시오.\"",
                    "호랑이는 입맛을 다셨습니다."
                ]
            },
            {
                art: "04-agree.png",
                emoji: "🍂",
                left: [
                    "\"허, 그거 그럴듯하구나.\"",
                    "\"좋다. 가을에 다시 오마.\""
                ],
                right: [
                    "호랑이는 어슬렁어슬렁 산으로 돌아갔어요.",
                    "할머니는 그 자리에 주저앉아 한숨을 쉬었지요.",
                    "\"어쩌면 좋누…\""
                ]
            }
        ]
    },
    {
        num: 2,
        title: "2장 · 팥죽 한 그릇씩",
        beats: [
            {
                art: "05-cry.png",
                emoji: "😭",
                left: [
                    "가을이 왔어요. 할머니는 팥을 거두어 커다란 솥에 팥죽을 쑤었습니다.",
                    "그러면서 엉엉 울었지요."
                ],
                right: [
                    "\"이 팥죽을 쑤고 나면 나는 잡아먹히는구나.\"",
                    "구수한 냄새가 마당 밖까지 퍼져 나갔어요."
                ]
            },
            {
                art: "06-chestnut.png",
                emoji: "🌰",
                left: [
                    "그때 밤 한 톨이 데구루루 굴러왔어요.",
                    "\"할머니, 왜 우세요?\"",
                    "할머니가 사정을 이야기했습니다."
                ],
                right: [
                    "\"팥죽 한 그릇만 주시면 제가 도와 드릴게요.\"",
                    "밤은 팥죽을 냠냠 먹고는 아궁이 잿속으로 쏙 들어갔지요."
                ]
            },
            {
                art: "07-turtle.png",
                emoji: "🐢",
                left: [
                    "이번에는 자라가 엉금엉금 기어왔어요.",
                    "\"할머니, 왜 우세요?\""
                ],
                right: [
                    "자라도 팥죽 한 그릇을 뚝딱 비웠습니다.",
                    "그러고는 부엌 물동이 속으로 첨벙 들어가 숨었지요."
                ]
            },
            {
                art: "08-dung.png",
                emoji: "💩",
                left: [
                    "다음은 개똥이 데굴데굴 굴러왔어요.",
                    "\"할머니, 왜 우세요?\"",
                    "\"아이고, 너까지…\""
                ],
                right: [
                    "개똥도 팥죽을 한 그릇 얻어먹었습니다.",
                    "그러고는 부엌 바닥 한복판에 철퍼덕 자리를 잡았지요."
                ]
            },
            {
                art: "09-mortar.png",
                emoji: "🪨",
                left: [
                    "쿵, 쿵, 쿵.",
                    "이번에는 절구가 뛰어왔어요.",
                    "\"할머니, 왜 우세요?\""
                ],
                right: [
                    "절구도 팥죽을 배불리 먹었습니다.",
                    "그러고는 부엌문 위 문틀에 낑낑 기어올라 자리를 잡았지요."
                ]
            },
            {
                art: "10-mat.png",
                emoji: "🧺",
                left: [
                    "마지막으로 멍석과 지게가 나란히 찾아왔어요.",
                    "\"할머니, 왜 우세요?\""
                ],
                right: [
                    "둘도 팥죽을 나눠 먹었습니다.",
                    "멍석은 마당에 납작 엎드렸고,",
                    "지게는 담 옆에 슬쩍 기대어 섰지요."
                ]
            }
        ]
    },
    {
        num: 3,
        title: "3장 · 그날 밤",
        beats: [
            {
                art: "11-arrive.png",
                emoji: "🌙",
                left: [
                    "해가 지자 호랑이가 어슬렁어슬렁 내려왔어요.",
                    "\"할멈! 팥죽 다 됐느냐!\""
                ],
                right: [
                    "부엌은 캄캄했습니다.",
                    "\"불도 안 켜 놓고 뭐 하는 게냐.\"",
                    "호랑이는 아궁이 앞에 쭈그리고 앉았지요."
                ]
            },
            {
                art: "12-pop.png",
                emoji: "💥",
                left: [
                    "호랑이가 잿속을 후후 불었어요.",
                    "그 순간이었습니다.",
                    "팍!"
                ],
                right: [
                    "잿속에 숨어 있던 밤이 튀어 올라 호랑이 눈을 딱 때렸어요.",
                    "\"아이고, 내 눈!\"",
                    "호랑이가 두 발로 눈을 감쌌지요."
                ]
            },
            {
                art: "13-bite.png",
                emoji: "😱",
                left: [
                    "호랑이는 눈을 식히려고 물동이로 달려갔어요.",
                    "앞발을 푹 담그는 순간,",
                    "콱!"
                ],
                right: [
                    "물동이 속 자라가 앞발을 힘껏 물었습니다.",
                    "\"으악! 이건 또 뭐야!\"",
                    "호랑이는 물동이를 끌어안은 채 펄쩍 뛰었어요."
                ]
            },
            {
                art: "14-slip.png",
                emoji: "🌀",
                left: [
                    "펄쩍 뛰어 착지한 곳은 하필 개똥 위였어요.",
                    "찌익—",
                    "\"어어어!\""
                ],
                right: [
                    "호랑이는 두 다리가 쭉 미끄러져 벌러덩 나자빠졌습니다.",
                    "허둥지둥 일어나 문밖으로 뛰쳐나가려는데,",
                    "쿵!"
                ]
            },
            {
                art: "15-roll.png",
                emoji: "🧺",
                left: [
                    "문틀 위에 있던 절구가 호랑이 머리 위로 떨어졌어요.",
                    "호랑이는 별을 보며 마당으로 굴러 나왔지요."
                ],
                right: [
                    "그러자 마당에 엎드려 있던 멍석이 벌떡 일어나,",
                    "호랑이를 둘둘둘 말아 버렸답니다.",
                    "\"이거 놔! 놓으란 말이야!\""
                ]
            },
            {
                art: "16-river.png",
                emoji: "🌊",
                left: [
                    "담 옆에 서 있던 지게가 성큼 다가왔어요.",
                    "돌돌 말린 호랑이를 척 지고는 뚜벅뚜벅 걸어 나갔습니다."
                ],
                right: [
                    "그러고는 강물에 텀벙 던져 버렸지요. 그 뒤로 호랑이는 그 마을에 얼씬도 하지 않았대요.",
                    "할머니는 남은 팥죽을 여섯에게 나누어 주었답니다."
                ]
            }
        ],
        moral: "밤도 자라도 개똥도 혼자서는 호랑이를 어쩌지 못했어요. 그런데 저마다 자기가 있던 자리에서 할 수 있는 일을 하나씩 하자 호랑이가 물러갔지요. 큰일은 큰 힘이 아니라 여럿의 작은 몫으로 되는 일이 많답니다.",
        question: "여럿이 힘을 모아 해낸 일에서 내 몫은 무엇이었나요?"
    }
];

function artFrame(src, emoji) {
    return `
        <div class="art-frame">
            <img src="images/${src}" alt="" onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';">
            <div class="art-fallback" style="display:none">${emoji}</div>
        </div>`;
}

function coverToc() {
    const item = s => `
        <button type="button" data-goto="${s.num}">
            <span class="toc-num">${s.num}</span>
            <span>${s.title.replace(/^\d+장 · /, '')}</span>
        </button>`;
    return `
        <nav class="cover-toc">
            <h2>차례</h2>
            ${CHAPTERS.map(item).join('')}
            <button type="button" data-goto-kind="quiz">
                <span class="toc-num">❓</span>
                <span>이야기 문제</span>
            </button>
        </nav>`;
}

function coverPage() {
    return `
        <div class="page page-cover">
            <div class="story-page-left story-page-left-full">
                ${artFrame('cover.png', '🥣')}
            </div>
            <div class="story-page-right">
                <h1>팥죽 할멈과 호랑이</h1>
                <p>팥죽 할멈과 호랑이는 지은이가 없는 구전 설화예요. 밤과 자라와 개똥과 절구와 멍석과 지게가 차례로 나서서 호랑이를 골탕 먹이는 이야기랍니다.</p>
                <p>부엌과 마당에 있던 물건들이 하나씩 힘을 보태는 짜임이 이 이야기의 재미예요. 하나하나는 보잘것없지만 자기가 있던 자리에서 할 수 있는 일을 하나씩 해내지요. 여럿이 힘을 모으는 이야기 가운데 우리나라에서 가장 널리 알려진 것이랍니다.</p>
                <p>팥죽은 동짓날에 쑤어 먹던 음식이에요. 붉은색이 나쁜 것을 물리친다고 여겨, 동지에 팥죽을 쑤어 대문이나 담에 뿌리는 풍습이 있었지요. 호랑이를 물리치는 이야기에 하필 팥죽이 나오는 것도 그런 까닭일 거예요.</p>
                ${coverToc()}
            </div>
        </div>`;
}

function spreadPage(chapter, beat, isFirst) {
    const badgeHtml = isFirst ? `<div class="spread-chapter-badge">${chapter.title}</div>` : '';
    const leftHtml = beat.left.map(p => `<p>${p}</p>`).join('');
    const rightHtml = beat.right.map(p => `<p>${p}</p>`).join('');
    return `
        <div class="page page-story">
            <div class="spread-art">
                ${badgeHtml}
                ${artFrame(beat.art, beat.emoji)}
            </div>
            <div class="spread-text">
                <div class="spread-text-left">${leftHtml}</div>
                <div class="spread-text-right">${rightHtml}</div>
            </div>
        </div>`;
}

function reflectionPage(chapter) {
    return `
        <div class="page page-reflection">
            <h2>이야기를 다 읽고</h2>
            <p class="reflection-moral">${chapter.moral}</p>
            <p class="reflection-question">${chapter.question}</p>
        </div>`;
}

const QUIZ = [
    { q: "할멈은 호랑이에게 무엇을 쑤어 주겠다고 했나요?", choices: ["떡국", "팥죽", "수제비"], answer: 1 },
    { q: "아궁이에 숨은 것은 무엇인가요?", choices: ["자라", "절구", "밤"], answer: 2 },
    { q: "물동이에 숨은 것은 무엇인가요?", choices: ["자라", "밤", "개똥"], answer: 0 },
    { q: "문 위에 올라앉은 것은 무엇인가요?", choices: ["멍석", "지게", "절구"], answer: 2 },
    { q: "호랑이를 둘둘 말아 버린 것은 무엇인가요?", choices: ["멍석", "이불", "그물"], answer: 0 },
    { q: "마지막에 호랑이를 지고 나간 것은 무엇인가요?", choices: ["수레", "지게", "가마"], answer: 1 }
];

function quizPage() {
    const items = QUIZ.map((item, i) => `
        <div class="quiz-item" data-qindex="${i}">
            <p class="quiz-question">${i + 1}. ${item.q}</p>
            <div class="quiz-choices">
                ${item.choices.map((c, ci) => `<button type="button" class="quiz-choice" data-choice="${ci}">${c}</button>`).join('')}
            </div>
        </div>`).join('');
    return `
        <div class="page page-quiz">
            <h2>이야기 문제</h2>
            <p class="quiz-intro-text" id="quizProgress">0 / 총 ${QUIZ.length}문항 완료</p>
            <div class="quiz-list">${items}</div>
        </div>`;
}

function endPage() {
    return `
        <div class="page page-end">
            ${artFrame('end.png', '🌟')}
            <h2>팥죽 할멈과 호랑이 이야기를 다 읽었어요!</h2>
            <a class="home-btn" href="../../../../../">학습 허브로 돌아가기</a>
        </div>`;
}

const PAGES = [
    { kind: 'cover' },
    ...CHAPTERS.flatMap(chapter => chapter.beats.map((beat, i) => ({ kind: 'spread', chapter, beat, isFirst: i === 0 }))),
    { kind: 'reflection', chapter: CHAPTERS[CHAPTERS.length - 1] },
    { kind: 'quiz' },
    { kind: 'end' }
];

const TWO_PAGE_KINDS = new Set(['spread', 'cover']);

let folioCounter = 0;
const FOLIOS = PAGES.map(p => {
    const width = TWO_PAGE_KINDS.has(p.kind) ? 2 : 1;
    const start = folioCounter + 1;
    folioCounter += width;
    return { start, width };
});

function renderPage(page) {
    switch (page.kind) {
        case 'cover':
            return coverPage();
        case 'spread':
            return spreadPage(page.chapter, page.beat, page.isFirst);
        case 'reflection':
            return reflectionPage(page.chapter);
        case 'quiz':
            return quizPage();
        case 'end':
            return endPage();
        default:
            return '';
    }
}

let current = 0;
let animating = false;

const spreadEl = document.getElementById('spread');
const prevBtn = document.getElementById('prevBtn');
const nextBtn = document.getElementById('nextBtn');
const indicatorEl = document.getElementById('pageIndicator');
const folioLeftEl = document.getElementById('folioLeft');
const folioRightEl = document.getElementById('folioRight');

function paint() {
    spreadEl.innerHTML = renderPage(PAGES[current]);
    prevBtn.disabled = current === 0;
    nextBtn.disabled = current === PAGES.length - 1;
    indicatorEl.textContent = `${current + 1} / ${PAGES.length}`;

    const folio = FOLIOS[current];
    folioLeftEl.classList.toggle('folio-center', folio.width === 1);
    if (folio.width === 2) {
        folioLeftEl.textContent = folio.start;
        folioRightEl.textContent = folio.start + 1;
        folioLeftEl.hidden = false;
        folioRightEl.hidden = false;
    } else {
        folioLeftEl.textContent = folio.start;
        folioLeftEl.hidden = false;
        folioRightEl.hidden = true;
    }

    spreadEl.querySelectorAll('[data-goto]').forEach(btn => {
        btn.addEventListener('click', () => {
            const chapterNum = Number(btn.dataset.goto);
            const idx = PAGES.findIndex(p => p.kind === 'spread' && p.chapter.num === chapterNum && p.isFirst);
            if (idx >= 0) goTo(idx);
        });
    });
    spreadEl.querySelectorAll('[data-goto-kind]').forEach(btn => {
        btn.addEventListener('click', () => {
            const idx = PAGES.findIndex(p => p.kind === btn.dataset.gotoKind);
            if (idx >= 0) goTo(idx);
        });
    });

    if (PAGES[current].kind === 'quiz') {
        initQuiz();
    }
}

function initQuiz() {
    let answeredCount = 0;
    const progressEl = document.getElementById('quizProgress');

    spreadEl.querySelectorAll('.quiz-item').forEach(item => {
        const qi = Number(item.dataset.qindex);
        const q = QUIZ[qi];
        item.querySelectorAll('.quiz-choice').forEach(btn => {
            btn.addEventListener('click', () => {
                if (item.classList.contains('graded')) return;
                item.classList.add('graded');
                const chosen = Number(btn.dataset.choice);
                item.querySelectorAll('.quiz-choice').forEach(b => {
                    const ci = Number(b.dataset.choice);
                    if (ci === q.answer) b.classList.add('correct');
                    else if (ci === chosen) b.classList.add('incorrect');
                });
                answeredCount++;
                progressEl.textContent = `${answeredCount} / 총 ${QUIZ.length}문항 완료`;
            });
        });
    });
}

function goTo(index) {
    if (animating || index === current || index < 0 || index >= PAGES.length) return;
    animating = true;
    const dir = index > current ? 'flip-next' : 'flip-prev';
    spreadEl.classList.add(dir);
    setTimeout(() => {
        current = index;
        paint();
    }, 230);
    setTimeout(() => {
        spreadEl.classList.remove('flip-next', 'flip-prev');
        animating = false;
    }, 480);
}

prevBtn.addEventListener('click', () => goTo(current - 1));
nextBtn.addEventListener('click', () => goTo(current + 1));

document.getElementById('tocLink').addEventListener('click', () => {
    // 그림책은 차례가 표지에 붙어 있다.
    const idx = PAGES.findIndex(p => p.kind === 'toc');
    goTo(idx >= 0 ? idx : 0);
});

document.addEventListener('keydown', (e) => {
    if (e.key === 'ArrowRight') goTo(current + 1);
    if (e.key === 'ArrowLeft') goTo(current - 1);
});

paint();
