const CHAPTERS = [
    {
        num: 1,
        title: "1장 · 바다를 건너온 궤",
        beats: [
            {
                art: "01-sea.png",
                emoji: "🌊",
                left: [
                    "아주 먼 옛날, 바다 건너 어느 나라에서 있었던 일이에요.",
                    "왕비가 아이 대신 커다란 알을 낳았습니다."
                ],
                right: [
                    "신하들이 웅성거렸어요.",
                    "\"상서롭지 못합니다.\"",
                    "왕은 한참을 말이 없었지요."
                ]
            },
            {
                art: "02-chest.png",
                emoji: "📦",
                left: [
                    "왕은 알을 비단에 싸서 나무 궤에 넣었어요.",
                    "궤 안에는 보물과 종들도 함께 넣었지요."
                ],
                right: [
                    "\"어디든 좋으니 살 만한 땅에 닿거라.\"",
                    "궤는 바다로 떠밀려 나갔습니다.",
                    "파도가 궤를 밀고 또 밀었어요."
                ]
            },
            {
                art: "03-magpie.png",
                emoji: "🐦",
                left: [
                    "며칠 뒤, 아진포라는 바닷가에서였어요.",
                    "까치 떼가 한자리에 모여 요란하게 울었습니다."
                ],
                right: [
                    "\"깍깍! 깍깍!\"",
                    "고기잡이 할머니가 이상해서 가 보았지요.",
                    "물가에 커다란 궤 하나가 떠 있었답니다."
                ]
            },
            {
                art: "04-open.png",
                emoji: "👦",
                left: [
                    "할머니가 밧줄을 걸어 궤를 끌어올렸어요.",
                    "끙— 하고 뚜껑을 열었지요."
                ],
                right: [
                    "안에서 사내아이 하나가 눈을 비비며 일어났습니다.",
                    "\"…여기가 어디예요?\"",
                    "할머니는 그 자리에 주저앉고 말았어요."
                ]
            },
            {
                art: "05-name.png",
                emoji: "🏠",
                left: [
                    "할머니는 아이를 집으로 데려갔습니다.",
                    "\"이름이 없으면 부를 수가 없지.\""
                ],
                right: [
                    "까치가 알려 주었으니 성은 석이라 했어요. 궤를 풀고 나왔으니 이름은 탈해라 했지요.",
                    "그날부터 할머니와 아이는 함께 살았답니다."
                ]
            }
        ]
    },
    {
        num: 2,
        title: "2장 · 토함산에서 내려다본 자리",
        beats: [
            {
                art: "06-grow.png",
                emoji: "🎣",
                left: [
                    "탈해는 고기를 잡고 그물을 깁고 나무를 했어요.",
                    "그러면서 틈만 나면 책을 읽었지요."
                ],
                right: [
                    "\"저 아이는 손도 야무진데 머리도 비상하구나.\"",
                    "마을 사람들이 혀를 내둘렀습니다.",
                    "탈해는 별자리도 땅 생김새도 훤히 알게 되었어요."
                ]
            },
            {
                art: "07-mountain.png",
                emoji: "⛰️",
                left: [
                    "어른이 된 탈해가 토함산에 올랐습니다.",
                    "이레 동안 산꼭대기에서 아래를 내려다보았지요."
                ],
                right: [
                    "\"살 만한 자리가 어디일까.\"",
                    "마침내 한 곳에 눈이 멎었어요.",
                    "초승달처럼 휘어진 언덕이었습니다."
                ]
            },
            {
                art: "08-house.png",
                emoji: "🏡",
                left: [
                    "그런데 그 자리에는 이미 큰 집이 있었어요.",
                    "호공이라는 사람의 집이었지요."
                ],
                right: [
                    "탈해는 한참을 담 밖에 서 있었습니다.",
                    "'저 자리가 아니면 안 되는데.'",
                    "그러다 그만 꾀를 하나 내고 말았어요."
                ]
            },
            {
                art: "09-trick.png",
                emoji: "⚒️",
                left: [
                    "탈해는 밤중에 담 밑에 숯과 숫돌을 묻었어요.",
                    "이튿날 아침 대문을 두드렸지요."
                ],
                right: [
                    "\"여기는 본디 우리 조상이 살던 집입니다.\"",
                    "\"무슨 소리요!\"",
                    "결국 두 사람은 관아로 갔답니다."
                ]
            },
            {
                art: "10-court.png",
                emoji: "⚖️",
                left: [
                    "\"무엇으로 증명하겠느냐?\"",
                    "\"저희 조상은 대장장이였습니다. 담 밑을 파 보십시오.\""
                ],
                right: [
                    "정말로 숯과 숫돌이 나왔어요.",
                    "관리가 고개를 끄덕였습니다.",
                    "\"집은 이 사람 것이다.\""
                ]
            }
        ]
    },
    {
        num: 3,
        title: "3장 · 돌려준 집",
        beats: [
            {
                art: "11-return.png",
                emoji: "🔑",
                left: [
                    "그날 저녁, 탈해가 호공을 찾아갔어요.",
                    "그러고는 대문 열쇠를 도로 내밀었습니다."
                ],
                right: [
                    "\"이 집은 어르신 것입니다.\"",
                    "\"…이게 무슨 짓이오?\"",
                    "\"제가 정말 갖고 싶었던 건 집이 아니었습니다.\""
                ]
            },
            {
                art: "12-friend.png",
                emoji: "🤝",
                left: [
                    "\"그럼 무엇이었소?\"",
                    "탈해가 씩 웃었어요."
                ],
                right: [
                    "\"이 자리를 알아본 사람이 또 있는지 보고 싶었습니다.\"",
                    "호공은 한참을 껄껄 웃었지요.",
                    "그날부터 둘은 둘도 없는 벗이 되었답니다."
                ]
            },
            {
                art: "13-king.png",
                emoji: "📜",
                left: [
                    "그 이야기가 임금의 귀에 들어갔어요.",
                    "임금이 탈해를 불러 앉혔습니다."
                ],
                right: [
                    "\"머리도 좋고 물러설 줄도 아는구나.\"",
                    "임금은 탈해를 사위로 삼았지요.",
                    "훗날 탈해는 신라의 임금이 되었답니다."
                ]
            },
            {
                art: "14-shore.png",
                emoji: "🌅",
                left: [
                    "임금이 된 탈해가 아진포를 다시 찾았어요.",
                    "할머니의 낡은 집은 그대로였습니다."
                ],
                right: [
                    "탈해는 신을 벗고 물가로 내려갔지요.",
                    "까치 몇 마리가 그 위를 맴돌았어요.",
                    "\"내가 여기서 시작했지.\""
                ]
            }
        ],
        moral: "탈해는 꾀로 집을 얻었지만 그것을 갖지는 않았어요. 이겨 놓고 돌려주었기 때문에 집 한 채 대신 사람 하나를 얻었지요. 이기는 것보다 이긴 다음에 무엇을 하느냐가 더 오래 남는답니다.",
        question: "다투어 이겨 본 적이 있나요? 그다음에는 어떻게 했나요?"
    }
];

function artFrame(src, emoji) {
    return `
        <div class="art-frame">
            <img src="images/${src}" alt="" onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';">
            <div class="art-fallback" style="display:none">${emoji}</div>
        </div>`;
}

function coverPage() {
    return `
        <div class="page page-cover">
            <div class="story-page-left story-page-left-full">
                ${artFrame('cover.png', '🌊')}
            </div>
            <div class="story-page-right">
                <h1>석탈해</h1>
                <p>석탈해 이야기는 삼국유사와 삼국사기에 함께 실려 전해져요. 신라의 네 번째 임금이 된 사람에 관한 이야기랍니다.</p>
                <p>탈해는 궤를 풀고 나왔다 해서 붙은 이름이라고 전해져요. 성인 석은 까치 작 자에서 새 조 자를 떼어 냈다고 하지요. 까치가 궤를 찾아 주었기 때문이랍니다.</p>
                <p>신라에서는 박씨, 석씨, 김씨 세 집안이 번갈아 임금을 냈어요. 석탈해는 그 가운데 석씨 집안의 첫 임금이랍니다.</p>
                <p>바다에 떠내려온 궤에서 사람이 나오는 이야기는 제주도와 남해 여러 섬에도 비슷하게 남아 있어요. 바다 건너에서 온 사람을 그렇게 기억한 것으로 보는 학자들이 많지요.</p>
            </div>
        </div>`;
}

function tocPage() {
    const itemHtml = s => `
        <li>
            <button type="button" data-goto="${s.num}">
                <span class="toc-num">${s.num}</span>
                <span>
                    <strong>${s.title.replace(/^\d+장 · /, '')}</strong>
                </span>
            </button>
        </li>`;
    const quizItemHtml = `
        <li>
            <button type="button" data-goto-kind="quiz">
                <span class="toc-num">❓</span>
                <span>
                    <strong>이야기 문제</strong>
                </span>
            </button>
        </li>`;
    const half = Math.ceil(CHAPTERS.length / 2);
    const leftItems = CHAPTERS.slice(0, half).map(itemHtml).join('');
    const rightItems = CHAPTERS.slice(half).map(itemHtml).join('') + quizItemHtml;
    return `
        <div class="page page-toc">
            <div class="story-page-left">
                <h2>차례</h2>
                <ul class="toc-list">${leftItems}</ul>
            </div>
            <div class="story-page-right">
                <ul class="toc-list">${rightItems}</ul>
            </div>
        </div>`;
}

function spreadPage(chapter, beat, isFirst) {
    const badgeHtml = isFirst ? `<div class="spread-chapter-badge">${chapter.title}</div>` : '';
    const leftHtml = beat.left.map(p => `<p>${p}</p>`).join('');
    const rightHtml = beat.right.map(p => `<p>${p}</p>`).join('');
    return `
        <div class="page page-story">
            <div class="spread-art">
                ${badgeHtml}
                ${artFrame(beat.art, beat.emoji)}
            </div>
            <div class="spread-text">
                <div class="spread-text-left">${leftHtml}</div>
                <div class="spread-text-right">${rightHtml}</div>
            </div>
        </div>`;
}

function reflectionPage(chapter) {
    return `
        <div class="page page-reflection">
            <h2>이야기를 다 읽고</h2>
            <p class="reflection-moral">${chapter.moral}</p>
            <p class="reflection-question">${chapter.question}</p>
        </div>`;
}

const QUIZ = [
    { q: "탈해는 무엇에 담겨 바다를 건너왔나요?", choices: ["작은 고깃배", "커다란 나무 궤", "속을 판 통나무"], answer: 1 },
    { q: "궤가 닿은 것을 알려 준 것은 무엇인가요?", choices: ["집집마다 개가 짖어서", "절에서 종이 울려서", "까치 떼가 울어서"], answer: 2 },
    { q: "탈해를 거두어 기른 사람은 누구인가요?", choices: ["바닷가의 할머니", "산속 절의 스님", "그 마을의 촌장"], answer: 0 },
    { q: "탈해가 토함산에 올라간 까닭은 무엇인가요?", choices: ["귀한 약초를 캐려고", "사람들을 피해 숨으려고", "살 곳을 살펴보려고"], answer: 2 },
    { q: "탈해가 호공의 집을 두고 한 일은 무엇인가요?", choices: ["되돌려주고 벗으로 삼았다", "그대로 제 집으로 차지했다", "제값을 치르고 사들였다"], answer: 0 },
    { q: "탈해는 나중에 무엇이 되었나요?", choices: ["가야의 임금", "신라의 임금", "배를 만드는 목수"], answer: 1 }
];

function quizPage() {
    const items = QUIZ.map((item, i) => `
        <div class="quiz-item" data-qindex="${i}">
            <p class="quiz-question">${i + 1}. ${item.q}</p>
            <div class="quiz-choices">
                ${item.choices.map((c, ci) => `<button type="button" class="quiz-choice" data-choice="${ci}">${c}</button>`).join('')}
            </div>
        </div>`).join('');
    return `
        <div class="page page-quiz">
            <h2>이야기 문제</h2>
            <p class="quiz-intro-text" id="quizProgress">0 / 총 ${QUIZ.length}문항 완료</p>
            <div class="quiz-list">${items}</div>
        </div>`;
}

function endPage() {
    return `
        <div class="page page-end">
            ${artFrame('end.png', '🌟')}
            <h2>석탈해 이야기를 다 읽었어요!</h2>
            <a class="home-btn" href="../../../../../">학습 허브로 돌아가기</a>
        </div>`;
}

function historyPage() {
    return `
        <div class="page page-history">
            <h2>여기서부터는 진짜 역사</h2>
            <p class="history-note">앞까지가 이야기였고, 여기부터는 기록에 남은 사실이에요.</p>
            <hr>
            <div class="history-body">
                <p>신라 초기에는 박씨, 석씨, 김씨 세 집안에서 번갈아 임금이 나왔어요. 석탈해는 서기 57년부터 80년까지 임금 자리에 있었다고 삼국사기에 적혀 있답니다.</p>
                <p>이야기 속 호공은 실제로 삼국사기에 여러 번 나오는 사람이에요. 탈해왕 때 나라의 큰 벼슬인 대보 자리에 올랐다고 적혀 있지요.</p>
                <p>탈해가 숯과 숫돌을 묻어 두었다는 대목은 그가 대장장이 집안 출신이라는 뜻으로 읽혀요. 쇠를 다루는 기술을 가진 사람들이 바다를 건너와 힘을 얻은 일을 이야기로 남긴 것으로 보는 학자가 많답니다.</p>
                <p>경주 토함산에는 지금도 석탈해를 모시는 사당이 있어요. 탈해왕릉이라 불리는 무덤도 경주에 남아 있지요.</p>
            </div>
        </div>`;
}

const PAGES = [
    { kind: 'cover' },
    { kind: 'toc' },
    ...CHAPTERS.flatMap(chapter => chapter.beats.map((beat, i) => ({ kind: 'spread', chapter, beat, isFirst: i === 0 }))),
    { kind: 'reflection', chapter: CHAPTERS[CHAPTERS.length - 1] },
    { kind: 'history' },
    { kind: 'quiz' },
    { kind: 'end' }
];

const TWO_PAGE_KINDS = new Set(['spread', 'toc', 'cover']);

let folioCounter = 0;
const FOLIOS = PAGES.map(p => {
    const width = TWO_PAGE_KINDS.has(p.kind) ? 2 : 1;
    const start = folioCounter + 1;
    folioCounter += width;
    return { start, width };
});

function renderPage(page) {
    switch (page.kind) {
        case 'cover':
            return coverPage();
        case 'toc':
            return tocPage();
        case 'spread':
            return spreadPage(page.chapter, page.beat, page.isFirst);
        case 'reflection':
            return reflectionPage(page.chapter);
        case 'history':
            return historyPage();
        case 'quiz':
            return quizPage();
        case 'end':
            return endPage();
        default:
            return '';
    }
}

let current = 0;
let animating = false;

const spreadEl = document.getElementById('spread');
const prevBtn = document.getElementById('prevBtn');
const nextBtn = document.getElementById('nextBtn');
const indicatorEl = document.getElementById('pageIndicator');
const folioLeftEl = document.getElementById('folioLeft');
const folioRightEl = document.getElementById('folioRight');

function paint() {
    spreadEl.innerHTML = renderPage(PAGES[current]);
    prevBtn.disabled = current === 0;
    nextBtn.disabled = current === PAGES.length - 1;
    indicatorEl.textContent = `${current + 1} / ${PAGES.length}`;

    const folio = FOLIOS[current];
    folioLeftEl.classList.toggle('folio-center', folio.width === 1);
    if (folio.width === 2) {
        folioLeftEl.textContent = folio.start;
        folioRightEl.textContent = folio.start + 1;
        folioLeftEl.hidden = false;
        folioRightEl.hidden = false;
    } else {
        folioLeftEl.textContent = folio.start;
        folioLeftEl.hidden = false;
        folioRightEl.hidden = true;
    }

    spreadEl.querySelectorAll('[data-goto]').forEach(btn => {
        btn.addEventListener('click', () => {
            const chapterNum = Number(btn.dataset.goto);
            const idx = PAGES.findIndex(p => p.kind === 'spread' && p.chapter.num === chapterNum && p.isFirst);
            if (idx >= 0) goTo(idx);
        });
    });
    spreadEl.querySelectorAll('[data-goto-kind]').forEach(btn => {
        btn.addEventListener('click', () => {
            const idx = PAGES.findIndex(p => p.kind === btn.dataset.gotoKind);
            if (idx >= 0) goTo(idx);
        });
    });

    if (PAGES[current].kind === 'quiz') {
        initQuiz();
    }
}

function initQuiz() {
    let answeredCount = 0;
    const progressEl = document.getElementById('quizProgress');

    spreadEl.querySelectorAll('.quiz-item').forEach(item => {
        const qi = Number(item.dataset.qindex);
        const q = QUIZ[qi];
        item.querySelectorAll('.quiz-choice').forEach(btn => {
            btn.addEventListener('click', () => {
                if (item.classList.contains('graded')) return;
                item.classList.add('graded');
                const chosen = Number(btn.dataset.choice);
                item.querySelectorAll('.quiz-choice').forEach(b => {
                    const ci = Number(b.dataset.choice);
                    if (ci === q.answer) b.classList.add('correct');
                    else if (ci === chosen) b.classList.add('incorrect');
                });
                answeredCount++;
                progressEl.textContent = `${answeredCount} / 총 ${QUIZ.length}문항 완료`;
            });
        });
    });
}

function goTo(index) {
    if (animating || index === current || index < 0 || index >= PAGES.length) return;
    animating = true;
    const dir = index > current ? 'flip-next' : 'flip-prev';
    spreadEl.classList.add(dir);
    setTimeout(() => {
        current = index;
        paint();
    }, 230);
    setTimeout(() => {
        spreadEl.classList.remove('flip-next', 'flip-prev');
        animating = false;
    }, 480);
}

prevBtn.addEventListener('click', () => goTo(current - 1));
nextBtn.addEventListener('click', () => goTo(current + 1));

document.getElementById('tocLink').addEventListener('click', () => {
    const idx = PAGES.findIndex(p => p.kind === 'toc');
    if (idx >= 0) goTo(idx);
});

document.addEventListener('keydown', (e) => {
    if (e.key === 'ArrowRight') goTo(current + 1);
    if (e.key === 'ArrowLeft') goTo(current - 1);
});

paint();
