const CHAPTERS = [
    {
        num: 1,
        title: "1장 · 벽에 걸린 주머니",
        beats: [
            {
                art: "01-boy.png",
                emoji: "👂",
                left: [
                    "옛날 어느 집에 이야기를 무척 좋아하는 도련님이 살았습니다.",
                    "사랑방에 어른들이 모이면 제일 먼저 달려가 앉았지요.",
                    "누가 이야기를 시작하면 밥 먹다 말고 숟가락을 놓을 정도였지요.",
                    "밤이 깊어도 졸지 않았어요."
                ],
                right: [
                    "\"그래서요? 그다음은요?\"",
                    "도련님은 눈을 반짝이며 재촉했어요.",
                    "마을 어른들은 그런 도련님을 귀여워해 이야기를 자꾸 들려주었답니다.",
                    "도련님은 그 이야기를 하나도 잊지 않았지요."
                ]
            },
            {
                art: "02-pouch.png",
                emoji: "📿",
                left: [
                    "그런데 도련님에게는 이상한 버릇이 하나 있었어요.",
                    "아무도 그 버릇을 알지 못했어요.",
                    "이야기를 다 듣고 나면 벽에 걸린 주머니에 대고 후 하고 불어 넣는 것이었습니다.",
                    "벽에 걸린 낡은 비단 주머니였지요."
                ],
                right: [
                    "\"이건 내 것이야. 아무한테도 안 줄 테다.\"",
                    "도련님은 주머니를 톡톡 두드렸어요.",
                    "그러고는 주머니 끈을 꽉 조여 매었지요.",
                    "그 이야기는 누구에게도 들려주지 않았답니다."
                ]
            },
            {
                art: "03-years.png",
                emoji: "⏳",
                left: [
                    "그렇게 열 해가 지났어요. 주머니는 터질 듯이 불룩해졌습니다.",
                    "도련님은 어느새 어른이 되었어요.",
                    "주머니가 끈이 끊어질 만큼 팽팽했지요."
                ],
                right: [
                    "도련님은 그동안 단 한 사람에게도 이야기를 들려준 적이 없었어요. 듣기만 하고, 넣기만 하고, 조여 매기만 했지요.",
                    "아무리 좋은 이야기를 들어도 마찬가지였어요.",
                    "주머니는 갈수록 무거워졌답니다."
                ]
            },
            {
                art: "04-angry.png",
                emoji: "😤",
                left: [
                    "주머니 속은 어두컴컴하고 답답했어요.",
                    "숨 쉴 틈도 없이 빽빽했지요.",
                    "이야기들은 서로 어깨를 부딪히며 투덜거렸습니다."
                ],
                right: [
                    "\"이게 사는 거냐?\"",
                    "누군가 화가 나서 소리쳤어요.",
                    "\"이야기는 사람 입에서 입으로 흘러 다녀야 하는 것을!\"",
                    "\"저 도련님이 우리를 십 년째 가둬 두고 있어!\"",
                    "주머니 안이 부글부글 끓었답니다."
                ]
            }
        ]
    },
    {
        num: 2,
        title: "2장 · 엿들은 하인",
        beats: [
            {
                art: "05-plot.png",
                emoji: "🗣️",
                left: [
                    "도련님이 장가가는 날을 앞둔 밤이었어요. 주머니 안이 웅성웅성 시끄러웠습니다.",
                    "온 집안이 잔치 준비로 부산했지요.",
                    "\"내일 그 애가 신부 집으로 간다지?\"",
                    "그런데 벽에 걸린 주머니가 들썩거렸어요."
                ],
                right: [
                    "\"이참에 혼쭐을 내 주자!\"",
                    "이야기들이 머리를 맞대었어요.",
                    "\"좋아. 이렇게 하자…\"",
                    "주머니 안에서 킬킬거리는 소리가 났답니다."
                ]
            },
            {
                art: "06-listen.png",
                emoji: "😳",
                left: [
                    "마침 문밖을 지나던 늙은 하인이 그 소리를 들었어요.",
                    "늦게까지 일하고 돌아가던 참이었어요.",
                    "하인은 숨을 죽이고 벽에 귀를 붙였습니다.",
                    "처음에는 제 귀를 의심했지요."
                ],
                right: [
                    "\"가는 길 우물가에 내가 독을 풀어 두마.\"",
                    "이야기들이 하나씩 제 몫을 정했어요.",
                    "\"나는 딸기밭이 되어 기다리지.\"",
                    "\"나는 신방 구석에 숨어 있겠다.\"",
                    "하인은 밤새 한숨도 자지 못했답니다."
                ]
            },
            {
                art: "07-well.png",
                emoji: "🪣",
                left: [
                    "이튿날 아침, 하인은 기어이 말고삐를 잡겠다고 나섰어요.",
                    "다른 하인들이 다 말렸는데도 말이지요.",
                    "혼례 행렬이 우물가에 이르자 도련님이 목이 마르다고 했습니다.",
                    "말고삐를 쥔 손에 힘이 잔뜩 들어갔어요."
                ],
                right: [
                    "\"물 한 바가지만 뜨거라.\"",
                    "하인이 딱 잘라 말했어요.",
                    "\"안 됩니다!\"",
                    "하인은 말을 멈추지 않고 그대로 지나쳐 버렸지요.",
                    "도련님이 눈을 부라렸답니다."
                ]
            },
            {
                art: "08-berry.png",
                emoji: "🍓",
                left: [
                    "얼마 뒤 길가에 탐스러운 딸기밭이 나타났어요.",
                    "빨갛게 익은 딸기가 주렁주렁했어요.",
                    "\"저것 참 먹음직스럽구나. 좀 따 오너라.\"",
                    "도련님이 말 위에서 손가락으로 가리켰지요."
                ],
                right: [
                    "\"그것도 안 됩니다!\"",
                    "하인은 뒤도 돌아보지 않았어요.",
                    "하인은 못 들은 척 말을 더 빨리 몰았습니다. 도련님의 얼굴이 붉으락푸르락해졌어요.",
                    "\"오늘 이놈이 아주 단단히 미쳤구나!\""
                ]
            }
        ]
    },
    {
        num: 3,
        title: "3장 · 신방 구석의 것",
        beats: [
            {
                art: "09-scold.png",
                emoji: "😠",
                left: [
                    "신부 집에 닿자 도련님이 하인을 불러 세웠어요.",
                    "잔칫상이 차려지고 사람들이 북적였어요.",
                    "\"오늘 네가 어찌 이리 무례하냐!\"",
                    "도련님의 목소리가 몹시 사나웠지요."
                ],
                right: [
                    "하인은 고개만 조아릴 뿐 아무 말도 하지 않았습니다.",
                    "\"주머니 속 이야기들이 그러더이다\"라고 할 수는 없었으니까요.",
                    "말해 봐야 믿어 줄 리 없었으니까요.",
                    "하인은 신방 쪽만 자꾸 살폈답니다."
                ]
            },
            {
                art: "10-room.png",
                emoji: "🕯️",
                left: [
                    "밤이 깊어 신방에 불이 켜졌어요.",
                    "신랑 신부가 방으로 들어갔어요.",
                    "그때 하인이 손에 몽둥이를 들고 방문을 벌컥 열었습니다.",
                    "하인은 부엌에서 몽둥이 하나를 집어 들었지요."
                ],
                right: [
                    "\"이놈이 미쳤구나!\"",
                    "사람들이 하인을 붙잡으려고 달려들었어요.",
                    "집안이 발칵 뒤집혔지요. 그래도 하인은 곧장 방 안으로 들어갔어요.",
                    "누가 말려도 소용없었지요."
                ]
            },
            {
                art: "11-snake.png",
                emoji: "🐍",
                left: [
                    "하인이 이불 밑 구석을 몽둥이로 툭 건드렸어요.",
                    "이불이 조금 부풀어 있었어요.",
                    "그러자 스르륵—",
                    "커다란 뱀 한 마리가 미끄러져 나와 창밖으로 달아났습니다.",
                    "그 순간이었지요."
                ],
                right: [
                    "방 안이 물을 끼얹은 듯 조용해졌어요. 도련님의 얼굴이 하얗게 질렸지요.",
                    "어른 팔뚝만 한 놈이었어요.",
                    "하인이 아니었으면 큰일 날 뻔한 것이었지요."
                ]
            },
            {
                art: "12-tell.png",
                emoji: "🔥",
                left: [
                    "하인이 그제야 지난밤에 들은 이야기를 다 털어놓았어요. 도련님은 한참을 말이 없었습니다.",
                    "우물 이야기부터 딸기밭 이야기까지요.",
                    "집에 돌아온 도련님은 주머니 끈을 풀어 활짝 열었어요.",
                    "도련님은 그제야 모든 것을 알았어요."
                ],
                right: [
                    "그날부터 도련님은 만나는 사람마다 이야기를 들려주었답니다.",
                    "이야기들이 하나둘 밖으로 흘러나왔지요.",
                    "어릴 적 들은 그 이야기들을, 하나도 빠짐없이.",
                    "주머니는 다시 홀쭉해졌답니다."
                ]
            }
        ],
        moral: "이야기는 손에 쥐고 있으면 썩고, 남에게 건네면 살아나요. 도련님이 십 년 동안 모은 것은 이야기가 아니라 답답함이었던 셈이지요.",
        question: "혼자만 알고 있는 좋은 것을 누군가에게 나눠 준 적이 있나요?"
    }
];

function artFrame(src, emoji) {
    return `
        <div class="art-frame">
            <img src="images/${src}" alt="" onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';">
            <div class="art-fallback" style="display:none">${emoji}</div>
        </div>`;
}

function coverToc() {
    const item = s => `
        <button type="button" data-goto="${s.num}">
            <span class="toc-num">${s.num}</span>
            <span>${s.title.replace(/^\d+장 · /, '')}</span>
        </button>`;
    return `
        <nav class="cover-toc">
            <h2>차례</h2>
            ${CHAPTERS.map(item).join('')}
            <button type="button" data-goto-kind="quiz">
                <span class="toc-num">❓</span>
                <span>이야기 문제</span>
            </button>
        </nav>`;
}

function coverPage() {
    return `
        <div class="page page-cover">
            <div class="story-page-left story-page-left-full">
                ${artFrame('cover.png', '📿')}
            </div>
            <div class="story-page-right">
                <h1>이야기 주머니</h1>
                <p>이야기 주머니는 지은이가 없는 구전 설화예요. 이야기를 남에게 들려주지 않고 가두어 두면 어떻게 되는지를 다룬 이야기랍니다.</p>
                <p>옛날에는 이야기가 곧 재산이었어요. 책이 귀하던 시절이라 이야기는 사람의 입에서 입으로만 옮겨 다녔지요. 그러니 듣기만 하고 남에게 들려주지 않는 사람은, 이야기가 흘러가는 길을 막는 사람이었던 셈이에요.</p>
                <p>이 이야기가 재미있는 것은 이야기 자신이 등장인물이 된다는 점이에요. 갇혀 지낸 이야기들이 화를 내고 말을 주고받지요. 이야기에 대한 이야기라, 옛사람들이 이야기를 어떻게 여겼는지가 그대로 담겨 있답니다.</p>
                ${coverToc()}
            </div>
        </div>`;
}

function spreadPage(chapter, beat, isFirst) {
    const badgeHtml = isFirst ? `<div class="spread-chapter-badge">${chapter.title}</div>` : '';
    const leftHtml = beat.left.map(p => `<p>${p}</p>`).join('');
    const rightHtml = beat.right.map(p => `<p>${p}</p>`).join('');
    return `
        <div class="page page-story">
            <div class="spread-art">
                ${badgeHtml}
                ${artFrame(beat.art, beat.emoji)}
            </div>
            <div class="spread-text">
                <div class="spread-text-left">${leftHtml}</div>
                <div class="spread-text-right">${rightHtml}</div>
            </div>
        </div>`;
}

function reflectionPage(chapter) {
    return `
        <div class="page page-reflection">
            <h2>이야기를 다 읽고</h2>
            <p class="reflection-moral">${chapter.moral}</p>
            <p class="reflection-question">${chapter.question}</p>
        </div>`;
}

const QUIZ = [
    { q: "도련님은 들은 이야기를 어떻게 했나요?", choices: ["책에 적어 두었다", "주머니에 가두었다", "금세 잊어버렸다"], answer: 1 },
    { q: "주머니 속 이야기들은 왜 화가 났나요?", choices: ["주머니가 좁아서", "서로 다투어서", "밖으로 못 나가서"], answer: 2 },
    { q: "이야기들의 말을 엿들은 사람은 누구인가요?", choices: ["늙은 하인", "도련님 어머니", "이웃집 아이"], answer: 0 },
    { q: "하인은 첫 번째로 무엇을 막았나요?", choices: ["말에서 내리는 것", "문을 여는 것", "우물물을 마시는 것"], answer: 2 },
    { q: "혼례 치르는 방에서 하인이 찾아낸 것은 무엇인가요?", choices: ["구석에 숨은 뱀", "깨진 그릇 조각", "불붙은 등잔"], answer: 0 },
    { q: "그 뒤로 도련님은 어떻게 했나요?", choices: ["주머니를 새로 샀다", "이야기를 들려주었다", "이야기를 안 들었다"], answer: 1 }
];

function quizPage() {
    const items = QUIZ.map((item, i) => `
        <div class="quiz-item" data-qindex="${i}">
            <p class="quiz-question">${i + 1}. ${item.q}</p>
            <div class="quiz-choices">
                ${item.choices.map((c, ci) => `<button type="button" class="quiz-choice" data-choice="${ci}">${c}</button>`).join('')}
            </div>
        </div>`).join('');
    return `
        <div class="page page-quiz">
            <h2>이야기 문제</h2>
            <p class="quiz-intro-text" id="quizProgress">0 / 총 ${QUIZ.length}문항 완료</p>
            <div class="quiz-list">${items}</div>
        </div>`;
}

function endPage() {
    return `
        <div class="page page-end">
            ${artFrame('end.png', '🌟')}
            <h2>이야기 주머니 이야기를 다 읽었어요!</h2>
            <a class="home-btn" href="../../../../../">학습 허브로 돌아가기</a>
        </div>`;
}

/* 읽고 나서 — 세계명작 트랙과 같은 형식이다. 동화틀은 쪽을 재서 나누지 않으므로
   펼침면마다 왼쪽·오른쪽 글을 손으로 나누어 둔다. */
const AFTERWORD = {
    title: '읽고 나서',
    emoji: '🗣️',
    spreads: [
        {
            art: 'end.png',
            left: [
                "이 이야기는 이야기에 대한 이야기입니다. 옛이야기 가운데 이런 것은 흔치 않습니다. 이야기를 하는 사람들이 스스로를 두고 지은 셈이지요.",
                "옛날에는 이야기가 책에 있지 않고 사람에게 있었습니다. 겨울밤에 한 사람이 풀어놓으면 듣던 사람이 다음 마을에서 다시 풀어놓았습니다. 그렇게 살아 움직이던 것입니다.",
                "다시 보면 도련님은 이야기를 아낀 것이 아닙니다. 가둔 것입니다. 들은 것을 하나도 내놓지 않고 주머니에 넣어 벽에 걸어 두었습니다.",
                "그러니 주머니 속에서 이야기들이 성이 난 것은 이야기가 원래 그런 것이기 때문입니다. 이야기는 옮겨 다녀야 삽니다."
            ],
            right: [
                "여러분이 지금 읽고 있는 이 책의 이야기들도 누군가가 입으로 옮겨 준 덕분에 여기까지 왔습니다.",
                "여러분이 다른 사람에게 옮겨 주고 싶은 이야기가 있나요? 답은 적어 두지 않겠습니다."
            ]
        }
    ]
};

function afterPage(spread, isFirst) {
    const head = isFirst ? `<h2>${AFTERWORD.title}</h2>` : '';
    // 그림은 오른쪽 위 모서리에 꽉 붙인다. 글은 그 아래로 이어진다.
    const art = spread.art ? `<div class="after-art">${artFrame(spread.art, AFTERWORD.emoji)}</div>` : '';
    const col = (ps) => ps.map(t => `<p>${t}</p>`).join('');
    return `
        <div class="page page-after">
            <div class="after-col after-col-left">
                ${head}
                ${col(spread.left)}
            </div>
            <div class="after-col after-col-right${spread.art ? ' after-col-image' : ''}">
                ${art}
                ${col(spread.right)}
            </div>
        </div>`;
}

const PAGES = [
    { kind: 'cover' },
    ...CHAPTERS.flatMap(chapter => chapter.beats.map((beat, i) => ({ kind: 'spread', chapter, beat, isFirst: i === 0 }))),
    { kind: 'reflection', chapter: CHAPTERS[CHAPTERS.length - 1] },
    { kind: 'quiz' },
    ...AFTERWORD.spreads.map((spread, i) => ({ kind: 'after', spread, isFirst: i === 0 })),
    { kind: 'end' }
];

const TWO_PAGE_KINDS = new Set(['spread', 'cover', 'after']);

let folioCounter = 0;
const FOLIOS = PAGES.map(p => {
    const width = TWO_PAGE_KINDS.has(p.kind) ? 2 : 1;
    const start = folioCounter + 1;
    folioCounter += width;
    return { start, width };
});

function renderPage(page) {
    switch (page.kind) {
        case 'cover':
            return coverPage();
        case 'spread':
            return spreadPage(page.chapter, page.beat, page.isFirst);
        case 'reflection':
            return reflectionPage(page.chapter);
        case 'after':
            return afterPage(page.spread, page.isFirst);
        case 'quiz':
            return quizPage();
        case 'end':
            return endPage();
        default:
            return '';
    }
}

let current = 0;
let animating = false;

const spreadEl = document.getElementById('spread');
const prevBtn = document.getElementById('prevBtn');
const nextBtn = document.getElementById('nextBtn');
const indicatorEl = document.getElementById('pageIndicator');
const folioLeftEl = document.getElementById('folioLeft');
const folioRightEl = document.getElementById('folioRight');

function paint() {
    spreadEl.innerHTML = renderPage(PAGES[current]);
    prevBtn.disabled = current === 0;
    nextBtn.disabled = current === PAGES.length - 1;
    indicatorEl.textContent = `${current + 1} / ${PAGES.length}`;

    const folio = FOLIOS[current];
    folioLeftEl.classList.toggle('folio-center', folio.width === 1);
    if (folio.width === 2) {
        folioLeftEl.textContent = folio.start;
        folioRightEl.textContent = folio.start + 1;
        folioLeftEl.hidden = false;
        folioRightEl.hidden = false;
    } else {
        folioLeftEl.textContent = folio.start;
        folioLeftEl.hidden = false;
        folioRightEl.hidden = true;
    }

    spreadEl.querySelectorAll('[data-goto]').forEach(btn => {
        btn.addEventListener('click', () => {
            const chapterNum = Number(btn.dataset.goto);
            const idx = PAGES.findIndex(p => p.kind === 'spread' && p.chapter.num === chapterNum && p.isFirst);
            if (idx >= 0) goTo(idx);
        });
    });
    spreadEl.querySelectorAll('[data-goto-kind]').forEach(btn => {
        btn.addEventListener('click', () => {
            const idx = PAGES.findIndex(p => p.kind === btn.dataset.gotoKind);
            if (idx >= 0) goTo(idx);
        });
    });

    if (PAGES[current].kind === 'quiz') {
        initQuiz();
    }
}

function initQuiz() {
    let answeredCount = 0;
    const progressEl = document.getElementById('quizProgress');

    spreadEl.querySelectorAll('.quiz-item').forEach(item => {
        const qi = Number(item.dataset.qindex);
        const q = QUIZ[qi];
        item.querySelectorAll('.quiz-choice').forEach(btn => {
            btn.addEventListener('click', () => {
                if (item.classList.contains('graded')) return;
                item.classList.add('graded');
                const chosen = Number(btn.dataset.choice);
                item.querySelectorAll('.quiz-choice').forEach(b => {
                    const ci = Number(b.dataset.choice);
                    if (ci === q.answer) b.classList.add('correct');
                    else if (ci === chosen) b.classList.add('incorrect');
                });
                answeredCount++;
                progressEl.textContent = `${answeredCount} / 총 ${QUIZ.length}문항 완료`;
            });
        });
    });
}

function goTo(index) {
    if (animating || index === current || index < 0 || index >= PAGES.length) return;
    animating = true;
    const dir = index > current ? 'flip-next' : 'flip-prev';
    spreadEl.classList.add(dir);
    setTimeout(() => {
        current = index;
        paint();
    }, 230);
    setTimeout(() => {
        spreadEl.classList.remove('flip-next', 'flip-prev');
        animating = false;
    }, 480);
}

prevBtn.addEventListener('click', () => goTo(current - 1));
nextBtn.addEventListener('click', () => goTo(current + 1));

document.getElementById('tocLink').addEventListener('click', () => {
    // 그림책은 차례가 표지에 붙어 있다.
    const idx = PAGES.findIndex(p => p.kind === 'toc');
    goTo(idx >= 0 ? idx : 0);
});

document.addEventListener('keydown', (e) => {
    if (e.key === 'ArrowRight') goTo(current + 1);
    if (e.key === 'ArrowLeft') goTo(current - 1);
});

paint();
