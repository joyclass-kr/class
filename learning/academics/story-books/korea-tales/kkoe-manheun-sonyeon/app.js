const CHAPTERS = [
    {
        num: 1,
        title: "1장 · 재로 꼰 새끼줄",
        beats: [
            {
                art: "01-boy.png",
                emoji: "🧒",
                left: [
                    "옛날 어느 고을에 꾀 많기로 이름난 소년이 살았습니다.",
                    "나이는 열두 살, 집은 몹시 가난했지요."
                ],
                right: [
                    "\"저 애는 어른 열이 못 푸는 것도 척척 풀어.\"",
                    "\"머리 하나는 정말 비상하다니까.\"",
                    "마을 사람들은 소년을 두고 그렇게 말하곤 했답니다."
                ]
            },
            {
                art: "02-envoy.png",
                emoji: "📜",
                left: [
                    "어느 날 이웃 나라에서 사신이 찾아왔어요. 사신은 두루마리를 펼치며 말했습니다.",
                    "\"여기 세 가지 문제가 있소.\""
                ],
                right: [
                    "\"사흘 안에 다 풀지 못하면, 이 나라에는 지혜로운 이가 없다고 하겠소.\"",
                    "원님의 얼굴이 굳어졌지요."
                ]
            },
            {
                art: "03-first.png",
                emoji: "🪢",
                left: [
                    "첫 번째 문제가 읽혔습니다.",
                    "\"재로 꼰 새끼줄을 가져오시오.\""
                ],
                right: [
                    "관아 마당이 술렁였어요.",
                    "\"재로 새끼를 꼰다고?\"",
                    "\"재가 손에 잡히기나 하나!\""
                ]
            },
            {
                art: "04-solve1.png",
                emoji: "🔥",
                left: [
                    "이틀이 지나도록 아무도 답을 내지 못했어요.",
                    "그때 열두 살짜리가 짚 한 단을 안고 마당으로 들어섰습니다.",
                    "\"저 꼬마가 뭘 한다고?\""
                ],
                right: [
                    "소년은 새끼를 단단히 꼬아 쟁반에 얹고 불을 붙였어요. 치이익— 불이 새끼를 따라 기어갔습니다.",
                    "불이 꺼지자 새끼줄 모양 그대로 재가 남아 있었지요."
                ]
            }
        ]
    },
    {
        num: 2,
        title: "2장 · 아홉 번 굽은 구슬",
        beats: [
            {
                art: "05-second.png",
                emoji: "🔵",
                left: [
                    "사신의 눈이 커졌어요.",
                    "\"좋소. 그럼 두 번째 문제요.\""
                ],
                right: [
                    "사신이 손바닥에 구슬 하나를 올려놓았습니다.",
                    "\"이 구슬 속 구멍은 아홉 번이나 굽어 있소.\"",
                    "\"여기에 실을 꿰어 보시오.\""
                ]
            },
            {
                art: "06-stuck.png",
                emoji: "😖",
                left: [
                    "어른들이 번갈아 달려들었어요.",
                    "실을 밀어 넣어 보고, 가는 대를 넣어 보고, 입으로 불어도 보고."
                ],
                right: [
                    "하지만 실은 첫 굽이도 넘지 못했습니다.",
                    "\"이건 사람이 할 수 있는 일이 아니오.\"",
                    "다들 고개를 저었지요."
                ]
            },
            {
                art: "07-ant.png",
                emoji: "🐜",
                left: [
                    "소년은 마당 구석에 쪼그리고 앉았어요.",
                    "그러고는 기어가던 개미 한 마리를 살며시 집어 들었습니다."
                ],
                right: [
                    "가느다란 실을 개미 허리에 조심조심 매었지요.",
                    "\"쉿. 가만있어 보게.\"",
                    "사람들이 목을 길게 빼고 들여다보았어요."
                ]
            },
            {
                art: "08-honey.png",
                emoji: "🍯",
                left: [
                    "소년은 구슬 한쪽 구멍에 꿀을 살짝 발랐어요. 그리고 반대쪽 구멍에 개미를 살그머니 놓아 주었습니다. 개미가 쏙 들어갔지요.",
                    "하나, 둘, 셋…"
                ],
                right: [
                    "마당이 쥐 죽은 듯 조용해졌어요.",
                    "그때 반대쪽 구멍에서 까만 더듬이가 쏙 나왔습니다.",
                    "\"나왔다! 나왔어!\""
                ]
            }
        ]
    },
    {
        num: 3,
        title: "3장 · 통나무의 위아래",
        beats: [
            {
                art: "09-third.png",
                emoji: "🪵",
                left: [
                    "사신의 얼굴이 붉어졌어요.",
                    "\"마지막 문제요!\""
                ],
                right: [
                    "사신이 껍질을 말끔히 벗긴 통나무 하나를 내놓았습니다.",
                    "\"이 나무의 위와 아래를 가려 보시오.\"",
                    "굵기가 어디나 똑같아 도무지 알 수 없었어요."
                ]
            },
            {
                art: "10-water.png",
                emoji: "💧",
                left: [
                    "소년은 물을 가득 채운 통을 가져다 달라고 했어요. 그러고는 통나무를 그 안에 가만히 눕혔습니다. 모두가 통나무만 들여다보았지요."
                ],
                right: [
                    "한참 뒤, 나무 한쪽이 슬며시 기울기 시작했어요.",
                    "\"뿌리 쪽이 더 촘촘하고 무겁습니다.\"",
                    "\"가라앉는 쪽이 아래입니다.\""
                ]
            },
            {
                art: "11-bow.png",
                emoji: "🙇",
                left: [
                    "사신은 한참 동안 아무 말이 없었어요.",
                    "그러더니 열두 살짜리 앞에 정중히 고개를 숙였습니다.",
                    "\"이 나라에는 지혜로운 이가 있소.\""
                ],
                right: [
                    "\"이 나라에는 지혜로운 이가 있소.\"",
                    "\"돌아가 그대로 아뢰겠소.\"",
                    "관아 마당에 함성이 터졌지요."
                ]
            },
            {
                art: "12-school.png",
                emoji: "📖",
                left: [
                    "원님이 소년을 불렀어요.",
                    "\"무엇이든 말해 보아라. 상을 내리마.\""
                ],
                right: [
                    "소년은 한참 생각하다가 대답했습니다.",
                    "\"마을에 서당을 하나 지어 주십시오.\"",
                    "\"저 말고도 배우고 싶은 아이가 많거든요.\""
                ]
            }
        ],
        moral: "소년이 푼 세 문제는 힘으로도 재주로도 되지 않는 것이었어요. 다만 다르게 생각했을 뿐이지요. 안 된다고 여기는 순간 길이 막히고, 다른 쪽에서 보는 순간 길이 열린답니다.",
        question: "안 된다고 여겼던 일을 다른 방법으로 풀어 본 적이 있나요?"
    }
];

function artFrame(src, emoji) {
    return `
        <div class="art-frame">
            <img src="images/${src}" alt="" onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';">
            <div class="art-fallback" style="display:none">${emoji}</div>
        </div>`;
}

function coverToc() {
    const item = s => `
        <button type="button" data-goto="${s.num}">
            <span class="toc-num">${s.num}</span>
            <span>${s.title.replace(/^\d+장 · /, '')}</span>
        </button>`;
    return `
        <nav class="cover-toc">
            <h2>차례</h2>
            ${CHAPTERS.map(item).join('')}
            <button type="button" data-goto-kind="quiz">
                <span class="toc-num">❓</span>
                <span>이야기 문제</span>
            </button>
        </nav>`;
}

function coverPage() {
    return `
        <div class="page page-cover">
            <div class="story-page-left story-page-left-full">
                ${artFrame('cover.png', '🧠')}
            </div>
            <div class="story-page-right">
                <h1>꾀 많은 소년</h1>
                <p>꾀 많은 소년은 지은이가 없는 구전 설화예요. 어른들이 쩔쩔매는 어려운 문제를 아이가 풀어내는 이야기 갈래에 속한답니다.</p>
                <p>이야기에 나오는 세 가지 문제는 우리나라 옛이야기에 오래도록 되풀이해 나오는 것들이에요. 재로 새끼줄 꼬기, 굽은 구슬에 실 꿰기, 통나무의 위아래 가리기. 다른 이야기에도 이 문제들이 하나씩 흩어져 나오는데, 이 책에서는 셋을 한자리에 모았답니다.</p>
                <p>굽은 구슬에 실을 꿰는 꾀는 옛 기록에도 남아 있어요. 개미 허리에 실을 매고 반대쪽 구멍에 꿀을 발라 두면 개미가 꿀 냄새를 따라 기어간다는 것이지요. 힘으로 되지 않는 일을 다른 생각으로 푸는 것이 이 이야기의 재미랍니다.</p>
                ${coverToc()}
            </div>
        </div>`;
}

function spreadPage(chapter, beat, isFirst) {
    const badgeHtml = isFirst ? `<div class="spread-chapter-badge">${chapter.title}</div>` : '';
    const leftHtml = beat.left.map(p => `<p>${p}</p>`).join('');
    const rightHtml = beat.right.map(p => `<p>${p}</p>`).join('');
    return `
        <div class="page page-story">
            <div class="spread-art">
                ${badgeHtml}
                ${artFrame(beat.art, beat.emoji)}
            </div>
            <div class="spread-text">
                <div class="spread-text-left">${leftHtml}</div>
                <div class="spread-text-right">${rightHtml}</div>
            </div>
        </div>`;
}

function reflectionPage(chapter) {
    return `
        <div class="page page-reflection">
            <h2>이야기를 다 읽고</h2>
            <p class="reflection-moral">${chapter.moral}</p>
            <p class="reflection-question">${chapter.question}</p>
        </div>`;
}

const QUIZ = [
    { q: "사신이 낸 첫 번째 문제는 무엇인가요?", choices: ["물로 불을 지피기", "재로 새끼줄 꼬기", "모래로 밥을 짓기"], answer: 1 },
    { q: "소년은 재 새끼줄을 어떻게 만들었나요?", choices: ["재를 물에 개어서", "재를 실에 발라서", "꼰 새끼를 태워서"], answer: 2 },
    { q: "굽은 구슬에 실을 꿸 때 무엇을 썼나요?", choices: ["개미와 꿀", "가는 대나무", "뜨거운 물"], answer: 0 },
    { q: "통나무의 위아래는 어떻게 가려냈나요?", choices: ["무게를 달아 보고", "껍질을 벗겨 보고", "물에 띄워 보고"], answer: 2 },
    { q: "세 문제를 다 푼 사람은 누구인가요?", choices: ["열두 살 소년", "고을의 원님", "이웃 나라 사신"], answer: 0 },
    { q: "소년이 상으로 바란 것은 무엇인가요?", choices: ["쌀 백 섬 받기", "마을에 서당 짓기", "벼슬자리 얻기"], answer: 1 }
];

function quizPage() {
    const items = QUIZ.map((item, i) => `
        <div class="quiz-item" data-qindex="${i}">
            <p class="quiz-question">${i + 1}. ${item.q}</p>
            <div class="quiz-choices">
                ${item.choices.map((c, ci) => `<button type="button" class="quiz-choice" data-choice="${ci}">${c}</button>`).join('')}
            </div>
        </div>`).join('');
    return `
        <div class="page page-quiz">
            <h2>이야기 문제</h2>
            <p class="quiz-intro-text" id="quizProgress">0 / 총 ${QUIZ.length}문항 완료</p>
            <div class="quiz-list">${items}</div>
        </div>`;
}

function endPage() {
    return `
        <div class="page page-end">
            ${artFrame('end.png', '🌟')}
            <h2>꾀 많은 소년 이야기를 다 읽었어요!</h2>
            <a class="home-btn" href="../../../../../">학습 허브로 돌아가기</a>
        </div>`;
}

const PAGES = [
    { kind: 'cover' },
    ...CHAPTERS.flatMap(chapter => chapter.beats.map((beat, i) => ({ kind: 'spread', chapter, beat, isFirst: i === 0 }))),
    { kind: 'reflection', chapter: CHAPTERS[CHAPTERS.length - 1] },
    { kind: 'quiz' },
    { kind: 'end' }
];

const TWO_PAGE_KINDS = new Set(['spread', 'cover']);

let folioCounter = 0;
const FOLIOS = PAGES.map(p => {
    const width = TWO_PAGE_KINDS.has(p.kind) ? 2 : 1;
    const start = folioCounter + 1;
    folioCounter += width;
    return { start, width };
});

function renderPage(page) {
    switch (page.kind) {
        case 'cover':
            return coverPage();
        case 'spread':
            return spreadPage(page.chapter, page.beat, page.isFirst);
        case 'reflection':
            return reflectionPage(page.chapter);
        case 'quiz':
            return quizPage();
        case 'end':
            return endPage();
        default:
            return '';
    }
}

let current = 0;
let animating = false;

const spreadEl = document.getElementById('spread');
const prevBtn = document.getElementById('prevBtn');
const nextBtn = document.getElementById('nextBtn');
const indicatorEl = document.getElementById('pageIndicator');
const folioLeftEl = document.getElementById('folioLeft');
const folioRightEl = document.getElementById('folioRight');

function paint() {
    spreadEl.innerHTML = renderPage(PAGES[current]);
    prevBtn.disabled = current === 0;
    nextBtn.disabled = current === PAGES.length - 1;
    indicatorEl.textContent = `${current + 1} / ${PAGES.length}`;

    const folio = FOLIOS[current];
    folioLeftEl.classList.toggle('folio-center', folio.width === 1);
    if (folio.width === 2) {
        folioLeftEl.textContent = folio.start;
        folioRightEl.textContent = folio.start + 1;
        folioLeftEl.hidden = false;
        folioRightEl.hidden = false;
    } else {
        folioLeftEl.textContent = folio.start;
        folioLeftEl.hidden = false;
        folioRightEl.hidden = true;
    }

    spreadEl.querySelectorAll('[data-goto]').forEach(btn => {
        btn.addEventListener('click', () => {
            const chapterNum = Number(btn.dataset.goto);
            const idx = PAGES.findIndex(p => p.kind === 'spread' && p.chapter.num === chapterNum && p.isFirst);
            if (idx >= 0) goTo(idx);
        });
    });
    spreadEl.querySelectorAll('[data-goto-kind]').forEach(btn => {
        btn.addEventListener('click', () => {
            const idx = PAGES.findIndex(p => p.kind === btn.dataset.gotoKind);
            if (idx >= 0) goTo(idx);
        });
    });

    if (PAGES[current].kind === 'quiz') {
        initQuiz();
    }
}

function initQuiz() {
    let answeredCount = 0;
    const progressEl = document.getElementById('quizProgress');

    spreadEl.querySelectorAll('.quiz-item').forEach(item => {
        const qi = Number(item.dataset.qindex);
        const q = QUIZ[qi];
        item.querySelectorAll('.quiz-choice').forEach(btn => {
            btn.addEventListener('click', () => {
                if (item.classList.contains('graded')) return;
                item.classList.add('graded');
                const chosen = Number(btn.dataset.choice);
                item.querySelectorAll('.quiz-choice').forEach(b => {
                    const ci = Number(b.dataset.choice);
                    if (ci === q.answer) b.classList.add('correct');
                    else if (ci === chosen) b.classList.add('incorrect');
                });
                answeredCount++;
                progressEl.textContent = `${answeredCount} / 총 ${QUIZ.length}문항 완료`;
            });
        });
    });
}

function goTo(index) {
    if (animating || index === current || index < 0 || index >= PAGES.length) return;
    animating = true;
    const dir = index > current ? 'flip-next' : 'flip-prev';
    spreadEl.classList.add(dir);
    setTimeout(() => {
        current = index;
        paint();
    }, 230);
    setTimeout(() => {
        spreadEl.classList.remove('flip-next', 'flip-prev');
        animating = false;
    }, 480);
}

prevBtn.addEventListener('click', () => goTo(current - 1));
nextBtn.addEventListener('click', () => goTo(current + 1));

document.getElementById('tocLink').addEventListener('click', () => {
    // 그림책은 차례가 표지에 붙어 있다.
    const idx = PAGES.findIndex(p => p.kind === 'toc');
    goTo(idx >= 0 ? idx : 0);
});

document.addEventListener('keydown', (e) => {
    if (e.key === 'ArrowRight') goTo(current + 1);
    if (e.key === 'ArrowLeft') goTo(current - 1);
});

paint();
