const CHAPTERS = [
    {
        num: 1,
        title: "1장 · 나정의 흰말",
        beats: [
            {
                art: "01-six.png",
                emoji: "🗣️",
                left: [
                    "옛날 서라벌 땅에는 여섯 마을이 있었습니다.",
                    "마을마다 촌장이 하나씩 있었지요."
                ],
                right: [
                    "어느 날 여섯 촌장이 언덕에 모여 앉았어요.",
                    "\"우리를 하나로 묶을 임금이 있어야 합니다.\"",
                    "\"그런 사람이 어디 있단 말이오?\""
                ]
            },
            {
                art: "02-light.png",
                emoji: "🌈",
                left: [
                    "그때 한 촌장이 남쪽을 가리켰습니다.",
                    "\"저, 저것 좀 보시오!\""
                ],
                right: [
                    "숲 너머 우물가에서 빛줄기가 하늘로 뻗어 있었어요. 번개도 아니고 노을도 아니었지요.",
                    "여섯 사람이 한꺼번에 언덕을 뛰어 내려갔습니다."
                ]
            },
            {
                art: "03-horse.png",
                emoji: "🐴",
                left: [
                    "우물가에 흰말 한 마리가 있었어요.",
                    "나정이라 부르던 우물이었지요."
                ],
                right: [
                    "말은 앞무릎을 꿇고 길게 울고 있었습니다.",
                    "\"히히힝—\"",
                    "사람들이 다가가자 말이 고개를 들었어요."
                ]
            },
            {
                art: "04-egg.png",
                emoji: "🥚",
                left: [
                    "말은 훌쩍 하늘로 솟아올라 사라졌습니다.",
                    "그 자리에 무언가 놓여 있었어요."
                ],
                right: [
                    "커다랗고 붉은 알 하나였지요.",
                    "\"이게… 대체 무엇이오?\"",
                    "여섯 사람이 알을 빙 둘러쌌습니다."
                ]
            },
            {
                art: "05-boy.png",
                emoji: "👶",
                left: [
                    "그때 알에 금이 쩍 갔어요.",
                    "쩌억—"
                ],
                right: [
                    "알을 깨고 사내아이 하나가 나왔습니다. 울지도 않고 눈을 말똥말똥 뜨고 있었지요.",
                    "촌장들은 그 자리에 넙죽 엎드렸어요."
                ]
            }
        ]
    },
    {
        num: 2,
        title: "2장 · 알영 우물의 아이",
        beats: [
            {
                art: "06-bath.png",
                emoji: "💧",
                left: [
                    "촌장들이 아이를 동쪽 냇물에 데려가 씻겼습니다.",
                    "그런데 이상한 일이 일어났어요."
                ],
                right: [
                    "씻길수록 아이 몸에서 빛이 났지요.",
                    "새들이 날아와 물가에 내려앉고,",
                    "짐승들이 나와 껑충껑충 뛰었답니다."
                ]
            },
            {
                art: "07-name.png",
                emoji: "📜",
                left: [
                    "\"이름을 무어라 지을까요?\"",
                    "한 촌장이 깨진 알 껍데기를 들어 보였어요."
                ],
                right: [
                    "\"보시오. 꼭 박처럼 생기지 않았소.\"",
                    "그래서 성은 박이 되었지요.",
                    "이름은 세상을 밝힌다는 뜻으로 혁거세라 했답니다."
                ]
            },
            {
                art: "08-dragon.png",
                emoji: "🐉",
                left: [
                    "바로 그날, 알영이라는 우물에서도 일이 있었어요.",
                    "닭 머리를 한 용이 나타났지 뭐예요."
                ],
                right: [
                    "용은 우물가에 여자아이 하나를 남기고 사라졌습니다.",
                    "그런데 아이의 입술이 새 부리처럼 길쭉했어요.",
                    "\"어쩌면 좋을꼬…\""
                ]
            },
            {
                art: "09-beak.png",
                emoji: "🕊️",
                left: [
                    "할머니 한 분이 아이를 냇물로 데려갔습니다.",
                    "조심조심 얼굴을 씻겼지요."
                ],
                right: [
                    "그러자 부리가 툭 떨어져 물 위로 떠내려갔어요. 아이가 배시시 웃었습니다.",
                    "사람들은 우물 이름을 따 알영이라 불렀답니다."
                ]
            }
        ]
    },
    {
        num: 3,
        title: "3장 · 서라벌",
        beats: [
            {
                art: "10-grow.png",
                emoji: "🌱",
                left: [
                    "두 아이는 여섯 마을이 함께 길렀어요.",
                    "이 집에서 밥을 먹고 저 집에서 잠을 잤지요."
                ],
                right: [
                    "혁거세는 활을 잘 쏘고 말을 잘 탔습니다.",
                    "알영은 곡식과 누에를 잘 알았어요.",
                    "둘은 자라면서 서로 가장 친한 벗이 되었답니다."
                ]
            },
            {
                art: "11-thirteen.png",
                emoji: "👑",
                left: [
                    "혁거세가 열세 살이 되던 해였어요.",
                    "여섯 촌장이 다시 언덕에 모였습니다."
                ],
                right: [
                    "\"이제 때가 되었소.\"",
                    "촌장들은 소년을 높은 자리에 앉혔지요.",
                    "\"우리 임금이십니다.\""
                ]
            },
            {
                art: "12-queen.png",
                emoji: "🤝",
                left: [
                    "혁거세는 알영을 왕비로 맞았어요.",
                    "\"나 혼자서는 아무것도 못 하오.\""
                ],
                right: [
                    "둘은 함께 마을을 돌았습니다. 혁거세는 논밭을 보고, 알영은 뽕나무를 보았지요.",
                    "사람들은 둘을 두 성인이라 불렀답니다."
                ]
            },
            {
                art: "13-seorabeol.png",
                emoji: "🏯",
                left: [
                    "나라 이름은 서라벌이라 했어요.",
                    "여섯 마을이 하나가 된 것이지요."
                ],
                right: [
                    "담을 헐어 길을 내고, 저잣거리를 열었습니다.",
                    "\"이제 우리는 한 나라 사람이오.\"",
                    "이 나라가 훗날 신라가 되었답니다."
                ]
            },
            {
                art: "14-people.png",
                emoji: "🌾",
                left: [
                    "가을이면 온 서라벌이 누렇게 물들었어요.",
                    "곳간마다 곡식이 그득했지요."
                ],
                right: [
                    "\"임금님 덕분이오.\"",
                    "혁거세는 고개를 저었습니다.",
                    "\"여섯 마을이 모였기 때문이지요.\""
                ]
            }
        ],
        moral: "여섯 촌장은 저마다 제 마을이 제일이라 여길 수도 있었어요. 그러나 하나로 묶일 것을 먼저 바랐지요. 나라가 선 것은 알에서 아이가 나왔기 때문이 아니라, 사람들이 함께하기로 마음먹었기 때문이랍니다.",
        question: "여럿이 힘을 모아 무언가를 해낸 적이 있나요?"
    }
];

function artFrame(src, emoji) {
    return `
        <div class="art-frame">
            <img src="images/${src}" alt="" onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';">
            <div class="art-fallback" style="display:none">${emoji}</div>
        </div>`;
}

function coverPage() {
    return `
        <div class="page page-cover">
            <div class="story-page-left story-page-left-full">
                ${artFrame('cover.png', '🥚')}
            </div>
            <div class="story-page-right">
                <h1>박혁거세</h1>
                <p>박혁거세 이야기는 삼국사기와 삼국유사에 함께 실려 전해져요. 신라를 연 첫 임금에 관한 이야기랍니다.</p>
                <p>혁거세는 세상을 밝힌다는 뜻이에요. 붉은 알이 박처럼 생겼다고 해서 성을 박이라 했다고 전해지지요.</p>
                <p>알에서 사람이 태어나는 이야기는 우리 옛 건국 이야기에 유난히 많아요. 북쪽의 주몽도, 남쪽의 김수로도, 바다를 건너온 석탈해도 모두 알에서 나왔지요. 알은 하늘에서 내려온 사람이라는 뜻으로 쓰인 것이랍니다.</p>
            </div>
        </div>`;
}

function tocPage() {
    const itemHtml = s => `
        <li>
            <button type="button" data-goto="${s.num}">
                <span class="toc-num">${s.num}</span>
                <span>
                    <strong>${s.title.replace(/^\d+장 · /, '')}</strong>
                </span>
            </button>
        </li>`;
    const quizItemHtml = `
        <li>
            <button type="button" data-goto-kind="quiz">
                <span class="toc-num">❓</span>
                <span>
                    <strong>이야기 문제</strong>
                </span>
            </button>
        </li>`;
    const half = Math.ceil(CHAPTERS.length / 2);
    const leftItems = CHAPTERS.slice(0, half).map(itemHtml).join('');
    const rightItems = CHAPTERS.slice(half).map(itemHtml).join('') + quizItemHtml;
    return `
        <div class="page page-toc">
            <div class="story-page-left">
                <h2>차례</h2>
                <ul class="toc-list">${leftItems}</ul>
            </div>
            <div class="story-page-right">
                <ul class="toc-list">${rightItems}</ul>
            </div>
        </div>`;
}

function spreadPage(chapter, beat, isFirst) {
    const badgeHtml = isFirst ? `<div class="spread-chapter-badge">${chapter.title}</div>` : '';
    const leftHtml = beat.left.map(p => `<p>${p}</p>`).join('');
    const rightHtml = beat.right.map(p => `<p>${p}</p>`).join('');
    return `
        <div class="page page-story">
            <div class="spread-art">
                ${badgeHtml}
                ${artFrame(beat.art, beat.emoji)}
            </div>
            <div class="spread-text">
                <div class="spread-text-left">${leftHtml}</div>
                <div class="spread-text-right">${rightHtml}</div>
            </div>
        </div>`;
}

function reflectionPage(chapter) {
    return `
        <div class="page page-reflection">
            <h2>이야기를 다 읽고</h2>
            <p class="reflection-moral">${chapter.moral}</p>
            <p class="reflection-question">${chapter.question}</p>
        </div>`;
}

const QUIZ = [
    { q: "여섯 촌장이 모여 의논한 것은 무엇인가요?", choices: ["농사지을 땅", "임금을 모실 일", "싸움을 벌일 일"], answer: 1 },
    { q: "나정 우물가에 있던 것은 무엇인가요?", choices: ["커다란 흰 바위", "하늘에서 내린 사다리", "무릎 꿇고 우는 흰말"], answer: 2 },
    { q: "말이 사라진 자리에 남은 것은 무엇인가요?", choices: ["붉은 알 하나", "금으로 된 상자", "푸른 구슬 하나"], answer: 0 },
    { q: "혁거세라는 이름은 무슨 뜻인가요?", choices: ["하늘에서 왔다", "말을 잘 탄다", "세상을 밝힌다"], answer: 2 },
    { q: "알영 우물에 나타난 것은 무엇인가요?", choices: ["닭 머리를 한 용", "커다란 흰 뱀", "금빛 잉어"], answer: 0 },
    { q: "혁거세가 세운 나라의 이름은 무엇인가요?", choices: ["아사달", "서라벌", "졸본"], answer: 1 }
];

function quizPage() {
    const items = QUIZ.map((item, i) => `
        <div class="quiz-item" data-qindex="${i}">
            <p class="quiz-question">${i + 1}. ${item.q}</p>
            <div class="quiz-choices">
                ${item.choices.map((c, ci) => `<button type="button" class="quiz-choice" data-choice="${ci}">${c}</button>`).join('')}
            </div>
        </div>`).join('');
    return `
        <div class="page page-quiz">
            <h2>이야기 문제</h2>
            <p class="quiz-intro-text" id="quizProgress">0 / 총 ${QUIZ.length}문항 완료</p>
            <div class="quiz-list">${items}</div>
        </div>`;
}

function endPage() {
    return `
        <div class="page page-end">
            ${artFrame('end.png', '🌟')}
            <h2>박혁거세 이야기를 다 읽었어요!</h2>
            <a class="home-btn" href="../../../../../">학습 허브로 돌아가기</a>
        </div>`;
}

function historyPage() {
    return `
        <div class="page page-history">
            <h2>여기서부터는 진짜 역사</h2>
            <p class="history-note">앞까지가 이야기였고, 여기부터는 기록에 남은 사실이에요.</p>
            <hr>
            <div class="history-body">
                <p>신라는 실제로 있었던 나라예요. 삼국사기는 신라가 기원전 57년에 섰다고 적고 있는데, 학자들은 여러 마을이 하나로 합쳐지며 나라 꼴을 갖춘 것을 그렇게 적은 것으로 봅니다.</p>
                <p>경주에는 지금도 나정이라는 자리가 남아 있어요. 발굴을 해 보니 우물 터와 함께 제사를 지내던 커다란 건물 자리가 나왔지요. 신라 사람들이 아주 오래도록 그곳에서 제사를 지냈다는 뜻이랍니다.</p>
                <p>신라에서는 박씨, 석씨, 김씨 세 집안이 번갈아 임금을 냈어요. 한 집안이 왕위를 이어받는 것이 당연해지기까지는 오랜 시간이 걸렸답니다.</p>
                <p>신라는 기원전 57년부터 서기 935년까지 이어졌어요. 거의 천 년 가까이 이어진 셈이니, 우리 역사에서 가장 오래 버틴 나라랍니다.</p>
            </div>
        </div>`;
}

const PAGES = [
    { kind: 'cover' },
    { kind: 'toc' },
    ...CHAPTERS.flatMap(chapter => chapter.beats.map((beat, i) => ({ kind: 'spread', chapter, beat, isFirst: i === 0 }))),
    { kind: 'reflection', chapter: CHAPTERS[CHAPTERS.length - 1] },
    { kind: 'history' },
    { kind: 'quiz' },
    { kind: 'end' }
];

const TWO_PAGE_KINDS = new Set(['spread', 'toc', 'cover']);

let folioCounter = 0;
const FOLIOS = PAGES.map(p => {
    const width = TWO_PAGE_KINDS.has(p.kind) ? 2 : 1;
    const start = folioCounter + 1;
    folioCounter += width;
    return { start, width };
});

function renderPage(page) {
    switch (page.kind) {
        case 'cover':
            return coverPage();
        case 'toc':
            return tocPage();
        case 'spread':
            return spreadPage(page.chapter, page.beat, page.isFirst);
        case 'reflection':
            return reflectionPage(page.chapter);
        case 'history':
            return historyPage();
        case 'quiz':
            return quizPage();
        case 'end':
            return endPage();
        default:
            return '';
    }
}

let current = 0;
let animating = false;

const spreadEl = document.getElementById('spread');
const prevBtn = document.getElementById('prevBtn');
const nextBtn = document.getElementById('nextBtn');
const indicatorEl = document.getElementById('pageIndicator');
const folioLeftEl = document.getElementById('folioLeft');
const folioRightEl = document.getElementById('folioRight');

function paint() {
    spreadEl.innerHTML = renderPage(PAGES[current]);
    prevBtn.disabled = current === 0;
    nextBtn.disabled = current === PAGES.length - 1;
    indicatorEl.textContent = `${current + 1} / ${PAGES.length}`;

    const folio = FOLIOS[current];
    folioLeftEl.classList.toggle('folio-center', folio.width === 1);
    if (folio.width === 2) {
        folioLeftEl.textContent = folio.start;
        folioRightEl.textContent = folio.start + 1;
        folioLeftEl.hidden = false;
        folioRightEl.hidden = false;
    } else {
        folioLeftEl.textContent = folio.start;
        folioLeftEl.hidden = false;
        folioRightEl.hidden = true;
    }

    spreadEl.querySelectorAll('[data-goto]').forEach(btn => {
        btn.addEventListener('click', () => {
            const chapterNum = Number(btn.dataset.goto);
            const idx = PAGES.findIndex(p => p.kind === 'spread' && p.chapter.num === chapterNum && p.isFirst);
            if (idx >= 0) goTo(idx);
        });
    });
    spreadEl.querySelectorAll('[data-goto-kind]').forEach(btn => {
        btn.addEventListener('click', () => {
            const idx = PAGES.findIndex(p => p.kind === btn.dataset.gotoKind);
            if (idx >= 0) goTo(idx);
        });
    });

    if (PAGES[current].kind === 'quiz') {
        initQuiz();
    }
}

function initQuiz() {
    let answeredCount = 0;
    const progressEl = document.getElementById('quizProgress');

    spreadEl.querySelectorAll('.quiz-item').forEach(item => {
        const qi = Number(item.dataset.qindex);
        const q = QUIZ[qi];
        item.querySelectorAll('.quiz-choice').forEach(btn => {
            btn.addEventListener('click', () => {
                if (item.classList.contains('graded')) return;
                item.classList.add('graded');
                const chosen = Number(btn.dataset.choice);
                item.querySelectorAll('.quiz-choice').forEach(b => {
                    const ci = Number(b.dataset.choice);
                    if (ci === q.answer) b.classList.add('correct');
                    else if (ci === chosen) b.classList.add('incorrect');
                });
                answeredCount++;
                progressEl.textContent = `${answeredCount} / 총 ${QUIZ.length}문항 완료`;
            });
        });
    });
}

function goTo(index) {
    if (animating || index === current || index < 0 || index >= PAGES.length) return;
    animating = true;
    const dir = index > current ? 'flip-next' : 'flip-prev';
    spreadEl.classList.add(dir);
    setTimeout(() => {
        current = index;
        paint();
    }, 230);
    setTimeout(() => {
        spreadEl.classList.remove('flip-next', 'flip-prev');
        animating = false;
    }, 480);
}

prevBtn.addEventListener('click', () => goTo(current - 1));
nextBtn.addEventListener('click', () => goTo(current + 1));

document.getElementById('tocLink').addEventListener('click', () => {
    const idx = PAGES.findIndex(p => p.kind === 'toc');
    if (idx >= 0) goTo(idx);
});

document.addEventListener('keydown', (e) => {
    if (e.key === 'ArrowRight') goTo(current + 1);
    if (e.key === 'ArrowLeft') goTo(current - 1);
});

paint();
