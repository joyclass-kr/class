const CHAPTERS = [
    {
        num: 1,
        title: "1장 · 개울가의 밤",
        beats: [
            {
                art: "01-poor.png",
                emoji: "🥣",
                left: [
                    "옛날 어느 마을에 돌쇠라는 사람이 살았습니다.",
                    "부지런하기로는 마을에서 첫손에 꼽혔지요.",
                    "그런데 이상하게도 늘 가난했어요."
                ],
                right: [
                    "\"밥그릇아, 너는 어째 늘 비어 있느냐.\"",
                    "\"허, 대답도 없구나.\"",
                    "돌쇠는 빈 그릇한테 말을 거는 사람이었답니다."
                ]
            },
            {
                art: "02-fire.png",
                emoji: "🔥",
                left: [
                    "어느 여름밤이었어요.",
                    "개울가를 지나는데 저만치에서 불빛이 어른거렸습니다.",
                    "\"이 밤중에 누가 불을 피우나?\""
                ],
                right: [
                    "가까이 가 본 돌쇠는 그 자리에 얼어붙었어요.",
                    "모닥불은 활활 타는데,",
                    "그 둘레에 아무도 없었거든요."
                ]
            },
            {
                art: "03-crab.png",
                emoji: "🦀",
                left: [
                    "그때 개울에서 게 한 마리가 저 혼자 걸어 나왔어요.",
                    "타박타박 불 쪽으로 가더니,",
                    "불 위에 척 올라앉는 게 아니겠어요?"
                ],
                right: [
                    "잠시 뒤, 다 익은 게가 허공으로 붕 떠올랐어요.",
                    "아그작. 아그작.",
                    "\"캬— 이 맛이지.\""
                ]
            },
            {
                art: "04-appear.png",
                emoji: "👺",
                left: [
                    "돌쇠는 바위 뒤에 납작 엎드렸습니다.",
                    "\"에이, 더워서 못 쓰겠다!\"",
                    "허공에서 뿔 하나가 쑥 나타났어요."
                ],
                right: [
                    "팔이 나오고 다리가 나오고, 커다란 도깨비가 통째로 나타났지요.",
                    "손에는 까만 감투가 들려 있었어요.",
                    "\"나도! 나도!\""
                ]
            },
            {
                art: "05-party.png",
                emoji: "🎶",
                left: [
                    "여기저기서 감투가 벗겨지더니 도깨비 여섯이 우르르 나타났어요.",
                    "제일 큰 도깨비가 노래를 하는데 음이 하나도 안 맞았습니다."
                ],
                right: [
                    "\"딸꾹! 좀, 딸꾹, 조용히 딸꾹—\"",
                    "작은 도깨비는 게 다리를 자꾸 떨어뜨렸어요.",
                    "\"그러게 두 손으로 들라니까!\""
                ]
            }
        ]
    },
    {
        num: 2,
        title: "2장 · 보이지 않는 사람",
        beats: [
            {
                art: "06-wrestle.png",
                emoji: "🤼",
                left: [
                    "\"자, 이제 씨름이다!\"",
                    "\"감투 쓰고는 씨름이 안 되지. 벗어라, 벗어!\"",
                    "도깨비들이 감투를 너럭바위에 툭툭 던졌습니다."
                ],
                right: [
                    "까만 감투 여섯 개가 놓인 곳은 바로 돌쇠가 엎드린 바위였어요.",
                    "\"으라차차!\"",
                    "도깨비들이 엉겨 붙어 흙먼지가 풀풀 일었지요."
                ]
            },
            {
                art: "07-steal.png",
                emoji: "🫣",
                left: [
                    "돌쇠의 손이 슬금슬금 올라갔어요.",
                    "손끝이 감투에 닿았습니다.",
                    "심장이 쿵쾅쿵쾅 뛰었지요."
                ],
                right: [
                    "돌쇠는 감투 하나를 끌어당겨 품에 넣고 냅다 뛰었어요.",
                    "\"어? 내 감투 어디 갔지?\"",
                    "\"네가 또 어디다 흘렸겠지!\""
                ]
            },
            {
                art: "08-mirror.png",
                emoji: "😳",
                left: [
                    "집에 온 돌쇠는 숨을 몰아쉬며 감투를 써 보았어요.",
                    "그러고는 거울 앞에 섰지요.",
                    "\"으악!\""
                ],
                right: [
                    "거울에는 아무것도 없었습니다.",
                    "손을 들어 보아도, 발을 굴러 보아도요.",
                    "\"허허… 이거 물건이구먼.\""
                ]
            },
            {
                art: "09-prank.png",
                emoji: "😄",
                left: [
                    "처음에는 그저 재미났어요.",
                    "빨랫줄을 툭 건드리면 이 서방이 하늘만 올려다보았지요.",
                    "\"바람도 없는데 왜 흔들려?\""
                ],
                right: [
                    "훈장님 갓을 살짝 눌러 보기도 했어요.",
                    "\"이놈들! 누가 내 갓을 건드렸느냐!\"",
                    "애먼 아이들만 벌을 섰답니다."
                ]
            },
            {
                art: "10-cow.png",
                emoji: "🐮",
                left: [
                    "제일 신났던 것은 김 영감네 소였어요.",
                    "돌쇠가 소 등에 훌쩍 올라타자 소가 깜짝 놀랐지요.",
                    "\"음머어어—!\""
                ],
                right: [
                    "소는 온 동네를 뛰어다녔습니다.",
                    "\"소가! 소가 저절로 뛴다!\"",
                    "웃음소리만 허공에 둥둥 떠다녔답니다."
                ]
            },
            {
                art: "11-market.png",
                emoji: "🍡",
                left: [
                    "며칠 뒤, 돌쇠는 감투를 쓰고 장터에 갔어요.",
                    "떡판 앞에서 그만 걸음이 멈췄습니다.",
                    "'하나쯤은 괜찮겠지.'"
                ],
                right: [
                    "떡 하나가 공중으로 슥 떠올랐다가 사라졌어요.",
                    "\"어라? 떡이 어디 갔지?\"",
                    "돌쇠의 손이 파르르 떨렸습니다."
                ]
            }
        ]
    },
    {
        num: 3,
        title: "3장 · 둥둥 떠다니는 빨간 점",
        beats: [
            {
                art: "12-rumor.png",
                emoji: "😰",
                left: [
                    "한 번이 두 번이 되고, 두 번이 열 번이 되었어요.",
                    "떡에서 과일로, 과일에서 옷감으로.",
                    "곳간이 하루가 다르게 차올랐지요."
                ],
                right: [
                    "\"귀신이 물건을 집어 간대!\"",
                    "장사꾼들은 물건을 끌어안고 밤잠을 설쳤습니다.",
                    "그런데 돌쇠는 마음이 점점 무거워졌어요."
                ]
            },
            {
                art: "13-hole.png",
                emoji: "🧵",
                left: [
                    "그러던 어느 밤이었어요.",
                    "화롯가에 벗어 둔 감투에 불티가 톡 튀었습니다.",
                    "얼른 껐지만 옆구리에 구멍이 났지요."
                ],
                right: [
                    "돌쇠는 궤짝을 뒤져 헝겊을 찾아냈어요.",
                    "그러고는 구멍에 대고 삐뚤빼뚤 기웠습니다.",
                    "하필이면 새빨간 헝겊이었어요."
                ]
            },
            {
                art: "14-spot.png",
                emoji: "🔴",
                left: [
                    "이튿날 장터 사람들이 웅성거렸어요.",
                    "\"저것 봐라!\"",
                    "허공에 새빨간 점 하나가 둥둥 떠다녔거든요."
                ],
                right: [
                    "기운 헝겊만 그대로 보였던 것이었지요.",
                    "돌쇠는 그것도 모르고 떡판으로 다가갔습니다.",
                    "\"저 빨간 게 범인이다!\""
                ]
            },
            {
                art: "15-caught.png",
                emoji: "🙌",
                left: [
                    "사람들이 우르르 달려들어 감투를 덥석 낚아챘어요.",
                    "감투가 벗겨지자 돌쇠가 불쑥 나타났지요.",
                    "장터가 물을 끼얹은 듯 조용해졌습니다."
                ],
                right: [
                    "\"떡값입니다. 그동안 스물세 개 가져갔습니다.\"",
                    "\"…세어 보기까지 했나?\"",
                    "\"예. 밤마다 세었습니다.\""
                ]
            },
            {
                art: "16-burn.png",
                emoji: "🔥",
                left: [
                    "그날 밤 돌쇠는 감투를 아궁이에 넣었어요.",
                    "빨간 헝겊이 제일 마지막까지 타올랐지요.",
                    "\"보이지 않는다고 없던 일이 되는 게 아니었구나.\""
                ],
                right: [
                    "이튿날 밤 개울가에 가 보니 도깨비 하나가 혼자 앉아 있었어요.",
                    "\"…미안하다.\"",
                    "\"됐다. 보이는 것도 살 만하더라.\""
                ]
            }
        ],
        moral: "돌쇠를 붙잡은 것은 빨간 헝겊이 아니었어요. 아무도 보지 않는다고 여기는 동안에도 돌쇠 자신은 다 보고 있었지요. 그래서 곳간이 차오를수록 마음이 무거워졌던 거예요.",
        question: "아무도 보지 않을 때, 나는 어떤 사람인가요?"
    }
];

function artFrame(src, emoji) {
    return `
        <div class="art-frame">
            <img src="images/${src}" alt="" onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';">
            <div class="art-fallback" style="display:none">${emoji}</div>
        </div>`;
}

function coverPage() {
    return `
        <div class="page page-cover">
            <div class="story-page-left story-page-left-full">
                ${artFrame('cover.png', '🎩')}
            </div>
            <div class="story-page-right">
                <h1>도깨비 감투</h1>
                <p>도깨비 감투는 지은이가 없는 구전 설화예요. 쓰면 몸이 보이지 않는 감투가 나오는 이야기랍니다.</p>
                <p>감투는 조선 시대에 어른 남자가 머리에 쓰던 작고 둥근 모자예요. 벼슬을 하면 쓸 수 있었기 때문에, 지금도 자리나 벼슬을 두고 감투를 쓴다는 말을 쓰지요.</p>
                <p>판본마다 감투를 얻는 자리가 달라요. 도깨비가 그냥 주고 가는 판본도 있고, 도깨비들이 노는 틈에 슬쩍 훔쳐 오는 판본도 있지요. 이 책은 훔쳐 오는 쪽을 따랐답니다.</p>
                <p>몸이 보이지 않게 해 주는 물건은 세계 곳곳의 이야기에 나와요. 그리스 신화의 하데스 투구, 독일 이야기의 요술 망토처럼요. 그런데 그 물건을 얻은 사람이 끝에 가서 잘되는 이야기는 드물답니다.</p>
            </div>
        </div>`;
}

function tocPage() {
    const itemHtml = s => `
        <li>
            <button type="button" data-goto="${s.num}">
                <span class="toc-num">${s.num}</span>
                <span>
                    <strong>${s.title.replace(/^\d+장 · /, '')}</strong>
                </span>
            </button>
        </li>`;
    const quizItemHtml = `
        <li>
            <button type="button" data-goto-kind="quiz">
                <span class="toc-num">❓</span>
                <span>
                    <strong>이야기 문제</strong>
                </span>
            </button>
        </li>`;
    const half = Math.ceil(CHAPTERS.length / 2);
    const leftItems = CHAPTERS.slice(0, half).map(itemHtml).join('');
    const rightItems = CHAPTERS.slice(half).map(itemHtml).join('') + quizItemHtml;
    return `
        <div class="page page-toc">
            <div class="story-page-left">
                <h2>차례</h2>
                <ul class="toc-list">${leftItems}</ul>
            </div>
            <div class="story-page-right">
                <ul class="toc-list">${rightItems}</ul>
            </div>
        </div>`;
}

function spreadPage(chapter, beat, isFirst) {
    const badgeHtml = isFirst ? `<div class="spread-chapter-badge">${chapter.title}</div>` : '';
    const leftHtml = beat.left.map(p => `<p>${p}</p>`).join('');
    const rightHtml = beat.right.map(p => `<p>${p}</p>`).join('');
    return `
        <div class="page page-story">
            <div class="spread-art">
                ${badgeHtml}
                ${artFrame(beat.art, beat.emoji)}
            </div>
            <div class="spread-text">
                <div class="spread-text-left">${leftHtml}</div>
                <div class="spread-text-right">${rightHtml}</div>
            </div>
        </div>`;
}

function reflectionPage(chapter) {
    return `
        <div class="page page-reflection">
            <h2>이야기를 다 읽고</h2>
            <p class="reflection-moral">${chapter.moral}</p>
            <p class="reflection-question">${chapter.question}</p>
        </div>`;
}

const QUIZ = [
    { q: "돌쇠가 개울가에서 처음 본 이상한 것은 무엇인가요?", choices: ["개울물이 거꾸로 흘러 올라갔다", "게가 저 혼자 불 위로 걸어갔다", "커다란 돌이 저 혼자 굴러다녔다"], answer: 1 },
    { q: "도깨비들은 왜 갑자기 눈에 보였나요?", choices: ["날이 밝았기 때문에", "돌쇠가 불렀기 때문에", "감투를 벗었기 때문에"], answer: 2 },
    { q: "돌쇠는 감투를 어떻게 얻었나요?", choices: ["씨름하는 틈에 훔쳤다", "도깨비에게 얻어 왔다", "무덤가에서 주웠다"], answer: 0 },
    { q: "감투를 쓰면 어떻게 되었나요?", choices: ["힘이 아주 세진다", "하늘을 날 수 있다", "몸이 보이지 않는다"], answer: 2 },
    { q: "돌쇠가 소 등에 올라타자 어떻게 되었나요?", choices: ["소가 온 동네를 뛰어다녔다", "소가 그 자리에 주저앉았다", "소가 돌쇠를 알아보았다"], answer: 0 },
    { q: "감투에 구멍이 난 까닭은 무엇인가요?", choices: ["쥐가 갉아 먹어서", "화롯불 불티가 튀어서", "못에 걸려 찢어져서"], answer: 1 },
    { q: "사람들은 무엇을 보고 돌쇠를 잡았나요?", choices: ["바닥에 찍힌 발자국", "떠다니는 빨간 점", "혼자 흔들리는 그림자"], answer: 1 },
    { q: "돌쇠는 마지막에 감투를 어떻게 했나요?", choices: ["남에게 팔았다", "땅에 묻어 두었다", "불에 태워 버렸다"], answer: 2 }
];

function quizPage() {
    const items = QUIZ.map((item, i) => `
        <div class="quiz-item" data-qindex="${i}">
            <p class="quiz-question">${i + 1}. ${item.q}</p>
            <div class="quiz-choices">
                ${item.choices.map((c, ci) => `<button type="button" class="quiz-choice" data-choice="${ci}">${c}</button>`).join('')}
            </div>
        </div>`).join('');
    return `
        <div class="page page-quiz">
            <h2>이야기 문제</h2>
            <p class="quiz-intro-text" id="quizProgress">0 / 총 ${QUIZ.length}문항 완료</p>
            <div class="quiz-list">${items}</div>
        </div>`;
}

function endPage() {
    return `
        <div class="page page-end">
            ${artFrame('end.png', '🌟')}
            <h2>도깨비 감투 이야기를 다 읽었어요!</h2>
            <a class="home-btn" href="../../../../../">학습 허브로 돌아가기</a>
        </div>`;
}

const PAGES = [
    { kind: 'cover' },
    { kind: 'toc' },
    ...CHAPTERS.flatMap(chapter => chapter.beats.map((beat, i) => ({ kind: 'spread', chapter, beat, isFirst: i === 0 }))),
    { kind: 'reflection', chapter: CHAPTERS[CHAPTERS.length - 1] },
    { kind: 'quiz' },
    { kind: 'end' }
];

const TWO_PAGE_KINDS = new Set(['spread', 'toc', 'cover']);

let folioCounter = 0;
const FOLIOS = PAGES.map(p => {
    const width = TWO_PAGE_KINDS.has(p.kind) ? 2 : 1;
    const start = folioCounter + 1;
    folioCounter += width;
    return { start, width };
});

function renderPage(page) {
    switch (page.kind) {
        case 'cover':
            return coverPage();
        case 'toc':
            return tocPage();
        case 'spread':
            return spreadPage(page.chapter, page.beat, page.isFirst);
        case 'reflection':
            return reflectionPage(page.chapter);
        case 'quiz':
            return quizPage();
        case 'end':
            return endPage();
        default:
            return '';
    }
}

let current = 0;
let animating = false;

const spreadEl = document.getElementById('spread');
const prevBtn = document.getElementById('prevBtn');
const nextBtn = document.getElementById('nextBtn');
const indicatorEl = document.getElementById('pageIndicator');
const folioLeftEl = document.getElementById('folioLeft');
const folioRightEl = document.getElementById('folioRight');

function paint() {
    spreadEl.innerHTML = renderPage(PAGES[current]);
    prevBtn.disabled = current === 0;
    nextBtn.disabled = current === PAGES.length - 1;
    indicatorEl.textContent = `${current + 1} / ${PAGES.length}`;

    const folio = FOLIOS[current];
    folioLeftEl.classList.toggle('folio-center', folio.width === 1);
    if (folio.width === 2) {
        folioLeftEl.textContent = folio.start;
        folioRightEl.textContent = folio.start + 1;
        folioLeftEl.hidden = false;
        folioRightEl.hidden = false;
    } else {
        folioLeftEl.textContent = folio.start;
        folioLeftEl.hidden = false;
        folioRightEl.hidden = true;
    }

    spreadEl.querySelectorAll('[data-goto]').forEach(btn => {
        btn.addEventListener('click', () => {
            const chapterNum = Number(btn.dataset.goto);
            const idx = PAGES.findIndex(p => p.kind === 'spread' && p.chapter.num === chapterNum && p.isFirst);
            if (idx >= 0) goTo(idx);
        });
    });
    spreadEl.querySelectorAll('[data-goto-kind]').forEach(btn => {
        btn.addEventListener('click', () => {
            const idx = PAGES.findIndex(p => p.kind === btn.dataset.gotoKind);
            if (idx >= 0) goTo(idx);
        });
    });

    if (PAGES[current].kind === 'quiz') {
        initQuiz();
    }
}

function initQuiz() {
    let answeredCount = 0;
    const progressEl = document.getElementById('quizProgress');

    spreadEl.querySelectorAll('.quiz-item').forEach(item => {
        const qi = Number(item.dataset.qindex);
        const q = QUIZ[qi];
        item.querySelectorAll('.quiz-choice').forEach(btn => {
            btn.addEventListener('click', () => {
                if (item.classList.contains('graded')) return;
                item.classList.add('graded');
                const chosen = Number(btn.dataset.choice);
                item.querySelectorAll('.quiz-choice').forEach(b => {
                    const ci = Number(b.dataset.choice);
                    if (ci === q.answer) b.classList.add('correct');
                    else if (ci === chosen) b.classList.add('incorrect');
                });
                answeredCount++;
                progressEl.textContent = `${answeredCount} / 총 ${QUIZ.length}문항 완료`;
            });
        });
    });
}

function goTo(index) {
    if (animating || index === current || index < 0 || index >= PAGES.length) return;
    animating = true;
    const dir = index > current ? 'flip-next' : 'flip-prev';
    spreadEl.classList.add(dir);
    setTimeout(() => {
        current = index;
        paint();
    }, 230);
    setTimeout(() => {
        spreadEl.classList.remove('flip-next', 'flip-prev');
        animating = false;
    }, 480);
}

prevBtn.addEventListener('click', () => goTo(current - 1));
nextBtn.addEventListener('click', () => goTo(current + 1));

document.getElementById('tocLink').addEventListener('click', () => {
    const idx = PAGES.findIndex(p => p.kind === 'toc');
    if (idx >= 0) goTo(idx);
});

document.addEventListener('keydown', (e) => {
    if (e.key === 'ArrowRight') goTo(current + 1);
    if (e.key === 'ArrowLeft') goTo(current - 1);
});

paint();
