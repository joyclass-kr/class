const CHAPTERS = [
    {
        num: 1,
        title: "1장 · 윗자리는 누가",
        beats: [
            {
                art: "01-feast.webp",
                emoji: "🍚",
                left: [
                    "어느 날 여우와 사슴과 두꺼비가 한자리에 모였습니다. 마을 잔치에 나란히 초대를 받았거든요.",
                    "셋 다 아침부터 잔뜩 차려입고 왔지요.",
                    "떡과 나물이 푸짐하게 차려진 잔칫상이 떡하니 놓여 있었지요. 김이 모락모락 나는 것이 보기만 해도 군침이 돌았답니다."
                ],
                right: [
                    "그런데 셋은 상 앞에서 우물쭈물하기만 했어요. 아무도 선뜻 앉지를 못했지요.",
                    "\"누가 윗자리에 앉지?\"",
                    "\"어르신 먼저 앉으시지요.\"",
                    "윗자리는 그중 가장 어른이 앉는 자리였거든요. 잘못 앉았다가는 두고두고 흉을 잡힐 판이었습니다."
                ]
            },
            {
                art: "02-rule.webp",
                emoji: "📜",
                left: [
                    "한참을 서 있던 여우가 헛기침을 하며 앞으로 나섰어요. 무언가 좋은 수가 떠오른 얼굴이었지요.",
                    "\"법도대로 하면 되지요.\"",
                    "여우가 헛기침을 하며 좌중을 둘러보았어요.",
                    "\"나이가 제일 많은 이가 윗자리에 앉는 겁니다. 예로부터 내려오는 법도 아니겠소.\""
                ],
                right: [
                    "\"옳거니, 그렇게 합시다.\"",
                    "\"그럼 각자 나이를 말해 보기로 하지요.\"",
                    "두꺼비만 아무 말이 없었지요.",
                    "사슴이 목을 길게 빼며 얼른 맞장구를 쳤습니다. 저도 자신이 있다는 듯한 얼굴이었지요.",
                    "두꺼비는 아무 말 없이 눈만 껌뻑껌뻑하고 있었답니다."
                ]
            },
            {
                art: "03-fox.webp",
                emoji: "🦊",
                left: [
                    "여우가 먼저 나섰어요. 헛기침을 두어 번 하고는 아주 점잖게 입을 열었습니다.",
                    "여우가 수염을 쓸어내렸어요.",
                    "\"나는 말이오,\"",
                    "\"하늘에 별이 몇 개인지 세던 시절에 태어났소.\"",
                    "상 앞이 조용해졌지요."
                ],
                right: [
                    "\"그때는 별이 겨우 열 개였지. 지금은 셀 수도 없이 많아졌지만 말이오.\"",
                    "여우는 수염을 쓸어내리며 잔뜩 으스댔습니다.",
                    "\"이만하면 윗자리는 내 것이지.\"",
                    "벌써 윗자리에 앉기라도 한 듯한 얼굴이었지요. 사슴은 그 꼴을 보고 눈을 가늘게 떴답니다."
                ]
            }
        ]
    },
    {
        num: 2,
        title: "2장 · 나이 겨루기",
        beats: [
            {
                art: "04-deer.webp",
                emoji: "🦌",
                left: [
                    "그러자 사슴이 대뜸 코웃음을 쳤어요. 콧방귀 소리가 어찌나 큰지 상이 다 울릴 지경이었지요.",
                    "\"허, 그거야 어린애 이야기지요.\"",
                    "사슴이 뿔을 흔들었어요.",
                    "\"내 이야기를 들으면 아마 놀라실 거요.\""
                ],
                right: [
                    "\"나는 저 앞산이 아직 조그만 언덕이던 시절에 뛰어놀았소.\"",
                    "\"내가 뿔로 긁어 놓은 자리가 바로 지금의 저 골짜기라오.\"",
                    "\"어떻소? 내가 더 윗사람이지요?\"",
                    "사슴이 뿔을 으쓱 치켜들자 여우의 얼굴이 굳어졌습니다. 질 수는 없다는 표정이었지요."
                ]
            },
            {
                art: "05-quarrel.webp",
                emoji: "😤",
                left: [
                    "\"허, 말도 안 되는 소리!\"",
                    "\"그쪽이야말로 지어낸 말 아니오?\"",
                    "잔칫상이 자꾸 식어 갔어요.",
                    "\"뭐요? 지금 나를 거짓말쟁이라 하는 거요?\"",
                    "여우와 사슴은 얼굴이 벌게지도록 다투기 시작했어요. 서로 삿대질까지 해 가면서요."
                ],
                right: [
                    "잔칫상은 손도 대지 못한 채 자꾸만 식어 갔습니다. 떡에서 나던 김도 어느새 사라졌지요.",
                    "두꺼비는 그동안 상 아래에 가만히 앉아 있기만 했답니다. 배를 볼록볼록하며 숨만 쉬고 있었어요.",
                    "\"저것들이 언제까지 저럴꼬.\""
                ]
            },
            {
                art: "06-tears.webp",
                emoji: "😢",
                left: [
                    "그때였습니다.",
                    "우와아앙—!",
                    "두꺼비가 울음을 터뜨린 것이었어요.",
                    "난데없는 울음소리에 여우와 사슴은 입을 딱 다물었어요. 다투던 것도 잊고 얼른 소리 난 쪽을 돌아보았지요."
                ],
                right: [
                    "두꺼비의 두 눈에서 눈물이 물줄기처럼 콸콸 솟구치고 있었습니다. 상 위의 나물 접시까지 축축하게 젖을 지경이었지요.",
                    "\"아니, 두꺼비. 갑자기 왜 우시오?\"",
                    "사슴도 놀라 눈을 껌뻑였지요.",
                    "여우가 어리둥절해서 물었답니다."
                ]
            }
        ]
    },
    {
        num: 3,
        title: "3장 · 두꺼비의 눈물",
        beats: [
            {
                art: "07-why.webp",
                emoji: "🌱",
                left: [
                    "두꺼비가 앞발로 눈물을 슥슥 닦으며 겨우 입을 열었어요. 목소리가 축축하게 잠겨 있었지요.",
                    "\"두 분 말씀을 듣자니 옛날 생각이 나서요.\"",
                    "두꺼비가 훌쩍이며 말을 이었어요.",
                    "\"참으려 해도 자꾸만 눈물이 나는군요.\""
                ],
                right: [
                    "\"제가 아주 어렸을 적에 나무 세 그루를 심었지요.\"",
                    "\"그 나무들이 지금 어찌 되었는지 아십니까?\"",
                    "듣고 있던 둘이 침을 꿀꺽 삼켰지요.",
                    "여우와 사슴이 슬그머니 서로를 쳐다보았습니다. 어쩐지 불길한 기분이 들었거든요."
                ]
            },
            {
                art: "08-trees.webp",
                emoji: "🌳",
                left: [
                    "두꺼비는 하늘을 한 번 올려다보고 천천히 말을 이었어요. 그 목소리가 어찌나 담담한지 더 무서웠지요.",
                    "여우와 사슴은 숨도 크게 쉬지 못했어요.",
                    "\"첫 번째 나무는 하늘을 떠받치는 기둥이 되었고,\"",
                    "\"두 번째 나무는 해와 달을 매다는 장대가 되었지요.\""
                ],
                right: [
                    "\"세 번째 나무는 별을 하늘에 박는 망치의 자루가 되었답니다.\"",
                    "\"그 나무들을 생각하니 그만…\"",
                    "여우와 사슴은 아무 말도 못 했지요.",
                    "두꺼비는 말끝을 흐리며 또 훌쩍였습니다. 어깨까지 들썩이며 우는 모습이 어찌나 서러워 보이던지요."
                ]
            },
            {
                art: "09-shock.webp",
                emoji: "😲",
                left: [
                    "여우와 사슴은 입을 딱 벌린 채 아무 말도 하지 못했습니다.",
                    "둘의 얼굴이 하얗게 질렸어요.",
                    "하늘도 해도 달도 별도, 두꺼비가 나무를 심고 난 뒤에야 생겼다는 말이었거든요. 그러니 나이를 견줄 것도 없었지요."
                ],
                right: [
                    "\"그, 그렇다면…\"",
                    "\"어르신이 제일 웃어른이시군요.\"",
                    "여우가 먼저 자리에서 일어났지요.",
                    "둘은 슬그머니 아랫자리로 물러났답니다.",
                    "조금 전까지 으스대던 것이 부끄러워 서로 얼굴도 들지 못했어요."
                ]
            },
            {
                art: "10-seat.webp",
                emoji: "🐸",
                left: [
                    "두꺼비는 엉금엉금 기어 윗자리로 올라갔어요. 자리에 앉더니 아주 흐뭇한 얼굴이 되었지요.",
                    "\"허허, 그럼 잘 먹겠습니다.\"",
                    "\"어이쿠, 두 분도 어서 드시지요.\"",
                    "두꺼비는 그제야 떡을 한 입 베어 물었답니다."
                ],
                right: [
                    "그런데 말이에요. 두꺼비가 정말 그 나무들을 심었는지는 아무도 알지 못한대요.",
                    "물어볼 사람이 하늘밖에 없으니까요.",
                    "여우와 사슴은 떡 한 조각도 못 먹었대요.",
                    "그날 잔칫상에서 제일 배불리 먹은 것은 두꺼비였다고 한답니다."
                ]
            }
        ],
        moral: "자랑은 부풀리기 쉬워요. 여우도 사슴도 두꺼비도 정말 그랬는지는 아무도 확인할 수 없었지요. 남보다 앞서려고 하는 말은 대개 그렇답니다.",
        question: "나를 크게 보이려고 사실보다 부풀려 말한 적이 있나요?"
    }
];

function artFrame(src, emoji) {
    return `
        <div class="art-frame">
            <img src="images/${src}" alt="" onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';">
            <div class="art-fallback" style="display:none">${emoji}</div>
        </div>`;
}

function coverToc() {
    const item = s => `
        <button type="button" data-goto="${s.num}">
            <span class="toc-num">${s.num}</span>
            <span>${s.title.replace(/^\d+장 · /, '')}</span>
        </button>`;
    return `
        <nav class="cover-toc">
            <h2>차례</h2>
            ${CHAPTERS.map(item).join('')}
            <button type="button" data-goto-kind="quiz">
                <span class="toc-num">❓</span>
                <span>이야기 문제</span>
            </button>
        </nav>`;
}

function coverPage() {
    return `
        <div class="page page-cover">
            <div class="story-page-left story-page-left-full">
                ${artFrame('cover.webp', '🐸')}
            </div>
            <div class="story-page-right">
                <h1>나이 자랑</h1>
                <p>나이 자랑은 지은이가 없는 구전 설화예요. 나이 겨루기, 상좌 다툼이라는 이름으로도 전해진답니다.</p>
                <p>옛날에는 나이가 곧 서열이었어요. 잔칫상에서 누가 윗자리에 앉느냐가 중요한 문제였고, 그래서 나이를 두고 겨루는 이야기가 여럿 생겼지요. 처음 만난 사람끼리 나이부터 묻는 우리 습관의 뿌리도 여기에 닿아 있답니다.</p>
                <p>두꺼비는 우리 옛이야기에서 늘 이런 자리를 맡아요. 크지도 빠르지도 않지만 마지막에 한 방을 날리는 쪽이지요. 남들이 놓친 것을 보아서 이기기도 하고, 이 이야기처럼 말솜씨로 이기기도 한답니다.</p>
                ${coverToc()}
            </div>
        </div>`;
}

function spreadPage(chapter, beat, isFirst) {
    const badgeHtml = isFirst ? `<div class="spread-chapter-badge">${chapter.title}</div>` : '';
    const leftHtml = beat.left.map(p => `<p>${p}</p>`).join('');
    const rightHtml = beat.right.map(p => `<p>${p}</p>`).join('');
    return `
        <div class="page page-story">
            <div class="spread-art">
                ${badgeHtml}
                ${artFrame(beat.art, beat.emoji)}
            </div>
            <div class="spread-text">
                <div class="spread-text-left">${leftHtml}</div>
                <div class="spread-text-right">${rightHtml}</div>
            </div>
        </div>`;
}


const QUIZ = [
    { q: "셋은 무엇 때문에 우물쭈물했나요?", choices: ["떡이 모자라서", "윗자리 때문에", "날이 저물어서"], answer: 1 },
    { q: "윗자리에는 누가 앉기로 했나요?", choices: ["힘이 가장 센 이", "달리기 빠른 이", "나이가 많은 이"], answer: 2 },
    { q: "여우는 언제 태어났다고 했나요?", choices: ["별을 세던 시절", "산이 생기기 전", "바다가 마른 때"], answer: 0 },
    { q: "사슴은 골짜기가 어떻게 생겼다고 했나요?", choices: ["비가 많이 와서", "바람이 깎아서", "자기 뿔로 긁어서"], answer: 2 },
    { q: "두꺼비는 왜 울었다고 했나요?", choices: ["옛 생각이 나서", "배가 고파서", "자리가 없어서"], answer: 0 },
    { q: "두꺼비가 심은 세 번째 나무는 무엇이 되었나요?", choices: ["하늘의 큰 기둥", "별 박는 망치 자루", "해를 매단 장대"], answer: 1 }
];

function quizPage() {
    const items = QUIZ.map((item, i) => `
        <div class="quiz-item" data-qindex="${i}">
            <p class="quiz-question">${i + 1}. ${item.q}</p>
            <div class="quiz-choices">
                ${item.choices.map((c, ci) => `<button type="button" class="quiz-choice" data-choice="${ci}">${c}</button>`).join('')}
            </div>
        </div>`).join('');
    return `
        <div class="page page-quiz">
            <h2>이야기 문제</h2>
            <p class="quiz-intro-text" id="quizProgress">0 / 총 ${QUIZ.length}문항 완료</p>
            <div class="quiz-list">${items}</div>
        </div>`;
}


/* 읽고 나서 — 세계명작 트랙과 같은 형식이다. 동화틀은 쪽을 재서 나누지 않으므로
   펼침면마다 왼쪽·오른쪽 글을 손으로 나누어 둔다. */
const AFTERWORD = {
    title: '읽고 나서',
    emoji: '🎂',
    spreads: [
        {
            art: 'end.webp',
            left: [
                "누가 가장 나이가 많은지를 다투는 이야기는 우리나라에도 있고 이웃 나라에도 있습니다. 짐승들이 저마다 아득한 옛일을 들어 자랑하는 짜임이 같습니다.",
                "이런 이야기가 웃긴 까닭은 아무도 확인할 수 없기 때문입니다. 하늘을 만들 때 있었다고 해도 아니라고 할 사람이 없습니다.",
                "다시 보면 두꺼비는 다른 것들처럼 오랜 옛일을 대지 않았습니다. 대신 눈물을 흘렸습니다.",
                "확인할 수 없는 자랑들 사이에서 확인할 수 없는 슬픔을 꺼낸 것이지요. 그 앞에서는 아무도 더 우길 수가 없었습니다."
            ],
            right: [
                "자랑은 부풀리기 쉽습니다. 여우도 사슴도 두꺼비도 정말 그랬는지는 아무도 확인할 수 없었습니다. 남보다 앞서려고 하는 말은 대개 그렇습니다.",
                "나를 크게 보이려고 사실보다 부풀려 말한 적이 있나요?"
            ]
        }
    ]
};

function afterPage(spread, isFirst) {
    const head = isFirst ? `<h2>${AFTERWORD.title}</h2>` : '';
    // 그림은 오른쪽 칸 맨 위 모서리에 꽉 붙인다. 학습 허브로 가는 길은 그 칸 맨 아래에 둔다.
    const art = spread.art ? `<div class="after-art">${artFrame(spread.art, AFTERWORD.emoji)}</div>` : '';
    const foot = spread.last ? `<p class="after-home"><a class="home-btn" href="../../../../../">학습 허브로 돌아가기</a></p>` : '';
    const col = (ps) => ps.map(t => `<p>${t}</p>`).join('');
    return `
        <div class="page page-after">
            <div class="after-col after-col-left">
                ${head}
                ${col(spread.left)}
            </div>
            <div class="after-col after-col-right${spread.art ? ' after-col-image' : ''}">
                ${art}
                ${col(spread.right)}
                ${foot}
            </div>
        </div>`;
}

const PAGES = [
    { kind: 'cover' },
    ...CHAPTERS.flatMap(chapter => chapter.beats.map((beat, i) => ({ kind: 'spread', chapter, beat, isFirst: i === 0 }))),
    { kind: 'quiz' },
    ...AFTERWORD.spreads.map((spread, i) => ({ kind: 'after', spread, isFirst: i === 0, last: i === AFTERWORD.spreads.length - 1 })),
];

const TWO_PAGE_KINDS = new Set(['spread', 'cover', 'after']);

let folioCounter = 0;
const FOLIOS = PAGES.map(p => {
    const width = TWO_PAGE_KINDS.has(p.kind) ? 2 : 1;
    const start = folioCounter + 1;
    folioCounter += width;
    return { start, width };
});

function renderPage(page) {
    switch (page.kind) {
        case 'cover':
            return coverPage();
        case 'spread':
            return spreadPage(page.chapter, page.beat, page.isFirst);
        case 'after':
            return afterPage({ ...page.spread, last: page.last }, page.isFirst);
        case 'quiz':
            return quizPage();
        default:
            return '';
    }
}

let current = 0;
let animating = false;

const spreadEl = document.getElementById('spread');
const prevBtn = document.getElementById('prevBtn');
const nextBtn = document.getElementById('nextBtn');
const indicatorEl = document.getElementById('pageIndicator');
const folioLeftEl = document.getElementById('folioLeft');
const folioRightEl = document.getElementById('folioRight');

function paint() {
    spreadEl.innerHTML = renderPage(PAGES[current]);
    prevBtn.disabled = current === 0;
    nextBtn.disabled = current === PAGES.length - 1;
    indicatorEl.textContent = `${current + 1} / ${PAGES.length}`;

    const folio = FOLIOS[current];
    folioLeftEl.classList.toggle('folio-center', folio.width === 1);
    if (folio.width === 2) {
        folioLeftEl.textContent = folio.start;
        folioRightEl.textContent = folio.start + 1;
        folioLeftEl.hidden = false;
        folioRightEl.hidden = false;
    } else {
        folioLeftEl.textContent = folio.start;
        folioLeftEl.hidden = false;
        folioRightEl.hidden = true;
    }

    spreadEl.querySelectorAll('[data-goto]').forEach(btn => {
        btn.addEventListener('click', () => {
            const chapterNum = Number(btn.dataset.goto);
            const idx = PAGES.findIndex(p => p.kind === 'spread' && p.chapter.num === chapterNum && p.isFirst);
            if (idx >= 0) goTo(idx);
        });
    });
    spreadEl.querySelectorAll('[data-goto-kind]').forEach(btn => {
        btn.addEventListener('click', () => {
            const idx = PAGES.findIndex(p => p.kind === btn.dataset.gotoKind);
            if (idx >= 0) goTo(idx);
        });
    });

    if (PAGES[current].kind === 'quiz') {
        initQuiz();
    }
}

function initQuiz() {
    let answeredCount = 0;
    const progressEl = document.getElementById('quizProgress');

    spreadEl.querySelectorAll('.quiz-item').forEach(item => {
        const qi = Number(item.dataset.qindex);
        const q = QUIZ[qi];
        item.querySelectorAll('.quiz-choice').forEach(btn => {
            btn.addEventListener('click', () => {
                if (item.classList.contains('graded')) return;
                item.classList.add('graded');
                const chosen = Number(btn.dataset.choice);
                item.querySelectorAll('.quiz-choice').forEach(b => {
                    const ci = Number(b.dataset.choice);
                    if (ci === q.answer) b.classList.add('correct');
                    else if (ci === chosen) b.classList.add('incorrect');
                });
                answeredCount++;
                progressEl.textContent = `${answeredCount} / 총 ${QUIZ.length}문항 완료`;
            });
        });
    });
}

function goTo(index) {
    if (animating || index === current || index < 0 || index >= PAGES.length) return;
    animating = true;
    const dir = index > current ? 'flip-next' : 'flip-prev';
    spreadEl.classList.add(dir);
    setTimeout(() => {
        current = index;
        paint();
    }, 230);
    setTimeout(() => {
        spreadEl.classList.remove('flip-next', 'flip-prev');
        animating = false;
    }, 480);
}

prevBtn.addEventListener('click', () => goTo(current - 1));
nextBtn.addEventListener('click', () => goTo(current + 1));

document.getElementById('tocLink').addEventListener('click', () => {
    // 그림책은 차례가 표지에 붙어 있다.
    const idx = PAGES.findIndex(p => p.kind === 'toc');
    goTo(idx >= 0 ? idx : 0);
});

document.addEventListener('keydown', (e) => {
    if (e.key === 'ArrowRight') goTo(current + 1);
    if (e.key === 'ArrowLeft') goTo(current - 1);
});

paint();
