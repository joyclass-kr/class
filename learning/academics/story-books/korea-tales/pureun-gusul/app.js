const CHAPTERS = [
    {
        num: 1,
        title: "1장 · 잉어가 준 구슬",
        beats: [
            {
                art: "01-carp.png",
                emoji: "🐟",
                left: [
                    "옛날 강가에 가난한 할아버지 내외가 살았습니다.",
                    "그날도 할아버지는 종일 그물을 던졌지만 잡힌 것이 없었어요."
                ],
                right: [
                    "해질 무렵에야 커다란 잉어 한 마리가 걸려 올라왔지요.",
                    "\"오늘 저녁은 걱정 없겠구나.\""
                ]
            },
            {
                art: "02-release.png",
                emoji: "💧",
                left: [
                    "그런데 잉어가 할아버지를 물끄러미 올려다보았어요.",
                    "커다란 눈에서 눈물이 뚝 떨어졌습니다."
                ],
                right: [
                    "할아버지는 한참을 들여다보다가 그물을 풀었어요.",
                    "\"에라, 오늘도 굶지 뭐.\"",
                    "잉어는 물속으로 쏙 사라졌답니다."
                ]
            },
            {
                art: "03-prince.png",
                emoji: "🔵",
                left: [
                    "그날 밤이었어요.",
                    "문을 두드리는 소리에 나가 보니 낯선 젊은이가 서 있었습니다."
                ],
                right: [
                    "\"저는 용왕의 아들입니다. 낮에 살려 주셔서 고맙습니다.\"",
                    "젊은이는 푸른 구슬 하나를 내밀었어요.",
                    "\"무엇이든 원하는 것을 말해 보십시오.\""
                ]
            },
            {
                art: "04-rich.png",
                emoji: "🏡",
                left: [
                    "\"쌀 한 가마니만 나오너라.\"",
                    "정말로 방 안에 쌀가마가 척 놓였어요."
                ],
                right: [
                    "얼마 지나지 않아 낡은 집은 기와집으로 바뀌었습니다.",
                    "마당의 개와 고양이도 날마다 배불리 먹었지요.",
                    "\"우리도 이런 날이 오는구나.\""
                ]
            }
        ]
    },
    {
        num: 2,
        title: "2장 · 쥐들의 왕",
        beats: [
            {
                art: "05-steal.png",
                emoji: "😈",
                left: [
                    "소문은 강 건너 마을까지 퍼졌어요.",
                    "욕심 많은 할멈 하나가 그 소문을 듣고 찾아왔습니다."
                ],
                right: [
                    "할멈은 이런저런 말로 할머니를 꾀어 구슬을 손에 넣었어요. 그러고는 나룻배를 타고 강을 건너가 버렸지요.",
                    "그날부터 기와집은 다시 낡은 집이 되었답니다."
                ]
            },
            {
                art: "06-decide.png",
                emoji: "🐕",
                left: [
                    "마당에서 개와 고양이가 마주 앉았어요.",
                    "\"우리가 찾아오자.\"",
                    "\"그래. 우리라도 해야지.\""
                ],
                right: [
                    "둘은 그날 밤 몰래 집을 나섰습니다.",
                    "강까지 가는 데만 한나절이 걸렸어요."
                ]
            },
            {
                art: "07-swim.png",
                emoji: "🌊",
                left: [
                    "강물은 넓고 깊었어요.",
                    "\"나는 헤엄을 못 치는데.\"",
                    "고양이가 발끝을 물에 담갔다 얼른 뺐습니다."
                ],
                right: [
                    "\"내 등에 타라.\"",
                    "개가 등을 내밀었어요.",
                    "고양이는 개 등에 납작 엎드려 강을 건넜답니다."
                ]
            },
            {
                art: "08-chest.png",
                emoji: "🔒",
                left: [
                    "할멈의 집에 몰래 숨어들었어요.",
                    "구슬은 방 안 궤짝 속에 있었습니다."
                ],
                right: [
                    "그런데 궤짝에는 커다란 자물쇠가 채워져 있었지요.",
                    "발톱으로 긁어도, 이빨로 물어도 꿈쩍하지 않았어요.",
                    "\"이걸 어쩌지…\""
                ]
            },
            {
                art: "09-ratking.png",
                emoji: "👑",
                left: [
                    "그때 고양이의 눈이 반짝였어요. 고양이는 벽 밑 쥐구멍 앞에 가만히 엎드렸습니다.",
                    "한참 뒤, 작은 왕관을 쓴 쥐 한 마리가 고개를 내밀었지요."
                ],
                right: [
                    "\"어이쿠!\"",
                    "고양이의 앞발이 쥐들의 왕 목덜미를 덥석 잡았어요.",
                    "\"자, 잠깐! 잠깐만요!\""
                ]
            }
        ]
    },
    {
        num: 3,
        title: "3장 · 강에 빠진 구슬",
        beats: [
            {
                art: "10-threat.png",
                emoji: "😼",
                left: [
                    "\"저 궤짝 속에 푸른 구슬이 있다.\"",
                    "\"날이 새기 전에 꺼내 오너라.\"",
                    "\"그, 그게 저희가 어떻게…\""
                ],
                right: [
                    "고양이가 발톱을 스윽 세웠어요.",
                    "\"못 하겠다면 이 집 쥐를 한 마리도 남기지 않겠다.\"",
                    "\"하겠습니다! 당장 하겠습니다요!\""
                ]
            },
            {
                art: "11-gnaw.png",
                emoji: "🐀",
                left: [
                    "쥐들이 우르르 몰려나왔어요.",
                    "사각사각, 사각사각."
                ],
                right: [
                    "수십 마리가 달라붙어 궤짝을 갉았습니다. 곧 구멍이 뚫리고 푸른 구슬이 데구루루 굴러 나왔지요.",
                    "고양이는 구슬을 입에 물고 쏜살같이 달렸답니다."
                ]
            },
            {
                art: "12-drop.png",
                emoji: "😱",
                left: [
                    "돌아오는 길, 다시 강을 건널 때였어요.",
                    "개가 자꾸 고개를 돌려 물었습니다.",
                    "\"구슬 잘 있냐?\""
                ],
                right: [
                    "고양이는 입을 꼭 다물고 고개만 끄덕였어요.",
                    "\"야, 구슬 잘 있냐니까!\"",
                    "\"있다니까—\"",
                    "퐁당."
                ]
            },
            {
                art: "13-fish.png",
                emoji: "🐠",
                left: [
                    "개는 화가 나서 뒤도 안 보고 집으로 가 버렸어요.",
                    "혼자 남은 고양이는 강가를 몇 번이나 오갔습니다."
                ],
                right: [
                    "해가 기울 무렵, 모래밭에 죽은 물고기 한 마리가 밀려와 있었어요.",
                    "배가 고팠던 고양이가 그것을 뜯어먹는데,",
                    "무언가 단단한 것이 이에 딱 걸렸지요."
                ]
            },
            {
                art: "14-home.png",
                emoji: "🏠",
                left: [
                    "푸른 구슬이었어요!",
                    "고양이는 구슬을 물고 집으로 달렸습니다."
                ],
                right: [
                    "\"아이고, 우리 고양이가 다 찾아왔구나!\"",
                    "그날부터 고양이는 따뜻한 방 안에서 지내게 되었고,",
                    "개는 마당에 남았지요. 둘이 사이가 나빠진 것도 그때부터래요."
                ]
            }
        ],
        moral: "개도 고양이도 똑같이 애를 썼어요. 그런데 마지막 한 걸음을 해낸 쪽만 방 안으로 들어갔지요. 함께 한 일을 한 사람의 공으로만 세면, 남은 쪽의 마음에는 서운함이 남는답니다.",
        question: "여럿이 함께 해낸 일에서, 나는 누구의 몫을 잊고 있었을까요?"
    }
];

function artFrame(src, emoji) {
    return `
        <div class="art-frame">
            <img src="images/${src}" alt="" onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';">
            <div class="art-fallback" style="display:none">${emoji}</div>
        </div>`;
}

function coverPage() {
    return `
        <div class="page page-cover">
            <div class="story-page-left story-page-left-full">
                ${artFrame('cover.png', '🔵')}
            </div>
            <div class="story-page-right">
                <h1>푸른 구슬</h1>
                <p>푸른 구슬은 지은이가 없는 구전 설화예요. 개와 고양이가 잃어버린 구슬을 찾아오는 이야기라, 개와 고양이의 구슬 찾기라는 이름으로도 전해진답니다.</p>
                <p>이 이야기는 유래담이기도 해요. 왜 개는 마당에서 지내고 고양이는 방 안에서 지내는지, 왜 개와 고양이는 사이가 나쁜지를 이야기 하나로 설명하지요. 우리 옛이야기는 이렇게 늘 보던 것에 까닭을 붙이기를 좋아한답니다.</p>
                <p>잃어버린 보물을 짐승 둘이 짝을 지어 되찾아 오는 이야기는 우리나라뿐 아니라 중국과 일본, 멀리 유럽에도 전해요. 물을 건널 때 헤엄 못 치는 쪽이 업혀 간다는 대목까지 여러 나라 이야기에 똑같이 나온답니다.</p>
            </div>
        </div>`;
}

function tocPage() {
    const itemHtml = s => `
        <li>
            <button type="button" data-goto="${s.num}">
                <span class="toc-num">${s.num}</span>
                <span>
                    <strong>${s.title.replace(/^\d+장 · /, '')}</strong>
                </span>
            </button>
        </li>`;
    const quizItemHtml = `
        <li>
            <button type="button" data-goto-kind="quiz">
                <span class="toc-num">❓</span>
                <span>
                    <strong>이야기 문제</strong>
                </span>
            </button>
        </li>`;
    const half = Math.ceil(CHAPTERS.length / 2);
    const leftItems = CHAPTERS.slice(0, half).map(itemHtml).join('');
    const rightItems = CHAPTERS.slice(half).map(itemHtml).join('') + quizItemHtml;
    return `
        <div class="page page-toc">
            <div class="story-page-left">
                <h2>차례</h2>
                <ul class="toc-list">${leftItems}</ul>
            </div>
            <div class="story-page-right">
                <ul class="toc-list">${rightItems}</ul>
            </div>
        </div>`;
}

function spreadPage(chapter, beat, isFirst) {
    const badgeHtml = isFirst ? `<div class="spread-chapter-badge">${chapter.title}</div>` : '';
    const leftHtml = beat.left.map(p => `<p>${p}</p>`).join('');
    const rightHtml = beat.right.map(p => `<p>${p}</p>`).join('');
    return `
        <div class="page page-story">
            <div class="spread-art">
                ${badgeHtml}
                ${artFrame(beat.art, beat.emoji)}
            </div>
            <div class="spread-text">
                <div class="spread-text-left">${leftHtml}</div>
                <div class="spread-text-right">${rightHtml}</div>
            </div>
        </div>`;
}

function reflectionPage(chapter) {
    return `
        <div class="page page-reflection">
            <h2>이야기를 다 읽고</h2>
            <p class="reflection-moral">${chapter.moral}</p>
            <p class="reflection-question">${chapter.question}</p>
        </div>`;
}

const QUIZ = [
    { q: "할아버지는 잉어를 어떻게 했나요?", choices: ["장에 내다 팔았다", "강으로 놓아 주었다", "끓여서 먹었다"], answer: 1 },
    { q: "푸른 구슬은 어떤 구슬이었나요?", choices: ["앞날이 보이는 구슬", "병을 고치는 구슬", "무엇이든 나오는 구슬"], answer: 2 },
    { q: "구슬을 훔쳐 간 것은 누구인가요?", choices: ["강 건너 욕심쟁이 할멈", "지나가던 늙은 나그네", "이웃 마을 아이들"], answer: 0 },
    { q: "고양이는 강을 어떻게 건넜나요?", choices: ["헤엄을 쳐서", "배를 얻어 타고", "개 등에 타고서"], answer: 2 },
    { q: "고양이는 구슬을 꺼내려고 누구를 붙잡았나요?", choices: ["쥐들의 왕", "집 지키는 개", "할멈의 손자"], answer: 0 },
    { q: "고양이는 구슬을 어디에서 다시 찾았나요?", choices: ["강가 모래 속", "죽은 물고기 뱃속", "할멈의 궤짝 속"], answer: 1 }
];

function quizPage() {
    const items = QUIZ.map((item, i) => `
        <div class="quiz-item" data-qindex="${i}">
            <p class="quiz-question">${i + 1}. ${item.q}</p>
            <div class="quiz-choices">
                ${item.choices.map((c, ci) => `<button type="button" class="quiz-choice" data-choice="${ci}">${c}</button>`).join('')}
            </div>
        </div>`).join('');
    return `
        <div class="page page-quiz">
            <h2>이야기 문제</h2>
            <p class="quiz-intro-text" id="quizProgress">0 / 총 ${QUIZ.length}문항 완료</p>
            <div class="quiz-list">${items}</div>
        </div>`;
}

function endPage() {
    return `
        <div class="page page-end">
            ${artFrame('end.png', '🌟')}
            <h2>푸른 구슬 이야기를 다 읽었어요!</h2>
            <a class="home-btn" href="../../../../../">학습 허브로 돌아가기</a>
        </div>`;
}

const PAGES = [
    { kind: 'cover' },
    { kind: 'toc' },
    ...CHAPTERS.flatMap(chapter => chapter.beats.map((beat, i) => ({ kind: 'spread', chapter, beat, isFirst: i === 0 }))),
    { kind: 'reflection', chapter: CHAPTERS[CHAPTERS.length - 1] },
    { kind: 'quiz' },
    { kind: 'end' }
];

const TWO_PAGE_KINDS = new Set(['spread', 'toc', 'cover']);

let folioCounter = 0;
const FOLIOS = PAGES.map(p => {
    const width = TWO_PAGE_KINDS.has(p.kind) ? 2 : 1;
    const start = folioCounter + 1;
    folioCounter += width;
    return { start, width };
});

function renderPage(page) {
    switch (page.kind) {
        case 'cover':
            return coverPage();
        case 'toc':
            return tocPage();
        case 'spread':
            return spreadPage(page.chapter, page.beat, page.isFirst);
        case 'reflection':
            return reflectionPage(page.chapter);
        case 'quiz':
            return quizPage();
        case 'end':
            return endPage();
        default:
            return '';
    }
}

let current = 0;
let animating = false;

const spreadEl = document.getElementById('spread');
const prevBtn = document.getElementById('prevBtn');
const nextBtn = document.getElementById('nextBtn');
const indicatorEl = document.getElementById('pageIndicator');
const folioLeftEl = document.getElementById('folioLeft');
const folioRightEl = document.getElementById('folioRight');

function paint() {
    spreadEl.innerHTML = renderPage(PAGES[current]);
    prevBtn.disabled = current === 0;
    nextBtn.disabled = current === PAGES.length - 1;
    indicatorEl.textContent = `${current + 1} / ${PAGES.length}`;

    const folio = FOLIOS[current];
    folioLeftEl.classList.toggle('folio-center', folio.width === 1);
    if (folio.width === 2) {
        folioLeftEl.textContent = folio.start;
        folioRightEl.textContent = folio.start + 1;
        folioLeftEl.hidden = false;
        folioRightEl.hidden = false;
    } else {
        folioLeftEl.textContent = folio.start;
        folioLeftEl.hidden = false;
        folioRightEl.hidden = true;
    }

    spreadEl.querySelectorAll('[data-goto]').forEach(btn => {
        btn.addEventListener('click', () => {
            const chapterNum = Number(btn.dataset.goto);
            const idx = PAGES.findIndex(p => p.kind === 'spread' && p.chapter.num === chapterNum && p.isFirst);
            if (idx >= 0) goTo(idx);
        });
    });
    spreadEl.querySelectorAll('[data-goto-kind]').forEach(btn => {
        btn.addEventListener('click', () => {
            const idx = PAGES.findIndex(p => p.kind === btn.dataset.gotoKind);
            if (idx >= 0) goTo(idx);
        });
    });

    if (PAGES[current].kind === 'quiz') {
        initQuiz();
    }
}

function initQuiz() {
    let answeredCount = 0;
    const progressEl = document.getElementById('quizProgress');

    spreadEl.querySelectorAll('.quiz-item').forEach(item => {
        const qi = Number(item.dataset.qindex);
        const q = QUIZ[qi];
        item.querySelectorAll('.quiz-choice').forEach(btn => {
            btn.addEventListener('click', () => {
                if (item.classList.contains('graded')) return;
                item.classList.add('graded');
                const chosen = Number(btn.dataset.choice);
                item.querySelectorAll('.quiz-choice').forEach(b => {
                    const ci = Number(b.dataset.choice);
                    if (ci === q.answer) b.classList.add('correct');
                    else if (ci === chosen) b.classList.add('incorrect');
                });
                answeredCount++;
                progressEl.textContent = `${answeredCount} / 총 ${QUIZ.length}문항 완료`;
            });
        });
    });
}

function goTo(index) {
    if (animating || index === current || index < 0 || index >= PAGES.length) return;
    animating = true;
    const dir = index > current ? 'flip-next' : 'flip-prev';
    spreadEl.classList.add(dir);
    setTimeout(() => {
        current = index;
        paint();
    }, 230);
    setTimeout(() => {
        spreadEl.classList.remove('flip-next', 'flip-prev');
        animating = false;
    }, 480);
}

prevBtn.addEventListener('click', () => goTo(current - 1));
nextBtn.addEventListener('click', () => goTo(current + 1));

document.getElementById('tocLink').addEventListener('click', () => {
    const idx = PAGES.findIndex(p => p.kind === 'toc');
    if (idx >= 0) goTo(idx);
});

document.addEventListener('keydown', (e) => {
    if (e.key === 'ArrowRight') goTo(current + 1);
    if (e.key === 'ArrowLeft') goTo(current - 1);
});

paint();
