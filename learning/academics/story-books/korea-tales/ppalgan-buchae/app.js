const CHAPTERS = [
    {
        num: 1,
        title: "1장 · 산에서 주운 부채",
        beats: [
            {
                art: "01-nap.png",
                emoji: "😴",
                left: [
                    "김 첨지는 일하기보다 낮잠 자기를 좋아하는 사람이었습니다.",
                    "그날도 산에 올라가 나무 그늘에 벌렁 드러누웠지요."
                ],
                right: [
                    "한숨 자고 일어나 보니 옆에 부채 두 자루가 놓여 있었어요.",
                    "하나는 새빨갛고, 하나는 새파랬지요.",
                    "\"이게 웬 떡이냐.\""
                ]
            },
            {
                art: "02-red.png",
                emoji: "🪭",
                left: [
                    "마침 덥던 참이라 빨간 부채를 집어 얼굴에 부쳤어요.",
                    "부채질 한 번, 두 번.",
                    "그런데 코끝이 간질간질하더니,"
                ],
                right: [
                    "쑤욱—",
                    "코가 손가락 하나만큼 길어졌지 뭐예요!"
                ]
            },
            {
                art: "03-blue.png",
                emoji: "😱",
                left: [
                    "\"으악! 이게 무슨 일이야!\"",
                    "김 첨지는 허둥지둥 파란 부채를 집어 들었습니다. 부치자마자 코가 스르륵 제자리로 돌아왔어요."
                ],
                right: [
                    "한참 멍하니 앉아 있던 김 첨지의 입가에 웃음이 번졌지요.",
                    "'옳거니. 이걸로 한몫 잡겠구나.'"
                ]
            },
            {
                art: "04-feast.png",
                emoji: "🎊",
                left: [
                    "마침 마을 부잣집에서 큰 잔치가 열렸어요. 김 첨지는 사람들 틈에 슬쩍 끼어들었습니다. 상석에 앉은 주인 영감이 껄껄 웃고 있었지요."
                ],
                right: [
                    "김 첨지는 기둥 뒤에 숨어 빨간 부채를 꺼냈어요.",
                    "살랑, 살랑, 살랑."
                ]
            }
        ]
    },
    {
        num: 2,
        title: "2장 · 집과 바꾼 부채",
        beats: [
            {
                art: "05-nose.png",
                emoji: "😲",
                left: [
                    "영감의 코가 쑥쑥 자라기 시작했습니다.",
                    "한 뼘, 두 뼘, 세 뼘."
                ],
                right: [
                    "\"으아악! 영감님 코가!\"",
                    "잔칫상이 뒤집히고 온 집안이 발칵 뒤집혔어요. 의원을 셋이나 불렀지만 아무도 고치지 못했지요."
                ]
            },
            {
                art: "06-cure.png",
                emoji: "🩺",
                left: [
                    "\"코를 고쳐 주는 이에게 큰 상을 내리겠소!\"",
                    "그제야 김 첨지가 어슬렁어슬렁 나타났습니다."
                ],
                right: [
                    "소매 속에서 파란 부채를 꺼내 몇 번 부치자, 영감의 코가 스르륵 줄어들었어요.",
                    "그런데 영감의 눈이 부채에 딱 붙어 버렸지요."
                ]
            },
            {
                art: "07-trade.png",
                emoji: "🏯",
                left: [
                    "\"그 부채를 나에게 파시오.\"",
                    "\"싫습니다. 제 밥줄인걸요.\"",
                    "\"그럼 이 집을 통째로 주겠소!\""
                ],
                right: [
                    "\"…예?\"",
                    "\"기와집도, 곳간도, 마당도 다 주겠소!\"",
                    "영감의 눈에서 불이 났어요."
                ]
            },
            {
                art: "08-moveout.png",
                emoji: "🧳",
                left: [
                    "그날로 영감네 식구가 짐을 싸서 집을 나섰습니다.",
                    "보따리를 머리에 이고, 지게에 세간을 지고, 항아리를 안고."
                ],
                right: [
                    "\"영감님, 정말 이래도 되는 겁니까?\"",
                    "\"시끄럽다! 저 부채만 있으면 집 열 채도 짓는다!\"",
                    "식구들은 울상이 되어 줄줄이 걸어 나갔지요."
                ]
            }
        ]
    },
    {
        num: 3,
        title: "3장 · 웬 지진이냐",
        beats: [
            {
                art: "09-play.png",
                emoji: "🪭",
                left: [
                    "빈 집 마당에 자리를 깔고 앉은 영감은 신이 났어요.",
                    "\"어디 한번 볼까?\""
                ],
                right: [
                    "빨간 부채를 살랑살랑 부치자 제 코가 쭉쭉 뻗어 올라갔습니다.",
                    "\"하하! 이거 재미있구나!\"",
                    "영감은 멈출 줄을 몰랐어요."
                ]
            },
            {
                art: "10-sky.png",
                emoji: "☁️",
                left: [
                    "코가 지붕을 넘고, 나무를 넘고, 구름을 뚫었어요.",
                    "\"더! 더!\""
                ],
                right: [
                    "지나가던 새가 깜짝 놀라 옆으로 비켜 날았지요.",
                    "그런데 그 위에는 하늘나라가 있었답니다. 영감은 그것을 몰랐어요."
                ]
            },
            {
                art: "11-emperor.png",
                emoji: "🍚",
                left: [
                    "그때 옥황상제는 한창 밥을 드시는 중이었습니다.",
                    "덜컹, 덜컹."
                ],
                right: [
                    "상이 흔들리고 국그릇에서 국물이 찰랑거렸어요.",
                    "\"웬 지진이냐!\"",
                    "옥황상제가 수저를 놓고 벌떡 일어섰지요."
                ]
            },
            {
                art: "12-pillar.png",
                emoji: "😡",
                left: [
                    "밖으로 나가 보니 구름을 뚫고 웬 벌건 기둥이 하나 솟아 있었어요.",
                    "\"이게 대체 무엇이냐!\""
                ],
                right: [
                    "\"난데없이 기둥이 하나 자랐사옵니다.\"",
                    "\"거참 흉하구나. 흔들리지 않게 꽁꽁 묶어 두어라!\"",
                    "신하들이 밧줄을 들고 달려들었습니다."
                ]
            },
            {
                art: "13-tied.png",
                emoji: "🪢",
                left: [
                    "아래에서는 영감이 펄쩍펄쩍 뛰었어요.",
                    "\"아야야야! 아파! 아프다고!\"",
                    "영감은 허겁지겁 파란 부채를 부쳤습니다."
                ],
                right: [
                    "그런데 코가 줄어들자 이번에는 몸이 하늘로 쑥 딸려 올라갔어요.",
                    "\"어어어? 이건 또 뭐야!\"",
                    "\"허, 기둥에 사람이 달렸구나.\""
                ]
            },
            {
                art: "14-fall.png",
                emoji: "💫",
                left: [
                    "하늘 신하들이 밧줄을 풀어 주자, 영감은 데굴데굴 굴러떨어져 마당 짚더미에 쿵 박혔습니다. 부채 두 자루는 어디론가 날아가 버렸지요."
                ],
                right: [
                    "그 꼴을 본 김 첨지는 슬그머니 문서를 꺼냈어요.",
                    "\"이 집은 부채로 얻은 것이니 제 것이 아닙니다.\"",
                    "영감네 식구는 그날로 제 집에 돌아왔답니다."
                ]
            }
        ],
        moral: "부자는 집을 주고 부채를 얻었고, 김 첨지는 부채를 주고 집을 얻었어요. 그런데 둘 다 남은 것이 없었지요. 제 힘으로 얻지 않은 것은 손에 오래 머물지 않는답니다.",
        question: "내가 가진 것 가운데 내 힘으로 얻은 것은 무엇인가요?"
    }
];

function artFrame(src, emoji) {
    return `
        <div class="art-frame">
            <img src="images/${src}" alt="" onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';">
            <div class="art-fallback" style="display:none">${emoji}</div>
        </div>`;
}

function coverToc() {
    const item = s => `
        <button type="button" data-goto="${s.num}">
            <span class="toc-num">${s.num}</span>
            <span>${s.title.replace(/^\d+장 · /, '')}</span>
        </button>`;
    return `
        <nav class="cover-toc">
            <h2>차례</h2>
            ${CHAPTERS.map(item).join('')}
            <button type="button" data-goto-kind="quiz">
                <span class="toc-num">❓</span>
                <span>이야기 문제</span>
            </button>
        </nav>`;
}

function coverPage() {
    return `
        <div class="page page-cover">
            <div class="story-page-left story-page-left-full">
                ${artFrame('cover.png', '🪭')}
            </div>
            <div class="story-page-right">
                <h1>빨간 부채 파란 부채</h1>
                <p>빨간 부채 파란 부채는 지은이가 없는 구전 설화예요. 부치기만 하면 코가 늘었다 줄었다 하는 부채 두 자루가 나오는 이야기랍니다.</p>
                <p>일본과 중국에도 이와 아주 닮은 이야기가 전해요. 특히 일본에는 코가 늘어나는 부채 이야기가 널리 알려져 있어서, 세 나라를 오가며 퍼진 이야기로 보는 학자가 많답니다.</p>
                <p>코가 길어진다는 생각이 우스운 까닭은, 코가 얼굴에서 가장 눈에 띄면서도 마음대로 할 수 없는 곳이기 때문이에요. 옛이야기는 이렇게 몸의 한 군데를 크게 부풀려 웃음을 만들곤 한답니다.</p>
                ${coverToc()}
            </div>
        </div>`;
}

function spreadPage(chapter, beat, isFirst) {
    const badgeHtml = isFirst ? `<div class="spread-chapter-badge">${chapter.title}</div>` : '';
    const leftHtml = beat.left.map(p => `<p>${p}</p>`).join('');
    const rightHtml = beat.right.map(p => `<p>${p}</p>`).join('');
    return `
        <div class="page page-story">
            <div class="spread-art">
                ${badgeHtml}
                ${artFrame(beat.art, beat.emoji)}
            </div>
            <div class="spread-text">
                <div class="spread-text-left">${leftHtml}</div>
                <div class="spread-text-right">${rightHtml}</div>
            </div>
        </div>`;
}

function reflectionPage(chapter) {
    return `
        <div class="page page-reflection">
            <h2>이야기를 다 읽고</h2>
            <p class="reflection-moral">${chapter.moral}</p>
            <p class="reflection-question">${chapter.question}</p>
        </div>`;
}

const QUIZ = [
    { q: "김 첨지가 산에서 주운 것은 무엇인가요?", choices: ["항아리 두 개", "부채 두 자루", "거울 두 개"], answer: 1 },
    { q: "빨간 부채로 부치면 어떻게 되나요?", choices: ["코가 짧아진다", "코가 빨개진다", "코가 길어진다"], answer: 2 },
    { q: "부자는 부채를 얻으려고 무엇을 내주었나요?", choices: ["살던 집을 통째로", "논밭의 절반쯤", "곳간의 쌀 전부"], answer: 0 },
    { q: "옥황상제는 상이 흔들리자 무엇인 줄 알았나요?", choices: ["벼락", "큰비", "지진"], answer: 2 },
    { q: "하늘나라에서는 솟아오른 코를 어떻게 했나요?", choices: ["기둥인 줄 알고 묶었다", "구름으로 덮어 두었다", "그냥 두고 보았다"], answer: 0 },
    { q: "김 첨지는 마지막에 집을 어떻게 했나요?", choices: ["그대로 차지했다", "주인에게 돌려주었다", "헐값에 팔았다"], answer: 1 }
];

function quizPage() {
    const items = QUIZ.map((item, i) => `
        <div class="quiz-item" data-qindex="${i}">
            <p class="quiz-question">${i + 1}. ${item.q}</p>
            <div class="quiz-choices">
                ${item.choices.map((c, ci) => `<button type="button" class="quiz-choice" data-choice="${ci}">${c}</button>`).join('')}
            </div>
        </div>`).join('');
    return `
        <div class="page page-quiz">
            <h2>이야기 문제</h2>
            <p class="quiz-intro-text" id="quizProgress">0 / 총 ${QUIZ.length}문항 완료</p>
            <div class="quiz-list">${items}</div>
        </div>`;
}

function endPage() {
    return `
        <div class="page page-end">
            ${artFrame('end.png', '🌟')}
            <h2>빨간 부채 파란 부채 이야기를 다 읽었어요!</h2>
            <a class="home-btn" href="../../../../../">학습 허브로 돌아가기</a>
        </div>`;
}

const PAGES = [
    { kind: 'cover' },
    ...CHAPTERS.flatMap(chapter => chapter.beats.map((beat, i) => ({ kind: 'spread', chapter, beat, isFirst: i === 0 }))),
    { kind: 'reflection', chapter: CHAPTERS[CHAPTERS.length - 1] },
    { kind: 'quiz' },
    { kind: 'end' }
];

const TWO_PAGE_KINDS = new Set(['spread', 'cover']);

let folioCounter = 0;
const FOLIOS = PAGES.map(p => {
    const width = TWO_PAGE_KINDS.has(p.kind) ? 2 : 1;
    const start = folioCounter + 1;
    folioCounter += width;
    return { start, width };
});

function renderPage(page) {
    switch (page.kind) {
        case 'cover':
            return coverPage();
        case 'spread':
            return spreadPage(page.chapter, page.beat, page.isFirst);
        case 'reflection':
            return reflectionPage(page.chapter);
        case 'quiz':
            return quizPage();
        case 'end':
            return endPage();
        default:
            return '';
    }
}

let current = 0;
let animating = false;

const spreadEl = document.getElementById('spread');
const prevBtn = document.getElementById('prevBtn');
const nextBtn = document.getElementById('nextBtn');
const indicatorEl = document.getElementById('pageIndicator');
const folioLeftEl = document.getElementById('folioLeft');
const folioRightEl = document.getElementById('folioRight');

function paint() {
    spreadEl.innerHTML = renderPage(PAGES[current]);
    prevBtn.disabled = current === 0;
    nextBtn.disabled = current === PAGES.length - 1;
    indicatorEl.textContent = `${current + 1} / ${PAGES.length}`;

    const folio = FOLIOS[current];
    folioLeftEl.classList.toggle('folio-center', folio.width === 1);
    if (folio.width === 2) {
        folioLeftEl.textContent = folio.start;
        folioRightEl.textContent = folio.start + 1;
        folioLeftEl.hidden = false;
        folioRightEl.hidden = false;
    } else {
        folioLeftEl.textContent = folio.start;
        folioLeftEl.hidden = false;
        folioRightEl.hidden = true;
    }

    spreadEl.querySelectorAll('[data-goto]').forEach(btn => {
        btn.addEventListener('click', () => {
            const chapterNum = Number(btn.dataset.goto);
            const idx = PAGES.findIndex(p => p.kind === 'spread' && p.chapter.num === chapterNum && p.isFirst);
            if (idx >= 0) goTo(idx);
        });
    });
    spreadEl.querySelectorAll('[data-goto-kind]').forEach(btn => {
        btn.addEventListener('click', () => {
            const idx = PAGES.findIndex(p => p.kind === btn.dataset.gotoKind);
            if (idx >= 0) goTo(idx);
        });
    });

    if (PAGES[current].kind === 'quiz') {
        initQuiz();
    }
}

function initQuiz() {
    let answeredCount = 0;
    const progressEl = document.getElementById('quizProgress');

    spreadEl.querySelectorAll('.quiz-item').forEach(item => {
        const qi = Number(item.dataset.qindex);
        const q = QUIZ[qi];
        item.querySelectorAll('.quiz-choice').forEach(btn => {
            btn.addEventListener('click', () => {
                if (item.classList.contains('graded')) return;
                item.classList.add('graded');
                const chosen = Number(btn.dataset.choice);
                item.querySelectorAll('.quiz-choice').forEach(b => {
                    const ci = Number(b.dataset.choice);
                    if (ci === q.answer) b.classList.add('correct');
                    else if (ci === chosen) b.classList.add('incorrect');
                });
                answeredCount++;
                progressEl.textContent = `${answeredCount} / 총 ${QUIZ.length}문항 완료`;
            });
        });
    });
}

function goTo(index) {
    if (animating || index === current || index < 0 || index >= PAGES.length) return;
    animating = true;
    const dir = index > current ? 'flip-next' : 'flip-prev';
    spreadEl.classList.add(dir);
    setTimeout(() => {
        current = index;
        paint();
    }, 230);
    setTimeout(() => {
        spreadEl.classList.remove('flip-next', 'flip-prev');
        animating = false;
    }, 480);
}

prevBtn.addEventListener('click', () => goTo(current - 1));
nextBtn.addEventListener('click', () => goTo(current + 1));

document.getElementById('tocLink').addEventListener('click', () => {
    // 그림책은 차례가 표지에 붙어 있다.
    const idx = PAGES.findIndex(p => p.kind === 'toc');
    goTo(idx >= 0 ? idx : 0);
});

document.addEventListener('keydown', (e) => {
    if (e.key === 'ArrowRight') goTo(current + 1);
    if (e.key === 'ArrowLeft') goTo(current - 1);
});

paint();
