import StemLegacyRedirect from "../stem-legacy-redirect";

export default function ConicIntegralAreasPage() {
  return <StemLegacyRedirect target="/arithmetic/stem/foundation?kind=multiple-integrals" />;
}
